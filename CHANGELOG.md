# Changelog

## Unreleased

- Established the `holyc-ocaml` Dune workspace and public `holyc_lib` library.
- Pinned TempleOS reference commit `c26482bb6ad3f80106d28504ec5db3c6a360732c`.
- Added byte-oriented source tracking, structured diagnostics, and the first streaming lexer slice.
- Added human and JSON token dumps with focused unit, golden, negative, and property tests.
- Added a bounded `#include` token stream with canonical hosted path checks, nested source provenance, include backtraces, a public library entry point, and the `holyc preprocess` command.
- Added TempleOS `#define` text capture and expansion with session-scoped lookup, redefinition history, replacement source maps, invocation and declaration provenance, deterministic dumps, and explicit recursion, nesting, and generated-byte limits.
- Added lexical scans that continue through exhausted include and definition frames, with ordered token and trivia source segments, multi-source diagnostics, exhaustive compound-operator split tests, and a matching native TempleOS oracle fixture.
- Added nested `#ifjit` and `#ifaot` branch selection with explicit compilation modes, raw inactive-branch scanning, cross-frame conditional state, and stable mismatch diagnostics.
- Added `#ifdef` and `#ifndef` with a session-owned model of TempleOS hash kinds, checked keyword and internal-type seeds, import filtering, local-variable shadowing, deterministic visibility dumps, and streaming symbol updates.
- Added `holyc dump-symbols` with deterministic human and JSON output, stable declaration IDs, generated-source provenance, direct-call shapes, a source-only view, and partial state after parser errors. The output describes parser visibility and does not claim semantic name resolution.
- Added a session-owned semantic symbol-table foundation with stable symbol and scope IDs, explicit task and lexical contexts, newest-first kind-filtered lookup, nth-match traversal across parents, checked scope ownership, 17 pinned internal-type entries, and deterministic provenance dumps. Nested collection, reference resolution, duplicate rules, and linkage remain separate work.
- Added source-ordered semantic collection for accepted top-level aggregate, global, prototype, and function-definition AST items. Each module receives its own scope, grouped and aggregate-attached declarators keep their lexical positions, and generated or included names retain their provenance. Duplicate rules, nested scopes, linkage, and reference resolution remain separate work.
- Added source-ordered aggregate forward reconciliation. Every forward starts a distinct identity, a definition completes only the newest same-name unresolved forward, and later definitions shadow instead of collapsing earlier types. Completed identities retain both declaration sites and use the definition symbol already attached to direct-member collection.
- Added semantic aggregate headers with distinct public primitive, intrinsic storage, and aggregate-identity bases plus pointer depths zero through four. Named backings resolve before the new identity is published, bases resolve afterward, later forward completion remains canonical, and every header token retains its source provenance. Layout, backing conversion, cycle checks, and inherited lookup remain separate work.
- Added source-ordered aggregate member type references. Each direct member binds public, intrinsic, or named aggregate bases against the identity visible when its definition is read; comma-declarator pointers, callback return pointers, callback indirection, and array-dimension origins remain distinct. Recursive member callback signatures and metadata evaluation remain separate work.
- Added closed aggregate byte layouts for packed classes, named and anonymous unions, one base at offset zero, arrays, pointers, callbacks, explicit `$$` positions, and negative displacements. Unresolved semantic dependencies remain errors instead of guessed values.
- Added an immutable aggregate member index with current-before-base lookup, source-compatible duplicate checks, and the exact `pad`, `reserved`, and `_anon_` exceptions.
- Added `holyc dump-layout` with versioned human and JSON output for checked aggregate layouts, stable identities, base and direct-member facts, 64-bit target values, complete provenance, and no partial stdout after parse or semantic failures.
- Added semantic aggregate scopes and source-ordered direct-member identities. Grouped declarators retain their positions, nested anonymous-union fields share the containing lookup namespace, and generated names retain their provenance. Member metadata evaluation and AST expression binding remain separate work.
- Added semantic function scopes for prototypes and definitions. Named fixed parameters retain their signature indexes, variadic signatures receive source-linked `argc` and `argv` entries, and automatic and static locals from nested statements share one source-ordered function namespace. Local type resolution, duplicate checks, storage, and ordinary reference resolution remain separate work.
- Added source-ordered function signature type resolution for prototypes and definitions. Return and fixed-parameter types bind to public, intrinsic, or canonical aggregate identities; callbacks resolve recursively; unnamed slots and default provenance remain intact; and top-level varargs retain the source-backed internal `I64` shape of `argc` and `argv`. Reconciliation, default evaluation, call checking, storage, ABI rules, and linkage remain separate work.
- Added checked function-local label resolution. Language `goto` statements bind forward or backward to language and structural assembly-block definitions with stable identities and complete provenance. Top-level occurrences, missing targets, duplicate language definitions, and mixed language/assembly definitions fail before mutation. Assembly operand references, address checks for repeated assembly definitions, unused-label warnings, and control-flow lowering remain separate work.
- Added deterministic `#if` evaluation for integer, character, and floating literals, definition-expanded constants, `defined`, HolyC precedence and comparison chains, 64-bit target arithmetic, retained branch lookahead, provenance-aware diagnostics, and a configurable expression-size limit.
- Added constant `#assert` evaluation with nonfatal `HCPP0024` warnings, retained token output, a diagnostic-preserving library result, and human and JSON CLI golden tests.
- Added source-ordered `#help_index` and `#help_file` metadata with explicit continuation handling, hosted path confinement without target reads, include and definition provenance, versioned dumps, and complete coverage of the pinned `KernelC.HH` directives.
- Added a checked parser and deterministic generated table for all 106 registers, 325 canonical opcodes, 49 aliases, and 924 instruction forms in the pinned `OpCodes.DD`, then exposed every register and opcode spelling to symbol conditionals.
- Added deterministic, provenance-carrying expansions for `__DATE__`, `__TIME__`, `__LINE__`, `__CMD_LINE__`, `__FILE__`, and `__DIR__`, with explicit CLI overrides and shared generated-text limits.
- Added deterministic full-tree lexer corpus reports over the exact pinned Git blobs. All 528 `.HC`, `.HH`, and `.PRJ` files tokenize without a diagnostic or crash, with NUL terminators and trailing payload bytes reported explicitly.
- Added a deterministic AOT parser corpus report for all 528 pinned source objects. The reviewed baseline records 21 parsed files, 16 frontend failures, 491 parser failures, no exclusions, and no read or internal errors; `--require-all` provides the strict gate.
- Added the first spanned AST and parser for empty input and ordered direct primitive global variables, with integrated preprocessing, provenance-aware `HCPARSE` diagnostics, deterministic human and JSON dumps, library entry points, and `parse` and `dump-ast` commands.
- Added explicit pointer layers for primitive global declarations, including the pinned four-star limit, definition-generated source provenance, stable depth and function-pointer diagnostics, and human and JSON CLI goldens.
- Added comma-separated primitive globals with a shared base type, independent pointer depths, explicit delimiter provenance, streaming symbol publication, and human and JSON CLI goldens.
- Added source-linked `public` and `static` prefixes on primitive globals, preserving their order and definition provenance without claiming linkage or storage behavior.
- Added source-linked `extern` and AOT-only `import` bindings on primitive globals, with a stable JIT rejection that does not publish the rejected name.
- Added source-linked `extern class` and `extern union` forward declarations, including all 14 names at `Kernel/KernelA.HH:4-17`, streaming class-symbol publication, distinct aggregate kinds, deterministic dumps, and stable rejection of bodies or declarator lists.
- Added source-linked class and union definitions with early name publication, ordered primitive and named member groups, pointer and array declarators, recursive anonymous unions, empty member semicolons, deterministic dumps, nested-brace recovery, and explicit diagnostics for deferred aggregate forms.
- Added intrinsic storage type specifiers and type-backed class and union headers, including the six pinned public integer unions, optional backing pointer layers, bounded recovery, and deterministic human and JSON dumps.
- Added source-spanned single-base clauses to class and union definitions, including the complete pinned Adam sprite hierarchy, representative kernel hash chains, source-order visibility, bounded recovery, and a dedicated second-base diagnostic. Inherited layout and lookup remain separate semantic work.
- Added global declarators directly after class and union definitions, plus scalar and recursive braced global initializer syntax. Tests cover the exact 17 initialized rows in `Compiler/CInit.HC`, streaming name publication, source provenance, stable dumps, and bounded malformed-list diagnostics. Initializer typing, evaluation, and storage remain separate semantic work.
- Added symbol-sensitive class and union type specifiers across globals, function returns and parameters, nested function pointers, automatic and static locals, arrays, and postfix casts. Tests parse the unfiltered opening declaration slice from `Compiler/CExts.HC`, retain generated-source provenance, honor local shadowing, and reject unresolved names without publishing a declaration.
- Added source-linked target identifiers for `_extern` and AOT-only `_import` primitive globals, with a stable missing-target diagnostic and no symbol publication after a JIT rejection.
- Added bound primitive function prototypes for ordinary and alternate-name externs and AOT imports, including return pointers, empty or variadic parameter lists, optional parameter names, punctuation provenance, function symbol publication, deterministic dumps, and explicit diagnostics for deferred parameter forms.
- Added unbound primitive function definitions through the common statement parser, including recursive symbol visibility, explicit empty and absent-at-EOF body states, split header and body provenance, deterministic dumps, bounded recovery, and a matching native TempleOS oracle fixture.
- Added primitive automatic and static local declarations in function bodies, including pointer layers, arrays, scalar initializers, per-declarator register requests, function-wide parser visibility, deterministic dumps, and explicit diagnostics for unsupported local forms. A native TempleOS fixture also fixes the source statement-continuation rule and confirms that local declarations in `for` initializers are rejected.
- Added ordered `interrupt`, `haserrcode`, `argpop`, and `noargpop` syntax on bound prototypes, with source provenance and tests against the checked TempleOS staging transitions.
- Added `reg` and `noreg` qualifiers on fixed and variadic function parameters, including explicit U64 register names, source-positioned AST nodes, generated-source provenance, and stable diagnostics for misplaced qualifiers.
- Added recursive function-pointer parameters with distinct return and declarator depths, empty, fixed, variadic, and nested signatures, exact punctuation provenance, deterministic dumps, and a checked hosted nesting limit.
- Added source-positioned default expressions to ordinary and nested function parameters, including non-trailing defaults, every audited binary operator, HolyC's unary-minus power rule, deterministic AST dumps, and bounded malformed-input recovery.
- Added primitive global array declarators with ordered dimensions, an explicit unsized first dimension, shared core expression syntax, bracket provenance, deterministic dumps, and source-specific diagnostics for malformed suffixes.
- Added `_intern` bindings with source-positioned target expressions, JIT and AOT syntax coverage, pinned `KernelB.HH` prototypes, deterministic dumps, and bounded malformed-target diagnostics.
- Added parenthesized call expressions with explicit omitted argument slots, nested-call precedence, punctuation provenance, deterministic dumps, and distinct diagnostics for missing separators and closing parentheses.
- Added parenthesis-free direct calls with retained fixed-parameter shapes, default holes, adjacent required arguments, recursive visibility, address-of and shadowing safeguards, deterministic dumps, stable diagnostics, and a matching native TempleOS fixture.
- Added bracket index expressions with repeated and mixed postfix chaining, source-positioned brackets, deterministic dumps, and distinct diagnostics for empty or unclosed indexes.
- Added direct and pointer member expressions with explicit access kinds, source-positioned member names, mixed postfix chaining, deterministic dumps, and a dedicated missing-member diagnostic.
- Added source-shaped `sizeof` terms with matched wrapper parentheses, unresolved named and member targets, uncapped target stars, deterministic dumps, and diagnostics for malformed targets and forbidden direct suffixes.
- Added source-shaped `offset` terms with required member paths, repeated nested members, matched wrapper parentheses, deterministic dumps, and diagnostics for malformed targets and forbidden direct suffixes.
- Added source-shaped `defined` terms that retain one operand token, distinguish identifier-shaped names from other tokens, preserve repeated wrapper parentheses, and enforce the pinned restricted postfix state.
- Added a distinct `lastclass` parameter-default node for ordinary and nested function prototypes, with exact keyword provenance, deterministic dumps, and diagnostics that reject expression continuations after the keyword.
- Added source-shaped top-level string and character statements with explicit `Print` and `PutChars` targets, variable-value empty markers, retained `Print` arguments, deterministic dumps, and bounded malformed-input diagnostics.
- Added top-level expression and empty statements plus source-shaped comma-linked statement groups, preserving every separator, `PutChars` comma boundaries, source order, and generated-source provenance.
- Added recursive compound statement blocks at top level and inside blocks, with explicit brace provenance, comma-linked block boundaries, deterministic dumps, and recovery at the matching close.
- Added source-shaped `if` and `else` statements with required parenthesized conditions, nearest-`if` association, recursive statement branches, deterministic dumps, provenance tracking, and bounded malformed-input recovery.
- Added source-shaped `while` statements with required parenthesized conditions, recursive statement bodies, nearest-`if` association across loops, deterministic dumps, provenance tracking, and bounded malformed-input recovery.
- Added source-shaped `do ... while` statements with a body parsed before the required trailing condition, an explicit final semicolon, deterministic dumps, provenance tracking, and bounded malformed-input recovery.
- Added source-shaped `for` statements with statement initializers, mandatory conditions, optional semicolon-free updates, comma-linked update sequences, deterministic dumps, provenance tracking, and the shared loop-depth guard.
- Added source-shaped `break` statements with optional retained semicolons, comma and `for` update boundaries, deterministic dumps, provenance tracking, and block-safe malformed-input recovery.
- Added valueless and value-bearing `return` statements with source-shaped comma and `for` update boundaries, deterministic dumps, provenance tracking, and block-safe malformed-input recovery.
- Added bounded and no-bound `switch` statements with implicit, single, ranged, and default labels, ordered ordinary statements, recursive `start:`/`end:` sub-switch regions, provenance tracking, deterministic dumps, and structural recovery diagnostics.
- Added function-pointer globals with separate return and declarator depths, recursive signatures, arrays, initializers, comma groups, aggregate-attached forms, source provenance, stable dumps, and name-specific diagnostics. Tests parse the four pinned callback definitions in `Kernel/KGlbls.HC` and their `extern` declarations in both compilation modes.
- Added recursive function-pointer members to class, union, and anonymous-union bodies, including return-pointer layers, comma groups, arrays, defaults, nested callbacks, variadic signatures, source provenance, stable dumps, and member-specific diagnostics. Pinned tests cover eight callbacks across `CMathODE`, `CDocEntry`, `CKeyDevEntry`, and `CKeyDevGlbls` in JIT and AOT modes.
- Added automatic and static function-pointer locals through the shared recursive callback grammar. Tests cover the exact declarations at `Demo/Games/Stadium/StadiumGen.HC:5` and `Demo/Graphics/Grid.HC:13`, function-local visibility, register requests, arrays, comma groups, generated-source provenance, stable dumps, recovery, and the source-backed rejection of direct callback initialization.
- Added recursive braced initializer syntax for static locals while preserving the pinned parser's expression-only rule for automatic locals. Tests cover the three direct source examples in `Adam/WinMgr.HC` and `Demo/GlblVars.HC`, JIT and AOT modes, nested and empty lists, trailing commas, generated-source provenance, stable scalar dumps, bounded nesting, and declaration-safe recovery.
- Added explicit `$$ = expression;` aggregate-offset directives as source-positioned class, union, and anonymous-union members. Tests cover all four direct forms in the pinned source set, JIT and AOT modes, ordinary expression precedence, generated-source provenance, stable dumps, and aggregate-safe recovery. Layout effects remain separate semantic work.
- Accepted every checked language-keyword spelling in aggregate member-name slots, matching TempleOS's contextual `TK_IDENT` and `PrsKeyWord` behavior. The lexer still emits explicit keyword tokens, and all other declaration-name contexts remain unchanged. Tests cover the 12 direct `U0 start;` members in the pinned Kernel and Adam sources and the complete `CKernel` region around its aligned member.
- Added source-ordered aggregate member metadata with distinct extended-string and expression value nodes. Tests cover all six members and 13 entries in `Demo/ClassMeta.HC:16-28`, duplicate names, each supported declarator shape, provenance, deterministic dumps, and the `HCPARSE0144` missing-value diagnostic. Evaluation and runtime lookup remain semantic work.
- Added structural `asm { ... }` statement nodes at top level and inside functions. The parser retains source-line groups, global, exported, and local labels, classifies tokens against every checked opcode, alias, directive, and register, and verifies all 45 brace-delimited blocks found in the pinned Compiler, Kernel, Adam, and Demo sources. Operand checking and encoding remain separate assembler work.
- Added function-local direct assembly nodes for adjacent opcodes and aliases with zero, one, or two first-form operands. Register, immediate, and memory forms retain size and segment prefixes, comma locations, source spelling, spans, and generated-source provenance. `HCPARSE0147` rejects top-level use; `HCPARSE0149` through `HCPARSE0152` report malformed operands. Operand validation, lowering, encoding, and execution remain separate work.
- Retained source-ordered `IMPORT`, `DU8`, `DU16`, `DU32`, `DU64`, `DUP`, `BINFILE`, `LIST`, `NOLIST`, `USE16`, `USE32`, and `USE64` items in function-local direct assembly. The parser preserves optional separators, required and optional semicolons, and generated-source provenance; rejects `ORG`, `ALIGN`, and standalone `DUP` in this form; and never reads a `BINFILE` path. `HCPARSE0153` through `HCPARSE0156` cover the audited restriction and malformed directive shapes. Directive evaluation, state changes, data emission, imports, and file inclusion remain assembler work.
