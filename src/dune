(include_subdirs qualified)

(rule
 (targets build_metadata.ml)
 (deps (universe))
 (action
  (chdir
   %{workspace_root}
   (with-stdout-to
    %{targets}
    (run %{exe:../tools/version_gen.exe})))))

(library
 (name holyc_lib)
 (public_name holyc-ocaml.lib)
 (foreign_stubs
  (language c)
  (names native_execution_stubs))
 (libraries dune-build-info unix yojson))
