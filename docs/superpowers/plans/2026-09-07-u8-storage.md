# U8 storage implementation plan

Goal: execute the #615 Sum fixture through the existing checked source pipeline.
Spec: [U8 storage design](../specs/2026-09-07-u8-storage-design.md).
Stack: OCaml >=5.1, Dune >=3.12, existing Alcotest/CLI suites.

Keep `holyc`, `holyc_lib`, `holyc-ocaml` and `holyc-ocaml.lib` unchanged.
Keep TempleOS `c26482bb6ad3f80106d28504ec5db3c6a360732c` read-only. Use the
configured Git identity and issue/branch/PR workflow. Builds and executable
invocations have one coordinator; no source/test mutations during verification.

- [x] Fetch and inspect main, remote checks, reference pin and issue #615.
- [x] Reproduce Sum's HCRUN0003 in JIT and AOT; pass existing array tests.
- [x] Add `test_integer_bytes.ml`, registration, source fixture and CLI inputs.
  Record the original CLI failure and the maintained group's unsupported-storage failures.
- [x] Extend `expression_lowering.ml` storage-value admission and checked strides.
  Keep narrow updates and numeric parameter/return restrictions explicit.
- [x] Extend `integer_interpreter.ml` private scalar classification, byte cells,
  zero-extending reads, narrowing stores and independently retained results.
  Replace cell-count-derived frame charges with exact checked layout charges.
- [x] Validate malformed width/type/stride joins, rejected frame escapes and foreign references,
  per-byte initialization, byte object bounds, overflow and allocation limits.
- [x] Run focused byte and prior storage groups; inspect real public/CLI results
  and exact limits. Review every failure against pinned source before changing
  expectations. Re-run after each production correction.
- [x] Update docs, source map and traceability with precise supported boundaries.
  Correct stale prompt continuation statements and add byte-specific requirements.
- [x] Run `opam exec -- dune build '@fmt' '@generated-check' '@all' '@install'` and
  `opam exec -- dune runtest`, reference/provenance PowerShell scripts, and
  lexer/parser corpus comparisons. Inspect complete retained logs.
- [ ] Obtain independent code review and address findings. Commit and rebuild
  embedded identity, push, and require OCaml 5.1.x, OCaml 5.3.x,
  dependency-review, Lexer corpus and Parser corpus checks on the final commit.
- [ ] Merge through protections, compare tested and merge trees, synchronize
  local main, rebuild/version-check, and inspect post-merge workflows.
- [ ] Record a verified checkpoint and the next missing executable connection.
  Keep the full compiler goal active until all original requirements are proven.

Local verification: OCaml 5.4.1, Dune 3.24.2, ocamlformat 0.29.0;
1,811 full-suite tests and CLI pass. All 19 byte groups, quoted Dune aliases,
82 reference checksums and 11 incremental provenance scenarios pass. Lexer
acceptance is 528/528; the parser JSON matches the committed baseline after
Windows newline normalization (25/528 standalone, 126/528 with prelude).
Independent review approved the code after exact-type and graph-only scope
checks. Remaining delivery boxes describe this pre-merge source snapshot;
#615 and #616 hold the final source/CI/merge evidence.
