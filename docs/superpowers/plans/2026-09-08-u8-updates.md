# U8 Updates Implementation Plan

> **For agentic workers:** Use superpowers:executing-plans task by task. Root
> serializes all builds/executable use and freezes compiled edits during checks.
> Independent reviewers receive bounded read-only work with exact scope.

**Goal:** Execute U8 compound and prefix/postfix updates through checked source
programs while preserving raw register results and native initializer evidence.

**Architecture:** Extend the existing checked update path with separate storage
width/result policy, then add source-grounded initializer invariance checks.
Reuse existing owners, references, regions, call contexts and reporting.

**Tech Stack:** OCaml, Dune, Alcotest, PowerShell, pinned TempleOS source.

**Spec:** `docs/superpowers/specs/2026-09-08-u8-updates-design.md` and Section 61
of `C:/Users/imike/Desktop/holyc-ocaml.txt`, tracked in issue #629 under #396.

## Global constraints

- Reference `c26482bb6ad3f80106d28504ec5db3c6a360732c`; public package names stay.
- Branch `feat/629-u8-updates`; configured human Git identity and helper required.
- No concurrent Dune/executable use or compiled edits during verification.
- Preserve all previous source gates, v1/v2 reports, word updates and byte stores.
- Raw runtime reference evidence is distinct from native initializer proof.

## Task 1: Checked U8 raw update execution

Files: `src/ir/expression_lowering.ml`, `src/ir/integer_interpreter.ml` and their
interfaces; new `test/test_integer_byte_updates.ml`, `test/dune`, `test/test_main.ml`.
Interfaces: existing canonical opcodes/flags/target types remain; the private VM
prepared operation retains storage width separately from old/new/full result
selection. No new public execution context or opcode is introduced.

- [x] Add the eight gates in both modes and inspect their HCRUN0003 failures.
- [x] Admit exact U8 update lvalues/results alongside words; use the existing
  destination-before-RHS plans for scalar, indexed and indirect operations.
- [x] Retain Stored_byte in VM preparation; compute unsigned compound bits,
  narrow only storage, and use new/old narrowed bytes for prefix/postfix.
- [x] Add full-vs-stored wrap controls for all 14 opcodes, immediate/runtime RHS,
  exact canonical flags/result/type checks and unchanged word controls; run GREEN.

## Task 2: Native initializer invariance

Files: `src/driver/integer_initializers.ml`; a small internal
`src/driver/integer_update_initializers.ml/.mli` if needed to keep analysis
focused; original frame/register evidence accessors where necessary; the new
byte-update tests and existing initializer/source-evidence groups.
Interfaces: validation consumes the original checked value/callee graph,
globals, optional declaring frame, and terminal declaration destination. It
returns success or an exact instruction/reason for the existing HCRUN0006 owner
diagnostic. Unknown proof fails; it never grants storage execution authority.

- [x] Add RED pairs for range-safe memory updates, terminal byte leaves and
  safely discarded helper updates, versus exposed-word, automatic-register and
  discarded outside-byte bit cases. Include nested calls and RHS aliasing.
- [x] Derive memory provenance from canonical address producers and original
  frame/global evidence, retaining noreg/source facts rather than assuming every
  hosted frame slot is native memory.
- [x] Track sound constant/range and result-use facts. Account for native
  result-memory redirection, compound overflow and the byte bit-operation hazard.
- [x] Apply checks to each original initializer graph and every transitive
  callee, preserving shifts/divisor guards, barriers, phases and prior effects.
- [x] Verify positive controls and each distinct rejection before broad tests.

Review added RED controls for identity folding that discards a bit result and
for byte-sized quotients with native-wide local divisors. The fixes require
positive native observation sinks and invariant reads across the entire callee
graph. A finite fixed point proves ordinary candidate locals only from all
bounded writes with no direct updates; explicit registers remain excluded.
Independent source review found no remaining blocker in these proofs.

## Task 3: Complete storage/API/CLI acceptance

Files: byte-update tests; `test/test_integer_program_cli.ml`, `test/dune`; new
`examples/integer-byte-updates.hc`; existing byte/update tests where a deliberate
unsupported fixture is promoted to a tested positive.

- [x] Cover locals, globals, statics, arrays, literals, pointers, aliases,
  address/index effects, argument order, loops, recursion and fresh execution.
- [x] Verify zero-divisor/unknown/bounds/lifetime faults retain earlier effects
  without an update store; cover exact and one-below relevant quotas.
- [x] Add the maintained example, measure actual counters, then assert its exact
  limits, one-below diagnostics, deterministic dumps and v1/v2 reports.
- [x] Run all eight API/CLI gates and surrounding word/byte/initializer suites.

The 43 byte-update groups pass, as does the full CLI executable. The maintained
example returns I64 42 and captures 3432 with 79 runtime / 10 preparation work,
12 persistent / 40 active-frame bytes, depth 2, 3 literal / 2 output bytes and
8 output-work units. The active frame includes Print's hidden count slot.
All exact limits pass together and each one-below diagnostic/capture matches.
The revision-check script verifies 34 gate/quota reports across both modes;
working-tree evidence precedes the required committed/merged identity rebuilds.

## Task 4: Review and protected integration

Files: README, changelog, byte/update/storage/IR/testing/compatibility/source-map
docs, traceability, plan and external prompt; issue #629, parent #396 and PR.

- [x] Document raw reference results, native result/register/bit-op distinctions,
  initializer proof and measured limits; preserve all 78 existing prompt fences.
- [x] Independently review production/tests, resolve findings with controls,
  and run full suite/CLI, quoted @fmt/@generated-check/@all/@install, 82 checksums,
  11 provenance scenarios and exact lexer/parser JSON plus normalized parser text.

Final local verification: 1,968 tests in 40.986 seconds plus the CLI checks;
all quoted packaging aliases, 82 checksums and 11 provenance scenarios passed.
Lexer JSON is identical to the preceding capture (528/528, no errors); parser
JSON and normalized text match the committed AOT baseline (25 standalone and
126 with the prelude). The final added controls cover I64/U64 destinations
with U8 RHS values and exact native BY_VAL/LOCK flags. Independent raw-path and
native-proof reviews found no remaining blockers.
- [ ] Commit/push/draft PR with human identity; rebuild committed identity and
  require OCaml 5.1/5.3, dependency review, Lexer and Parser on final source.
- [ ] Normal protected merge; compare source/merge trees, rebuild merged identity,
  repeat maintained gates, verify post-merge CI and clean synchronized main,
  update project/issues/prompt evidence, and continue the full compiler goal.
