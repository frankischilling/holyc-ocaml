# Reference source map

All findings in this document refer to TempleOS commit `c26482bb6ad3f80106d28504ec5db3c6a360732c`.

## Compiler assembly order

`Compiler/Compiler.PRJ` includes kernel headers first, enables conversion of extern declarations to imports while loading later kernel headers, and then loads compiler units in this order: templates, extensions, initialization, exceptions, lexer, hashes, assembler, parser, optimizer, backend, and final optimization passes. The OCaml architecture keeps the same conceptual dependencies without copying the source's global-state layout.

## Lexer state

`Kernel/KernelA.HH` defines `CLexFile` and `CCmpCtrl`. `Compiler/Lex.HC:LexFilePush` maintains the lexical frame stack, while `LexGetChar` owns one-byte pushback and line tracking. Root depth is -1 and the first pushed frame has depth 0. `LexBackupLastChar` saves the caller cursor and one-character lookahead before a push; `LexGetChar` restores that state and continues without an intermediate EOF after a buffer ends. This return can happen while `Lex` is still scanning one token or trivia item. The OCaml source model stores each full byte buffer and its precomputed line starts, links its lexer cursor to the caller cursor, and records the consumed ranges as ordered spans. Each `Lexer_frame` also retains its kind and relevant include, definition, or predefined-value provenance. Hosted include and generated-text depth are tracked separately.

`Kernel/StrA.HC` shows that the lexer is byte oriented. Identifier starts include ASCII letters, underscore, at sign, and bytes 128 through 255. Digits are accepted after the first byte. The hosted lexer preserves non-ASCII bytes rather than requiring UTF-8.

`LexGetChar` treats byte zero as the end of its current buffer. Several committed `.HC` files place an inserted binary payload after that terminator. The corpus lexer enables this behavior explicitly and records the terminator offset and trailing byte count. The ordinary hosted lexer keeps its stricter embedded-NUL diagnostic for untrusted standalone source.

The `KW_DEFINE` replacement loop removes backslash followed by LF or CR, including CRLF. `Compiler/LexLib.HC:LexExtStr` also consumes an immediate backslash before it asks the normal lexer for the next string. `Kernel/KernelA.HH` uses the first form in continued definitions, and `Kernel/KernelC.HH` uses the second form in help metadata. The raw lexer retains these newline forms as `line-continuation` trivia. A backslash without a following line ending remains an invalid source byte.

`Compiler/Lex.HC:93-105` uses `CCF_IN_QUOTES` to make `LexDollar` serialize a saved DolDoc entry back into quoted string text, and `LexInStr` uses `dollar_cnt` to retain its two delimiters. This is why the unescaped quotes in `Demo/DolDoc/TextDemo.HC:20`, `Demo/MsgLoop.HC:4`, and `Adam/Opt/Utils/MemRep.HC:207` do not end their HolyC strings. The hosted lexer has no live `CDoc`, so its string scanner recognizes an uppercase command name with an optional comma payload and copies that complete single-dollar range literally. An unmatched or unrecognized dollar follows the ordinary raw-string path, and doubled dollars retain the explicit plain-source escape behavior.

## Include paths and source frames

The `KW_INCLUDE` branch in `Compiler/Lex.HC:Lex` requires a string token, calls `ExtDft` with `HC.Z`, resolves the result through `FileNameAbs`, and pushes the file with `LexIncludeStr`. `Doc/PreProcessor.DD` rules out a C-style angle-bracket form.

`Kernel/BlkDev/DskStrA.HC:FileNameAbs` delegates to `DirNameAbs`, which starts relative paths from `Fs->cur_dir`; it does not consult the including file's directory. `DirNameAbs` handles root, `::`, home, drive, dot, and parent components. `ExtDft` adds its default only when no extension is present.

`Frontend.Include_resolver` preserves that working-directory rule and adds ordered hosted include roots. `Frontend.Lexer_frame` retains the active canonical path, display spelling, source ID, cursor, include origin, caller, and source depth. `Frontend.Preprocessor` pushes the resolved source and allows the active scanner to continue into the caller when the included bytes end. It labels canonical root confinement, cycle checks, and depth and byte caps as hosted security behavior because the pinned lexer does not provide those rejections. [The preprocessor notes](preprocessor.md) record the command, diagnostics, and current directive boundary.

## Definition capture and expansion

The `KW_DEFINE` branch in `Compiler/Lex.HC:Lex` sets `CCF_NO_DEFINES` before reading the name. It then skips only space, tab, and byte `0x1f`, as established by `char_bmp_non_eol_white_space` in `Kernel/StrA.HC`. Replacement capture retains raw bytes, removes LF and CRLF continuations, recognizes `//` only outside double quotes, and leaves block comments for the replacement's later lexical pass. Empty and EOF-terminated definitions are valid.

`CHashDefineStr` in `Kernel/KernelA.HH` stores the name, text, source location, and `cnt = -1`. `Compiler/CHash.HC` and `Kernel/KDefine.HC` provide the related definition APIs. `Kernel/KHashA.HC:HashAdd` inserts at the front of a bucket and `HashFind` returns the first match, which makes the newest same-name definition visible while older entries remain allocated.

Identifier recognition in `Compiler/Lex.HC:Lex` checks `HTT_DEFINE_STR` before it returns a token. A match calls `LexIncludeStr` with a copy of the stored text and marks the pushed `CLexFile` with `LFSF_DEFINE`. `Compiler/CExcept.HC:ParenWarning` and `Compiler/PrsExp.HC` inspect that flag, so the OCaml frame model keeps definitions distinct from physical includes for later parser work.

`Frontend.Definition` assigns source-ordered identities, retains replacement spans and byte-segment maps, and keeps both current lookup state and redefinition history. `Driver.Session` owns that environment because the native `CmpCtrlNew` points definition lookup at the task hash table. `Frontend.Preprocessor` creates a logical source for each expansion, adds invocation and declaration provenance to tokens and diagnostics, and leaves the caller available to the same lexical scan at replacement EOF. Active-definition cycle checks and definition depth and byte budgets are hosted security rules; the pinned lexer has no equivalent guard.

## Predefined compiler values

`Kernel/KernelA.HH:2035-2043` defines `__DATE__`, `__TIME__`, `__LINE__`, `__CMD_LINE__`, `__FILE__`, and `__DIR__` as definition strings containing narrow `#exe` bodies. `Doc/Directives.DD` publishes the same six definitions. The line and file forms read the active lexical frame. The command-line form also requires `CCF_CMD_LINE` and a source depth below one. `Compiler/Lex.HC:LexFilePush` establishes root depth -1 and the first included depth 0.

`Compiler/CMisc.HC:StreamDir` calls `DirFile` on the active `full_name` and injects a quoted string through `StreamPrint`. `Kernel/StrPrint.HC:MPrintDate` renders `%D` as `MM/DD/YY`, while `MPrintTime` renders `%T` as `HH:MM:SS`.

`Frontend.Predefined` keeps those six spellings and recognizes their exact standard bodies by comparing raw token sequences. Whitespace may differ without allowing separate tokens to fuse. `Frontend.Preprocessor` expands each value into a bounded generated lexical frame with invocation provenance. An exact source definition retains its declaration span; a different source definition with the same name remains ordinary replacement text. Date and time come from explicit deterministic configuration. File and directory results use canonical hosted paths, so compatibility reports do not claim byte equality with TempleOS drive names. General `#exe` execution remains future compile-time VM work.

## Help directives and source links

The `KW_HELP_INDEX` branch in `Compiler/Lex.HC:Lex` replaces `CCmpCtrl.cur_help_idx` with one string. `Compiler/LexLib.HC:LexExtStr` receives `lex_next=FALSE`, so it joins more text only through an immediate backslash followed by another string token. `Kernel/KernelC.HH:195-196` contains the pinned continuation example. An empty string remains stored on the controller, but `HashSrcFileSet` treats it as no active index.

The `KW_HELP_FILE` branch applies `ExtDft(...,"DD.Z")`, passes the result to `FileNameAbs`, creates a public `HTT_HELP_FILE` source symbol, and calls `HashSrcFileSet`. `Kernel/BlkDev/DskStrA.HC:FileExtDot` establishes the extension rule. `FileNameAbs` and `DirNameAbs` establish default-directory, root, drive, dot, and parent handling. Their call to `Kernel/StrA.HC:StrUtil` removes leading and trailing whitespace and other control bytes as documented in the preprocessor notes. `Kernel/KHashB.HC:HashSrcFileSet` writes `FL:<full_name>,<line>` and copies a nonempty current index.

`Frontend.Help_metadata` keeps source-ordered index events and help-file records. `Frontend.Preprocessor` attaches source spans, include stacks, and definition traces, while `Frontend.Include_resolver.resolve_metadata` produces a confined hosted path without reading the named file. `holyc preprocess --dump-help-metadata` emits the versioned report. The pinned `Kernel/KernelC.HH` test observes all 104 index directives and 20 help-file directives with no diagnostic. Semantic hash insertion and DolDoc loading remain outside this slice.

## JIT and AOT conditional selection

`Kernel/KernelA.HH` defines `CCF_AOT_COMPILE` at bit 35. `Compiler/CMain.HC:CmpBuf` sets the flag before it joins an AOT compilation, while a normal controller starts without it. The OCaml preprocessor mirrors that distinction with an explicit `Jit` or `Aot` configuration and defaults to JIT. Host operating-system details do not affect the selection.

The `KW_IFAOT` and `KW_IFJIT` cases in `Compiler/Lex.HC:Lex` continue into the matching branch. A false condition scans with `LexGetChar` until the first depth-one `#else` or its `#endif`. Nested depth increases for all five opener spellings: `#if`, `#ifdef`, `#ifndef`, `#ifaot`, and `#ifjit`. The scan calls `Lex` only after it sees `#`, so ordinary discarded identifiers do not expand and inactive includes or definitions do not run. `PrsKeyWord` reads the resolved hash entry, which also permits a definition to supply the directive name.

`LexGetChar` removes exhausted include and definition frames during the search. `Frontend.Preprocessor` therefore owns conditional state above individual `Lexer_frame` values, and `Frontend.Lexer.scan_to_directive_marker` advances through inactive bytes without ordinary tokenization. A conditional can open in an included or definition-backed frame and close after that frame returns to its caller.

Outside `CCF_IN_IF`, the pinned lexer discards an unmatched `#endif`, treats an unmatched `#else` as a request to scan forward, and returns EOF without a mismatch diagnostic when a false branch is unterminated. The hosted stream reports `HCPP0015` through `HCPP0018` instead. [Issue #27](https://github.com/frankischilling/holyc-ocaml/issues/27) records that diagnostic difference and the missing oracle fixtures. The `KW_IFJIT` path also returns `TK_IFAOT` while `CCF_IN_IF` is set; that source quirk concerns later `#if` expression parsing and is not part of the mode-only state.

## Symbol-presence conditionals

The `KW_IFDEF` and `KW_IFNDEF` branches in `Compiler/Lex.HC:Lex` set `CCF_NO_DEFINES` while they read one identifier. Identifier lookup still finds the matching hash entry, but a definition's replacement text does not enter the stream. `#ifdef` continues when `cc->hash_entry` is non-null; `#ifndef` continues when it is null.

`CmpCtrlNew` initializes `hash_mask` to `HTG_TYPE_MASK-HTT_IMPORT_SYS_SYM`. The 17 `HTT_*` bits in `Kernel/KernelA.HH` show that this is a compiler-symbol predicate rather than a definition-only predicate. Imports are excluded. `Compiler/CMain.HC:LexStmt2Bin` builds the local and global table chain for the current compilation mode. Before that hash lookup, `Lex` calls `MemberFind` against `local_var_lst`; a local match suppresses `HashFind` and leaves `hash_entry` null.

`Compiler/AsmInit.HC:AsmHashLoad` inserts language keywords, assembly keywords, opcodes, registers, and all 17 records from `Compiler/CInit.HC:internal_types_table` into `cmp.asm_hash`. `Driver.Session` seeds every one of those static compiler names from checked generated data. `Frontend.Symbol_visibility` models every hash kind, masked lookup, and local shadows with deterministic identities and source provenance. The parser can publish later declarations to the same environment between calls to `Frontend.Preprocessor.next`.

The generated seed contains 48 language keywords, 25 assembly keywords, 17 internal types, 106 registers, 325 canonical opcodes, and 49 opcode aliases. The standalone preprocessor still cannot discover unparsed HolyC declarations. The source corpus has no active `#ifdef` or `#ifndef` occurrence, so focused fixtures establish this slice's behavior.

## Conditional and assertion expressions

The `KW_IF` branch in `Compiler/Lex.HC:Lex` sets `CCF_IN_IF`, reads one token, and calls `LexExpression`. It treats the returned 64 bits as the branch predicate and leaves the token following the expression in the ordinary lexer lookahead. `KW_ASSERT` uses the same evaluator but reports a warning when the result is false.

The assertion branch calls `LexWarn(cc,"Assert Failed ")` and then returns through `lex_end`. `Compiler/CExcept.HC:LexWarn` increments `warning_cnt` without incrementing `error_cnt` or throwing. `Frontend.Preprocessor` therefore queues `HCPP0024` as a warning, while `preprocess_detailed` returns that warning beside the retained tokens and the CLI exits successfully. TempleOS prints the warning at the current lookahead token. The hosted renderer instead uses the directive span and records the lookahead as related context; the compatibility report identifies that diagnostic-only difference.

`Compiler/PrsExp.HC:LexExpression2Bin` calls the normal expression parser, appends return operations, compiles the resulting intermediate code, and returns executable memory. `LexExpression` calls that code without forcing the result to I64 or F64. Native conditions can therefore use any expression term available to the current compiler controller, including calls, globals, memory, and compiler state. The hosted constant evaluator implements only the terms that do not need semantic state or execution. [Issue #33](https://github.com/frankischilling/holyc-ocaml/issues/33) records the remaining VM path.

`PrsExpression2` obtains binary identity, precedence, and association from `cmp.binary_ops`. `Compiler/CInit.HC:CmpFillTables` puts power and shifts in `PREC_EXP`, marks power right associated, and leaves shifts left associated. The parser moves unary minus below power unless parentheses force the negative value to become the base. Consecutive comparison operations use `IC_PUSH_CMP` and combine adjacent comparisons. `Compiler/OptPass012.HC` establishes the constant integer, floating, comparison, and logical behavior used by the hosted evaluator. Its result-class assignments make complement and integer shifts I64. Its immediate folding converts U64-class bits to F64 through the signed I64 field, compares F64 equality by raw bits, and retains an F64 result for unary `!`. Logical operations consume both operands rather than introducing a C-preprocessor short-circuit rule.

`PrsUnaryTerm` handles `defined` without setting `CCF_NO_DEFINES`, so ordinary definition expansion may replace its operand first. This is intentionally different from the `KW_IFDEF` and `KW_IFNDEF` paths. A local variable makes `defined` true, while it suppresses hash lookup and makes `#ifdef` false. The default hash mask still excludes imports.

`Frontend.Conditional_expression` parses with the checked operator records, stores explicit I64, U64, and F64 values, and returns its first unconsumed token. `Frontend.Preprocessor` owns the one-token lookahead so selected body text and adjacent conditional boundaries survive expression parsing. The hosted node limit, stable arithmetic diagnostics, and inert recovery after an invalid condition are project safety rules rather than native TempleOS behavior.

## Tokens and operators

`Kernel/KernelA.HH` assigns numeric values to compound tokens. `Compiler/CInit.HC:CmpFillTables` defines two-character tokens, three-character shifts, and expression precedence. `Compiler/OpCodes.DD` supplies the keyword spellings consumed by `Compiler/AsmInit.HC`.

Generated tables retain the complete language keyword list and operator spellings. Numeric TempleOS token IDs remain available for audit and deterministic dumps, while OCaml variants provide compiler identity after lexing.

## Operator recognition and precedence

`CmpFillTables` builds three `dual_U16_tokens` arrays. `Lex` checks them in order. The first array recognizes spellings such as `!=`, `&&`, `*=`, `++`, and `->`; the second handles alternatives with the same first byte, including `&=`, `--`, `<<`, and `^^`; the third supplies `-=` and `/=`. The `/` entries for `/*` and `//` have no token result because they enter nested block-comment or line-comment handling.

Shift assignments are not ordinary entries in those arrays. After recognizing `<<` or `>>`, `Lex` reads one more byte and changes the token when it sees `=`. Dot handling similarly distinguishes a floating literal, `.`, `..`, and `...`. The `$$` form comes from the lexer's dollar handling and has no `TK_*` compound-token ID.

`cmp.binary_ops` contains 31 records. Each record combines a precedence constant and any explicit association flag in the high half with an IC number in the low half. Some entries, including multiplication and addition, have no association flag in the source. `Frontend.Operator` exposes that state as `Unspecified` instead of assigning a conventional default.

`Doc/HolyC.DD` lists the same precedence bands but omits `%=` from its assignment line. `Compiler/CInit.HC` includes `TK_MOD_EQU` mapped to `IC_MOD_EQU`, so the generated source follows the compiler table and records the prose omission as a documentation difference.

`tools/operator_table_source.ml` validates token IDs, association flags, precedence constants, every dual sequence, required IC numbers, all binary mappings, and the lexer-only forms. `tools/operator_table_gen.ml` writes their source lines and the four pinned checksums to `src/generated/operator_tables.ml`. Expression parsing remains outside this slice.

## Opcode database records

`Compiler/AsmInit.HC:AsmHashLoad` parses `Compiler/OpCodes.DD` with the compiler lexer. Register statements select one of eight register kinds and pair a spelling with a numeric register value. `KEYWORD` and `ASM_KEYWORD` statements each contain a spelling and integer ID. An `OPCODE` statement contains one canonical spelling, up to 32 implicitly delimited instruction forms, an optional colon-led alias list, and a final semicolon.

`AsmPrsInsFlags` accepts the eight opcode modifiers, operand-size markers 16 and 32, `+` and `/` selectors, eight punctuation flags, and `$$`. Each form has no more than four opcode bytes and two `ST_ARG_TYPES` operands. Argument sizes come from the five masks initialized by `AsmHashLoad`. The pinned file contains 106 registers, 48 language keywords with IDs 0 through 47, 25 assembler directives with IDs 64 through 88, 325 canonical opcodes, 49 aliases, and 924 forms. The leading comma on `REP_OUTSB` causes the source loader to create an empty first form; the checked data keeps it.

`tools/opcode_table_source.ml` tokenizes and validates that complete grammar. It rejects missing semicolons, duplicate names or IDs, unexpected and reordered records, incomplete keyword ranges, malformed aliases, unknown arguments, out-of-range numeric fields, excess bytes, and excess forms. `tools/opcode_table_gen.ml` writes the deterministic OCaml table with the reference commit, `Compiler/OpCodes.DD` checksum, original source lines, and every parsed field. The lexer and `Asm_directive` use the keyword subsets, while `Driver.Session` publishes registers, canonical opcodes, and aliases with their source hash kinds. [The assembler notes](assembler.md) give the field-level map. Operand matching and encoding remain future assembler work.

## Assembly block dispatch and structure

`Compiler/PrsStmt.HC:PrsStmt` recognizes `KW_ASM`. The top-level AOT path calls `PrsAsmBlk(cc,0)`, while top-level JIT and function compilation use `CmpJoin(cc,CMPF_ASM_BLK)`. `Compiler/Asm.HC:PrsAsmBlk` requires a brace-delimited body, routes `HTT_ASM_KEYWORD` records to directive handling, routes `HTT_OPCODE` records to instruction parsing, and recognizes ordinary, double-colon exported, and `@@` local label forms. `Compiler/AsmLib.HC` supplies the separate assembly expression and line-listing paths. `Compiler/AsmInit.HC` establishes directive identities and opcode alias records. `Doc/Asm.DD` documents the three label forms and typed data directives.

`Frontend.Parser` retains this boundary as an `assembly_block_statement`. `Frontend.Ast` keeps the keyword, braces, source-line display groups, labels, and classified body tokens. `Asm.Opcode`, `Asm.Directive`, and `Asm.Register` resolve spellings through the checked generated data rather than a second handwritten list. Canonical and alias opcodes remain distinct while sharing one canonical identity. Human and JSON AST dumps retain include, expansion, and definition provenance.

`reference/assembly-blocks.json` records a raw-token audit of committed `.HC` and `.HH` files under `Compiler`, `Kernel`, `Adam`, and `Demo`. The pinned tree has 45 brace-delimited blocks in 40 files, including six blocks inside another source block. Tests rescan those roots with the streaming lexer and compare every path, source line, and context with the checked inventory. They also parse the top-level and nested blocks in `Demo/Asm/AsmAndC1.HC` and `Demo/Asm/AsmAndC2.HC` in both compilation modes. Operand grammar, directive semantics, fixups, encoding, and execution are not part of this slice.

## Direct assembly dispatch

`Compiler/PrsStmt.HC:PrsStmt` recognizes `HTT_OPCODE` and `HTT_ASM_KEYWORD` hash entries before ordinary expression dispatch. Without an active function it reports `Use Asm Blk`. With a function it joins compilation through `CMPF_ASM_BLK | CMPF_ONE_ASM_INS`; `Compiler/CompilerA.HH` assigns that second flag bit value 2. In `Compiler/Asm.HC:PrsAsmBlk`, the flag skips the opening-brace path. The routine consults `tmpo->ins[0].arg1` and `arg2` for operand parsing, dispatches assembler keywords by ID, and can continue when the next hash entry is another opcode or assembler keyword.

`Frontend.Parser` implements direct runs as a source-ordered `inline_assembly_statement` item stream. It derives zero-, one-, or two-operand instruction arity from the first checked `OpCodes.DD` form and retains adjacent items without using physical newlines as delimiters. `Compiler/Asm.HC:PrsAsmArg` grounds the public register, immediate, and memory forms, including size and segment prefixes. Each operand keeps its classified tokens, comma, span, and include or definition origin. `Compiler/Asm.HC:PrsAsmDefine`, `PrsBinFile`, and the `PrsAsmBlk` switch ground syntax for `IMPORT`, `DU8`, `DU16`, `DU32`, `DU64`, postfix `DUP`, `BINFILE`, `LIST`, `NOLIST`, `USE16`, `USE32`, and `USE64`. Directive nodes retain optional commas, semicolons, arguments, spans, and provenance without evaluating them or reading a file. The parser also preserves the source restrictions on function-local `ORG`, `ALIGN`, type-sized prefixes, and standalone `DUP`. Visible compiler symbols can shadow opcode, register, and directive spellings. Tests cover exact instruction excerpts from Adam, Kernel, and Demo sources plus mixed directive runs in both compilation modes. `HCPARSE0147` records the top-level restriction; `HCPARSE0149` through `HCPARSE0156` record malformed operands, forbidden direct directives, and malformed directive arguments. Direct labels, directive effects, operand validation, lowering, encoding, and execution remain pending.

## Whole-tree parser measurement

`Compiler/Compiler.PRJ` fixes the source order for the original compiler, and `Compiler/PrsLib.HC`, `PrsExp.HC`, `PrsVar.HC`, and `PrsStmt.HC` provide the parser behavior against which individual syntax slices are audited. The corpus measurement does not reinterpret those files as a formal grammar. It runs the current OCaml parser over every committed `.HC`, `.HH`, and `.PRJ` object so unsupported source stays visible between focused audits.

`Driver.Corpus.Parse` verifies the checkout and exact commit, reads each root from its Git blob, and confines includes to the verified checkout. Its standalone side starts a fresh session for every root at the configured corpus directory. Its comparison side reads the direct include orders from `Compiler/Compiler.PRJ` and `Kernel/Kernel.PRJ`, then builds a prelude from the four direct `.HH` includes in compiler project order: `KernelA.HH`, `CompilerA.HH`, `KernelB.HH`, and `KernelC.HH`. Each target receives copied definition and parser-visible symbol environments plus an explicit working directory equal to the measured source's directory, so declarations cannot leak between targets and sibling includes follow the state that the pinned build and load files establish with `Cd(__DIR__)`. `Compiler/CMisc.HC:82-90` derives that value from the active lexical frame; `Compiler/Lex.HC:698-707` still resolves through the task directory with `FileNameAbs`. This is a project-aware corpus policy, not a general source-relative include rule. The versioned report records the policy, both project directories, and every target's effective directory. Both sides enable the `LexGetChar` physical-NUL boundary for physical sources while leaving ordinary hosted and generated input strict.

The AOT comparison covers all 528 objects at `c26482bb6ad3f80106d28504ec5db3c6a360732c`. Standalone parsing accepts 25 files and reports 36,999 accumulated diagnostics. The project prelude accepts 126 files and reports 20,981 diagnostics; 101 files parse only with the prelude. Its 402 first failures split into 370 unresolved-name cases and 32 other frontend or parser boundaries. Complete single-dollar DolDoc commands inside string literals account for three recent prelude successes: `Adam/Opt/Utils/MemRep.HC`, `Demo/DolDoc/TextDemo.HC`, and `Demo/MsgLoop.HC`; `TextDemo.HC` also succeeds standalone. The direct-assembly class fallback adds `Kernel/KInts.HC`. The two unnamed string definition headings in `KernelA.HH` remove `HCPP0010` from the report without changing a phase classification. All 38 former missing-relative-include first failures advance: 33 stop in the parser and five reach a later generated-source preprocessor boundary. Neither side has a read or internal error. The prelude itself encounters 23 diagnostics while processing the headers, and the report retains that count rather than presenting the seed as a clean compiler build. [`reference/parser-corpus-aot.json`](../reference/parser-corpus-aot.json) records both outcomes and the first error for every path. This is syntax coverage evidence, not evidence that any accepted file type-checks, lowers, or runs.

## Primitive raw types and internal names

`Kernel/KernelA.HH` assigns raw IDs 2 through 15. The low bit is `RTF_UNSIGNED`; `RT_PTR` deliberately aliases signed `RT_I64` at ID 10. The same block marks `RT_F32` as unimplemented, `RT_UF32` as unimplemented and fictitious, and `RT_UF64` as fictitious. These slots remain visible in generated audit data but are not semantic HolyC primitive constructors.

`Compiler/CInit.HC:internal_types_table` contains 17 records. Several raw IDs have more than one name. In particular, `Bool` shares `RT_I8`, while the names ending in `i` provide internal storage for public declarations. `Compiler/AsmInit.HC:AsmHashLoad` inserts every record into the compiler hash and leaves the final record for each raw ID in `cmp.internal_types`.

The public `I16`, `U16`, `I32`, `U32`, `I64`, and `U64` names are unions declared near the start of `Kernel/KernelA.HH`. Their leading internal type names select whole-value storage, while their members expose smaller signed and unsigned views. `Primitive_type` records this union-backed declaration form but does not calculate member layout yet.

`tools/primitive_type_source.ml` validates the constants, the six union headers, and every internal record. `tools/primitive_type_gen.ml` embeds the pinned commit, both source checksums, and original line numbers in `src/generated/primitive_raw_types.ml`. `Sema.Primitive_type` uses those records for the 12 supported semantic identities.

## Declarations and core call expressions

`Compiler/PrsStmt.HC:PrsStmt` checks whether a top-level identifier resolves to `HTT_CLASS` or `HTT_INTERNAL_TYPE`. It advances past that type token and calls `PrsGlblVarLst`. `Compiler/PrsStmt.HC:PrsGlblVarLst` repeatedly calls `Compiler/PrsVar.HC:PrsType`, accepts a comma for another declarator, and otherwise requires and consumes a semicolon. `Compiler/CMain.HC:CmpJoin` calls the statement path repeatedly, preserving top-level source order without requiring `main`.

Before type dispatch, `PrsStmt` recognizes `KW_PUBLIC`, `KW_STATIC`, `KW_INTERRUPT`, `KW_HASERRCODE`, `KW_ARGPOP`, and `KW_NOARGPOP`. `Compiler/CompilerA.HH` assigns the calling keywords IDs 44 through 47 and maps their staging bits directly to `Ff_INTERRUPT` through `Ff_NOARGPOP`. The calling cases retain the existing `FSG_FUN_FLAGS2` group plus assembly state, and the interrupt case adds `FSF_NOARGPOP`. The `public` case follows the same preservation rule. The `static` case retains assembly state while clearing the other staged bits. `PrsFunJoin` copies only `FSG_FUN_FLAGS1` into stored function flags, while `PrsGlblVarLst` separately checks public state when it publishes a name. `Frontend.Parser` keeps each modifier token as an ordered, source-linked AST node. Parser tests reproduce the staging transitions with `Sema.Function_flag.apply_modifier`; parsing does not apply export, linkage, storage, interrupt, or argument-cleanup behavior.

After those modifiers, `PrsStmt` recognizes ordinary `KW_EXTERN` and `KW_IMPORT`, whose keyword IDs are 10 and 27. They select the `PRS0_EXTERN` and `PRS0_IMPORT` declaration modes defined as `0x000004` and `0x000005` in `Compiler/CompilerA.HH`. The `_extern` and `_import` paths use keyword IDs 11 and 28, consume a target identifier before the type, set the `FSF__` staging bit, and select `PRS0__EXTERN` or `PRS0__IMPORT`. `PrsGlblVarLst` rejects both import modes unless `CCF_AOT_COMPILE` is set. `Kernel/KernelC.HH:10` supplies a grouped ordinary example: `public extern U8 *rev_bits_table,*set_bits_table;`. `Kernel/KernelB.HH:37` and `Kernel/KernelC.HH:23-29` supply alternate-name globals, while `Compiler/CExts.HC:42-46` supplies alternate-name arrays. The OCaml parser retains one source-linked binding node after the modifier list, stores an alternate target as a separate identifier, and applies the same AOT gate to explicit imports. Semantic function and global resolution now apply `OPTf_EXTERNS_TO_IMPORTS` from an explicit declaration snapshot before reconciliation, while retaining the original source binding. Alternate target lookup and address resolution remain unavailable.

The option check occurs at `Compiler/PrsStmt.HC:997` for `_extern` and line 1042 for ordinary `extern`. The first branch jumps to the `_import` path and preserves the explicit target name; the second jumps to the ordinary import path and later uses the declared name. `extern class` and `extern union` return through `PrsClass` before line 1042, so the option does not rewrite aggregate forwards. Both converted forms reach the import checks at `PrsGlblVarLst:265-278` for functions and lines 312-323 for globals, which reject JIT compilation. A later AOT code-heap definition still redirects a converted global import because the alias test at lines 350-363 accepts any preceding `HTT_GLBL_VAR`. The semantic tests retain that alias rather than inventing an import barrier for globals.

`KW__INTERN` has keyword ID 14 and selects `PRS0__INTERN`, whose mode value is `0x000002`. Unlike an alternate-name binding, `PrsStmt` obtains its target by calling `Compiler/PrsExp.HC:LexExpressionI64`. The `PrsGlblVarLst` function path assigns that evaluated value to `exe_addr`, sets `Ff_INTERNAL`, and clears `Cf_EXTERN`. The pinned tree contains 60 `_intern` declarations, all in `Kernel/KernelB.HH`, including `Bsf`, `Bt`, `ATan`, and `GetTSC`. The OCaml AST stores this target as a source-positioned expression and accepts it in JIT and AOT modes. Parsing does not evaluate the expression, assign the address, or apply the flags.

`Compiler/PrsVar.HC:PrsType` consumes ordinary `*` tokens before it reads the identifier. It increments `ptr_stars_cnt` for each token and throws an error when the count exceeds `PTR_STARS_NUM`. `Kernel/KernelA.HH` defines that limit as four. `Compiler/PrsLib.HC:PrsClassNew` allocates five adjacent class records for the base type and pointer depths one through four. `Compiler/CExcept.HC:ICClassPut` checks the same bound when assigning a class to intermediate code.

`Frontend.Parser` accepts `modifier* binding? type-specifier declarator (, declarator)* ;`, where a modifier is `public`, `static`, or one of the four calling modifiers. A binding may be ordinary `extern` or `import`, alternate `_extern target` or `_import target`, or `_intern target-expression`. Each declarator starts with zero through four return-pointer stars. It then carries either one identifier or a parenthesized function-pointer indirection, required name, and recursive signature. Array dimensions and an optional scalar or recursively braced initial value follow that core. A type specifier is one of the 12 public primitive spellings, one of the 11 intrinsic storage spellings, or an identifier currently visible as `Symbol_visibility.Class`. The parser reuses the modifier list, binding, and base type across the declaration and restarts declarator pointer, signature, and array state for each name, matching the saved inputs in `PrsGlblVarLst`. Pinned primitive examples include `Demo/MultiCore/Primes.HC:2`, `Kernel/KDefine.HC:16`, and `Kernel/KernelC.HH:10`; the first 27 lines of `Compiler/CExts.HC` supply named class returns and parameters. `Kernel/KernelC.HH:17` supplies the bound `public argpop extern` order. Type names remain ordinary identifier tokens, matching the compiler hash-table design rather than introducing lexer-only type keywords. A local entry shadows a class name for parsing, and an unknown capitalized spelling remains unresolved. Automatic braced local initializers remain rejected; static braced local syntax follows the separate `PrsStaticInit` path described below. The aggregate declaration paths are also described below.

The source audit for named and intrinsic types follows `Compiler/PrsStmt.HC:1028-1053` and `:1156-1172`, where `HTT_CLASS|HTT_INTERNAL_TYPE` controls declaration dispatch, and `Compiler/PrsVar.HC:285-348`, where `PrsType` validates the class and then parses pointer, function-pointer, and array structure. Tests prepend the 11 required forward names and parse the first 27 lines of `Compiler/CExts.HC` without filtering declarations. That produces 24 function prototypes with named return and parameter types, including `DocNew`, `PrsType`, and `PrsClass`. The AST represents source types as `Primitive_type_specifier`, `Internal_type_specifier`, or `Named_type_specifier`; all three retain full source and generated-definition provenance. The representation itself remains syntactic. Later checked passes assign semantic identities to aggregate headers, direct member heads, and top-level function returns and recursive fixed-parameter signatures.

The `KW_EXTERN` branch in `Compiler/PrsStmt.HC` calls `PrsKeyWord` on the next identifier. A `KW_CLASS` or `KW_UNION` result enters `PrsClass` with `is_extern` set, then joins the ordinary semicolon path. In that extern branch, `PrsClass` requires a name, creates the class record, moves the current spelling into it, adds the record to the global hash table, sets `Cf_EXTERN`, attaches its source, and only then calls `Lex`. `Kernel/KernelA.HH:4-17` contains 14 declarations that rely on this early publication. `Doc/ScopingLinkage.DD:46` and `:85` list the form in the JIT and AOT tables.

The OCaml parser routes exact ordinary `extern` plus `class` or `union` prefixes before binding parsing. Its AST keeps the modifiers, binding, distinct aggregate kind, keyword, name, semicolon, and complete source segments. After consuming the semicolon, it publishes the name with class identity before requesting later input. The same syntax works in JIT and AOT modes, repeated forwards remain in source order, and a following conditional or declaration sees the name. Missing names use `HCPARSE0107`. A missing semicolon, comma list, or body uses `HCPARSE0108`; body recovery tracks braces so a member semicolon does not terminate recovery early. The parser can retain the published name as a named type specifier in globals, function returns and parameters, nested function pointers, automatic and static locals, arrays, and postfix casts. The parser does not clear extern state, compare duplicate declarations, or construct layout. Semantic aggregate reconciliation later assigns the canonical identities consumed by header and direct-member type resolution.

`Compiler/PrsStmt.HC:PrsClass` handles a nonextern `class` or `union` by requiring the common `TK_IDENT` token, reusing an extern entry when available or creating a new `CHashClass`, inserting the selected entry before `Lex`, and clearing `Cf_EXTERN`. It does not call `PrsKeyWord` for the aggregate name, so keyword spellings are ordinary names here. If the next `Lex` returns a colon, the routine requests one selected `HTT_CLASS` symbol, rejects a following comma, stores the base pointer, and adds the base size before it calls `PrsVarLst`. This makes a visible keyword-spelled class win before keyword dispatch in later type and base positions. `Compiler/PrsVar.HC:PrsVarLst` skips empty semicolons, records the current class or union base, recursively handles an unnamed `union`, parses each member through `PrsType`, restarts a declaration group after a comma, and finishes at the matching brace. It passes a temporary function-signature slot to `PrsType`; a returned callback sets `CMemberLst.fun_ptr` and `MLF_FUN`. In class mode it also enables `CCF_CLASS_DOL_OFFSET` and accepts repeated `$$ = LexExpression;` directives before the next member. The later `PRS1B_CLASS` branch assigns offsets and metadata. `Doc/HolyC.DD` describes named unions as class-like after declaration, permits one base class, and defines `$$` as the current class offset in this context. The consecutive `Kernel/KernelA.HH:116-179` prefix supplies 12 ordinary definitions with primitive members, self references, named member types, grouped names, pointers, and arrays.

`Frontend.Parser` models these bodies with a distinct aggregate-definition item. It publishes the name immediately after reading it and before looking ahead for a base or the opening brace, so source-shaped self references and preexisting forwards share the class-symbol path. An optional base node retains the colon and one visible class or union name as separate source-spanned values. Each ordinary member group retains its primitive, intrinsic, or visible named type and source-ordered declarators. Each declarator independently retains zero through four return-pointer layers, an optional recursive function-pointer signature, array dimensions, metadata entries, and its comma or semicolon. Its name may use any checked language-keyword spelling because TempleOS treats keyword recognition contextually, the same rule the parser applies to global, parameter, and local declarators. Recursive anonymous unions, empty member semicolons, and aggregate-offset directives remain explicit nodes. An offset node retains the `$$` marker, equals sign, ordinary expression tree, semicolon, complete span, and generated-source provenance. Both braces, a standalone semicolon or attached global list, all generated-source segments, and the class versus union spelling appear in stable human and JSON dumps. JIT and AOT parsing use the same syntax. `HCPARSE0121` rejects an invalid base and `HCPARSE0126` rejects a second base. Other `HCPARSE0109` through `HCPARSE0125` diagnostics cover missing delimiters and nested named definitions; `HCPARSE0133` and `HCPARSE0134` cover callback-member punctuation and names; `HCPARSE0142` and `HCPARSE0143` cover offset punctuation; and `HCPARSE0144` reports a metadata name without a value. Anonymous-union and expression recursion are each capped at 256 for hosted safety. Aggregate reconciliation, header resolution, direct-member and callback type references, member-list callback flags, closed byte layout, inherited member lookup, and duplicate checks are implemented in later semantic passes. Symbol-dependent offset and extent evaluation, metadata evaluation and lookup, backing conversions, subinteger access, storage allocation, and linkage remain pending.

The intended Compiler, Kernel, Adam, and Demo source set contains four direct aggregate-offset directives. `Demo/Lectures/NegDisp.HC:35` uses `$$=-128;`; `Kernel/KernelA.HH:447` and `:3422` use alignment expressions that include the current `$$`; and `Kernel/KernelA.HH:3806` uses `$$=64;`. Tests retain those expressions without rewriting them, cover the shared class, named-union, and anonymous-union member loop in JIT and AOT modes, and verify macro and include provenance. No native oracle is required for syntax acceptance because `PrsVarLst` states the dispatch and punctuation directly. The closed semantic pass now applies literal and `$$` forms; expressions that need symbol, `sizeof`, `offset`, or compile-time execution state remain explicit dependencies.

`Compiler/Lex.HC:493-514` returns `TK_IDENT` after attaching an identifier's hash entry. `Compiler/PrsLib.HC:31-37` turns such a token into a keyword identity only when `PrsKeyWord` is called, while `Compiler/PrsVar.HC:310-347` returns to the ordinary identifier path for a declarator name. `Compiler/PrsVar.HC:223` and `:521` show that globals, parameters, locals, and members all reach that one routine, and `Compiler/PrsExp.HC:335`, `:373`, and `:996` repeat the test for `sizeof`, `offset`, and ordinary member access. The OCaml frontend keeps keyword token kinds for clearer dumps, then admits them through one shared name-position predicate covering those seven slots. Class and union type names and base names are unchanged. The intended source set contains 12 direct `U0 start;` members: eight in `Kernel/KernelA.HH`, two in `Adam/Gr/Gr.HH`, and two in `Adam/Gr/SpriteMesh.HC`. Eight further corpus files reach the other slots, among them `Demo/TimeIns.HC:10`, `Kernel/BlkDev/DskFmt.HC:3`, `Adam/Gr/GrMath.HC:9`, `Kernel/KExts.HC:8`, and `Adam/Gr/SpriteNew.HC:41`. Tests cover all checked keyword spellings in both modes, every aggregate member path, recursive callback names, provenance, unchanged statement-keyword dispatch without an active local, the complete `KernelA.HH:430-449` `CKernel` body, and the pinned local, parameter, and `offset` lines.

`Compiler/Lex.HC:493-499` checks the current function member list before the global hash tables. `Compiler/PrsExp.HC:761-766` handles that local-variable entry before the keyword switch at lines 910-947, and `Compiler/PrsStmt.HC:1183-1184` routes the same condition through expression parsing. The OCaml parser now performs the same active-local check before expression and statement keyword dispatch. Parameters, prior locals, nested-block locals, and definition-generated names retain ordinary identifier nodes and provenance. Keyword spellings without an active local binding still follow the normal keyword path. `Adam/Gr/GrMath.HC` consequently advances from `j<start` at line 57 to the unresolved `CTask` type at line 96. `Demo/TimeIns.HC` advances to its unsupported `#exe`, moving one first failure from parser to frontend classification. No native oracle is needed because the pinned lexer and parser state the lookup order directly.

`Compiler/PrsStmt.HC:209-409` inserts each `HTT_GLBL_VAR` into the compilation global table. `Kernel/KHashA.HC:169-189` places a new entry at the head of its bucket; lines 40-106 return the first matching entry while walking tables in chain order. `Compiler/CMain.HC:60-75` puts compilation globals before `cmp.asm_hash` for AOT and before `Fs->hash_table` for JIT. A same-named global and keyword therefore coexist, but `Lex` selects the global. `PrsKeyWord` no longer reports a keyword, `PrsStmt.HC:1156-1179` chooses an expression statement, and `PrsExp.HC:867-904` handles the global variable. `Frontend.Parser` applies that source-ordered rule at top level and inside functions while retaining keyword behavior before publication. Focused JIT and AOT tests cover arithmetic, statement-leading expressions, function-body reads, and definition provenance. A scan of the pinned Compiler, Kernel, Adam, and Demo sources found no authored top-level global-variable example, so the acceptance fixture is synthetic. Recovery after unsupported function headers parses local declarations at top level in `Adam/DolDoc/DocGet.HC`, `Apps/Psalmody/JukePuppet.HC`, and `Kernel/FunSeg.HC`; the new rule removes 11 later recovery diagnostics without changing first errors.

`Compiler/PrsStmt.HC:62-138` and `PrsFunJoin` insert a named `HTT_FUN` into the same compilation table. The chain and hash selection above therefore apply before `PrsExp.HC:842-865` dispatches the function record to `PrsFunCall`. `Frontend.Parser` retains the selected function shape for explicit and parenthesis-free calls and leaves address-of as an identifier operand. Tests cover publication order, default slots, JIT and AOT modes, and definition-generated provenance. A scan of all 48 checked keyword spellings found no authored top-level function example in the pinned Compiler, Kernel, Adam, or Demo roots. The synthetic fixtures establish the boundary, and the complete AOT parser corpus report is byte-for-byte unchanged.

`Compiler/PrsVar.HC:496-520` bounds the register qualifiers: `PrsVarLst` reads `reg` and `noreg` before calling `PrsType`, and `PrsType` never looks for them again. `I64 *reg value` therefore names the parameter `reg` and reports the following `value`, which is what the OCaml parser now does. `HCPARSE0013` remains for a qualifier that follows a complete parameter.

`Compiler/PrsVar.HC:660-688` handles member metadata after layout has assigned a member size and offset. It repeats while the next token is an identifier, stores that spelling as the metadata name, and chooses `LexExtStr` for a string or `LexExpression` for every other value. `Compiler/LexLib.HC:47-64` defines name lookup, while lines 248-276 join adjacent string tokens. `Kernel/KernelA.HH:792-799` defines `MLMF_IS_STR` and `CMemberLstMeta`. The OCaml AST retains source-ordered entries on each declarator, distinguishes an extended string from an expression, records every string segment and its combined decoded bytes, and preserves duplicate names. Tests parse all six metadata-bearing members and 13 entries in `Demo/ClassMeta.HC:16-28` in JIT and AOT modes, then cover callback, array, pointer, comma-group, anonymous-union, contextual-keyword, generated, included, and malformed forms. Runtime list construction, value evaluation, lookup precedence, and `MLMF_IS_STR` remain semantic work.

When the token after the closing brace is not a semicolon, `Compiler/PrsStmt.HC:PrsClass` calls `PrsGlblVarLst` with the completed aggregate type. `PrsGlblVarLst` handles pointer and array suffixes, comma groups, publication, and initialization. `Compiler/PrsVar.HC:PrsVarInit`, `PrsVarInit2`, and `PrsGlblInit` establish the recursive scalar and braced initializer shape. The OCaml AST retains these global declarators directly on the aggregate definition and uses the same initializer tree for ordinary globals. A byte-preserving structural scan finds 40 valid attached-global definitions in the pinned Compiler, Kernel, Adam, and Demo `.HC` and `.HH` set: six pointer forms, 18 array forms, eight comma groups, and four initialized forms. Tests parse the exact 17 initialized descriptor rows at `Compiler/CInit.HC:1-14` in both compilation modes. `HCPARSE0127` through `HCPARSE0130` cover malformed initial values and the 256-level hosted nesting limit. Initializer typing, evaluation, storage, and relocations remain pending.

The pinned Compiler, Kernel, Adam, and Demo `.HC` and `.HH` set contains 37 inherited class headers and no inherited union header. This count uses a byte-preserving scan because four Demo headers live in files with binary payload markers. Tests parse all 17 definitions in `Adam/Gr/Gr.HH:18-115`, including its base plus 16 derived sprite classes, in both modes. Representative kernel fixtures retain `CHashSrcSym:CHash`, `CHashExport:CHashSrcSym`, and `CHashFun:CHashClass`. A focused union fixture covers the shared source path without treating it as corpus evidence.

`Compiler/PrsVar.HC:PrsType` consumes a checked class or internal type plus zero through four pointer stars before it tests for `KW_CLASS` or `KW_UNION`. On that path it calls `PrsClass` and stores the preceding type in `fwd_class`. `Compiler/CInit.HC:1-14` defines the exact intrinsic spellings and raw types. `Kernel/KernelA.HH:67-113` uses this form to build the six public integer unions, while `CDate`, `CICType`, `CAbsCntsI64`, and the color wrappers use public integer backings. `Doc/HolyC.DD` ties this syntax to whole-value and subinteger access. The OCaml aggregate node retains the optional backing type, every backing pointer layer, and complete provenance. `HCPARSE0124` rejects an unknown backing and `HCPARSE0125` rejects a declaration binding before a backed definition. Tests parse the pinned forms in JIT and AOT modes. Direct fixed-call target policy now follows these backing chains for the narrow `PrsFunCall` raw-type comparison. General backing compatibility, whole-value conversion, subinteger access, and layout effects remain pending.

`Compiler/PrsVar.HC:PrsArrayDims` runs at the end of `PrsType`. It accepts `[]` only while the linked dimension list is empty, evaluates each nonempty dimension with `LexExpressionI64`, rejects negative results, multiplies the linked `total_cnt` values, and checks the closing bracket. The parser implements the syntax boundary: ordered bracket nodes, an optional first expression, and the shared source-precedence expression tree. It does not evaluate signs, compute totals, or allocate storage. `Compiler/Lex.HC:322` supplies an expression dimension. `Compiler/Asm.HC:393` supplies an array suffix followed by an initializer; the parser now retains that complete declaration without evaluating it. `Adam/Gr/GrGlbls.HC:28-35` supplies pointer and multidimensional member forms represented by aggregate member nodes. `PrsArrayDims` rejects all array suffixes in `PRS1B_FUN_ARG`, and the prototype parser retains that rejection as `HCPARSE0011`.

If the token after a declared name is `(`, `PrsGlblVarLst` dispatches bound modes to `PrsFunJoin` and unbound mode to `PrsFun`. `PrsFunJoin` registers or updates a `CHashFun`, retains the return class, and calls `PrsVarLst` with `PRS1_FUN_ARG`. In that mode, `PrsVarLst` accepts types, pointer layers, optional parameter names, commas, defaults, register requests, and a terminal `TK_ELLIPSIS`; `PrsDotDotDot` marks `Ff_DOT_DOT_DOT`. `PrsArrayDims` rejects an array suffix when the parser mode is `PRS1B_FUN_ARG`. `Kernel/KernelB.HH:44`, `122-163`, and `270-274` supply alternate-name prototypes, while `Compiler/CExts.HC:10-40` supplies ordinary prototypes. Unbound examples include `Compiler/CMisc.HC:Option`, `Kernel/FunSeg.HC:HasLower`, `Adam/AMem.HC:TaskMemAlloced`, and `Demo/Exceptions.HC:D1`.

`PrsFun` shares `PrsFunJoin` for header construction and then calls `PrsStmt` once. Since `PrsFunJoin` registers the function first, its name is available to a recursive body and to preprocessing triggered before the first body token. A semicolon is an explicit empty statement. At TK_EOF, `PrsStmt` follows its `sm_done` exit without constructing a body statement. The OCaml AST keeps those outcomes separate as an empty-statement body and an absent optional body. It also keeps bound forms on the prototype path, so `extern U0 F() {}` is not accepted as an unbound definition.

`PrsVarLst` processes `KW_REG` and `KW_NOREG` both before a type and immediately after it. `reg` assigns `REG_ALLOC`; `noreg` assigns `REG_NONE`. Only `reg` attempts to consume a following identifier, and it does so only when `DefineMatch` finds that spelling in `ST_U64_REGS`. `Compiler/CInit.HC:222-223` defines the accepted sequence as the 16 canonical U64 registers from `RAX` through `R15`. `Kernel/KernelA.HH:1797-1799` assigns `REG_UNDEF=-128`, `REG_NONE=32`, and `REG_ALLOC=33`. Repeated qualifiers are legal, with later assignments replacing `_reg` for compiler behavior. `PrsVarLst` passes the resulting value to both `MemberLstNew` and `PrsDotDotDot`; the latter gives the same request to `argc` and `argv`. `Compiler/LexLib.HC:157-180` omits `reg` from `MemberLstCmp`, so the request does not participate in header identity. `Sema.Register_request` keeps the ordered source requests, validates canonical register numbers, and derives the final source value. Every recursive signature driver uses that representation, while `Sema.Local_type_resolution` keeps its existing public accessors over the same checked value. `Doc/Options.DD` places `OPTf_NO_REG_VAR` at a later optimization boundary rather than changing this grammar. Allocation, request conflicts, option application, and ABI lowering are not implemented.

The OCaml prototype parser accepts bound forms whose return and parameter types are primitive or visible class names. It preserves ordered declaration and calling modifiers, return pointers, both parentheses, comma-separated named or unnamed parameters, parameter pointers, recursive function-pointer parameter declarators, optional ordinary or `lastclass` defaults, a distinct terminal `...`, and the semicolon. It also preserves every `reg` or `noreg` qualifier, its position before or after the type, and an optional explicit U64 register. The accepted explicit-register set is derived from the first checked R64 record for register numbers 0 through 15 in `Generated.Opcode_table`; aliases and narrower register families remain parameter identifiers. Qualifiers that follow a complete parameter receive `HCPARSE0013`. A completed prototype is published with `Symbol_visibility.Function` only after its semicolon. JIT imports fail before the function name can be published. The definition parser accepts the corresponding unbound header, publishes it after the closing parenthesis, and passes its body through the common statement parser. Separate semantic passes now create the function scope and resolve return and recursive fixed-parameter types. Register allocation, flag validation, default substitution, header matching, storage, and linkage remain outside this syntax slice.

When `cc->htc.fun` is active, `Compiler/PrsStmt.HC:PrsStmt` routes a known `HTT_CLASS` or `HTT_INTERNAL_TYPE` to `PrsVarLst` with automatic-local mode. A leading `static` selects `PRS0_STATICS` and the same local list path. In automatic mode, `PrsVarLst` accepts `reg` and `noreg` immediately after the shared type for each declarator. An automatic `reg` may consume a following spelling from `ST_U64_REGS`. The routine then parses pointer layers, the local name, array dimensions, an optional initializer, and a comma or semicolon. Automatic ordinary initializers use one `PrsExpression`; static ordinary initializers enter `PrsStaticInit` and its recursive `PrsVarInit2` path. It calls `MemberAdd` before compiling the initializer. The parser mirrors that publication point, so the new name is available in its own initializer and later declarators.

`Adam/Snd/SndMath.HC` demonstrates repeated `reg` qualifiers in one group. `Demo/Asm/AsmAndC1.HC` uses `noreg`, `Demo/Asm/AsmAndC2.HC` requests `R15`, and `Kernel/KMisc.HC` groups initialized static locals. `Frontend.Ast.local_declaration` retains storage, the shared primitive or named type specifier, and ordered declarators. Each declarator retains its register request, explicit register, pointer layers, optional recursive callback signature, dimensions, initializer, and delimiter. Scalar initializers remain unchanged on both storage classes; static ordinary locals may also retain recursive braced trees. The parser also publishes named parameters before entering a body and exposes the synthetic `argc` and `argv` names in variadic functions. These names live in one function-wide syntax context because the source stores locals on the function member list rather than a nested block table. Semantic name binding, offsets, storage, lifetime, shadowing rules, register allocation, initializer evaluation, dimension inference, and array layout remain pending. Automatic braced initializers receive `HCPARSE0104`, matching the pinned expression-only branch.

`Compiler/PrsVar.HC:215-240` defines `PrsStaticInit`; lines 534-589 call it for static locals, while lines 590-618 call `PrsExpression` for automatic locals. Direct pinned examples are `Adam/WinMgr.HC:38`, `Demo/GlblVars.HC:21-25`, and `Demo/GlblVars.HC:62-66`. A line-start scan found exactly those three braced forms among 21 static declarations in the intended Compiler, Kernel, Adam, and Demo `.HC` and `.HH` set. That scan records direct evidence rather than a complete grammar census. The parser shares the recursive initial-value node with globals, preserves the existing scalar dump schema, and uses `HCPARSE0138` through `HCPARSE0141` for malformed static trees. The fixed hosted nesting limit is 256. Brace-aware recovery leaves the following declaration available. No native oracle is needed for this syntax boundary because the two source branches name their parser routines directly.

`Compiler/PrsVar.HC:408-524` passes the callback metadata returned by the shared `PrsType` path into the local member, then sets `MLF_FUN`. Its automatic-storage and initializer work is at lines 590-618, and its comma restart and termination are at lines 691-714. The OCaml local declarator therefore reuses `Frontend.Ast.function_pointer_declarator`, including outer return pointers, one through four callback indirections, recursive parameters, defaults, varargs, arrays, register requests on automatic declarations, and source provenance. The accepted name is published as a function-local variable before the next token is requested. That local shadows class and direct-function syntax without becoming a directly callable function entry. The two directly audited automatic declarations are `Demo/Games/Stadium/StadiumGen.HC:5` and `Demo/Graphics/Grid.HC:13`; this is evidence for those lines, not an exhaustive grammar census. No static callback local was found in the pinned corpus, so static syntax is covered through the shared source path without a corpus claim. `HCPARSE0135` reports malformed punctuation, `HCPARSE0136` reports a missing name, and `HCPARSE0137` rejects direct callback initialization at the boundary documented by the `Grid.HC` comment. Both pinned examples assign their callbacks later. Semantic function-pointer construction and typed indirect-call checking are implemented for parameter, automatic, and static callback values. Storage, callback value reads, lowering, and execution remain pending.

`Compiler/PrsVar.HC:PrsType` first consumes zero through four return pointer stars. Its function-pointer branch then requires `(`, one through four more stars, an optional identifier in function-argument mode, `)`, and a signature opening parenthesis. It calls `PrsFunJoin` with a null name and stores the resulting metadata through `CMemberLst.fun_ptr`; `PrsVarLst` marks that member with `MLF_FUN`. Since `PrsFunJoin` calls `PrsVarLst` again, function-pointer parameters may nest. Pinned examples include `Kernel/QSort.HC:1`, `Kernel/KernelB.HH:49-53`, and `Kernel/KernelC.HH:17-22, 517, and 603-605`. `Frontend.Ast.function_pointer_declarator` keeps the two pointer depths distinct and recursively records signature parameters. A hosted depth limit of 32 rejects deliberately nested denial-of-service input with `HCPARSE0017`; this limit is not a TempleOS language rule.

The same `PrsType` result reaches `PrsGlblVarLst` through `tmpf_fun_ptr`. That branch allocates pointer-sized storage, copies the signature into `CHashGlblVar.fun_ptr`, sets `GVF_FUN`, and still inserts a global-variable hash entry. The OCaml parser follows that split: a global declarator may carry the recursive function-pointer node, but its published symbol remains `Global_variable`, so parenthesis-free direct-call routing does not treat it as a function. Return stars, one through four declarator stars, defaults, nested callbacks, arrays after the signature, initializers, comma groups, and aggregate-attached forms retain their own locations. The four definitions at `Kernel/KGlbls.HC:32-35` and the matching `extern` declarations at `Kernel/KernelC.HH:256,396,517,719` parse in both JIT and AOT modes. Together with the initialized form at `Adam/ABlkDev/FileMgr.HC:568` and the callback array at `Demo/Graphics/WallPaperFish.HC:55`, these make ten directly evidenced lines. This line-anchored byte audit is not a structural whole-corpus count. `HCPARSE0131` reports missing global callback punctuation, `HCPARSE0132` reports a missing stored name, and the shared nesting guard remains `HCPARSE0017`. Semantic type construction and typed indirect-call checking for function-pointer globals are implemented. Storage, callback value reads, lowering, and execution remain later work.

The class and union branch keeps the same recursive `PrsType` result on the member rather than forwarding it to `PrsGlblVarLst`. The OCaml aggregate-member declarator mirrors that distinction: it keeps return stars outside the callback, one through four callback indirections, recursive parameters and defaults, a variadic marker, array suffixes, and its group delimiter, but it never publishes the member as a top-level symbol. Pinned tests cover `CMathODE` at `Kernel/KernelA.HH:251-289`, `CDocEntry` at lines 1191-1222, and `CKeyDevEntry` plus `CKeyDevGlbls` at lines 3754-3771 in JIT and AOT modes. The `CDocEntry` fixture excludes only line 1221, `#assert !($$&7)`, because the bounded constant preprocessor does not accept runtime `$$`; the test retains the source through the closing brace at line 1222. A line-anchored scan finds 26 exact `(*name)(` or `(**name)(` member forms in `KernelA.HH`. This is a checked set of locations, not a complete declarator grammar census. `HCPARSE0133` reports malformed member callback punctuation, `HCPARSE0134` reports a missing member name, and `HCPARSE0017` remains the shared recursive limit. The member-type pass resolves the callback return head and complete recursive signature and records `MLF_FUN` (`0x8`); closed layout assigns pointer-sized storage and an offset, and the member index exposes direct and inherited lookup. Default binding and evaluation, call checking, lowering, and execution remain pending.

The local callback checks are reproducible through `test/test_parser.ml`, `test/cli/parse-function-pointer-local.hc`, and the paired human, JSON, and error goldens. Those tests retain the exact two pinned declarations, run direct shapes in JIT and AOT modes, and cover function-local visibility, generated-source provenance, stable dumps, `HCPARSE0135` through `HCPARSE0137`, and recovery to a following global. No native oracle was needed for this syntax slice because the checked `PrsType` and `PrsVarLst` paths state the callback handoff directly.

In `Compiler/PrsVar.HC:PrsVarLst`, the `PRS1B_FUN_ARG` branch checks for `=` on each member. A following `KW_LASTCLASS` sets `MLF_LASTCLASS` and consumes exactly that token. Every other default is compiled with `LexExpression2Bin`, executed, converted to the parameter type, and copied when it produces string storage. Both branches then set `MLF_DFT_AVAILABLE`, and there is no rule requiring defaults to trail. The OCaml AST mirrors this split with distinct expression and `lastclass` default values while retaining the shared equals sign. `Compiler/PrsExp.HC:PrsExpression2` supplies the precedence loop and its special unary-minus handling for `IC_POWER`; `PrsUnaryTerm` supplies the supported primary and prefix forms. The binary metadata comes from `Compiler/CInit.HC`, with precedence constants from `Compiler/CompilerA.HH`.

`Frontend.Parser` uses that checked binary table directly for ordinary parameter defaults, array dimensions, call arguments, indexes, member chains, postfix updates, primitive or named postfix casts, `sizeof`, `offset`, and `defined` terms, and `_intern` targets. Only entries marked `ASSOCF_RIGHT` group to the right; all remaining entries group to the left, as the source loop does. The parser represents an absent default separately from an expression or `lastclass` value and applies the same split inside function-pointer signatures. Every term, operator, and `lastclass` keyword retains source and definition provenance. This stage does not execute expressions, perform the source conversions, set member or internal-function flags, store strings, fill omitted call arguments, validate index, member, update, cast, `sizeof`, or `offset` operands, resolve the truth of a `defined` term, or assign executable addresses. Unrecognized cast targets and special forms receive `HCPARSE0020`, while a fixed 256-level depth guard reports `HCPARSE0021`.

`Compiler/PrsExp.HC:PrsFunCall` accepts an opening parenthesis when present and walks the fixed member list in declaration order. For a member carrying `MLF_DFT_AVAILABLE`, a current `)`, a current `,`, or a call made without parentheses selects the stored default. `PrsFunCall` tracks the optimized class of each explicit argument. When a selected default also carries `MLF_LASTCLASS` and a previous explicit argument established a class, it substitutes that class's unstarred name string. Otherwise it leaves the stored default value in place. Parsing retains the declaration marker but does not perform this call-time substitution. `Doc/HolyC.DD` documents both parenthesis-free default-only calls and the non-trailing default example `Test(,3);`. The pinned compiler itself uses consecutive holes in `PrsStmt(cc,,,cmp_flags)`, leading holes in `GetStr(,,GSF_SHIFT_ESC_EXIT)`, and a middle hole while loading `OpCodes.DD`.

The OCaml AST distinguishes `Parenthesized_call` from `Parenthesis_free_call`. A parenthesized node keeps both delimiters and an ordered slot list. A slot is explicitly omitted or contains an expression, and it owns the comma that follows it. Omitted slots have zero-width source locations at their insertion points, with definition and generated-source provenance copied from the delimiter. Calls bind before all audited binary operators and can nest or follow a parenthesized callee. `HCPARSE0024` reports a missing argument separator and `HCPARSE0025` reports a missing closing parenthesis.

The `HTt_FUN` branch in `PrsUnaryTerm` consumes a direct function name and enters `PrsFunCall` without requiring `(`. When parentheses are absent, every defaulted fixed parameter selects its stored default without consuming input. Every required fixed parameter instead calls `PrsExpression`, and the variadic-extra loop is skipped because `needs_right_paren` is false. The separate `&` branch returns a direct function address. `Kernel/KExcept.HC:throw` supplies a common two-default example. The source documentation covers `Dir;`, while a controlled native run additionally confirms adjacent required expressions, default holes, an empty variadic tail, binary continuation, and address-of suppression.

Each accepted function symbol now retains a compact call shape containing source-order fixed parameters, optional names, default availability, and the variadic marker. Definitions publish it before parsing their body, which permits recursive parenthesis-free calls. A local name or a newer nonfunction symbol suppresses the direct-call path. The call node records every defaulted fixed parameter as an omitted slot and every adjacent required expression as a provided slot. It does not fabricate parentheses or commas. `HCPARSE0105` reports a missing required expression, and `HCPARSE0106` rejects a parenthesis-free call when its function entry lacks retained call shape. An explicit parenthesized call does not require that parser shape. Human and JSON dumps preserve include and definition provenance. `test/oracle/parenthesis-free-calls.json` records all seven native checks against commit `c26482bb6ad3f80106d28504ec5db3c6a360732c`. Direct calls receive fixed and variadic semantic slots through `HC-SEMA-DIRECT-CALL-SLOT-RESOLUTION`; typed function-pointer values receive the same slot rules through `HC-SEMA-TYPED-INDIRECT-CALLS`; and checked direct or pointer member callbacks use `HC-SEMA-MEMBER-CALLBACK-CALLS`. All three paths feed the checked target policy and expression decisions. Default evaluation and substitution, callback storage reads, arbitrary computed callback calls, IC flag emission, lowering, and execution remain pending.

The address-of safeguard has a matching semantic rule. `Compiler/PrsExp.HC:620-671` recognizes `&` followed by a noninternal `HTt_FUN` before generic address construction. JIT chooses either the external `exe_addr` slot or a definition address; AOT rejects a bare extern outside assembly and otherwise creates `IC_ABS_ADDR`. Both paths assign `RT_PTR`, whose generated primitive table entry aliases `RT_I64`. `Sema.Function_call_resolution` therefore keeps a distinct direct-function binding shape with the selected `Module_expression_binding.publication`, exact `Function_resolution.resolved_declaration`, and a checked JIT extern-slot, JIT immediate, or AOT absolute path. `Sema.Function_call_expression_result` preserves that identity and the internal zero-depth type through address-of. `PrsStmt.HC:235-276` establishes the relevant state: `_extern` clears `Cf_EXTERN`, while ordinary imports retain it; `_intern` clears it but sets `Ff_INTERNAL`, keeping that spelling outside the noninternal address branch. Ordinary unresolved AOT externs and imports now fail at the source operator, and internal functions fail explicitly rather than being assigned a false path. No host address, IC instruction, import, relocation, or AOT fixup is created. [Issue #286](https://github.com/frankischilling/holyc-ocaml/issues/286) records function-value retention, and [issue #288](https://github.com/frankischilling/holyc-ocaml/issues/288) records mode selection.

The `[` branch of `Compiler/PrsExp.HC:PrsUnaryModifier` calls `PrsExpression`, requires a matching `]`, and returns `PE_UNARY_MODIFIERS`, so indexes can repeat or mix with calls. The same branch consults `_tmpad`, resolved classes, pointer depth, array and register flags, and element sizes while constructing address arithmetic. The OCaml AST stores the source-level base, brackets, and index expression. It assigns the same postfix binding used by calls and emits `HCPARSE0026` when the closing bracket is missing.

For semantic typing, local and global construction at `PrsExp.HC:761-904` supplies the adjacent class plus separately retained array dimensions. The index branch rejects a base without pointer stars, assigns `tmpc1=tmpc-1` for a pointer element, and updates `_tmpad` for an array dimension. It sets `ICF_RES_TO_INT` on the subscript before multiply and add form the address. A partial multidimensional index retains array and register-address state; the final index clears it before the later dereference boundary. The OCaml expression result follows those transitions without performing the arithmetic. It carries declaration-owned array rank, removes one rank or pointer layer per bracket, keeps primitive form and aggregate identity, and records the subscript's integer-conversion requirement separately. `HCSEMA0046` reports a proven scalar or exhausted array at the rejected opening bracket. Stride calculation, scaling, address formation, storage access, and IC lowering remain unimplemented.

The `.` and `TK_DEREFERENCE` cases in `PrsUnaryModifier` converge before requiring `TK_IDENT` and calling `MemberFind`. The source keeps direct and pointer access distinct while it determines whether the current expression represents an address, value, pointer, or register-backed object. `CmpFillTables` constructs `->` as the `TK_DEREFERENCE` compound token, whose numeric ID is defined in `Kernel/KernelA.HH`. `MemberFind` in `LexLib.HC` searches the current class before following its one `base_class` link at each step. After lookup, `PrsUnaryModifier` adds `CMemberLst.offset`, replaces the current class with `member_class`, restores retained array dimensions, and uses `MLF_FUN` plus `PrsPopDeref` for a callback field. The OCaml semantic source view retains the base, access kind, operator and member origins, and spelling. Expression-result analysis applies the direct or pointer shape check, requires a completed aggregate published before the caller, performs current-before-base lookup, and retains the queried and declaring aggregates, inheritance depth, stable member symbol, resolved type, byte offset, dimensions, and callback flag. It assigns array, callback, lvalue, or object categories without performing address arithmetic or reading storage. `HCPARSE0027` reports a missing identifier; `HCSEMA0046` reports a known invalid base, incomplete aggregate, missing member, or foreign member index. An unavailable outer base stays unresolved. Offset addition, final dereference, use counts, indirect calls, and IC lowering remain unimplemented.

The `TK_PLUS_PLUS` and `TK_MINUS_MINUS` cases in `PrsUnaryModifier` set `CCF_POSTINC` or `CCF_POSTDEC`, set the postfix precedence to `PREC_UNARY_POST`, consume one token, and return `PE_DEREFERENCE`. `PrsExpression2` maps those flags to `IC__PP` or `IC__MM`, whose numeric identities are `0x27` and `0x28`. Unlike calls, indexes, and members, the update does not return to `PE_UNARY_MODIFIERS`. The OCaml parser records the operand, update kind, operator, and combined provenance in a distinct postfix node. It then permits only a binary continuation. `HCPARSE0028` reports a call, index, member, or second update that attempts to continue the postfix chain. Semantic expression typing now requires a checked primitive or pointer lvalue and preserves its type and class. Old-value behavior, mutation, pointer scaling, and IC lowering remain unimplemented.

The `(` case in `PrsUnaryModifier` first handles a function expression through `PrsFunCall`. For any other operand it calls `PrsType` with `PRS0_TYPECAST`, rewrites the current IC class, adds `IC_HOLYC_TYPECAST`, requires `)`, and returns `PE_UNARY_MODIFIERS`. `PrsUnaryTerm` separately rejects a parenthesis whose first token resolves to a class or internal type and directs the user to postfix syntax. `Doc/HolyC.DD` states the same rule, and forms such as `ptr(U8 *)++`, `buf[0](U32)`, and `value(F64)` occur throughout the pinned tree. The OCaml parser treats `(` followed by a checked public primitive, intrinsic storage spelling, or visible class symbol as a cast and treats other contents as call arguments. Its AST stores the complete operand, both parentheses, one of the three target type forms, zero through four pointer layers, and combined provenance. Casts return to the modifier loop, while a cast after a terminal update remains invalid. `HCPARSE0029` rejects C-style prefix casts and `HCPARSE0030` reports an unclosed target. Checked zero-depth primitive numeric casts now lower to `IC_HOLYC_TYPECAST` as recorded under issue #462. Array and function-pointer targets, conversion, warnings, and pointer or aggregate cast lowering remain unimplemented.

The `KW_SIZEOF` case in `PrsUnaryTerm` sets `PREC_TERM`, consumes zero or more opening parentheses, calls `PrsSizeOf`, requires the same number of closing parentheses, and returns `PE_MAYBE_MODIFIERS`. `PrsSizeOf` requires an identifier, resolves it among local variables, internal types, classes, globals, functions, and exports, loops over dot-separated members, and then consumes every trailing pointer star. That star loop does not use the `PTR_STARS_NUM` limit from `PrsType`. A pointer target forces the result to `sizeof(U8 *)`; other targets use class size, array byte count, or function debug range before the routine adds an immediate I64 result. The OCaml AST preserves the keyword, wrapper pairs, unresolved target, complete member path, target stars, and combined provenance. It intentionally follows the repeated-member loop even though `Doc/HolyC.DD` claims only one member level. Because the source returns `PE_MAYBE_MODIFIERS`, only a postfix cast can follow the raw term; calls, indexes, member accesses, and updates become available after that cast. `HCPARSE0031` through `HCPARSE0034` cover malformed target shapes and direct suffixes. The semantic source view carries that complete syntax into function and executable top-level trees together with the exact source-ordered root query. Local, module, supplied outer, and complete-miss roots remain distinguishable without a second mutable lookup. Public primitive and checked scalar values use their retained type evidence. In function bodies, a completed class or union root and a zero-rank by-value aggregate object retain the source-visible completed layout size. Every member segment comes from `Aggregate_member_index`, including the current-before-base search in `Compiler/LexLib.HC:67-85`, and the final member uses its full checked layout size. The trailing-star rule is applied only after a valid root or member path. Root arrays, pointer traversal, top-level and unmappable outer aggregate roots, and function debug ranges remain unsupported without partial IR. Issues #484, #486, and #488 record these evaluation steps.

The adjacent `KW_OFFSET` case has the same `PREC_TERM`, wrapper, and `PE_MAYBE_MODIFIERS` behavior, but it calls `PrsOffsetOf`. That routine resolves the first identifier as a local variable, class, or global variable, requires a dot, and follows one or more member names. Each step adds `CMemberLst.offset` and moves to `member_class`; the final sum is emitted as `IC_IMM_I64`. The OCaml AST records the unresolved root and every member segment. It follows the executable `do ... while` loop despite the one-level statement in `Doc/HolyC.DD`. Unlike `PrsSizeOf`, this routine has no pointer-star loop. `HCPARSE0035` through `HCPARSE0039` cover missing roots, missing dots or member names, unmatched wrappers, and forbidden direct suffixes. The function-binding pass resolves a local root for warning-count purposes and omits each member spelling, matching the source decrement after incidental local lookup. The expression-result pass now resolves source-visible completed aggregate publications in both result owners, keeps every checked lookup and cumulative offset, and produces internal `I64`. Local and global object roots and immediate lowering remain unimplemented.

The parser consumes `Frontend.Preprocessor.next` through a bounded seven-token buffer. The extra bounded lookahead recognizes an aggregate keyword after the source maximum of four backing pointer stars, and it diagnoses a fifth star for both visible and unknown backing names without scanning unbounded input. It captures the current include stack and definition trace with each token, publishes an accepted declaration, prototype, or aggregate forward before requesting the token after its comma or semicolon, publishes an aggregate definition before requesting its optional base or first body token, and publishes an accepted function definition before requesting its first body token. It returns no public AST if any error occurred. The second token distinguishes an unresolved declaration name from `name:` without consuming either path. Declaration recovery stops at a semicolon or EOF. Aggregate-body recovery tracks nested braces. Statement recovery stops at a closing brace while parsing a block, so one malformed child does not consume the block boundary. Publishing matches the source insertion points and lets a following `#ifdef` observe the name. A rejected JIT import is diagnosed before declarator parsing, so no rejected name is published. `Frontend.Ast` records the root-module span and the primary span plus ordered source segments for each accepted modifier, binding, aggregate backing, aggregate base colon and name, aggregate keyword, aggregate brace, member declaration, anonymous union, empty member separator, symbol or expression target, type, pointer layer, identifier, array bracket and expression, call parenthesis, call slot, index bracket, member operator and name, postfix update operator, cast parenthesis and target, `sizeof` keyword, wrapper, member, and star, `offset` keyword, wrapper, and member, delimiter, empty parameter entry, declarator, declaration group, function header and optional body, local storage marker, local register request, local initializer, statement separator, block brace, conditional keyword and parenthesis, goto keyword and target, label name and colon, lock keyword and body, and both sides of a try/catch pair. Empty parameter entries retain source order by recording the number of preceding concrete parameters; they do not alter semantic call shapes. Definition-generated nodes retain both their expansion and definition sites. `Frontend.Ast_dump` renders the versioned `holyc-ast-v1` human and JSON formats.

The `KW_DEFINED` case sets `PREC_TERM`, consumes zero or more opening parentheses, inspects exactly one token, consumes it with one `Lex` call, closes the wrappers, and returns `PE_MAYBE_MODIFIERS`. It emits true only when that token is `TK_IDENT` and has a hash entry or local-variable entry; every other case emits false. `PrsKeyWord` confirms that language keywords remain `TK_IDENT` values backed by keyword hash entries, so the OCaml AST classifies both keywords and ordinary identifiers as name-shaped while retaining every other token as non-name. The parser keeps the keyword, wrapper pairs, one operand spelling and classification, and combined provenance. `HCPARSE0040` through `HCPARSE0042` cover a missing token at EOF, unmatched wrappers, and forbidden direct suffixes. The function-binding pass resolves a name-shaped local operand for warning-count purposes; non-name operands create no query. Hash lookup, Boolean selection, and `IC_IMM_I64` lowering remain unimplemented. No language-expression use was found in the pinned Compiler, Kernel, Adam, or Demo `.HC` and `.HH` files, so focused fixtures provide the current gate.

`Compiler/LexLib.HC:LexExtStr` consumes every consecutive `TK_STR`, removes each intermediate terminator, and appends the next decoded bytes. `Compiler/PrsExp.HC:PrsUnaryTerm` uses that helper for an ordinary string primary, so the same rule reaches calls and scalar global initializers. `Compiler/PrsVar.HC` also calls it directly for its string-storage initializer paths. The OCaml literal records the combined spelling, decoded bytes, and location plus one physical segment per source token. A comma or operator ends the run. Exact slices from `Adam/DolDoc/DocInit.HC`, `Adam/DolDoc/DocWidgetWiz.HC`, `Adam/ABlkDev/DskChk.HC`, and `Kernel/KCfg.HC` cover calls, globals, and statements in both compilation modes.

`Compiler/PrsStmt.HC:PrsStmt` sends a statement-position `TK_STR` or `TK_CHAR_CONST` token to `PrsFunCall` instead of discarding the literal. In `Compiler/PrsExp.HC:PrsFunCall`, a character marker selects `PutChars` and a string marker selects `Print`. A zero character marker or a combined string whose first byte is zero is consumed before the fixed argument is parsed, which gives `'' value;` and `"" fmt,args;` their variable-value forms. String combination happens first, so `"" "ready";` is a nonempty fixed format. A nonempty marker remains the first token of the fixed argument expression, so a continuation such as `'A'+1;` belongs to that argument. Comma-led expressions after a `Print` format become its variadic arguments. `Doc/HolyC.DD` documents all four forms, and `Kernel/KExts.HC` supplies the corresponding prototypes.

The OCaml AST stores this behavior as a statement with an explicit `Print` or `PutChars` target. It retains the marker even when a following expression supplies the fixed argument, records whether the fixed expression began at or after the marker, and keeps each `Print` argument and its leading comma. A `Print` statement still requires its own semicolon. A `PutChars` statement may instead end at a sequence comma, so its semicolon field is optional. The same node is accepted at top level, recursively inside a block, and as a parsed function body. Definitions and includes keep their normal origin metadata. `HCPARSE0043`, `HCPARSE0044`, and `HCPARSE0046` cover a missing fixed expression, an empty `Print` argument, and a missing required semicolon. This slice does not resolve either runtime symbol, check argument types, lower the call, or execute it.

At the start of its main loop, `PrsStmt` consumes every comma. A lone semicolon is an empty statement; otherwise the ordinary path calls `PrsExpression`. When semicolon parsing is enabled, that expression may end with `;` or at a comma. After blocks, empty statements, labels, literal calls, and expressions, a comma returns control to the same loop. `Compiler/CMain.HC:LexStmt2Bin` and `CmpJoin` repeatedly enter this path, which preserves top-level source order in JIT and AOT modes.

The OCaml parser represents ordinary expression statements, empty statements, primitive local declarations, `goto`, labels, and statement sequences directly. A sequence retains leading commas, ordered statement elements, and every comma following an element, including repeated and trailing separators. It also represents the source path after `PrsVarLst`: unless the next token is `}`, `PrsStmt` keeps parsing without requiring a comma. The same representation is used at top level, inside blocks, and for function-definition bodies, although local declarations require an active function context. Known global, parameter, local, or function symbols can begin an expression; only an absent identifier immediately followed by `:` becomes a label, while other unresolved identifiers remain on the declaration path. `HCPARSE0047` reports an expression without `;` or a statement comma. `HCPARSE0048` reports another unsupported statement form encountered after a comma. Function-wide parameter, local, and label identity is implemented separately; semantic control flow, typing, storage, and ordinary expression binding remain pending.

The opening-brace branch of `PrsStmt` calls `Lex`, recursively invokes `PrsStmt` until `}` or `TK_EOF`, and then consumes the closing brace. If the following token is a comma, the enclosing statement loop continues. A comma before the closing brace does not terminate the block; it asks the recursive statement loop for another child. The OCaml AST mirrors that control flow with a recursive block node holding both braces and ordered children. Empty blocks, nested blocks, primitive local children inside functions, `goto` and label children, a block followed by a sequence comma, and blocks used as function bodies are represented directly. `HCPARSE0049` reports EOF before the close and retains the opening brace as a secondary location. `HCPARSE0050` reports an unmatched close at top level. `HCPARSE0051` caps hosted recursion at 256 blocks; no corresponding language limit was identified in the pinned source. Semantic scopes, local storage, lowering, and execution remain pending.

`Compiler/PrsStmt.HC:PrsIf` requires `(`, calls `PrsExpression`, and requires `)` before emitting `IC_BR_ZERO` and passing the next input to `PrsStmt`. It then uses `PrsKeyWord` to test for `KW_ELSE`. When present, it emits a jump around the false branch and calls `PrsStmt` a second time. The recursive calls make a following `else` part of the nearest unfinished conditional. `Compiler/CompilerA.HH` assigns `KW_IF` value 6 and `KW_ELSE` value 7. `Doc/HolyC.DD` demonstrates that the condition follows HolyC expression rules with `if (13<=age<20)`.

The OCaml AST keeps the `if` keyword, both condition parentheses, the condition expression, the then branch, and an optional else keyword and branch. Each branch uses the common statement parser, so blocks, comma-linked sequences, output literals, empty statements, expressions, and nested conditionals retain their existing shapes and provenance. `HCPARSE0052` through `HCPARSE0056` report malformed parentheses, missing branches, or an unmatched `else`. `HCPARSE0057` caps hosted conditional recursion at 256; no corresponding source-language limit was identified. This slice does not reproduce truth conversion, `IC_BR_ZERO`, label allocation, control-flow construction, semantic checks, lowering, or execution.

`Compiler/PrsStmt.HC:PrsWhile` requires `(`, calls `PrsExpression`, requires `)`, emits `IC_BR_ZERO`, and passes the next input to `PrsStmt` with the loop's completion label. It then emits `IC_JMP` back to the condition label. `Compiler/CompilerA.HH` assigns `KW_WHILE` value 9, and `Kernel/Job.HC` contains representative single-statement and compound loop bodies.

The OCaml AST keeps the `while` keyword, both condition parentheses, the condition expression, and the recursively parsed body. The common statement path preserves blocks, comma-linked sequences, output literals, empty statements, expressions, nested loops, conditionals, and `break`. Tests also lock nearest-`if` else association on both sides of a loop boundary. `HCPARSE0058` through `HCPARSE0060` report malformed parentheses or a missing body. `HCPARSE0061` caps hosted loop recursion at 256 and shares its count with post-test loops; no corresponding source-language limit was identified. Semantic break resolution and its jump fragment are implemented separately. This syntax slice does not reproduce truth conversion, block placement, `IC_BR_ZERO`, the loop back edge, complete lowering, or execution.

`Compiler/PrsStmt.HC:PrsDoWhile` emits its entry label before it passes the body to `PrsStmt`. It then requires `KW_WHILE`, `(`, an expression, `)`, and `;` in that order. The final `IC_BR_NOT_ZERO` targets the entry label, followed by the break label. `Compiler/CompilerA.HH` assigns `KW_DO` value 15. `Kernel/Job.HC:TaskRstAwaitingMsg` provides a representative compound body followed by `while (task=task->popup_task);`.

The OCaml AST retains both keywords, the recursively parsed body, both parentheses, the condition, and the final semicolon as separate source locations. The common statement path supports pre-test and post-test loop nesting, `break`, and nearest-`if` else association. `HCPARSE0062` through `HCPARSE0066` cover a missing body and each required trailing component; an empty condition uses `HCPARSE0018`. The shared `HCPARSE0061` guard covers mixed loop nesting. Semantic break resolution and its jump fragment are implemented separately. Truth conversion, block placement, `IC_BR_NOT_ZERO`, complete lowering, and execution remain later work.

`Compiler/PrsStmt.HC:PrsFor` requires an opening parenthesis and passes its initializer to `PrsStmt` with normal semicolon handling. It then requires an expression and semicolon. The update is absent when the next token is `)`; otherwise it calls `PrsStmt(cc,try_cnt,NULL,0)`, which disables semicolon checking and permits comma-linked updates before the closing parenthesis. The body uses ordinary statement parsing with the completion label. `Compiler/CompilerA.HH` assigns `KW_FOR` value 8. `Kernel/StrPrint.HC` contains empty initializers, `Adam/Opt/Utils/StrUtils.HC` contains empty and comma-linked updates, and `Doc/HolyC.DD:AddNums` shows the conventional three-part form.

The OCaml AST records the keyword, parentheses, initializer statement, required condition and separator, optional update statement, and body independently. Header statements reuse the common sequence nodes, and recursive bodies preserve `goto`, labels, `break`, and nearest-`if` else association. Definition and include frames keep their normal token origins. The source local-declaration branch continues the same `PrsStmt` call after its semicolon unless it reaches `}`. In a `for` header, it therefore consumes the condition before `PrsFor` can read it and the native compiler reports `Missing )`. The hosted parser rejects the same shape with `HCPARSE0069`. Other `HCPARSE0067` through `HCPARSE0071` diagnostics, plus the shared expression and loop-depth diagnostics, cover malformed headers and bodies. Goto and break targets and their checked jump fragments are implemented separately. Truth conversion, deferred update control flow, complete lowering, and execution remain pending.

The `KW_BREAK` branch in `Compiler/PrsStmt.HC:PrsStmt` consumes the keyword, rejects a null `lb_break`, emits `IC_JMP` to the active target, and enters `sm_semicolon`. That shared terminator accepts a semicolon, permits a comma to continue the statement loop, and does nothing when `CMPF_PRS_SEMICOLON` is clear. `PrsWhile`, `PrsDoWhile`, and the body of `PrsFor` pass their completion labels into the recursive statement parser. `Compiler/CompilerA.HH` assigns `KW_BREAK` value 19. `Doc/HolyC.DD` supplies representative switch-case uses.

The OCaml AST implements the syntax boundary as a distinct `Break_statement` with the keyword, an optional semicolon, and a combined location. Comma-linked breaks and `for` updates keep the semicolon absent. The parser accepts the node in all current statement contexts and defers the source `lb_break` requirement to semantic control-flow validation. `HCPARSE0072` reports an invalid active terminator, and recovery preserves an enclosing block close. Unit tests cover both compile modes, loops, conditionals, boundary variants, includes, definitions, deterministic dumps, and invalid input. Target resolution and checked jump creation are implemented separately; block placement, complete control-flow lowering, and execution remain pending.

The `KW_RETURN` branch in `Compiler/PrsStmt.HC:PrsStmt` rejects a missing `cc->htc.fun`, emits a `SysUntry` call for each active `try`, and uses an immediate semicolon to distinguish a valueless return from one that must parse an expression. A value produces `IC_RETURN_VAL` using the function return class. Both forms set `CCF_HAS_RETURN`, jump to `cc->lb_leave`, and enter `sm_semicolon`. That shared boundary accepts `;` or a comma when semicolon checking is enabled and accepts the caller boundary unchanged when it is disabled. `Compiler/CompilerA.HH` assigns `KW_RETURN` value 12. `Compiler/BackC.HC` contains valueless returns, `Kernel/FunSeg.HC` contains early returned values, and `Doc/HolyC.DD` documents `return res;`.

The OCaml AST represents this syntax with a `Return_statement` containing the keyword, optional expression, optional semicolon, and combined location. An immediate semicolon is the only valueless form. A value may end at a semicolon, statement comma, or semicolon-free `for` update boundary. `HCPARSE0073` reports a bad boundary after a value, and `HCPARSE0074` reports a missing value without consuming an enclosing block close. Tests cover JIT and AOT parsing, each current recursive statement context, comma and update boundaries, expression precedence and postfix forms, include and definition provenance, deterministic dumps, and malformed input. Function-context and return-type checks, try unwinding, function flags, leave-label resolution, IR lowering, and execution remain pending.

The `KW_GOTO` branch in `Compiler/PrsStmt.HC:PrsStmt` requires `TK_IDENT`, finds or creates a `CMT_GOTO_LABEL`, increments its use count, emits `IC_JMP`, and enters `sm_semicolon`. The unresolved-identifier path leaves a local variable on the expression route; otherwise it finds or creates the same record, rejects `CMF_DEFINED`, marks the record defined, emits `IC_LABEL`, and requires a colon. It also rejects a missing current function. `Compiler/PrsLib.HC:COCGoToLabelFind` searches language and assembly labels in the current code-control list, while `COCDel` reports unresolved records and warns about unused definitions. `Compiler/CompilerA.HH` assigns `KW_GOTO` value 17. `Compiler/UAsm.HC` and `Kernel/Job.HC` supply representative forward jumps and shared completion labels. `Doc/HolyC.DD` recommends `goto` because the language has no `continue`, and `Doc/ScopingLinkage.DD` records function scope plus global-object collision behavior.

The OCaml AST uses distinct `Goto_statement` and `Label_statement` nodes. A goto keeps the keyword, identifier target, optional semicolon, and combined location. A label keeps the absent symbol's name, colon, and combined location. The parser selects a label only for an identifier absent from the current environment and immediately followed by `:`; known globals and functions keep expression routing. Both nodes participate in comma-linked sequences, and a goto in a semicolon-free `for` update stops at the enclosing `)`. Tests cover JIT and AOT syntax, forward and backward shapes, definitions, includes, deterministic dumps, symbol collisions, and malformed targets. `HCPARSE0075` reports a missing or nonidentifier target, and `HCPARSE0076` reports a bad active terminator. Top-level syntax remains available to tooling, but the semantic label pass rejects it. That pass supplies stable function-local identities, binds forward and backward language gotos, and diagnoses missing or duplicate language definitions. Assembly operand references, repeated assembly-definition address checks, unused warnings, reconciled global collisions, IC lowering, and execution remain pending.

`Compiler/PrsStmt.HC:PrsStmt` handles `KW_LOCK` by consuming the keyword, incrementing `CCmpCtrl.lock_cnt`, recursively parsing one ordinary statement, and decrementing the count. `Compiler/PrsLib.HC:ICAdd` adds `ICF_LOCK` whenever that count is nonzero. `Compiler/CompilerA.HH` and `Compiler/OpCodes.DD` both assign `lock` keyword ID 42. `Doc/HolyC.DD` describes the intended prefix behavior. `Demo/MultiCore/Lock.HC` demonstrates braced and unbraced forms, while `Demo/MultiCore/LoadTest.HC` supplies another compound example.

The OCaml AST represents the source region as a distinct `Lock_statement` containing the keyword, one recursively parsed body, and the combined location. The body can be a block, an unbraced statement, another lock, or a comma-linked sequence. Tests cover JIT and AOT syntax, conditionals, loops, empty bodies, definitions, includes, deterministic dumps, malformed input, and a separate 256-level hosted recursion guard. `HCPARSE0077` reports a missing body and `HCPARSE0078` reports excessive nesting. The syntax node does not yet propagate `ICF_LOCK`, validate applicable operations, select x86 prefixes, lower IR, or execute code.

`Compiler/PrsStmt.HC:PrsSwitch` distinguishes `switch (expression)` from `switch [expression]`. The parenthesized form emits `IC_SWITCH`; the bracketed no-bound form emits `IC_NOBOUND_SWITCH`. Its body accepts implicit `case:`, explicit `case expression:`, and inclusive `case low...high:` labels. An implicit case starts at zero or follows the previous case value. The source swaps reversed range endpoints before adding every value to the jump table. `KW_DFT` is the compiler's internal constant for the source spelling `default`, as confirmed by `Compiler/OpCodes.DD`. `start:` opens a sub-switch region, `end:` closes the active region, and ordinary region contents return to `PrsStmt` with the region's break label. `Compiler/UAsm.HC`, `Kernel/Compress.HC`, `Kernel/StrPrint.HC`, `Demo/NullCase.HC`, and `Demo/SubSwitch.HC` provide no-bound, ranged, implicit, nested, and grouped examples. `Doc/HolyC.DD` records the same forms and the special front-porch restrictions.

The OCaml AST keeps the switch keyword, delimiter mode, expression, matching delimiters, braces, and ordered body elements. A case pattern is an explicit implicit, single-expression, or ranged-expression variant. Default labels remain separate from cases, ordinary statements stay in source order, and each matched `start:`/`end:` pair becomes a recursive sub-switch node. The shared statement path permits switches in blocks, loop bodies and updates, conditionals, locks, and both sides of a try pair. Definition and include frames retain every delimiter and label origin in human and JSON dumps. The switch diagnostic codes from `HCPARSE0084` to `HCPARSE0097`, with `HCPARSE0089` unused, cover hosted depth limits and malformed switch structure. Recovery stops at the next case, default, start, end, or enclosing switch close, and any error suppresses the public AST. Break target resolution and jump fragments are implemented separately. Case evaluation, implicit numbering, endpoint swapping, duplicate and default checks, front-porch validation, jump-table construction, `IC_SWITCH`, `IC_NOBOUND_SWITCH`, `IC_SUB_CALL`, `IC_RET`, complete lowering, and execution remain pending.

`Compiler/PrsStmt.HC:PrsTryBlk` looks up `SysTry` and `SysUntry`, creates handler and completion labels, emits the setup call, and passes the next input to `PrsStmt` with `try_cnt + 1`. After the body it emits `SysUntry`, requires `KW_CATCH`, and parses the handler through the same ordinary statement path and incremented count. This makes braces optional on both sides and lets a nested `return` use the active count to emit the required `SysUntry` calls. `Compiler/CompilerA.HH` and `Compiler/OpCodes.DD` assign IDs 5 and 3 to `try` and `catch`. `Compiler/PrsExp.HC`, `Kernel/Job.HC`, and `Demo/Exceptions.HC` establish unbraced, braced, and nested forms. `Doc/HolyC.DD` documents the pair and identifies lowercase `throw` as a function.

The OCaml AST represents the complete pair as one `Try_catch_statement` containing both keywords, two recursively parsed ordinary statements, and the combined location. Tests cover both compilation modes, blocks, unbraced bodies, nesting, conditionals, loops, locks, comma boundaries, definitions, includes, deterministic dumps, malformed input, and a separate 256-level hosted recursion guard. `HCPARSE0079` through `HCPARSE0081` report a missing body, keyword, or handler. A standalone `catch` reaches the pinned expression parser's generic `Missing expression` diagnostic; `HCPARSE0082` retains rejection with a more specific hosted message. `HCPARSE0083` reports excessive nesting. `SysTry` and `SysUntry` calls, handler labels, exception transfer, return unwinding, lowering, and execution remain pending.

`Compiler/PrsStmt.HC:PrsNoWarn` loops while the current token is an identifier, requires `cc->local_var_entry`, sets `MLF_NO_UNUSED_WARN`, and consumes one comma after a target when present. The `KW_NO_WARN` dispatch advances once before entering that routine and then uses the common statement terminator. `Compiler/Lex.HC` establishes that `local_var_entry` is selected before hash-table lookup. `Compiler/LexLib.HC:MemberFind`, the `CMemberLst` definitions in `Kernel/KernelA.HH`, `Doc/HolyC.DD`, and `Doc/Options.DD` ground the later member flag and warning behavior. Six pinned files contain eight live statements; `Adam/Ctrls/CtrlsSlider.HC` contains another form inside text that generates HolyC later.

`Frontend.Parser` retains a distinct `no_warn_statement` with ordered targets, target commas, an optional semicolon, and full provenance. It accepts only names reported by the active function-local parser context, including fixed parameters, variadic `argc` and `argv`, and prior locals. The outer statement parser retains a second consecutive comma as its own separator, and a named `for` update may end at `)` only after the target's trailing comma. `HCPARSE0157` covers invalid target tokens or nonlocal names; `HCPARSE0158` covers missing delimiters and boundaries. The corpus change removes one recovered `HCPARSE0048` from `Demo/MultiCore/Palindrome.HC` without changing its earlier `CDoc` blocker or any phase count.

`Driver.Function_expression_binding` adds accepted targets to the same publication-ordered stream used for ordinary local identifiers, but keeps them as suppression events. An initialized local contributes a separate reset after its initializer. The driver also emits a distinct query for the root of `sizeof` and `offset`, and for a name-shaped `defined` operand. `Sema.Local_warning_analysis` then copies each typed binding's member mask, adds `MLF_NO_UNUSED_WARN`, and exposes ordinary, query, suppression, and combined counts. It applies the final `PrsStmt` conditions in binding order: a flagged count above one reports an unneeded `no_warn` except for `_anon_`; an unflagged zero count reports an unused variable only when `OPTf_WARN_UNUSED_VAR` is enabled. Prototypes have no function-completion warnings. The pass is immutable and retains origins for the binding, queries, and every suppression.

This count covers ordinary identifier nodes represented by the current expression-binding pass. The explicit decrements in `PrsExp.HC` confirm that member/type lookup roles need their own audit; unresolved `sizeof`, `offset`, `defined`, type syntax, labels, and assembly tokens remain outside the result. One option mask applies to the checked batch until compile-time `Option(...)` execution supplies source-positioned states. `HCSEMA0031` through `HCSEMA0033` cover invalid event and analysis inputs; `HCSEMA0034` and `HCSEMA0035` identify the two warning results. Issue #226 records this boundary.

`Compiler/PrsStmt.HC:PrsGlblVarLst` consumes a comma before it restarts a global list, while `Compiler/PrsVar.HC:PrsType` may return no identifier in global mode. A following semicolon therefore terminates the group without creating another symbol. The OCaml AST retains both punctuation marks, and global type resolution treats the semicolon as the final variable's effective declaration terminator. The pinned intended corpus contains one authored `,;` instance: `I64 cur_dc,;` at `Demo/Graphics/Life.HC:4`. Supporting it advances that file's first project-prelude result to unresolved `GrBlot` at line 10.

`Compiler/PrsStmt.HC:PrsFunJoin` reads a bound function's parameter list but does not inspect the token that follows `)`. Every function branch in `PrsGlblVarLst` returns immediately after that call. A semicolon after a bound prototype is therefore optional, and the following token belongs to the next top-level item. The AST stores a written semicolon with its location and represents an omission as absent. It never fabricates punctuation or absorbs the next declaration. `Kernel/KernelC.HH:179-180` and `Demo/AcctExample/TOS/TOSExt.HC:306-307` contain consecutive prototypes with the first semicolon omitted. Supporting those forms makes `Kernel/KernelC.HH`, `Kernel/KMisc.HC`, and `TOSExt.HC` parse with the project prelude, while `MakeTOS.HC` advances into `TOSMisc.HC`. The parser-only report rises from 118 to 121 project-prelude parses and falls from 21,140 to 21,021 diagnostics.

`Compiler/PrsVar.HC:PrsVarInit2` consumes an array initializer's opening brace only when one is present. For a fixed dimension it then reads exactly the declared element count and accepts a closing brace before returning. `Adam/DolDoc/DocHighlight.HC:3-7` uses this form for the 17-element `highlight_hash_type_colors` table. The AST represents the missing opening brace explicitly, retains nested fixed-dimension element groups, and records an optional written closing brace. The hosted parser uses positive literal bounds after definition expansion and caps each dimension at 1,000,000 elements; other constant expressions require the later constant-evaluation pass and receive `HCPARSE0159` instead of a guessed count. `HCPARSE0160` reports a missing element separator. This change makes `DocHighlight.HC` parse with the project prelude, raising that count from 121 to 122 and reducing diagnostics from 21,021 to 21,019. Storage emission and runtime initialization remain untested.

After `PrsClass` returns at global scope, the class/union branch in `PrsStmt` takes the common semicolon path only when the current token is `;`. Otherwise it hands the completed aggregate type to `PrsGlblVarLst`; `PrsType` can encounter another `class` or `union` there. `Demo/Graphics/Pick3D.HC:27-38` relies on this path by placing `class PObj` immediately after the closing brace of `class PCSprite` without a semicolon. The OCaml AST retains a written definition semicolon as a source location and records this omission as absent. The parser accepts the omission only before an immediate `class` or `union`, leaving EOF and unrelated syntax on their existing diagnostic paths. The corpus phase totals do not change, but `Pick3D.HC` advances from `HCPARSE0115` at line 37 to unresolved `DCDepthBufAlloc` at line 83 and each corpus view loses one diagnostic.

## Parser symbol publication and dumps

`Kernel/KernelA.HH:685-702` defines the 17 `HTT_*` categories used by compiler hash entries. `Kernel/KHashB.HC:HashTypeNum`, `HashVal`, and `HashSrcFileSet` show that kind, value, and source information belong to the entry rather than its spelling alone. `Compiler/PrsStmt.HC:PrsClass`, `PrsFun`, and `PrsGlblVarLst` publish classes, functions, and globals at different source-order boundaries. `Compiler/PrsVar.HC:PrsVarLst` inserts locals before parsing later input, while `PrsStmt` uses the current hash and local-variable results to choose declaration, expression, and assembly routes. `Compiler/CHash.HC:MapFileWrite` demonstrates source-linked symbol reporting at the compiler boundary. `Doc/ScopingLinkage.DD` documents shadowing, duplicate policies, JIT parent-task lookup, and the separate AOT and JIT categories.

`Frontend.Symbol_visibility` represents this parser-owned environment with monotonic IDs, explicit kinds, an origin variant, optional direct-function call shape, and separate local contexts. Parser publication now stores the full AST location rather than only its primary span, so definition-generated declarations retain the generated frame, invocation site, and definition site. Human and JSON serializers emit schema `holyc-symbol-visibility-v2` and the exact reference commit. Both walk entries by stable ID; local names use lexical order because their set order has no source meaning. An optional filter omits pinned entries without renumbering source declarations.

`holyc dump-symbols` runs the same bounded preprocessing and parser path as `parse`. Its default output includes the complete pinned seed, while `--source-only` selects declarations that came from source spans. Diagnostics go to stderr. Even when an error suppresses the public AST, the command prints the state accumulated before the error and returns status 1. The dump explains parser routing only. Aggregate reconciliation is available through the semantic library API rather than this parser-state view; imports, exports, aliases, addresses, storage, runtime task construction, and loader records remain work for issue #161.

## Semantic symbol order and declaration scopes

`Kernel/KHashA.HC:_HASH_ADD` prepends an entry to its bucket. `SYS_HASH_SINGLE_TABLE_FIND` visits that chain in order and decrements `RCX` for each matching spelling and type mask. `SYS_HASH_FIND` then follows `CHashTable.next` with the remaining counter. `Compiler/CMain.HC:CmpJoin` builds the compiler-local chain: the local table points to the compilation global table, which points to `Fs->hash_table` in JIT mode or to the relevant compiler tables in AOT mode. `Doc/ScopingLinkage.DD` describes the visible consequence as newest-name shadowing followed by parent-task lookup. `Kernel/KHashB.HC:HashSrcFileSet` keeps source metadata on the selected entry.

`Sema.Symbol` gives symbols and scopes separate typed IDs and retains pinned, source, generated-source, or synthesized origins. `Sema.Symbol_table` stores same-name entries newest first, filters lookup through an explicit semantic-kind set, carries nth-match lookup through one explicit parent chain, and rejects a scope or symbol owned by another table. Scope and symbol dumps use creation order, not hash iteration, under schema `holyc-semantic-symbol-table-v1`. `Driver.Session` owns the table and seeds the 17 internal type records built in `Compiler/CInit.HC`.

`Compiler/PrsStmt.HC:PrsClass` inserts an aggregate before handing any attached declarators to `PrsGlblVarLst`; `PrsFunJoin` supplies function entries and `HashSrcFileSet` records their source. `Driver.Semantic_collection` walks the completed AST in that order and creates checked declarations for `Sema.Declaration_collection`. The semantic batch creates one module scope below the task root and retains module-item and grouped-declarator positions without using AST values as hash keys.

The extern branch of `PrsClass` calls `PrsClassNew` and `HashAdd` for every forward. The definition branch selects the newest visible `HTT_CLASS` entry, reuses it only while `Cf_EXTERN` is set, and otherwise adds a new class record. It clears that flag without comparing the class or union keyword. `Sema.Aggregate_resolution` reproduces this module-local identity rule over the checked declaration collection: every forward starts an identity, only the newest same-name unresolved identity can be completed, and later definitions shadow completed identities. Both source sites and aggregate spellings remain available. The completed identity uses the definition symbol so the independently collected member scope remains attached to its canonical entry. `Driver.Aggregate_resolution` verifies AST kind, position, name, and provenance before invoking the pure resolver.

`Compiler/PrsVar.HC:PrsType` selects a class or intrinsic record and advances through at most four adjacent pointer records before it calls `PrsClass` for a definition header. A named backing is therefore fixed against the state visible before the new definition. `PrsClass` publishes or completes that definition before it consumes `: Base`, so the base lookup sees the current identity. `Compiler/OptLib.HC:OptClassFwd` later follows the distinct `fwd_class` backing relation. `Driver.Aggregate_header_resolution` preserves these two lookup points with a source-ordered map rather than querying the completed module table. `Sema.Type` distinguishes public primitive spellings, intrinsic storage spellings, and aggregate identities; `Sema.Aggregate_header_resolution` retains depths zero through four and all definition, keyword, backing, pointer, colon, and base-name origins. References selected through forwards use the canonical definition symbol supplied by aggregate reconciliation.

`Compiler/PrsVar.HC:PrsVarLst` calls `MemberAdd` as each declarator is completed. A recursive anonymous union passes the same `CHashClass`, so its fields enter the containing member list. `Compiler/LexLib.HC:MemberAdd` appends the list entry while maintaining a separate lookup tree; `MemberFind` searches the current class before following `base_class`. `Driver.Member_collection` retains the AST member path, flattens anonymous-union fields into the containing aggregate namespace, and associates the resulting `Sema.Member_collection` scope with the existing top-level aggregate symbol. Offset directives, empty separators, and metadata names add no member symbol.

For each declaration group, `PrsVarLst` restores the shared class entry before calling `PrsType` on the next comma declarator. `PrsType` consumes up to four ordinary pointer stars. Its callback branch keeps return-type stars outside the declarator parentheses, records one through four callback indirections inside them, calls `PrsFunJoin` for the anonymous `CHashFun`, and then calls `PrsArrayDims`. `PrsVarLst` stores that function record in `CMemberLst.fun_ptr` and sets `MLF_FUN`; `MemberLstDel`, `MemberLstSize`, and the two `PrsExp.HC` member-expression paths later consume the complete record. `Driver.Member_type_resolution` validates the declaration, aggregate identity, header, member scope, and AST together; publishes the current canonical identity before reading its members; and binds public, intrinsic, or named member and callback-parameter heads through a source-order map. `Sema.Member_type_resolution` retains the resolved return head, every pointer origin, complete recursive signature, array rank, dimension origins, member path, declarator position, and a derived flag mask without binding or evaluating defaults or evaluating extents. Ordinary objects receive zero; callback members receive `MLF_FUN` (`0x8`).

`Kernel/KernelA.HH:774-818` defines `CMemberLst.flags` as `U16` and assigns seven masks from `MLF_DFT_AVAILABLE` through `MLF_STATIC`. `Compiler/PrsVar.HC`, `Compiler/LexLib.HC`, `Compiler/PrsStmt.HC`, and `Compiler/PrsExp.HC` supply 23 named assignments or consumers for defaults, `lastclass`, owned string defaults, callbacks, varargs, warning suppression, and static locals. `tools/member_flag_source.ml` verifies those facts against all five pinned file digests and rejects changed definitions, order, assignments, or consumers. `tools/member_flag_gen.ml` emits the deterministic `src/generated/member_flags.ml` registry, and `Sema.Member_flag` exposes its typed masks and provenance. Aggregate members apply the callback distinction. Fixed parameters apply warning suppression, callback, default, `lastclass`, and accepted string-storage bits independently at every callback level; top-level synthetic `argc` and `argv` each apply the vararg bit. Local records apply the callback and static bits at declaration time. Local-warning analysis derives the later no-unused-warning bit for accepted suppressions. The recorded cleanup, sizing, call, address, storage, and execution consumers remain separate boundaries. Issues #218, #220, #222, and #226 track these applications.

`Compiler/PrsStmt.HC:PrsClass` adds one base size before entering a class body and adds `neg_offset` after it leaves. `Compiler/PrsVar.HC:PrsVarLst` places a class member at `tmpc->size` and advances by its exact byte count, while a union places a member at `union_base` and keeps the largest end. There is no automatic member alignment. An anonymous union recurses with `tmpc->size` as its new base, including when the caller is already a union. `PrsArrayDims` permits only the first extent to be empty, evaluates other extents through `LexExpressionI64`, rejects negative values, and multiplies the stored total. Repeated `$$` directives evaluate through `LexExpression`, replace the active class cursor or union base, and retain the largest negative displacement. `Demo/Lectures/NegDisp.HC` uses that negative form; `Kernel/KernelA.HH:447`, `:3422`, and `:3806` supply explicit alignment and origin changes.

`Driver.Aggregate_layout` pairs those expressions with the checked header and member identities from the same AST. `Sema.Aggregate_layout` then calculates source-ordered closed layouts: primitive sizes come from the generated type registry, pointers and callbacks occupy eight bytes, a prior by-value aggregate contributes its completed size, classes remain packed, named unions overlap at zero, nested anonymous unions start at the current aggregate end, and a base remains a prefix at offset zero. Target expression operations use `Int64`; array counts and byte metadata use checked arithmetic. Stable `HCSEMA0001` through `HCSEMA0009` failures keep unresolved constants, calls, `sizeof`, `offset`, `defined`, later definitions, by-value cycles, invalid dimensions, division faults, non-finite results, and metadata overflow visible. Member metadata, subinteger access, backing conversion, and runtime allocation remain separate work.

`Compiler/LexLib.HC:MemberAdd` calls `MemberFind` before each insertion. The search checks the current class and then follows `base_class`; only `pad`, `reserved`, and `_anon_` bypass the duplicate error. Equal spellings remain below the existing lookup-tree root, which makes the earliest direct occurrence win even though the general compiler hash tables use newest-first chains. `Driver.Aggregate_member_index` reconciles the header, member-type, and layout batches. `Sema.Aggregate_member_index` then validates direct and inherited duplicates, keeps source-ordered direct members, and builds a separate immutable name index. Results retain queried and declaring aggregate identities, inheritance depth, resolved type, callback shape, byte layout, anonymous-union path, and provenance. `HCSEMA0010` through `HCSEMA0013` report invalid batches, missing earlier bases, duplicates, and unindexed aggregate queries. Lookup does not mutate the semantic table or emulate `use_cnt`; expression resolution will consume these facts later. `Sema.Aggregate_layout_dump` renders a completed index as schema `holyc-aggregate-layout-v1`, and `holyc dump-layout` runs the full checked aggregate pipeline before emitting either its human or JSON form.

`Compiler/PrsStmt.HC:PrsFunJoin` uses `PrsVarLst` to append fixed parameters to a `CHashFun`. An unnamed fixed parameter advances the signature position without adding a member. `Compiler/PrsVar.HC:PrsDotDotDot` appends `argc` and then `argv`; `PrsFun` uses that same function member list for the automatic and static local branches reached through `PrsStmt`. `Driver.Function_collection` associates each parsed prototype or definition with its existing top-level symbol, retains those parameter gaps, synthesizes the variadic pair at the ellipsis location, and walks recursive statements in parser order. `Sema.Function_collection` creates one function scope and records parameters before locals. It preserves declaration groups, storage classification, repeated names, and complete source provenance without introducing block-local lookup.

`PrsGlblVarLst` hands the selected return class to `PrsFunJoin`, while `PrsVarLst` assigns one eight-byte member slot to each fixed parameter. Its recursive `PrsType` branch keeps return pointers outside a callback declarator and requires one through four indirection stars inside it. An unnamed slot receives `MLF_NO_UNUSED_WARN`, a returned callback receives `MLF_FUN`, every default receives `MLF_DFT_AVAILABLE`, and `lastclass` adds `MLF_LASTCLASS`. `LexExpression2Bin` sets `CCF_HAS_MISC_DATA` while parsing string or binary-insertion storage; the accepted string-expression path then produces `MLF_STR_DFT_AVAILABLE`. `MemberLstCmp` and `PrsFunCall` keep those per-slot distinctions instead of imposing a trailing-default rule. `PrsDotDotDot` adds internal `I64` members named `argc` and `argv`, marks each with `MLF_DOT_DOT_DOT`, copies the ellipsis register request to both, and gives `argv` the compiler placeholder dimension 127. `Driver.Function_type_resolution` replays aggregate publication in module order, pairs each accepted function with its declaration and function scope, and resolves return and recursive fixed-parameter types against the aggregate identity visible at that declaration. `Sema.Function_type_resolution` retains unnamed slots, default kinds and origins, callback nesting, named top-level parameter bindings, ordered register requests, exact private parameter masks, and the synthetic vararg shape, requests, and masks. The same recursive signature construction serves member, global, and local callbacks. It validates the complete immutable batch without evaluating a default or applying register allocation, storage, linkage, call, or ABI rules. Function identity reconciliation remains separate and deliberately ignores retained register requests. The frontend does not yet accept `TK_INS_BIN` in a default expression, so that path remains explicit rather than being inferred from string handling.

`PrsFunJoin` compares the newly parsed header with the record it just replaced only when `OPTf_WARN_HEADER_MISMATCH` is enabled. The return check uses exact class-record identity. `MemberLstCmp` checks the parameter spelling, member class, member-class base, default availability, string-storage bit, and evaluated default payload. `MemberAdd` assigns `member_class_base` only for local variables, leaving that field null for function parameters on both sides of this comparison. It does not inspect register requests, `MLF_LASTCLASS`, function flags, callback signatures, or array dimensions. Callback declarators compare as internal `I64i` pointers at their declarator depth. A `lastclass` default compares as zero because `CAlloc` initialized the record and that parser branch does not write `dft_val`. String defaults use `StrCmp`, so bytes after the first NUL do not matter.

The argument loop is deliberately asymmetric. Its limit is the replaced header's fixed `arg_cnt`, while synthetic `argc` and `argv` remain linked after the fixed members. Reaching the limit succeeds only when both lists still have a record or both end together; one remaining list fails. An old variadic header can therefore ignore a longer current tail, while reversing the declarations can expose a mismatch. `Sema.Function_header_analysis` preserves that control flow, consumes the current declaration's option snapshot, and reports `HCSEMA0037` for a return mismatch or `HCSEMA0038` for an argument mismatch. `HCSEMA0036` rejects missing or wrongly classified compile-time values. The analysis is exact once those payloads are supplied, but it does not evaluate default expressions or substitute them at calls. [Issue #236](https://github.com/frankischilling/holyc-ocaml/issues/236) records this boundary.

Pinned header tests retain `Dir` at `Kernel/KernelC.HH:283`, `Copy` at `Adam/ABlkDev/ADskA.HC:61`, and `Zip` at `Adam/ABlkDev/ADskB.HC:150`. They check both the original declaration text and the semantic mask on each fixed slot.

`Compiler/PrsStmt.HC:PrsStmt` routes automatic and static declarations to `PrsVarLst` with the current function record. `PrsVarLst` selects `REG_ALLOC`, an explicit `ST_U64_REGS` entry, `REG_NONE`, or `REG_UNDEF` for each declarator; calls `PrsType`; records `MLF_FUN` or `MLF_STATIC`; retains dimensions; and distinguishes automatic expression initialization from static recursive initialization. Its local path stops after the declaration semicolon, while every nested statement still uses the same function member list. `Driver.Local_type_resolution` follows the aggregate identities visible at the function's module item, pairs recursive local AST nodes with the function collection, and resolves ordinary or callback types. `Sema.Local_type_resolution` keeps declaration and declarator order, storage origins, register requests, array brackets, initializer shapes, delimiters, and generated-source provenance. It derives a private mask for each declarator: `0x0` for an automatic object, `0x8` for an automatic callback, `0x40` for a static object, and `0x48` for a static callback. Nested callback parameters do not change the owning local. Pinned fixtures at `Kernel/KMisc.HC:87`, `Adam/WinMgr.HC:38`, and `Adam/Gr/SpriteMesh.HC:65` exercise static objects; the static-callback mask follows the same direct source branch because the pinned corpus contains no such declaration. Validation is read-only and rejects mismatched sessions, scopes, source order, symbols, aggregate targets, dimensions, and declaration groups. Completed binding validation and function-local occurrence binding are supplied by the passes below. `Sema.Function_frame_layout` supplies checked extent values and semantic stack locations separately. Initializer evaluation, later warning mutation, static allocation, register allocation, static-address lowering, and ABI behavior remain separate work. The direct non-static object address fragment is recorded below. Issues #200 and #222 track this boundary.

`Compiler/LexLib.HC:MemberAdd` calls `MemberFind` before adding a function parameter or local. The shared `CHashFun` member list makes an ordinary repeated spelling invalid across every parameter, automatic local, static local, and nested statement. `pad`, `reserved`, and `_anon_` are the exact exceptions. Equal keys follow the existing member-tree node, so `MemberFind` continues to select the earliest inserted exception. `Compiler/Lex.HC` checks the current function's member list before global tables, while `PrsVarLst` publishes a local after its type and array syntax but before its initializer. `Driver.Function_binding_index` reconciles the function and local type batches with their collected symbols. `Sema.Function_binding_index` then validates source order, ownership, kinds, and positions before building immutable per-function indexes. `HCSEMA0014` through `HCSEMA0016` cover invalid batches, ordinary duplicates, and unindexed queries. Lookup is read-only, and issue #202 tracks this boundary.

`Compiler/PrsVar.HC:373-402,521-532,590-628` gives every ordinary and synthetic parameter an eight-byte slot, calculates each local's total byte size, and places automatic locals through the source's size-dependent alignment classes. `Compiler/PrsStmt.HC:114-123` adds 16 bytes for saved `RBP` and the return address; lines 140-170 round the completed negative local cursor down to eight bytes. `Compiler/PrsExp.HC:761-807` consumes the retained displacement for an `RBP`-relative address and routes static locals separately. `Driver.Function_frame_layout` joins the exact binding, type, aggregate-layout, dimension, and AST evidence for checked definitions. Its dimension converter and `Driver.Aggregate_layout` share one checked parser-operator mapping. `Sema.Function_frame_layout` retains named and synthetic parameter slots, source-ordered automatic and static locations, total-size alignment, zero-sized locations, register-request gaps, and a nonnegative local-frame byte size. Static locations have no frame slot. Array multiplication, displacement arithmetic, and final-size conversion are checked, and every failure returns no partial layout. The hosted pass also rejects unresolved and fractional final dimensions; the pinned `ToI64` path truncates `F64` instead (`Compiler/PrsExp.HC:1140-1151`, `Kernel/KernelB.HH:121`). Human and JSON reports use schema `holyc-function-frame-layout-v1`. [Issue #558](https://github.com/frankischilling/holyc-ocaml/issues/558) records this boundary.

`Compiler/PrsExp.HC:761-802` builds the ordinary non-static identifier address as `IC_RBP`, `IC_IMM_I64` with the member displacement, and `IC_ADD`; `Compiler/CInit.HC:27,36,71` fixes the three instruction shapes, and `Compiler/CompilerA.HH:30,39,83` fixes their identities. `Ir.Frame_address_lowering` consumes the checked identifier result and exact frame binding without repeating name lookup or layout arithmetic. A supported object location produces the three-instruction fragment with one pointer layer over its checked location type, exact source spans, and consecutive caller-owned identities. Static locals, callback declarators, module or outer bindings, direct functions, unsupported positive parameter arrays, and maximum-depth pointer values remain explicit unsupported paths. Loading, storing, index scaling, planner integration, execution, and machine emission are separate. [Issue #560](https://github.com/frankischilling/holyc-ocaml/issues/560) records this boundary.

`Compiler/Lex.HC:493-499` supplies the lookup precedence, `Compiler/PrsExp.HC:761-904` separates ordinary local lowering from global cases, `Compiler/PrsExp.HC:303-380` and `936-947` supply the specialized query behavior, and `Compiler/PrsVar.HC:521-614` supplies the publication and initializer-reset boundaries. `Driver.Function_expression_binding` visits ordinary expression nodes in source order, including array dimensions before publication and initializers afterward. `Sema.Function_expression_binding` begins with parameters plus `argc` and `argv`, publishes each indexed local once, keeps it through the remainder of the function, and returns either the selected binding or an explicit nonlocal candidate. It retains ordinary occurrences, specialized queries, `no_warn` suppressions, and post-initializer resets as separate events. The ordinary occurrence list still excludes member names, type syntax, `sizeof`, `offset`, `defined`, labels, and assembly tokens. The query list contains only `sizeof` and `offset` roots and name-shaped `defined` operands; it excludes member-path names and non-name operands. `HCSEMA0017` through `HCSEMA0019` report invalid batches and publication drift; `HCSEMA0031` and `HCSEMA0032` report inconsistent suppression or reset events. The pass is deterministic and read-only. Module lookup and local-warning classification consume different parts of this result, while member resolution, query evaluation, typing, storage, and lowering remain separate work. Issues #204, #226, and #228 track these boundaries.

`Kernel/KHashA.HC:29-106` searches the newest entry in a bucket before older entries and follows the linked table chain only after the current table. `HashAdd` at lines 169-188 prepends a record. `Compiler/CMain.HC:60-75` links a compilation-local table to its compilation-unit table, then to a JIT task table or an AOT parent and assembler table. `Compiler/Lex.HC:493-509` stops at a function member before it searches that chain. `PrsClass`, `PrsFunJoin`, and `PrsGlblVarLst` publish aggregate, function, and global records before their bodies or initializers, so visibility depends on source position rather than the completed AST.

`Driver.Module_expression_binding` reconciles the declaration collection with aggregate, function, and global semantic results. It builds one mixed publication stream, retains each source-site symbol, and associates joined aggregate and function sites with their canonical identities. `Sema.Module_expression_binding` advances that stream through each ordered function header. Locals keep precedence, the current function is available for recursion, and the newest visible module publication wins across kinds. A later declaration cannot leak into an earlier body. An absent name remains an outer candidate for a later JIT task, parent-task, AOT parent, assembler, export, or loader pass. `HCSEMA0020` and `HCSEMA0021` report invalid input and a missing function publication. The pass is read-only; expression-kind validation, task-level linkage, use counts, typing, storage, and lowering remain separate work. Issue #206 tracks this boundary.

`Compiler/CMain.HC:CmpJoin` and `LexStmt2Bin` repeatedly compile `PrsStmt` results, placing executable top-level statements on the same source timeline as declarations. Issues #308 through #326 establish source-ordered binding, outer lookup, immutable trees, typed leaves, scalar results, member paths, offsets, and direct or module-global callback calls. Issue #328 adds the selected-member call route from `Compiler/PrsExp.HC:998-1045`: direct or pointer access calls `MemberFind`, `MLF_FUN` selects callback storage, the stored `fun_ptr` becomes the active signature, and `PrsFunCall` binds its slots. Issue #330 follows the dimension path in `Compiler/PrsVar.HC:521-532` and `Compiler/PrsExp.HC:1002-1100`: a callback member keeps `MLF_FUN` and its dimensions together, and every subscript restores `CCF_FUN_EXP` until the call. The OCaml result reuses the completed aggregate index and indirect binder. It retains the global base, exact current-or-inherited lookup across parentheses and subscripts, computed callee result, stored recursive signature, fixed and variadic results, count, return class, and result identity. Ordinary members, invalid access shapes, and partial or excessive callback-member indexing fail with `HCSEMA0039` or `HCSEMA0057`; unrelated computed callees remain separate work. No callback address, storage read, default payload, IR, execution, or AOT `IET_MAIN` record is fabricated.

Issue #336 follows `Kernel/KernelA.HH:877-887`, `Compiler/CMain.HC:65-75`, `Compiler/Lex.HC:490-513`, and `Compiler/PrsExp.HC:421-530,867-904` for calls through scalar outer globals with checked callback metadata. The selected `CHashGlblVar` payload supplies the recursive header after JIT task-parent or AOT enclosing-compilation lookup. The top-level result uses the shared indirect argument binder and retains the exact outer occurrence, table binding, fixed and defaulted slots, variadic tail, count, return class, and expression-result identity. Callback arrays are covered by issue #344; ordinary outer objects and metadata-free records remain unavailable as callees. Runtime storage reads, address selection, default evaluation, IR, execution, and `IET_MAIN` emission are not part of this boundary.

Issue #340 follows `Compiler/PrsVar.HC:521-532` and `Compiler/PrsExp.HC:867-904,1016-1020,1055-1100` for function-body calls through callback arrays. Automatic and static locals, module globals, and metadata-bearing outer globals retain the recursive callback header alongside their declared rank. Every bracket restores the callable expression flag in the pinned parser, so the OCaml resolver accepts only an exact bracket chain rooted at one of those checked values. It retains the typed base, every integer-target subscript result, the recursive signature, fixed and variadic slot records, return class, and exact outer binding when present. Partial, excessive, and unindexed array calls receive `HCSEMA0039` or `HCSEMA0046`; an ordinary array never gains a callback signature. Accepted function parameters do not carry array dimensions, while issue #344 applies the same exact-rank rule to top-level outer callback arrays. Unrelated computed callees remain deferred. The pass does not calculate an indexed address, read callback storage, evaluate defaults, lower IR, or execute the call.

Issue #344 applies the same pinned global-array path to executable top-level expressions whose missing module name resolves through a supplied JIT task-parent or AOT enclosing-compilation table. `Compiler/PrsExp.HC:867-904` restores the `CHashGlblVar` dimension chain and callback header, lines 1055-1100 consume one dimension per bracket while preserving `CCF_FUN_EXP`, and lines 1016-1020 enter `PrsFunCall` only after the computed callee remains callable. The OCaml result accepts an exact bracket chain, types every subscript through the integer path, and retains the completed callee, outer occurrence, selected binding, recursive signature, fixed and variadic results, return class, and result identity. Grouping is retained because an otherwise unknown top-level name follows the parser's declaration disambiguation path. Zero, partial, and excessive ranks receive `HCSEMA0057`. Address calculation, storage reads, bounds checks, evaluation, IR, ABI placement, and execution remain unimplemented.

`Kernel/KernelA.HH:647-652` gives `CHashTable` one explicit `next` link. `Kernel/KHashA.HC:29-106` exhausts the newest-first records in one table before following that link. Each task owns a hash table at `Kernel/KTask.HC:215`, and `Spawn` at lines 244-273 links a child table to its parent task. `Compiler/AsmInit.HC:197-208` terminates the Adam task chain at `cmp.asm_hash`. `Compiler/CMain.HC:60-74` instead points a nested AOT compilation at its enclosing compilation and a top-level AOT compilation directly at the assembler table. `Compiler/Lex.HC:493-509` checks function locals before this chain, while `Compiler/PrsExp.HC:761-907` distinguishes export-system, function, global, and class records and rejects a missing ordinary name.

`Sema.Outer_environment` validates immutable snapshots of those mode-specific successors. JIT requires a current task at depth zero, ordered parent tasks, and one final assembler table. AOT accepts ordered enclosing compilations followed by one assembler table, including an assembler-only top-level shape. Each table accepts entries oldest to newest and indexes the final same-name record without flattening table precedence. `Sema.Outer_expression_binding` preserves local and module results and resolves only an explicit outer candidate. Its result retains the occurrence, stable symbol, record kind, entry order, table role, and table order. `HCSEMA0022` rejects malformed environments, `HCSEMA0023` rejects mismatched batches, and source-positioned `HCSEMA0024` rejects an ordinary name absent from the complete chain. The public driver and library paths are read-only. They do not construct live task tables, assign addresses, resolve alternate targets, create imports or fixups, choose loader records, validate an expression against the selected record kind, count uses, type expressions, allocate storage, or lower code. Issue #208 tracks this boundary.

`Compiler/PrsStmt.HC:209-447` allocates and publishes each `CHashGlblVar` with `HashAdd` before calling `PrsGlblInit`, then repeats the sequence for the next comma declarator. `Compiler/PrsVar.HC:1-204` routes every scalar leaf of a recursive braced initializer through `PrsExpression`. `Driver.Global_initializer_binding` collects the corresponding ordinary identifier occurrences and keeps their zero-based nested element paths. `Sema.Global_initializer_binding` validates each owner against the global-record and module-publication batches, advances the mixed publication stream through that owner, and selects the newest visible module record before consulting the supplied JIT or AOT outer chain. This makes self and earlier-declarator references visible without leaking later declarations backward. `HCSEMA0025` reports invalid semantic associations, and `HCSEMA0026` reports a name absent from both lookup stages. The pass does not type, evaluate, convert, store, relocate, lower, or execute initializer values. Issue #210 tracks this boundary.

The same global loop calls `PrsType` before allocating the current record. `Compiler/PrsVar.HC:247-283` parses each array suffix and sends an explicit dimension to `LexExpressionI64`; only the first dimension may be empty. `PrsType` completes that work at lines 285-370 before control returns to the `HashAdd` site. `Sema.Global_binding_environment` exposes the shared checked publication cursor. `Sema.Global_dimension_binding` stops it before the current global, resolves the dimension's ordinary identifier occurrences against earlier module records and then the supplied outer chain, and publishes the owner only after its dimensions have been processed. `Driver.Global_dimension_binding` validates the AST and semantic dimension origins, preserves empty suffixes, and assigns one occurrence order across a multidimensional declarator. `HCSEMA0027` reports invalid associations, and `HCSEMA0028` reports a name absent from both lookup stages. The pass does not evaluate an extent, apply 64-bit constant rules, type it, allocate storage, change layout, lower code, or execute it. Issue #212 tracks this boundary.

`Compiler/PrsStmt.HC:PrsGlblVarLst` calls `PrsType` for every ordinary, bound, and aggregate-attached global declarator. It stores the selected class and optional callback header on `CHashGlblVar`; callback globals remain in the variable hash category. `PrsArrayDims` records the ordered array chain, permits an empty first dimension, and requires expressions thereafter. `PrsClass` publishes a completed aggregate before sending attached declarators through that path. `Driver.Global_type_resolution` therefore replays canonical aggregate publication instead of consulting the completed module table. `Sema.Global_type_resolution` retains each symbol, source position, public, intrinsic, or aggregate type, zero through four return pointers, full recursive callback signature, structural dimensions, initializer shape, delimiter, and provenance. It validates ownership and order without evaluating extents or initializers, reconciling repeated globals, applying linkage, assigning storage, or emitting relocations.

`PrsFunJoin` combines its mode-specific lookup with flags on the newest same-name `CHashFun`. JIT reuses only a record whose `Cf_EXTERN` bit remains set; AOT reuses any newest record that is not marked `HTF_IMPORT`. `_HASH_ADD` and the two hash lookup routines establish the newest-first selection order. `PrsFun` and the bound `_extern` and `_intern` paths clear `Cf_EXTERN`, while `import` and `_import` set `HTF_IMPORT`. `Driver.Function_resolution` verifies those source binding shapes against the checked function batch. `Sema.Function_resolution` then retains the original identity symbol, ordered declaration sites, final state, and each replaced header under an explicit JIT or AOT mode. Header comparison and direct function-call slot resolution consume this result in separate passes. Evaluated defaults, parent-task lookup, alternate target binding, address assignment, import and export record emission, call lowering, and ABI behavior remain outside this pass.

`Compiler/PrsExp.HC:PrsFunCall` advances through `tmpf->arg_cnt` fixed members in declaration order. An opening comma, a closing parenthesis, or a parenthesis-free call selects `member->dft_val` only when `MLF_DFT_AVAILABLE` is present. A provided fixed expression advances the same one slot. The later loop accepts only expressions for the synthetic `argc` and `argv` tail and records the number in `argc_cnt`; a nonvariadic extra reaches the closing-parenthesis error path. `Sema.Function_call_resolution` applies that shape to direct identifier calls and to typed function-pointer values collected by `Driver.Function_call_resolution`. A direct result keeps the declaration header visible through `Sema.Module_expression_binding` and the joined canonical identity. A typed indirect result keeps the selected parameter, automatic or static local, global binding, or aggregate member lookup; its checked recursive callback signature; and whether source used an identifier, explicit dereference, direct member, or pointer member. Member selection follows `PrsExp.HC:998-1045`: `MemberFind` supplies the exact `CMemberLst`, `MLF_FUN` admits only callback members, `PrsPopDeref` preserves the callable value, and the following `(` enters indirect `PrsFunCall`. All paths retain the exact call node, fixed slot sources, provided variadic positions, and a semantic source view for every provided value. Prefix, postfix-update, binary, and cast views retain their recursive forms and origins. Named type targets and callback signatures must carry the exact aggregate identities published before the owning function item; stale same-name, wrong-module, and foreign-table inputs receive `HCSEMA0039`. Object, array, and callback shapes remain distinct. Arbitrary computed callees, aggregate callees, and outer candidates remain explicit deferred results. `HCSEMA0040` through `HCSEMA0042` cover a missing required slot, an extra fixed argument, and an omitted variadic expression. Issue #238 records direct call-slot resolution; issue #280 records named typed indirect-call resolution; and issue #290 records member callback calls. Target-side `F64` or integer policy is implemented under issue #240, and the subsequent expression issues retain their existing source-result rules for all resolved call forms. Issue #282 records `lastclass` substitution from the previous provided fixed expression. Issue #284 records a semantic result for every selected default: exact source and parameter identity, parameter type, forwarded class, default kind, and the immediate or AOT string path selected at lines 457-470. Default evaluation and payload copying, AOT string allocation and fixups, callback storage reads, arbitrary computed callback calls, lowering, execution, and ABI emission remain pending.

For a provided fixed expression, `PrsFunCall` runs passes 0 and 1, forwards `tmpi->ic_class` and `tmpm->member_class` with `OptClassFwd`, and compares their raw types at lines 477-484. A scalar `RT_F64` target marks a non-`F64` actual result with `ICF_RES_TO_F64`; otherwise an `F64` actual result receives `ICF_RES_TO_INT`. The array guard is structurally absent from accepted function parameters because `PrsArrayDims` rejects that context. Defaults enter the preceding `IC_IMM_I64` or AOT string path, while variadic expressions enter the later loop without this fixed-member comparison. `Sema.Function_call_conversion_policy` follows resolved aggregate backings for the target half of this branch. It stops at a pointer or callback, retains defaults and variadic values as separate paths, and rejects a nonterminating backing chain with `HCSEMA0044`. A backing header participates only after its source publication point, so a later completion cannot change an earlier caller. `HCSEMA0043` covers inconsistent semantic inputs. `PrsUnaryTerm` gives integer and character literals `RT_I64` or `RT_U64`, float literals `RT_F64`, and string literals a `U8` pointer at lines 679-700. `Sema.Function_call_conversion_decision` uses those literal facts, preserves them through parentheses, and records the exact combined flag intent. The `sizeof` path ends with an `RT_I64` immediate at lines 303-351, `offset` does the same at lines 353-381, `$$` selects an `RT_I64` or pointer result at lines 707-721, and `defined` produces an `RT_I64` Boolean at lines 936-953. The decision module therefore places all four on the non-`F64` side of the same comparison without evaluating them. At lines 960-1055, `PrsUnaryModifier` parses a postfix type, updates pointer state, replaces the preceding expression's class, and adds `IC_HOLYC_TYPECAST`. `Compiler/PrsVar.HC:285-319` supplies either the visible named class record or one of its four adjacent pointer records. `OptPass012.HC:87-110` then retains or folds that target class through `OptClassFwd`. The semantic view follows the same result: scalar `F64` and `F64i` casts use the floating class; other primitive and pointer casts use the non-`F64` class; and a by-value aggregate cast follows only backing headers visible before the caller. An unbacked class or union remains on the integer path. `PrsExp.HC:597-675` and `742-759` dispatch the eight prefix forms. The unary-plus and unary-minus paths forward the recursive operand class. `OptPass012.HC:153-191` retains that class for unary minus and logical not, including a logical-not `F64` operand. Address-of produces a pointer result, so it uses the non-`F64` path. The `IC_COM` branch at lines 153-160 assigns internal `I64` to the pushed result after optional immediate folding, so bitwise complement also uses the non-`F64` path independently of its operand class. Dereference removes one checked pointer-class step, retains array and callback address behavior, and then uses the resulting source-visible class in the same comparison. `PrsExp.HC:81-118` constructs `IC_PP_`, `IC_MM_`, `IC__PP`, or `IC__MM` with the forwarded operand class, and `OptPass012.HC:292-298` keeps that class for all four update forms. The semantic decision forwards the recursively audited class while leaving mutation and old-value behavior outside this boundary. `HCSEMA0045` rejects a foreign policy batch. Issue #240 records target policy, issue #242 records literal decisions, issue #244 records integer-primary decisions, issue #246 records primitive postfix-cast decisions, issue #248 records named aggregate postfix-cast decisions, issue #250 records prefix decisions, issue #255 records postfix-update decisions, issue #257 records prefix-update decisions, issue #261 records complement decisions, and issue #266 records pointer dereference results. General expression typing must supply every remaining actual class.

`PrsAddOp` and `PrsExpression2` build binary IC trees from the checked table at `Compiler/CInit.HC:288-329`. `OptFixupBinaryOp1` selects the higher forwarded raw class for ordinary binary operations. `OptPass012.HC:299-305` overrides power to `F64`; lines 693-823 finish logical and comparison results as `I64`; and the shift, arithmetic, and bitwise branches otherwise retain the shared operand-class result. `Sema.Function_call_resolution` stores the generated IC identity, operator origin, and both recursive operands. `Sema.Function_call_conversion_decision` applies those result classes when its operands are audited. An `F64` operand forces the floating path, two integer-side operands use the integer path, and an unavailable operand leaves the result unresolved unless the other side already forces `F64`. For assignment, `PrsExpression2` requires a dereference-shaped destination whose forwarded class is a pointer or internal storage class. `OptPass012.HC:824-897` keeps that destination as the result, marks the right operand for F64 or integer conversion, and selects F64 execution separately for compound arithmetic. `Sema.Function_call_expression_result` records those facts without applying a store. Issues #252 and #272 record the ordinary binary and assignment boundaries. Pointer scaling, value evaluation, assignment effects, and IC emission remain unimplemented.

`Compiler/PrsExp.HC:761-807` starts parameter, local, static-local, array, and callback expressions from their retained member record. `Compiler/PrsExp.HC:830-904` applies the corresponding source-visible global lookup. The array and callback branches keep an address-shaped result; ordinary objects dereference to their stored class before the call comparison. `Driver.Function_call_resolution` joins the exact `Module_expression_binding` occurrence with the existing function, local, or global type record by stable symbol ID. `Sema.Function_call_resolution` retains the resolved type and a separate object, array, or function-pointer shape. `Sema.Function_call_conversion_decision` sends arrays, callbacks, and `argv` to the integer result path, sends scalar values through `OptClassFwd`, and leaves an outer candidate unresolved. `HCSEMA0039` rejects foreign batches, mismatched owners, and an occurrence from another function traversal. Issue #259 records this classification boundary. Storage access, evaluation, IC construction, and ABI lowering are not part of it.

Issue #264 moves the source-result classification into `Sema.Function_call_expression_result`. The pass walks each provided fixed expression with a deterministic identity sequence and retains its exact source view, origin, known type, value category, and forwarded result class. Literal classes come from `PrsUnaryTerm`; casts come from `PrsUnaryModifier`; bound identifiers come from the local and global branches at lines 761-904; binary joins follow `OptFixupBinaryOp1`; power, comparison, logical, update, and complement overrides follow `OptPass012`. Address-of and update operands are walked in lvalue context. Issue #266 adds the checked dereference rule from `PrsExpression2` lines 97-118 and 145-154. `Sema.Type.dereference` removes one pointer layer while preserving the base identity. A source class with no pointer stars remains unchanged, including integer and `U0` address classes; the `U0` warning belongs to the later optimizer branch at `OptPass012.HC:275-289`. An array keeps its declared element type because `CCF_ARRAY` already supplies an address; a callback becomes a function value because `PrsPopDeref` removes the pending data dereference. The parent context decides whether the result remains an lvalue or is consumed as an object. An unknown outer operand remains unresolved. Issue #276 extends the same traversal to every provided variadic expression after the fixed slots. The later variadic loop at `PrsExp.HC:494-502` has no parameter member, so `Sema.Function_call_conversion_decision` retains the typed result and its actual class without a target conversion. Issue #278 covers a nested resolved direct call. Issues #280 and #290 apply the same result boundary to named and member callback calls: the nested result retains the indirect-call record and derives its type from the selected callback signature. `PrsFunCall` stores the active return class on the call and cleanup IC sequence at lines 547-586. Pointer returns remain address values, and by-value aggregate returns use backing headers visible before the caller. Issue #282 retains `PrsFunCall`'s `last_class` state: a provided fixed expression replaces it after `OptClassFwd`, an ordinary default leaves it unchanged, and `MLF_LASTCLASS` consumes the remembered base spelling. The semantic record keeps the exact prior expression plus that spelling; no prior typed result remains explicit. An unsupported computed callback callee remains unresolved rather than receiving a guessed signature. `HCSEMA0045` checks table and policy-traversal ownership, and `HCSEMA0046` rejects inconsistent nested-call ownership. Lastclass string allocation, variadic promotion, argument-count construction, callback storage reads, arbitrary computed callback calls, ABI placement, lowering, and execution remain pending.

Issue #342 follows all three `$$` branches at `Compiler/PrsExp.HC:707-721`. Assembly expressions choose an AOT absolute-address record or JIT immediate using `aotc->rip`. Class-offset mode produces the current `class_dol_offset` as internal `RT_I64`. The ordinary branch instead creates `IC_RIP` with internal `RT_PTR`, whose primitive-table representation is zero-depth `RT_I64`. `Sema.Function_call_expression_result` now retains that ordinary branch as an address value with an integer result class in function bodies and executable top-level statements. Grouping, call arguments, and binary parents preserve the leaf. Assignment and update contexts reject it as a non-lvalue. No concrete RIP value, class-offset evaluation, assembly fixup, IC instruction, relocation, or execution is created by this semantic slice.

Issue #292 applies that expression-result traversal to function returns. `Compiler/PrsStmt.HC:1111-1119` uses `return_class->size` to warn about missing or unexpected values, emits `IC_RETURN_VAL` with the declared class, and jumps to `lb_leave`. `Compiler/OptPass3.HC:249-257` compares the declared and expression raw classes to select `ICF_RES_TO_F64` or `ICF_RES_TO_INT`. `Compiler/OptPass4.HC:415-433` preserves the effective raw return type, while `Compiler/OptPass789A.HC:779-786` materializes the integer-side return through RAX. The OCaml semantic record keeps the source-ordered return, keyword and statement origins, optional checked expression, exact declared type, conversion intent, and value-presence warning fact. It does not create either return IC, a leave label, an epilogue, or executable ABI behavior.

Issue #294 applies the same traversal to function conditions. `Compiler/PrsStmt.HC:459-575` parses `if`, `while`, `do while`, and `for` conditions as ordinary expressions, then emits `IC_BR_ZERO` for exits or `IC_BR_NOT_ZERO` for the `do while` back edge. `Compiler/OptPass3.HC:285-319` folds zero tests for both integer and F64 immediates. The OCaml record keeps the statement role, keyword and statement origins, checked result class, and nested call identity. It does not insert `IC_TO_BOOL` or create branch control flow.

Issue #296 applies the expression traversal to function switch selectors. `Compiler/PrsStmt.HC:578-617` distinguishes parentheses from brackets, parses one ordinary expression, emits the range-origin subtraction, and selects `IC_SWITCH` or `IC_NOBOUND_SWITCH`. `Compiler/OptPass3.HC:269-273` retains the forwarded default label, and `Compiler/OptPass789A.HC:1012-1017` preserves the no-bound flag at final dispatch. The OCaml record keeps the explicit mode, keyword and statement origins, checked result class, and nested call identity. It does not subtract the lowest case, construct a jump table, or emit either switch operation.

Issue #298 retains function switch case values. `Compiler/PrsStmt.HC:673-714` assigns an omitted case from the preceding value and calls `LexExpressionI64` for each explicit single or ranged endpoint. `Compiler/PrsExp.HC:1140-1153` executes that expression and applies `ToI64` only when the returned raw type is `RT_F64`. `Compiler/PrsStmt.HC:767-792` later validates the total range, detects duplicates, and fills the jump table. The OCaml record keeps implicit, single, and ranged patterns distinct; both endpoints stay paired; and only an explicit `F64` result carries integer-conversion intent. It does not evaluate a value or perform the later range and table work.

Issue #300 retains ordinary function expression statements. `Compiler/PrsStmt.HC:1205-1207` sends the statement through `PrsExpression(cc,NULL,TRUE)`. `Compiler/PrsExp.HC:264-300` appends `IC_END_EXP` with `ICF_RES_NOT_USED` when that final argument is true. The OCaml semantic record keeps recursive source order, statement origin, checked expression facts, and nested call identity with the same discarded-result intent. It does not create the IC instruction, execute side effects, dispose of a runtime value, or include the separate implicit `Print` and `PutChars` statement path.

Issue #302 retains the separate implicit output path. `Compiler/PrsExp.HC:383-413` selects `PutChars` for a character marker and `Print` for a string marker. A zero marker is consumed before the fixed expression, while a nonzero marker remains as that expression. Lines 430-502 then bind the fixed slot and accept extra expressions only for `Print`. `Doc/HolyC.DD:35` states the same standalone behavior. The OCaml semantic record preserves the target, marker origin, fixed-source form, checked fixed value, ordered extra values, nested calls, and discarded-result intent. It does not look up either runtime header, validate formatting, promote varargs, lower a call, or execute output.

Issue #304 implements the next lookup boundary. `Compiler/PrsExp.HC:394-404` passes `HTT_FUN` to `HashFind`; `Kernel/KHashA.HC:40-78` shows that the mask is applied before the name comparison and that the search continues through older bucket entries, while lines 80-112 continue through the table chain. The OCaml pass therefore updates its module view only for function publications and searches each outer table from newest to oldest for a matching function record. A module match retains the active typed header and canonical function identity. An outer match retains the exact table and entry because the current outer snapshot has no signature payload. No match reports `HCSEMA0048` at the original string or character marker. Fixed-slot conversion and call lowering remain separate work.

Issue #306 implements fixed and variadic binding after that lookup. `Compiler/PrsExp.HC:430-454` consumes the selected header's members in order. Lines 455-468 choose a declared default when no value is present and distinguish the AOT string path. Lines 473-484 forward both classes and select `ICF_RES_TO_F64` or `ICF_RES_TO_INT`; lines 491-511 accept the remaining expressions only for the `Print` variadic path. The OCaml result keeps provided and defaulted slots distinct, records each omission and conversion, and retains only the tail after the fixed parameter count as variadic values. A caller may supply a checked outer header only for the exact symbol selected by issue #304; otherwise the output remains deferred. `HCSEMA0050` and `HCSEMA0051` reject missing required and excess nonvariadic values. Format parsing, evaluated default payloads, vararg promotion, call lowering, and output remain separate work.

Issue #346 applies the target half of this path to executable top-level statements. `Compiler/PrsStmt.HC:1201-1203` uses the same `PrsFunCall(cc,NULL,FALSE,NULL)` branch outside a function. `Compiler/CMain.HC:35-89` constructs the JIT task or AOT parent chain and feeds top-level statements to `LexStmt2Bin` in source order. The OCaml module groups the existing typed fixed and trailing roots by output identity, retains the literal marker separately from an empty-marker following expression, and publishes only function headers whose module item precedes the statement. A same-name aggregate or global does not hide an older function. Lookup then follows the exact outer environment stored on the typed batch. A module match keeps its checked header, resolved declaration, and canonical identity; an outer match keeps the exact entry without inventing a signature. `HCSEMA0058` rejects inconsistent inputs, and `HCSEMA0059` reports a missing header at the marker. Parameter binding, format handling, lowering, `IC_END_EXP`, and output remain separate work.

Issue #348 applies the fixed-slot and variadic half of `PrsFunCall` to those selected top-level targets. `Compiler/PrsExp.HC:430-454` consumes header members and provided values in order. Lines 455-468 choose a declared default and distinguish the AOT string path. Lines 473-484 attach integer or `F64` result intent, and lines 491-511 retain excess values only for a variadic header. One shared OCaml rule module now plans those paths for both function-body and top-level adapters. The top-level result keeps its exact target and roots, provided and defaulted slots, conversion intent, and remaining variadic roots. An outer target without a checked header for the same symbol remains deferred. `HCSEMA0061` and `HCSEMA0062` reject missing required and excess nonvariadic values. Format parsing, evaluated default payloads, vararg promotion, lowering, `IC_END_EXP`, and output remain separate work.

Issue #350 gives executable top-level conditions their own checked consumer view. `Compiler/PrsStmt.HC:459-564` parses each condition as an ordinary expression, then chooses `IC_BR_ZERO` for `if`, `while`, and `for`, or `IC_BR_NOT_ZERO` for the `do while` back edge. `Compiler/OptPass3.HC:285-319` accepts both integer and `F64` immediate values in those zero tests. The OCaml result reuses the typed root, retains its containing top-level statement, contiguous identity, role, keyword origin, and nested call identity, and records the later branch sense separately. `HCSEMA0063` rejects a foreign table or discontinuous condition indexes. No Boolean conversion, branch IC, label, execution, or `IET_MAIN` record is created.

Issue #352 gives executable top-level switch selectors their own checked consumer view. `Compiler/PrsStmt.HC:578-617` parses an ordinary selector and chooses `IC_SWITCH` for parentheses or `IC_NOBOUND_SWITCH` for brackets. `Compiler/OptPass3.HC:269-273` treats both operations alike when forwarding the default label, while `Compiler/OptPass789A.HC:1012-1017` preserves the no-bound flag through final emission. The OCaml result reuses the typed root and retains its containing statement, contiguous identity, explicit mode, switch keyword origin, and nested call identity. `HCSEMA0064` rejects a foreign table or discontinuous selector indexes. No evaluation, range subtraction, label, jump table, switch IR, execution, or `IET_MAIN` record is created.

Issue #354 adds the matching executable top-level case view. `Compiler/PrsStmt.HC:663-718` distinguishes an implicit case from a single expression or a range, assigns implicit values in sequence, evaluates explicit values through `LexExpressionI64`, swaps reversed bounds, and expands every value into the case list. `Compiler/PrsExp.HC:1140-1152` shows that `LexExpressionI64` converts an `RT_F64` result before returning it. The OCaml result deliberately stops earlier: it keeps the implicit, single, or ranged pattern, the ellipsis, both ranged endpoints, the exact typed roots, and `ICF_RES_TO_INT` intent on an `F64` value. `HCSEMA0065` rejects foreign, discontinuous, missing, or excess input. It does not evaluate a value, assign the implicit sequence, normalize or expand a range, check duplicates, create labels, build a jump table, emit switch IR, execute code, or create `IET_MAIN`.

Issue #356 enforces the source boundary on explicit top-level returns. `Compiler/PrsStmt.HC:1089-1120` accepts `return` only while `cc->htc.fun` names an active function, then emits `IC_RETURN_VAL` and a leave jump. `Compiler/CMain.HC:1-27` separately changes the final ordinary top-level expression into `IC_RETURN_VAL2` followed by `IC_RET`. The OCaml validator walks only executable top-level statements and reports `HCSEMA0066` at the first explicit `return`; it deliberately skips function definitions and does not disable the later implicit-result rule. Lowering both return operations remains future work.

Issue #316 applies the same scalar result engine to executable module roots after issues #312 and #314 have supplied complete source trees and typed identifier leaves. `Compiler/PrsExp.HC:PrsUnaryTerm`, `PrsTerm`, `PrsExpression2`, and `PrsExpression` define literal classes, module record shapes, lvalue use, operator results, and discarded expression statements. `Compiler/OptLib.HC:OptClassFwd` and `Compiler/OptPass012.HC` define forwarded integer and `F64` classes plus conversion flags. The OCaml adapter carries the immutable identifier batch through the existing recursive traversal. It retains statement and root roles, stable result IDs, source origins, exact module value types, array ranks, direct-function address paths, lvalue checks, result classes, execution classes, and assignment conversions. Later issues add checked direct and callback calls; outer records without type payloads remain unavailable instead of becoming integers. `HCSEMA0057` rejects mismatched table, tree, mode, member-index, policy, or identifier inputs. The pass is read-only and does not evaluate side effects, lower IR, execute code, or emit `IET_MAIN`.

Issue #318 removes the deliberate top-level object-member boundary while retaining the same recursive engine. `Compiler/PrsExp.HC:PrsTerm` selects direct or pointer access, validates the base class, calls `MemberFind`, keeps the member type and dimensions, and chooses callback or storage behavior. `Compiler/LexLib.HC:MemberFind` searches the queried aggregate before its base chain. `OptClassFwd` supplies the source-visible integer or `F64` class for a by-value member. The OCaml result retains the exact member-index lookup, declaring aggregate, inheritance depth, byte layout, type, array rank, callback shape, lvalue category, and backing class. The same postfix indexing, update, assignment, and scalar rules then consume that result. Wrong access shapes, incomplete aggregates, and missing members retain `HCSEMA0046` at the operator or member name. Outer records without checked type payloads, use counting, address calculation, calls, evaluation, lowering, execution, and `IET_MAIN` remain separate.

Issue #320 implements the distinct class-record route through `PrsOffsetOf`. `Compiler/PrsExp.HC:353-381` requires a direct member path, begins at zero, adds each `CMemberLst.offset`, advances through `member_class`, and emits `IC_IMM_I64` with intrinsic `I64`. `PrsTerm` selects this routine for `HTT_CLASS`, while `MemberFind` supplies the current-class and inherited lookup order. The OCaml result therefore keeps a separate offset category, the source-visible aggregate publication, every checked lookup, the type needed by the next segment, each cumulative offset, and the final integer value. It does not turn the base into an object or forward an `F64`-backed final member to `F64`. A bare base, pointer syntax, missing or incomplete member, pointer continuation, or nonaggregate continuation reports `HCSEMA0046` at its source. The parser still follows its existing declaration ambiguity: a class name at statement start is a declaration head, so offset fixtures enter expression parsing before the class token. Runtime address calculation, use counting, general constant evaluation, IR, execution, and `IET_MAIN` remain separate.

Issue #520 applies that lookup contract to the standalone `KW_OFFSET` source form. Function-body and executable top-level trees retain the keyword, balanced wrapper pairs, root spelling and source-visible publication, and every member spelling and origin as one expression object. The result pass resolves members against the immutable index in source order, rebuilds the retained source object with those exact lookup identities, records each intermediate type and cumulative offset, and returns one internal-`I64` offset result. An absent publication is deliberately retained until this pass so an unknown root receives the same source-positioned `HCSEMA0046` family as incomplete roots and invalid continuations. This slice implements aggregate publications only; the local-variable and global-object branches present in `PrsOffsetOf` remain later work.

Issue #522 consumes the checked standalone result through the same owner-neutral planner as direct aggregate offsets. Before identity allocation, the lowerer verifies the exact retained source refinement and the complete internal-`I64`, integer, offset, rank, path, and cumulative-value evidence. It emits one zero-operand `IC_IMM_I64` with the exact standalone expression span, byte payload, zero flags, and consecutive identities. Transparent wrappers, integer composition, mixed `F64` producer conversion, and expression-statement termination work in function bodies and executable top-level statements under both modes. No member lookup, layout arithmetic, object-address calculation, interpretation, optimization, or execution is repeated or added.

Issue #494 applies that class-record route to checked function-body expressions. The function resolver retains the exact source-visible aggregate occurrence and publication as a specialized base instead of treating it as an ordinary unresolved value. The shared expression engine accepts that base only beneath direct member access, reuses `Aggregate_member_index` for every current-before-base lookup, and carries the same cumulative `I64` offset path through grouping and numeric composition. Invalid ownership, a bare base, pointer syntax, missing members, and invalid continuation fail with `HCSEMA0046` before a checked result batch is exposed. Because a visible class name at the start of a statement still enters declaration parsing, focused call-argument fixtures begin with a numeric operand before reaching the class token.

Issue #492 consumes that completed top-level path at the canonical IR boundary. `Compiler/PrsLib.HC:79-97` records the immediate data, target type, source, flags, and insertion order supplied by `PrsOffsetOf`; `Compiler/CInit.HC:27` confirms the zero-argument, one-result `IC_IMM_I64` shape. The lowerer checks the retained internal `I64` offset result and requires its final value to match the last cumulative member segment before allocating identities. It emits one immediate with the exact expression span and composes it through grouping, integer arithmetic, mixed `F64` conversion intent, and `IC_END_EXP` in both modes. Ordinary object-member access remains unsupported, and no lookup, layout arithmetic, runtime address calculation, interpretation, or execution occurs in this slice.

Issue #496 confirms that the same owner-neutral lowerer consumes the checked function-body paths from issue #494. Direct, inherited, nested, grouped, and numeric function results retain the same immediate shape, exact spans, zero flags, and consecutive identities as top-level results. Mixed `F64` use marks only the offset producer with `ICF_RES_TO_F64`, and an ordinary function expression statement appends `IC_END_EXP`. The planner still rejects object-member results and exposes no partial sequence for inconsistent semantic evidence.

`PrsFunJoin` initializes a new record with `Cf_EXTERN` and the `FSG_FUN_FLAGS1` subset of parser staging. Reused records keep their stored calling flags. Each header replaces `HTF_PUBLIC`, while `HashSrcFileSet` may accumulate `HTF_PRIVATE`; parsing may likewise accumulate `Ff_DOT_DOT_DOT` and the derived `Ff_RET1`. The `_intern`, `_extern`, import, and definition paths then mutate that selected record in source order. `PrsExp.HC`, `Kernel/KHashB.HC`, `Kernel/FunSeg.HC`, `Compiler/CHash.HC`, and `Compiler/AsmResolve.HC` consume the resulting domains differently. `Sema.Function_record_classification` replays those transitions and exposes call access, hash value behavior, runtime lookup, map visibility, and AOT resolution and publication policy. It deliberately keeps the function type mask, hash flags, shared class flags, and stored function flags separate. Issue #191 tracks this classification boundary.

`PrsGlblVarLst` does not call the function join routine for objects. Each global declarator receives a new `CHashGlblVar`, after which a definition may update only the newest prior same-name global. JIT requires that prior record to carry `HTF_UNRESOLVED`; AOT code-heap output accepts any prior global state. The older record receives `GVF_ALIAS` and copies the newer definition's address fields. These updates form immediate edges rather than a transitive identity merge, and no type comparison gates them. AOT data-heap output instead reports its same-name case as unimplemented. The allocation option is checked only in the default definition arm: ordinary and `_intern` definitions select the data heap when bit 35 is set, while extern and import forms stay on their binding-specific paths. An AOT data-heap initializer is rejected before `PrsGlblInit`; JIT retains initializer processing. `Driver.Global_resolution` checks each parsed binding against the resolved global-type batch, while `Sema.Global_resolution` keeps every record, mode-specific state, derived storage policy, alternate target provenance, and immediate alias edge. It applies the AOT initializer and same-name restrictions without allocating memory. Address evaluation, heap allocation, visibility, warnings, parent-task lookup, import resolution, and relocation emission remain separate work.

`Kernel/KernelA.HH` defines the global hash type, nine `HTF_*` bits, and six `GVF_*` bits used by these records. `Compiler/PrsStmt.HC:PrsGlblVarLst` assigns the parser-side combinations. `Compiler/PrsExp.HC`, `Kernel/KHashB.HC`, `Compiler/CHash.HC`, and `Compiler/AsmResolve.HC` distinguish value access, ownership, map eligibility, and AOT publication. `tools/global_record_flag_source.ml` checks those definitions and consumer anchors against seven pinned file digests, and `tools/global_record_flag_gen.ml` produces `src/generated/global_record_flags.ml`. `Sema.Global_record_classification` applies the checked masks to each reconciled record with its ordered public/static staging, `OPTf_KEEP_PRIVATE` snapshot, and agreement on the two resolution-affecting option bits. It retains import names, `GVF_DATA_HEAP`, access policy, and the absence of an AOT export on a data-heap definition without allocating storage or emitting a loader record. Issue #189 tracks this classification boundary; issue #232 applies the data-heap option.

`Compiler/PrsStmt.HC` creates or finds a `CMT_GOTO_LABEL` for both gotos and language definitions. `Compiler/PrsLib.HC:COCGoToLabelFind` includes `CMT_ASM_LABEL` in that lookup, and `COCDel` rejects undefined entries while warning about unused definitions. `Compiler/Asm.HC` publishes function-local assembly definitions to the same code-control list and increments their use count to suppress that warning. `Driver.Label_resolution` walks the same recursive statement routes as function collection and retains language gotos plus global, exported global, and `@@` local assembly-block definitions. `Sema.Label_resolution` validates the batch before mutation, assigns one stable label identity per spelling and function, orders identities by first occurrence, and uses the actual definition as symbol provenance. Repeated assembly definitions retain their count because `Compiler/Asm.HC:1032-1062` accepts them only when assembler layout proves that their addresses match. Issue #174 tracks that check.

Issues #164, #166, #168, #170, #172, #175, #177, #179, #181, #183, #185, #187, #189, #191, #194, #200, #202, #204, #206, #208, #210, #212, #214, #216, #218, #220, #222, #224, #226, #228, #230, #232, #236, #238, #240, #242, #244, #246, #248, and #250 record the checked scope, declaration, member, label, type, identity, record-classification, closed-layout, function-binding, expression-binding, local-warning, header-comparison, direct-call, conversion-policy, literal-decision, integer-primary-decision, postfix-cast-decision, prefix-decision, and compiler-option boundaries. Header resolution covers definition backings and bases in one module; member type resolution covers direct member heads, complete recursive callback signatures, and the exact `MLF_FUN` distinction between callback and object members; closed layout covers primitive and prior aggregate storage, arrays, pointers, callbacks, bases, unions, explicit positions, and negative displacements; function type resolution covers top-level prototypes and definitions, including recursive parameter callbacks and source-derived member-list masks; global type resolution covers ordinary and aggregate-attached globals, including recursive callbacks and structural arrays; local type resolution covers automatic and static declarations, including recursive callbacks, structural arrays, and declaration-time `MLF_FUN` and `MLF_STATIC` masks; the function binding index validates their completed shared namespaces; function expression binding selects visible parameters and locals at each publication point and retains specialized local queries; module expression binding selects the newest source-visible aggregate, function, or global publication for ordinary occurrences and specialized name queries; outer expression binding follows a supplied JIT or AOT table snapshot; global initializer binding applies those module and outer rules immediately after each owning global publication; global dimension binding applies them immediately before it; function-default binding advances through the current header without installing parameter locals; direct-call resolution maps each syntactically direct function-body call to the visible header and canonical joined target; call conversion policy follows each provided fixed target to its `F64` or integer result path; literal conversion decisions combine exact literal source classes with that target; integer-primary decisions apply the checked non-`F64` class of current-position, size, offset, and definition-query forms; primitive postfix-cast decisions apply the explicit target class assigned by the source cast path; named aggregate postfix-cast decisions bind the source-visible aggregate identity and forward it through the headers already published before the caller; and prefix decisions retain recursive operands and classify the source-backed unary and address paths. Function and global reconciliation remain limited to checked declarations from one module and their separate JIT or AOT rules. Record classification retains source-backed consumer intent without performing allocation, fixup, or emission.

Issues #252, #255, #257, and #259 add the binary, postfix-update, prefix-update, and bound-identifier decision boundaries to the issue sequence above. Their checked scope is the source-selected result class of those expressions when used as direct-call arguments. Bound identifiers reuse prior type and binding passes; the other listed paths retain their recursive operand classifications. Issue #274 adds the shared primitive-or-pointer lvalue check for all four updates. Issue #276 applies the same typed-result traversal to provided variadic expressions while keeping the fixed-member conversion branch separate. Issue #278 applies the source-visible return class to nested resolved direct calls while retaining deferred callback identities. Storage access, remaining indirect-call typing, assignment and update effects, mutation, pointer scaling, old-value behavior, variadic promotion, and ABI placement remain outside those boundaries.

No semantic pass resolves named aggregate casts outside the documented function-expression source view, resolves `offset`, evaluates unsupported `sizeof` roots, or calculates runtime member addresses and use counts; binds or evaluates defaults nested in callback members; supplies type payloads for metadata-free outer records; compares function headers; evaluates defaults, local or global initializers, metadata, or symbol-dependent array extents; implements subinteger access; allocates local or global storage; constructs live runtime task inheritance or compiler chains; completes task-level linkage; resolves alternate binding addresses; emits import and export records; resolves assembly operand references; validates repeated assembly-definition addresses; warns about unused labels; or constructs control flow. Global initializer and extent names have source-ordered bindings, but their values remain unevaluated. Epic #161 tracks the remaining symbol and linkage consumers, while epic #193 tracks the remaining class and layout work.

`Compiler/PrsStmt.HC:62-138` finds or inserts the named `CHashFun` before `PrsFunJoin` calls `PrsVarLst`. The caller at lines 140-207 assigns that returned record to `cc->htc.fun` and `cc->htc.local_var_lst` only after the complete header has been parsed. `Compiler/PrsVar.HC:485-529` still adds each named parameter to the function member list before it parses the default at lines 619-658, but `Compiler/Lex.HC:493-509` consults only the currently active local list before the hash chain. An ordinary parameter spelling inside a default therefore does not select that parameter. The current named function is already in the module table, so a recursive default does select its current publication. Missing module names follow the JIT or AOT successor selected at `Compiler/CMain.HC:60-74`. `Compiler/PrsExp.HC:383-480` consumes expression and `lastclass` defaults at calls, while `Compiler/LexLib.HC:157-180` compares headers; neither later behavior is part of name binding.

`Sema.Module_binding_environment` validates the mixed module stream and supplied outer snapshot once. `Driver.Function_default_binding` pairs each top-level named function AST with its resolved declaration and collects ordinary identifiers from fixed-parameter expression defaults. `Sema.Function_default_binding` advances through the current publication, binds module records before outer records, retains distinct source and joined canonical identities, and keeps parameter and occurrence positions plus generated-source provenance. A `lastclass` default remains on its resolved parameter without an ordinary occurrence. `HCSEMA0029` reports invalid cross-pass associations, and `HCSEMA0030` reports an ordinary name absent from both lookup stages. The pass is read-only and does not evaluate, convert, store, or substitute a default; compare headers; check calls; or traverse defaults nested inside callback, member, global, or local declarators. [Issue #214](https://github.com/frankischilling/holyc-ocaml/issues/214) records this boundary.

`Compiler/PrsExp.HC:383-430` resolves a call through one `CHashFun`, and lines 544-586 read that same record while constructing the call sequence. `Sema.Function_call_resolution` now retains the exact resolved declaration already checked against the source-visible module publication, together with the active header and canonical identity. Replacement headers therefore remain distinguishable across source order in both JIT and AOT mode. `Sema.Function_call_expression_result` exposes the same declaration as the stable join key for `Sema.Function_record_classification`; no name-based linkage reconstruction or call emission occurs at this boundary.

Issue #528 consumes the exact direct-call declaration retained at `Compiler/PrsExp.HC:383-430` and joins it to the declaration snapshot whose flags select the branches at lines 555-571. `Sema.Function_call_target_classification` retains the typed call, resolved and classified declarations, classified record, and total access policy. Replacement headers stay source ordered in JIT and AOT mode; a foreign resolution receives `HCSEMA0067` rather than a name-based fallback. Internal-opcode decoding, argument and cleanup policy, address selection, and IC construction remain later work.

Issue #542 applies that join to executable top-level direct calls. `Compiler/CMain.HC:CmpJoin` returns executable input to the same expression parser, so `Sema.Top_level_function_call_target_classification` consumes the exact declaration retained for each source-positioned module call and exposes the same five-way call-access policy. Replacement declarations remain distinct in JIT and AOT mode, while `HCSEMA0068` rejects evidence from a separate resolution. This boundary does not construct top-level ICs or `IET_MAIN`.

## Compiler option state

`Kernel/KernelA.HH` assigns 12 compiler-option bit indices. They are not pre-shifted masks. Bits 0 and 1 control echo and trace behavior, bits 16 through 19 control warnings, and bits 32 through 37 affect linkage, symbol retention, allocation, optimization, and code emission. The ranges 2 through 15 and 20 through 31 are unused in the pinned definitions.

`Compiler/Lex.HC:CmpCtrlNew` creates a controller with `OPTf_WARN_UNUSED_VAR` and `OPTf_WARN_HEADER_MISMATCH` enabled, giving an initial mask of `0x90000`. `Compiler/CMain.HC` copies a parent controller's option mask when compilation is nested. This makes the options part of compile-stream state rather than process-wide settings.

`Compiler/CMisc.HC:Option` and `GetOption` operate on `Fs->last_cc->opts`. `Option` calls `BEqu`, whose `_BEQU` implementation in `Kernel/KUtils.HC` uses `BTS` or `BTR` followed by `ADC`; its result is the bit's state before the change. The OCaml API exposes the same return contract as a pure update that yields the new mask and previous state.

The audited core consumers establish these phase assignments:

| Option | Bit | Initially enabled | Observed core use |
| --- | ---: | --- | --- |
| `OPTf_ECHO` | 0 | No | Lexer echo and compiler option display |
| `OPTf_TRACE` | 1 | No | Parser and compiler trace output |
| `OPTf_WARN_UNUSED_VAR` | 16 | Yes | Function-level unused-variable diagnostics |
| `OPTf_WARN_PAREN` | 17 | No | Unnecessary-parenthesis diagnostics |
| `OPTf_WARN_DUP_TYPES` | 18 | No | Duplicate local type diagnostics |
| `OPTf_WARN_HEADER_MISMATCH` | 19 | Yes | Function header comparison |
| `OPTf_EXTERNS_TO_IMPORTS` | 32 | No | Declaration parsing and AOT linkage |
| `OPTf_KEEP_PRIVATE` | 33 | No | Private source symbol retention |
| `OPTf_NO_REG_VAR` | 34 | No | Register-variable optimization passes |
| `OPTf_GLBLS_ON_DATA_HEAP` | 35 | No | Global allocation and AOT initialization rules |
| `OPTf_NO_BUILTIN_CONST` | 36 | No | Floating constant selection in the backend |
| `OPTf_USE_IMM64` | 37 | No | Import call lowering and emission; marked incomplete in the source |

`tools/compiler_option_source.ml` validates the definitions, source comments, initial mask, public API, `_BEQU` behavior, and core references while ignoring occurrences in comments and literals. `tools/compiler_option_gen.ml` writes the pinned checksums and line references to `src/generated/compiler_options.ml`. `Sema.Compiler_option` provides immutable lookup and mask operations. Function and global resolution consume `OPTf_EXTERNS_TO_IMPORTS`; global resolution consumes `OPTf_GLBLS_ON_DATA_HEAP`; record classification consumes `OPTf_KEEP_PRIVATE` and checks agreement on resolution-affecting bits; local warning analysis consumes `OPTf_WARN_UNUSED_VAR`. A live compiler controller, source-positioned `Option(...)` execution, and the remaining downstream effects are not implemented.

## Function and calling flags

`CHashFun` inherits the `U16 flags` field declared by `CHashClass` in `Kernel/KernelA.HH`. `Cf_EXTERN` and `Cf_INTERNAL_TYPE` occupy bits 0 and 1 of that shared field. The eight `Ff_*` function positions are contiguous from bit 8 through bit 15; the lower-byte gap is part of the representation and is not compacted in the OCaml model.

`Compiler/CompilerA.HH` defines a second flag set used while `PrsStmt` reads a declaration. `FSF_PUBLIC`, `FSF_ASM`, `FSF_STATIC`, and `FSF__` occupy bits 0 through 3 of this temporary value. The four calling modifiers use the same masks as their stored `Ff_*` counterparts. `FSG_FUN_FLAGS1` selects only those four stored bits, while `FSG_FUN_FLAGS2` adds public parser state.

`PrsFunJoin` copies `FSG_FUN_FLAGS1` into `CHashFun.flags` and transfers `FSF_PUBLIC` to the hash type separately. It derives `Ff_RET1` only when the fixed argument block is nonempty, no varargs marker is present, and the eight-byte argument slots fit a signed 16-bit return immediate. This gives a fixed-argument range of 1 through 4,095.

The declaration modifier assignments are order-sensitive. `interrupt` sets both `FSF_INTERRUPT` and `FSF_NOARGPOP`. The calling modifiers and `public` retain existing calling, public, and assembly state. `static` keeps assembly state but clears the other staged bits. A leading underscore handled by `_extern` or `_import` adds `FSF__` without clearing state.

`PrsExp.HC` selects caller cleanup with `(Ff_RET1 || Ff_ARGPOP) && !Ff_NOARGPOP`. `OptPass789A.HC` applies the same rule in function epilogues. That backend also uses `Ff_INTERRUPT` for interrupt save/restore and `IRETQ`, and it removes an eight-byte error-code slot when `Ff_HASERRCODE` is present. `PrsVar.HC` marks varargs with `Ff_DOT_DOT_DOT`; `OptPass3.HC` accounts for the synthetic `argc` and `argv` members. Internal functions are dispatched as IC operations in `PrsExp.HC`, excluded from ordinary call clobbering in `OptPass6.HC`, and omitted from address-to-function lookup in `Kernel/FunSeg.HC`.

`tools/function_flag_source.ml` checks 12 pinned source digests, the function hash type, definitions, source expressions, structure ownership, modifier assignments, record transitions, and downstream consumers while ignoring comments and literals. `tools/function_flag_gen.ml` emits immutable nested modules and source references in `src/generated/function_flags.ml`. `Sema.Function_flag` provides typed lookup and pure transition helpers, and `Sema.Function_record_classification` applies those values to reconciled function identities without changing the symbol table. The parser retains all four calling modifier tokens on bound prototypes. Header comparison intentionally omits these flags, matching `MemberLstCmp`. Modifier-combination diagnostics, call lowering, and native ABI emission remain later work. [The ABI notes](holy-abi.md) keep that boundary explicit.

## Typed outer global records

`Kernel/KernelA.HH:877-887` gives `CHashGlblVar` a class pointer, optional function-pointer header, dimension count, flags, and address fields. `Compiler/CMain.HC:65-75` builds the JIT task-parent or AOT enclosing-compilation hash-table chain, `Compiler/Lex.HC:490-513` selects the matching record, and `Compiler/PrsExp.HC:867-904` reads the global class, array dimensions, and callback header when forming an expression.

`Sema.Outer_environment` can attach that checked type shape to an immutable outer global entry. `Sema.Function_call_expression_result` consumes the exact `Outer_expression_binding` traversal used by the function call pipeline and retains both its source occurrence and selected entry. The executable top-level path consumes the parallel `Top_level_outer_expression_binding` traversal and retains its distinct occurrence type with the same selected entry. A scalar object follows its primitive, pointer, or aggregate type; a positive rank produces an array value; and a recursive callback header produces a callback value. Records without checked metadata remain unavailable. A rank-zero callback header binds function-body and top-level call slots. Function-body and executable top-level typing also accept a fully indexed callback array and retain its typed base and subscript results. Both accepted routes determine the return type without reading the stored callback address. The constructor rejects negative ranks, metadata on a non-global record, and aggregate identities from another semantic session. Expression typing also rejects a stale batch, a different compilation mode, or a batch that omits a checked function. These slices assign no address, read no outer storage, emit no relocation, and execute no callback. [Issue #332](https://github.com/frankischilling/holyc-ocaml/issues/332) records the function-expression value boundary, [issue #334](https://github.com/frankischilling/holyc-ocaml/issues/334) records executable top-level values, [issue #336](https://github.com/frankischilling/holyc-ocaml/issues/336) records executable top-level outer callback calls, [issue #338](https://github.com/frankischilling/holyc-ocaml/issues/338) records scalar function-body outer callback calls, [issue #340](https://github.com/frankischilling/holyc-ocaml/issues/340) records fully indexed function-body callback arrays, and [issue #344](https://github.com/frankischilling/holyc-ocaml/issues/344) records fully indexed executable top-level outer callbacks.

## Intermediate-code operation table

`Compiler/CompilerA.HH` defines `IC_END` at `0x00` through `IC_ATAN` at `0xB8`, followed by `IC_ICS_NUM` at `0xB9`. The numeric range is contiguous. The same file defines four argument shapes and four structural categories in `CIntermediateStruct`.

`Compiler/CInit.HC:intermediate_code_table` supplies one record per code. Each record contains the argument shape, result count, structural category, `fpop`, `not_const`, three zero padding bytes, and a display name. `Compiler/OptPass012.HC` uses the argument count to rebuild expression trees, sets `CCF_NOT_CONST` from `not_const`, and pushes results according to `res_cnt`. `Compiler/OptLib.HC` uses `fpop` while arranging floating-stack behavior. Other parser, optimizer, diagnostic, and backend units also query these fields.

The `IC_*` constant name and the display name are not interchangeable. Thirteen records differ:

| Numeric code | Constant | Display name |
| ---: | --- | --- |
| `0x8F` | `IC_BR_EQU_EQU2` | `BR_2EQU_EQU` |
| `0x90` | `IC_BR_NOT_EQU2` | `BR_2NOT_EQU` |
| `0x91` | `IC_BR_LESS2` | `BR_2LESS` |
| `0x92` | `IC_BR_GREATER_EQU2` | `BR_2GREATER_EQU` |
| `0x93` | `IC_BR_GREATER2` | `BR_2GREATER` |
| `0x94` | `IC_BR_LESS_EQU2` | `BR_2LESS_EQU` |
| `0xA8` | `IC_SWAP_I64` | `SWAP_U64` |
| `0xAB` | `IC_MIN_I64` | `I64_MIN` |
| `0xAC` | `IC_MIN_U64` | `U64_MIN` |
| `0xAD` | `IC_MAX_I64` | `I64_MAX` |
| `0xAE` | `IC_MAX_U64` | `U64_MAX` |
| `0xB0` | `IC_SQR_I64` | `SQRI64` |
| `0xB1` | `IC_SQR_U64` | `SQRU64` |

`tools/intermediate_code_source.ml` parses both tables without deriving one name from the other. `tools/intermediate_code_gen.ml` emits an exhaustive `Ir.Opcode.t` variant, typed metadata, safe lookups, pinned checksums, and original line numbers.

`Kernel/KernelA.HH:1602-1632` defines the low instruction flags, both three-bit floating-stack fields, and the high optimizer and lock flags. Its `CIntermediateCode` records combine those flags with an opcode, source line, target class, data union, and tree links. `Compiler/PrsLib.HC:ICAdd` appends those records in source order, while `Compiler/OptPass012.HC` rebuilds argument links from the table's counts.

`Ir.Instruction_sequence` turns that evidence into an immutable checked sequence. It uses the generated opcode metadata for operand and result validation, admits only the pinned flag fields, and retains explicit IDs, the independent `ic_class`-shaped target type, payload bits and bytes, and source spans. It rejects duplicate or out-of-order values and a produced value without a target type. Its deterministic `holyc-ir-v1` text is a hosted inspection schema, not an upstream TempleOS format.

`Compiler/PrsExp.HC:679-700` gives the first direct lowering cases. `TK_I64` and `TK_CHAR_CONST` both emit `IC_IMM_I64`; a negative `cur_i64` selects internal `RT_U64`, while every other value selects `RT_I64`. `TK_F64` copies the exact 64-bit payload into `IC_IMM_F64` with `RT_F64`. `TK_STR` creates a string-misc record and emits `IC_STR_CONST` with the adjacent `RT_U8` pointer class. `Compiler/PrsExp.HC:728-747` recursively enters `PrsExpression` for a parenthesized value and returns without another `ICAdd`, so grouping adds no runtime instruction. Lines 750-759 consume unary plus without selecting an opcode. Lines 614-675 select `IC_COM`, `IC_NOT`, `IC_UNARY_MINUS`, `IC_DEREF`, and `IC_ADDR`; lines 133-162 add prefix operations after their operand from inner to outer order. Address-of increments the result class and uses `OptFree` to remove an immediately preceding dereference IC before insertion. `Compiler/CInit.HC:49-53` gives all five operations one argument and one result. `OptPass012.HC:153-192` folds immediate operands only during optimization. Its complement branch assigns internal `I64`, logical not and unary minus retain the current type, dereference removes one pointer class when present, and address-of adds one pointer class. `OptPass789A.HC:449-464` keeps their distinct backend paths. `Compiler/PrsLib.HC:79-97` records provenance on each inserted instruction. `Ir.Literal_lowering` reproduces those opcode, payload, type, source, operand-order, and cancellation choices through the checked sequence constructor. It retains literal and operator spans and keeps caller-owned IR IDs separate from semantic traversal identities. Its typed-result entry point follows the immutable checked operand retained by `Sema.Function_call_expression_result`. The literal instruction uses the leaf result type, and each emitted prefix uses its own semantic result type, so `~*"a"` records `U8*`, `U8`, and internal `I64` in order. Typed address-of removes an immediately inner dereference before validating the emitted identity count, keeps `IC_ADDR`, and drops the dereference identity and span. `HCIRL0001` rejects missing or excess emitted identities; `HCIRL0002` rejects checked pointer-depth overflow; `HCIRL0003` rejects incomplete or inconsistent typed-literal metadata. A supported wrapper chain or semantic shape around a nonliteral returns `Not_literal` without fabricated IR. Updates, memory execution, and constant folding remain outside the typed boundary. Decoded string bytes remain unchanged; termination, misc-data ownership, and final storage belong to a later constant-pool boundary.

Issue #433 extends the typed-result entry point through semantic parentheses. The traversal is iterative, grouping consumes no identity, and the literal leaf supplies its type and instruction span. A grouped nonliteral returns `Not_literal`. Issue #435 adds the same transparent treatment for unary plus, including mixed grouping. Issue #438 then emits unfused `IC_UNARY_MINUS` chains for typed minus prefixes, with one caller-owned identity per minus operator. Issues #440 and #442 add logical not and bitwise complement. Issue #444 retains the checked operand result at every wrapper and uses it to emit typed `IC_DEREF` chains. Issue #446 adds typed `IC_ADDR` and applies the immediate-dereference cancellation before identity validation.

`Compiler/PrsExp.HC:15-61` completes a binary expression after both operands and applies source-defined pointer adjustment around that operation. Lines 167-255 emit source terms from left to right while precedence controls when the operation is completed. `Compiler/CInit.HC:60-97` classifies the audited binary operations as two-argument, one-result forms. `Compiler/OptPass012.HC:40-61` then pops the right operand into `arg2_tree` before the left operand into `arg1_tree`; `Compiler/OptPass3.HC:68-88` preserves those slots while applying per-result conversions. `Sema.Function_call_expression_result` retains the corresponding checked pair in source order. It does not emit binary IR, scale pointers, choose short-circuit edges, or evaluate the operation.

Issue #450 uses those retained edges for the first multi-node IR lowering path. `Ir.Expression_lowering` accepts integer and character leaves beneath transparent grouping and unary plus, then emits 19 shift, arithmetic, bitwise, comparison, and logical operations. The full tree is validated before emission. An explicit worklist keeps deep left-associated input off the OCaml call stack, and the module owns consecutive instruction and value allocation. Each parent consumes the value produced by its source-left child before the value produced by its source-right child. The result is checked again by `Ir.Instruction_sequence.create` and rendered as `holyc-ir-expression-v1`. F64, `IC_POWER`, assignments, identifiers, calls, pointers, conversions, pointer scaling, storage, short-circuit control flow, and execution are not claimed by this slice.

Issue #490 admits the checked string leaf to that planner without broadening its numeric operations. `Compiler/PrsExp.HC:692-697` creates `IC_STR_CONST` with the internal `U8` pointer class and the bytes decoded by `LexExtStr` at `Compiler/LexLib.HC:248-285`. `Compiler/CInit.HC:29` records zero arguments, one result, and a constant-folding barrier. `Ir.Expression_lowering` therefore requires an internal `U8 *` address result at pointer depth one and array rank zero, then emits one zero-operand instruction with the exact bytes, exact leaf span, zero flags, and consecutive identities. Parentheses are transparent, and function-body and executable top-level owners share the JIT and AOT form. Storage allocation, terminator insertion, constant-pool placement, conversions, interpretation, and execution are not claimed; inconsistent metadata reports `HCIRL0004`, while an unsupported surrounding operation exposes no partial sequence.

Issue #452 composes the source prefix records within that tree. `Compiler/PrsExp.HC:133-162,614-675` inserts `IC_UNARY_MINUS`, `IC_NOT`, or `IC_COM` after the operand; `Compiler/CInit.HC:49-53` gives each one argument and one result. `OptPass012.HC:153-192` keeps minus and logical-not results on the operand class and changes complement to internal `I64` before any later backend path. `Ir.Expression_lowering` retains those checked types and operator spans, validates the prefix edge before allocation, and emits each unary node before its binary parent. The same explicit worklist handles mixed function-body and executable top-level trees.

Issue #454 adds `IC_DEREF` and `IC_ADDR` to that composition. `PrsExp.HC:151-158` removes one pointer class for dereference when present, adds one for address-of, and frees an immediately preceding dereference before it inserts the address. The lowering plan applies the same cancellation before identity allocation. It keeps `IC_ADDR`, points it at the canceled dereference's operand value, and omits the dereference identity and span. Transparent grouping and unary plus do not block the match; a dereference outside an address remains. Pointer-typed intermediate values are admitted only for these two prefixes, so pointer arithmetic and comparison remain explicit unsupported shapes. Updates, identifier storage, floating composition, folding, execution, and machine selection remain separate work.

Issue #456 adds the pure `F64` arithmetic branch. `Compiler/OptLib.HC:96-179` classifies two floating operands as `FBO1_F64` and records conversions when an operand is not already floating. `Compiler/OptPass012.HC:308-629` handles multiplication, division, addition, and subtraction for both integer and floating classes. `Compiler/OptPass789A.HC:518-565` selects `ICFMul`, `ICFDiv`, `ICFAdd`, or `ICFSub` when `ICF_USE_INT` is clear, and `Compiler/BackFB.HC:488-570` supplies those floating consumers. The checked lowerer admits only trees whose leaves, operands, and result already have internal `F64`; it emits `IC_IMM_F64` leaves and the four operations without folding. Unary minus and logical not use the existing source-backed prefix path. Mixed integer and `F64` input remains unsupported until conversion flags have a checked representation.

Issue #458 adds that mixed conversion representation. `Kernel/KernelA.HH:1602` assigns bit zero to `ICF_RES_TO_F64`. In `OptFixupBinaryOp1`, a mixed pair selects the floating class, an integer non-immediate producer receives the flag, and an integer immediate is rewritten to `IC_IMM_F64`. The canonical lowerer records the conversion request on the integer producer before optimization. Grouping and unary plus forward it to the instruction they retain, and a composite integer subtree marks only its root. An immediate remains `IC_IMM_I64` with its exact integer payload so lowering does not perform target arithmetic; the later optimizer owns the pinned rewrite. Issue #478 applies the same producer rule to comparisons. Power, modulo, result-to-integer conversions, evaluation, and machine selection remain separate work.

Issue #498 extends that representation to modulo. `Compiler/PrsExp.HC:15-61,167-255` builds ordinary `IC_MOD` expressions. `Compiler/OptLib.HC:92-178` selects the floating class when either forwarded operand is `F64` and records integer-producer conversion intent. `Compiler/OptPass012.HC:435-455` folds integer and floating constants or retains the operation, while `Compiler/OptPass3.HC:481-489` keeps modulo in the ordinary binary tree. `Compiler/OptPass789A.HC:530-534` selects `ICMod` for integer execution and `ICFMod` otherwise. The checked lowerer now admits pure and mixed `F64` modulo, preserves the integer payload under `ICF_RES_TO_F64`, and emits an unflagged `IC_MOD`. Folding, numerical remainder evaluation, and backend execution remain outside this boundary.

Issue #500 applies the same common-domain rule to `IC_AND_AND`, `IC_OR_OR`, and `IC_XOR_XOR`. `Compiler/PrsExp.HC:15-61,167-195` constructs the logical tree, including the `IC_NOP1` boundary used by `&&` and `||`. `Compiler/OptLib.HC:92-179` selects `F64` when either forwarded operand is floating and marks an integer producer for conversion. `Compiler/OptPass012.HC:693-713` folds constants or retains the operation before forcing its result to internal `I64`; `Compiler/OptPass3.HC:480-493` keeps all three binary nodes. `Compiler/OptPass789A.HC:597-605` selects their ordinary consumers. Canonical lowering emits an unflagged logical instruction, retains internal `I64`, and records any producer conversion without evaluating truth or constructing a branch rewrite.

Issue #502 extends the floating common domain to `IC_AND`, `IC_OR`, and `IC_XOR`. `Compiler/PrsExp.HC:15-61,167-255` constructs the ordinary bitwise nodes. `Compiler/OptLib.HC:92-179` selects `F64` when either forwarded operand is floating and marks the integer producer. `Compiler/OptPass012.HC:458-483` folds a constant floating pair by applying the bitwise operation to its raw payload and selecting `IC_IMM_F64`; otherwise it retains the binary operation and chosen result class. `Compiler/OptPass3.HC:480-493` keeps all three tree nodes, and `Compiler/OptPass789A.HC:536-549` selects their raw-bit consumers. Canonical lowering keeps internal `F64`, leaves the operation unflagged, and records integer conversion intent without evaluating the payload.

Issue #504 adds `IC_SHL` and `IC_SHR` to that floating common domain. `Compiler/PrsExp.HC:15-61,167-255` constructs both shift nodes. `Compiler/OptLib.HC:92-179` selects `F64` when either forwarded operand is floating and marks an integer producer. `Compiler/OptPass012.HC:229-266` folds a constant floating pair to `IC_IMM_F64`, retains the selected result class otherwise, and owns the later constant-shift rewrite. `Compiler/OptPass3.HC:480-493` keeps both tree nodes, and `Compiler/OptPass789A.HC:506-516` selects `ICShift`. Canonical lowering keeps internal `F64`, leaves the shift unflagged, and records conversion intent without evaluating or rewriting it.

Issue #506 carries the fixed-argument comparison into canonical root lowering. `Compiler/PrsExp.HC:470-484` runs the expression passes, compares the forwarded argument class with the fixed parameter class, and sets `ICF_RES_TO_F64` or `ICF_RES_TO_INT` on the final producer. `Kernel/KernelA.HH:1602-1603` fixes those bit values, and `Compiler/OptLib.HC:103-140` uses them when forwarding a producer into later optimization. Provided function-body and executable top-level arguments retain the comparison on their checked root. `Ir.Expression_lowering` validates the retained class, forwards it through grouping and unary plus, and marks only the final producer without converting its payload or constructing a call.

Issue #508 lowers checked function conditions into canonical branch fragments. `Compiler/PrsStmt.HC:460-504,529-545` appends `IC_BR_ZERO` after the conditions of `if`, `while`, and `for`; lines 506-527 append `IC_BR_NOT_ZERO` after a `do while` condition. `Compiler/CInit.HC:153-154` gives both branches one argument and no result. `Ir.Condition_lowering` consumes the retained semantic role, lowers the condition expression, and appends the exact branch with a caller-owned target and complete condition span. Block placement, statement bodies, fallthrough edges, loop closure, optimization, interpretation, and backend emission remain later work.

Issue #510 applies the same fragment to executable top-level conditions. The checked view from issue #350 already retains the exact typed root, root origin, and zero or nonzero branch sense selected by `Compiler/PrsStmt.HC:459-564`. `Ir.Condition_lowering` consumes those facts directly, preserves the top-level expression span, and shares the verified branch constructor with function conditions. It does not create an `IET_MAIN` record, place blocks, build fallthrough edges, close loops, optimize, interpret, or emit machine code.

Issue #512 lowers checked function returns into canonical fragments. `Compiler/PrsStmt.HC:1090-1122` parses an optional expression, emits typed `IC_RETURN_VAL` when a value exists, and appends `IC_JMP` to `lb_leave` for both forms. `Compiler/CInit.HC:25,98` fixes the one-argument return-value shape and zero-argument jump shape. `Ir.Return_lowering` consumes the semantic result from issue #292, preserves its root conversion intent and complete statement span, and accepts a caller-owned leave block. Try unwinding, leave-block placement, epilogue instructions, warning policy, optimization, interpretation, and backend emission remain later work.

Issue #514 connects resolved function-local language labels to checked control-flow fragments. `Compiler/PrsStmt.HC:1121-1131` finds or creates the goto-label record, increments its use count, and emits `IC_JMP`; lines 1182-1199 define the same record and emit `IC_LABEL`. `Ir.Goto_label_lowering` assigns caller-seeded block identities in semantic first-occurrence order, preserves every occurrence span, and emits both zero-argument instructions with the same block payload and zero flags. Structural assembly labels, repeated assembly definitions, block partitioning, graph construction, optimization, interpretation, and backend emission remain later work.

Issues #516 and #518 carry function-body breaks through semantic resolution and checked IR fragments. `Compiler/PrsStmt.HC:486-563` creates done labels for `while`, `do while`, and `for`, but parses the `for` initializer and update without a break target. Lines 578-600 create the outer switch target, lines 632-659 create one target per subswitch, and lines 742-747 pass the current target through ordinary switch statements. The `KW_BREAK` branch at lines 1133-1138 rejects a missing target and emits `IC_JMP` otherwise. `Sema.Break_resolution` retains stable function-local region identities, exact nearest-target bindings, break counts, and complete provenance. `Ir.Break_lowering` then assigns caller-seeded blocks to all regions and emits source-ordered `IC_JMP` fragments with exact spans. Block placement, complete loop and switch graphs, and execution remain later work.

Issue #530 lowers the first checked direct-call subset. `Compiler/PrsExp.HC:544-586` emits `IC_CALL_START`, the selected call, caller cleanup, and `IC_CALL_END`; lines 572-582 choose the cleanup form from the same stored function flags used by the epilogue. `Ir.Direct_call_lowering` requires exact ownership between the typed result and `Sema.Function_call_target_classification`, accepts only the ordinary direct-executable access path with zero fixed parameters and no variadic tail, and emits the four checked instructions with one result. The canonical target symbol appears on start, call, and end; cleanup carries zero bytes and selects `IC_ADD_RSP1` only when `(RET1 or ARGPOP) and not NOARGPOP` holds. At that issue boundary, calls with arguments, internal operations, externs, imports, and indirect targets remained later work.

Issue #532 composes one provided fixed argument through the same sequence. `Compiler/PrsExp.HC:438-490` parses and optimizes every fixed argument in its own COC list and retains the result conversion on that list's final producer. Lines 544-554 emit `IC_CALL_START`, append those lists in parameter order, and add `ICF_PUSH_RES` to each final producer before call selection. `Ir.Direct_call_lowering` delegates the single accepted argument to the canonical expression planner, adds the push bit without losing a retained conversion bit, then emits the call tail with eight cleanup bytes. Defaults, additional fixed arguments, variadics, and unsupported expression trees remain explicit boundaries.

Issue #534 replays that fixed loop for every provided slot. Resolution arguments, typed fixed results, and active signature parameters must agree exactly. Each supported expression starts at the prior tree's next identities, and its unique result producer receives `ICF_PUSH_RES` before the next tree begins. The call tail follows the complete ordered list, uses the next value as its result, and carries eight cleanup bytes per fixed argument. A declared default or unsupported tree in any position exposes no partial sequence; variadic handling remains separate.

Issue #536 covers the empty tail of the variadic branch at `Compiler/PrsExp.HC:491-530`. The source creates an `IC_IMM_I64` head for the hidden count, stores the number of supplied variadic values, and increments the cleanup slot count for that hidden argument. The checked lowerer accepts exact zero-count/empty-result evidence, takes the internal `I64` type from the active header's synthesized `argc` binding, emits the pushed zero after fixed arguments, and includes its eight bytes in caller cleanup. Nonempty tails remain unsupported until their typed expression lists are composed.

Issue #538 composes the remaining lists in that same variadic branch. The retained I64 count must equal the number of typed variadic results. Each result is canonically lowered after the hidden count in source order, and its unique producer receives `ICF_PUSH_RES` without a fixed-target conversion. The cleanup formula mirrors lines 575-582: eight bytes for each fixed slot, one hidden count slot, and each supplied variadic slot. An unsupported expression exposes no partial sequence.

Issue #540 covers `Compiler/PrsExp.HC:555-571` for noninternal direct targets. The exact classified declaration snapshot selects `IC_CALL` for an executable record, `IC_CALL_INDIRECT2` for a JIT extern address slot, `IC_CALL_IMPORT` for an AOT import, or `IC_CALL_EXTERN` for an AOT extern. The canonical function symbol records the corresponding executable, slot, or relocation intent without choosing a host address. Argument composition and cleanup remain identical across those forms.

Issue #544 carries the same call composer into executable top-level input. `Compiler/CMain.HC:CmpJoin` returns each module statement to the expression compiler, while `Compiler/PrsExp.HC:438-586` retains the fixed loop, hidden variadic count, supplied tail, selected call, and cleanup sequence. The top-level adapter checks the exact typed result and declaration-order target, normalizes its header, arguments, canonical symbol, record, and access policy, and then uses the existing composer without a synthetic function owner. Complete statement graphs, callbacks, defaults, internal operations, `IET_MAIN`, and execution remain later work.

Issue #478 lowers the six comparisons in the floating execution domain. `Compiler/CInit.HC:305-311` maps their source spellings to the comparison IC opcodes. `Compiler/OptLib.HC:96-177` gives `OptFixupBinaryOp1` the same common-class and producer-conversion rules used by arithmetic. `Compiler/OptPass012.HC:724-823` then sets `ICF_USE_F64` on nonconstant floating comparisons and changes an ordinary comparison result to internal `I64`. The canonical lowerer keeps source-left and source-right order, flags each integer operand subtree root with `ICF_RES_TO_F64`, flags the comparison with `ICF_USE_F64`, and retains its checked internal `I64` result. The two flags combine when the Boolean itself feeds floating arithmetic. Pure, mixed, nested, and executable top-level comparisons use the same iterative worklist in JIT and AOT mode. The checked sequence and x87 verifiers accept the result. Constant folding, the comparison-to-zero rewrite, branch comparisons, numerical execution, and machine selection remain later work.

Issue #480 lowers HolyC's backtick operator. `Compiler/CInit.HC:64,288` records `IC_POWER` as a right-associated, two-argument, one-result operation without an implicit floating-stack pop. `Compiler/PrsExp.HC:133-147` keeps unary minus outside a completed power expression. `Compiler/OptPass012.HC:299-305` assigns internal `F64` to the result and marks every non-`F64` operand producer with `ICF_RES_TO_F64`, including both sides of an integer-only expression. The canonical lowerer preserves those checked edges and emits source-left, source-right, then `IC_POWER`; the power instruction itself has no `ICF_USE_F64` bit. Function bodies and executable top-level code share this path in JIT and AOT mode. `Compiler/OptPass789A.HC:503-504` and `Compiler/BackFA.HC:266-330` later select `ICFPow` and the `SYS_POW` call, so constant folding, numerical evaluation, machine selection, and execution remain outside this slice.

Issue #462 lowers checked primitive postfix casts. `Compiler/PrsExp.HC:1017-1055` records whether the existing operand carried `CCf_PAREN`, applies the parsed target class, and appends `IC_HOLYC_TYPECAST` with that Boolean in `ic_data`. `Compiler/CompilerA.HH:53` assigns opcode `0x1F`, while `Compiler/CInit.HC:48` records one argument and one result. The OCaml lowerer consumes the exact checked edge from issue #460, emits the operand before the cast, and keeps the public target type plus the full cast span. The payload is one only when the immediate source operand is an ordinary parenthesized expression. `Compiler/OptPass012.HC:87-110` folds an immediate cast or removes an ungrouped retained cast by transferring its class and flags; a grouped retained cast survives. `Compiler/OptPass4.HC:571` recognizes the remaining opcode, and `Compiler/OptPass789A.HC:445` moves its value for machine selection. This slice stops before those optimizer and backend behaviors.

Issue #464 lowers accepted ordinary expression statements through their source-defined terminator. `Compiler/PrsStmt.HC:1204-1210` calls `PrsExpression` with `end_exp` enabled for the ordinary statement path. `Compiler/PrsExp.HC:264-295` then appends `IC_END_EXP`; `Compiler/CompilerA.HH:22` identifies that opcode, and `Compiler/CInit.HC:19` gives it one argument and no result. The canonical OCaml form consumes the exact final expression value, carries `ICF_RES_NOT_USED` from `Kernel/KernelA.HH:1611`, and retains the complete statement span. It allocates an instruction identity but no result identity. `Compiler/OptPass4.HC:540-552` later transfers the unused-result bit to the producer before removing the terminator; optimization remains outside this slice.

Issue #546 connects standalone executable top-level direct calls to that terminator path. The target-aware adapter requires the exact expression-statement root and declaration-order call target, lowers the complete call through issue #544's shared composer, then appends one `IC_END_EXP` consuming the `IC_CALL_END` result. The terminator keeps the statement span, consumes one instruction identity and no value identity, and is never exposed after an unsupported call, ownership mismatch, invalid root role, or identity failure. Nested calls in larger expressions and optimization remain separate work.

Issue #548 turns that fragment into one checked executable top-level body. `Compiler/PrsLib.HC:98-105` allocates and zeroes the COC head, then assigns `IC_END`; `Compiler/CMain.HC:CmpJoin` routes each executable module statement through the compiler independently. `Ir.Top_level_statement_lowering` therefore requires one exact typed statement and call target, appends one spanless `IC_END` after the source-backed `IC_END_EXP`, builds one caller-identified entry block, and constructs `Ir.Top_level_body` with the retained item position and statement span. Stream, block, instruction, and value identities and compiler options remain caller-owned. Multiple statements, `IET_MAIN`, interpretation, and machine emission remain later work.

Issue #550 threads that per-statement construction across an ordered batch. `Compiler/CMain.HC:CmpJoin` invokes `LexStmt2Bin` for each executable module item, so the OCaml adapter retains one stream and entry block per exact semantic statement rather than merging unrelated COC lists. Caller-seeded stream and block identities advance once per body; instruction and value cursors advance by each checked sequence. Compiler-option snapshots remain aligned with their statements. Mismatched counts, unsupported paths, invalid ownership, verifier failures, and identity exhaustion expose no partial batch. Issue #556 adds mixed statement selection; combined graphs and `IET_MAIN` emission remain later work.

Issue #552 reuses the same per-statement body constructor for ordinary expression statements. `Compiler/PrsStmt.HC:1204-1210` sends those statements through `PrsExpression` with `end_exp`, and `Compiler/CMain.HC:CmpJoin` returns the resulting COC through the same top-level compilation boundary. `Ir.Top_level_statement_lowering.lower_expression` therefore consumes the exact sole expression root through the existing numeric statement lowerer, then shares the direct-call path's spanless `IC_END`, entry block, metadata, x87 verification, and cursor logic. Integer and mixed-F64 trees, ordinary current position, resolved `defined`, primitive `sizeof`, and checked aggregate offsets enter this boundary. Unsupported storage reads and nonexpression control statements expose no partial body.

Issue #554 threads that ordinary-expression constructor across a source-ordered batch. `Compiler/CMain.HC:CmpJoin` and `LexStmt2Bin` establish the per-item executable route, `Compiler/PrsLib.HC:98-105` establishes independent COC initialization and the structural `IC_END`, and `Compiler/PrsExp.HC:264-295` supplies each expression's `IC_END_EXP`. Equal-length statement and compiler-option lists produce one body per statement in exact source order. Stream and block identities advance once per body; instruction and value cursors continue from each checked result. Empty input preserves every cursor. A count mismatch, invalid option snapshot, unsupported expression, invalid statement root, child failure, or identity exhaustion exposes no partial batch, even after an earlier statement completed internally. Issue #556 adds mixed direct-call and expression selection while preserving this cursor and failure behavior.

Issue #556 joins the two batch routes without splitting or reordering the executable source stream. `Compiler/CMain.HC:CmpJoin` and `LexStmt2Bin` establish one independent compilation boundary per item, `Compiler/PrsStmt.HC:1204-1210` sends ordinary statements through `PrsExpression`, and `Compiler/PrsExp.HC:264-295,438-586` supplies the expression terminator and direct-call construction. `Ir.Top_level_batch_lowering.lower_mixed` first indexes sole expression-statement roots by semantic result identity, then requires the classified target's exact physical root-expression object. Target-list order is therefore irrelevant. A root enters the direct-call path only when that exact call has an identifier callee backed by a source-visible module function publication; nested calls and global or outer callback calls stay on the ordinary-expression path. Missing, duplicate, foreign, unrelated, unmatched, or ambiguous targets fail before a body is exposed. Each source statement then independently reuses the direct-call or ordinary-expression constructor, retaining its options, item position, span, access policy, and body order. Stream and block IDs advance once per body, instruction and value cursors continue from the exact preceding result, empty input preserves every cursor, and a later child failure hides every internally completed prefix. Callback invocation, broader storage reads, combined graphs, final implicit-result selection, `IET_MAIN`, interpretation, optimization, and machine emission remain separate work.

Issue #466 lowers the ordinary current-position branch. `Compiler/PrsExp.HC:707-721` distinguishes assembler output, class-offset evaluation, and the ordinary `IC_RIP` expression; only the last route enters this module. `Compiler/CompilerA.HH:158` assigns `IC_RIP` value `0x75`, and `Compiler/CInit.HC:134` records zero arguments, one result, no structural class, and the no-folding constraint. The checked lowerer requires the internal zero-depth `RT_PTR` storage form retained by issue #342, then emits one typed producer with the exact source span and no payload. `OptPass789A.HC:137-147` selects the concrete instruction address and adds the AOT `AAT_ADD_U64` adjustment later, so neither behavior is fabricated in canonical IR.

Issue #468 retains the operand needed before ordinary `defined` can be lowered. `Compiler/PrsExp.HC:936-954` consumes zero or more wrapper parentheses, inspects exactly one token, and treats it as true only when the token is `TK_IDENT` with a local or hash entry. `Compiler/Lex.HC:493-514` supplies the identifier token and current hash entry, while `Compiler/PrsLib.HC:79-97` supplies the source-positioned insertion boundary. Both OCaml AST adapters create the same checked semantic record with the operand's name or non-name shape, exact spelling and origin, and the full expression origin. The record survives function and executable top-level composition in both modes. Issues #470, #472, and #474 consume that evidence for the local, compilation-unit, and outer-chain Boolean branches described next.

Issue #470 resolves the source-local part of ordinary `defined`. `Compiler/Lex.HC:493-514` checks `cc->htc.local_var_lst` before any hash table, and `Compiler/PrsExp.HC:936-954` accepts either that local entry or the selected hash entry. The function adapter therefore links each name operand to its exact source-ordered query. Parameters, synthetic `argc` and `argv`, and locals already published at that point produce one; a non-name token produces zero. A later local, another function's binding, or an absent name remains a nonlocal candidate. Executable top-level names also remain deferred because this slice does not reproduce the hash-chain lookup. The validator requires the query's role, spelling, origin, and physical identity in the containing function, which prevents a matching-looking query from another traversal from entering lowering.

Issue #472 resolves the compilation-unit part of that hash lookup inside function bodies. `Compiler/Lex.HC:38-40` selects the ordinary type-mask entries from the active hash-table list, `Compiler/Lex.HC:493-514` searches that list only after the local table misses, and `Compiler/CMain.HC:60-75` places the current compilation-unit table ahead of the JIT or AOT outer chain. The module expression pass replays aggregate, function, and global publications for specialized name queries at the same source boundary used by ordinary identifier occurrences. An earlier publication or the current function makes `defined` true; a later or absent module name remains an outer candidate. A visible local still wins. The query retains the exact selected publication object, and validation rejects evidence outside the owning module batch.

Issue #474 resolves the remaining candidate through the immutable outer-environment batch already used for ordinary identifiers. `Compiler/CMain.HC:60-75` links the active compilation-unit table to the current task table in JIT mode, an enclosing compilation table in nested AOT mode, or the assembler table at the end of either chain. `Kernel/KHashB.HC:HashFind` searches each table newest-first and follows `next`. The outer-expression pass preserves local and module query results, attaches the exact outer table binding to a supported match, and records an explicit undefined result only after the complete chain misses. The function-call pass accepts that batch only when it owns the same symbol table and exact module-expression source. A hit lowers as one and a complete miss as zero. Defines, keywords, type-only records, registers, opcodes, directives, and other unrepresented hash families remain separate lookup work.

Issue #476 applies the same source order to executable top-level `defined` expressions. These queries have no function-local list, so each statement starts with the aggregate, function, and global publications visible before that statement, then continues through the exact JIT or AOT outer batch described above. The top-level binding pass keeps specialized queries separate from ordinary identifier occurrences and assigns each query to its owning statement. A supported module or outer record selects one; a complete outer-chain miss selects zero. The semantic tree requires the exact physical query object, role, spelling, origin, and statement ownership before exposing that result. This does not weaken ordinary resolution: an ordinary missing top-level identifier still reports `HCSEMA0054`.

Known values use the `IC_IMM_I64` identity in `Compiler/CompilerA.HH:30` and its zero-argument, one-result metadata in `Compiler/CInit.HC:27`. The canonical instruction keeps internal `I64`, payload zero or one, the full expression span, and module-owned identities. Grouping, supported numeric composition, primitive casts, mixed `F64` conversion intent, and `IC_END_EXP` reuse the existing expression worklist. A function candidate without the outer batch needed to finish its lookup returns no partial sequence. Unrepresented hash-record families, interpretation, optimization, backend emission, and execution remain later work.

Issue #460 records the semantic operand edge required for postfix-cast lowering. `Compiler/CInit.HC:48` gives `IC_HOLYC_TYPECAST` one argument and one result. `PrsUnaryModifier` appends it after the existing expression, and `OptPass012.HC:87-110` reads that immediate predecessor when it folds or retains the cast. The checked expression result now keeps the exact child returned by the recursive traversal. Nested casts point to their immediate cast child, while a parenthesized or binary operand retains that separate checked node. Function-body and executable top-level results share the rule in JIT and AOT mode. The edge does not yet select the `was_paren` payload, allocate an IR identity, convert an immediate, fold the cast, or emit `IC_HOLYC_TYPECAST`.

The control-flow subset is explicit in later consumers. `OptPass012.HC:1089-1185` forwards labels and handles the conditional branch family; `OptPass3.HC:269-390,497-504` groups switches, branches, jumps, the end marker, and labels; `OptPass4.HC:520-545` begins dead code only after `IC_JMP` and `IC_RET`; and `OptPass789A.HC:152-294,418,1037` emits branches, jumps, labels, and returns while giving `IC_END` no bytes. `BackC.HC:649-742` consumes the switch default and jump-table labels. `Ir.Control_flow` records those categories, target shapes, and fallthrough boundaries for every opcode.

Effect evidence is distributed rather than stored in one TempleOS enum. `CInit.HC:17-201` supplies structural classes and `not_const`; `OptPass012.HC:20-67,275-305,866-938,1185-1252` consumes those facts and enumerates special operations; `OptPass3.HC`, `OptPass4.HC`, and `OptPass6.HC` retain memory, call, and assembly distinctions; and `OptPass789A.HC` emits port I/O, cache flush, page invalidation, register or flag access, timestamp reads, queue mutation, calls, and inline assembly. `Ir.Effects` records conservative independent access axes and keeps `not_const` separate from runtime observation.

`Ir.Block_graph` applies that evidence to one checked function boundary. It preserves the insertion order established by `ICAdd`, stores stable label-shaped block identities without reproducing the mutable queue, and validates single or ordered switch targets against a private function index. Successors retain explicit target order plus source-order fallthrough. Duplicate identities, instructions after a terminator, final fallthrough, repeated stream ends, and content after `IC_END` fail before a graph is returned. Its `holyc-ir-graph-v1` text is a hosted inspection schema. Function lowering, instruction-level effect refinement, relocation rules, optimization, and execution remain later work under [the IR epic](https://github.com/frankischilling/holyc-ocaml/issues/396).

`Ir.X87_stack` combines the next source facts without flattening them. `CInit.HC:17-201` supplies one `fpop` value for every opcode. `Kernel/KernelA.HH:1602-1636` defines the independent operand and result conversions, `ICF_USE_F64`, comparison transfers, and both three-bit suppression fields. `OptLib.HC:101-177,302-350,429-477` and `OptPass3.HC:220-267,376-393,476` show how floating operands and chained comparisons are prepared. `BackFA.HC:1-73,525-542`, `BackFB.HC:226-478`, and `OptPass789A.HC:44-73,189-294,1035-1062` establish suppression-slot order, the balanced `IC_PUSH_CMP` result conversion, comparison machine-stack transfers, call conversion handling, and the final emission boundary. The OCaml verifier propagates an abstract depth over the checked graph, rejects invalid masks or edge depths, preserves call depth around explicit conversions, and requires empty x87 state at exits. Its trace is an inspection aid, not evidence of numerical compatibility or chosen machine bytes.

`PrsFunJoin` in `Compiler/PrsStmt.HC` assembles the source-visible return type, ordered argument members, and function flags before `PrsFun` creates a private `CCodeCtrl` stream for the body. `PrsFun` emits the enter and leave boundaries, compiles that stream, and restores the enclosing compiler state. `Compiler/PrsLib.HC:COCCompile` consumes that one function stream, while `Compiler/CMain.HC:LexStmt2Bin` compiles executable top-level statements through a separate path. `Ir.Function_body` retains that distinction: it joins one function symbol, its declaration and body scopes, return type, ordered parameter and local facts, stored flags, compiler-option snapshot, spans, and a checked graph. The constructor also owns x87 verification. It does not claim that the frontend can lower a function or that the same wrapper represents top-level executable code.

`LexStmt2Bin` creates a fresh `CCodeCtrl` for one executable top-level statement, calls `PrsStmt`, conditionally rewrites the last expression result, appends `IC_RETURN_VAL2` and `IC_RET` when required, and sends that stream to `COCCompile`. `Cmp` calls this path in input order under the active JIT or AOT compiler state. `Ir.Top_level_body` retains the stream identity, source-item position, option snapshot, span, checked graph, and x87 result without attaching the named-function metadata from `PrsFun`. The OCaml wrapper begins after instruction construction, so it does not yet perform the result rewrite or emit either return operation.

## TempleOS BIN header and patch records

`Kernel/KernelA.HH` defines the `'TOSB'` signature, the seven-field `CBinFile` header, 21 named `IET_*` values, five reserved numeric slots, and eight `AAT_*` adjustment operations. The header occupies 32 bytes. `Compiler/CMain.HC:Cmp` writes the code and initialized-data body immediately after that header, places the patch table after the body, terminates it with `IET_END`, and rounds the complete file size to 16 bytes.

`Kernel/KLoad.HC:Load` treats the byte after the header as `module_base`. `LoadPass1` registers exports, resolves or defers imports, applies absolute-address patches, and creates heap allocations. `LoadPass2` walks the same variable-length records and calls `IET_MAIN` entries. Relative import fields receive `target - field_address - field_width`; immediate fields receive the target itself. The source retains fictitious zero-width import values, marks 64-bit exports as not implemented, and marks three heap forms as not really used. The OCaml model preserves those comments instead of treating every loader switch case as a compatibility claim.

`Kernel/KStart16.HC` locates the boot patch table, and `Kernel/KStart32.HC:CPatchTableAbsAddr` performs the boot absolute-address pass. Compiler producers and consumers in `AsmResolve.HC`, `Asm.HC`, `PrsVar.HC`, `BackFA.HC`, `BackLib.HC`, `BackC.HC`, `OptPass3.HC`, and `OptPass789A.HC` establish which record and adjustment kinds reach assembler, parser, optimizer, and backend paths.

`tools/bin_record_source.ml` requires that exact 13-file source set. It validates checksums, table order, numeric gaps, status comments, header fields, writer behavior, loader formulas, both passes, boot handling, and source consumers while ignoring comments and literals. `tools/bin_record_gen.ml` emits typed entries, adjustments, record shapes, relocation metadata, loader actions, checksums, and line references to `src/generated/bin_records.ml`. `Backend.Bin_spec` exposes that data through `Holyc_lib.Templeos_bin_spec`.

This slice is an executable format specification, not format compatibility. It has no serializer, parser, loader model, generated module, or actual TempleOS loader result. [The BIN format notes](templeos-bin-format.md) describe the byte grammar and the validation still required for M8.

## Literals

Issue #429 retains literal data before IR construction. `Sema.Function_call_resolution.argument_expression_kind` stores `cur_i64`-shaped target values for integer and character nodes, exact `F64` bits, and decoded string bytes. Both AST adapters validate the frontend literal family before constructing that variant. The function-body call path and `Sema.Top_level_expression_tree` use the same representation, and `Sema.Function_call_expression_result.result_source` carries it into each typed view without changing source origin, grouping, prefix structure, type, category, class, array rank, or result identity. `Compiler/PrsExp.HC:679-700` defines the literal payload branches, `Compiler/CInit.HC:27-29` confirms their zero-argument, one-result leaf shapes, and `Compiler/PrsLib.HC:79-97` attaches source provenance when lowering later creates an instruction. Issue #431 consumes that payload when the typed result points directly to the literal leaf. Issues #433 and #435 unwrap parentheses and unary plus before that copy. Issue #438 emits typed unary-minus instruction chains after that copy. Issue #440 adds typed logical-not chains on the same walk, including mixed minus-then-not order. Issue #442 adds typed bitwise-complement chains, assigning internal `I64` at each `IC_COM`. Issue #444 adds checked operand links to semantic wrappers and emits typed dereference chains while preserving the exact leaf and intermediate types. Instruction and value identities remain caller-owned. String allocation and termination, address-of, constant folding, and execution remain later work.

`Compiler/Lex.HC:Lex` accumulates integers in a target `I64`, so overflow wraps. Hex and binary prefixes are case insensitive. Character constants store up to eight decoded bytes in little-endian order. `LexInStr` defines the recognized escapes. The hosted lexer diagnoses unterminated literals instead of relying on an in-memory NUL terminator.

## Comments

The pinned lexer nests `/* ... */` comments. It consumes `//` through the end of the line. The OCaml lexer retains both as leading trivia so later diagnostics and formatting tools can recover their exact bytes.

## Diagnostics

`Compiler/CExcept.HC` prints the current token, file, line, and source line before raising the compiler exception. Hosted diagnostics retain those source facts in structured values with stable codes. Rendering is separate from construction so JSON and terminal output share the same diagnostic.

## Interpretation decisions

`Kernel/KernelA.HH:1118-1129` places the saved part of `CDocBin` between `start` and `end`: four `U32` fields named `num`, `flags`, `size`, and `use_cnt`. `Adam/DolDoc/DocFile.HC:5-24` reads those 16-byte headers and their following payloads after the document text. `Compiler/Lex.HC:158-180` exposes the selected record through `TK_INS_BIN` or `TK_INS_BIN_SIZE`, and `Compiler/PrsExp.HC:681,698-706` consumes those tokens as expression literals. `Adam/DolDoc/DocInit.HC:12,31` maps `IB` and `IS` to those DolDoc command types.

- Files are finite OCaml byte strings. Ordinary hosted lexing diagnoses an embedded NUL. The verified corpus path applies the pinned buffer-termination rule, decodes a following saved DolDoc table when the text refers to it, and reports every trailing payload byte.
- Lines increment on LF, matching `LexGetChar`. CR is whitespace but does not start a new line by itself.
- A raw lexer command returns `#` as punctuation. `#include` and `#define` execute only through the separate preprocessed stream, so tooling can still inspect directive and replacement tokens.
- Extensionless hosted includes try `.HC.Z` and then the decompressed `.HC` form used by the pinned Git checkout. Transparent TempleOS `.Z` decompression remains unimplemented.
- Allowed-root enforcement, active-path cycle diagnostics, and nesting and size limits are hosted security differences. They are not attributed to the native TempleOS lexer.
- Ordinary dollar-delimited text remains comment trivia outside strings. Complete commands inside strings are retained as literal bytes but are not parsed or rendered. In a NUL-terminated saved DolDoc source, `$IB,...,BI=n$` and `$IS,...,BI=n$` select a numbered `CDocBin` record and become typed literal tokens. `$$` remains the HolyC current-position token. Other DolDoc commands are not interpreted by this slice.
- Strict hosted decoding rejects truncated headers, truncated payloads, duplicate record numbers, and missing references. The pinned Git objects contain newline-normalized trailers whose declared sizes cannot reliably locate every following record. Corpus mode uses an explicit recovery path and marks shortened or missing records incomplete; it never presents those bytes as an intact oracle payload.
