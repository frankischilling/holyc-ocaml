# Testing holyc-ocaml

Run the local checks with:

```text
dune build @fmt
dune build @generated-check
dune build
dune runtest
powershell -File tools/verify-reference.ps1
```

Unit tests cover source positions, spans, token construction, literal decoding, comments, diagnostic rendering, include and generated-value frames, predefined values, primitive, internal, and named declaration types, declaration modifiers and bindings, comma-separated groups, pointer layers, strict extraction of the complete opcode database, operator tables, primitive type metadata, compiler options, function flags, member-list flags, global record flags, the complete intermediate-code table, and the TempleOS BIN specification. The generated-table tests reject malformed statements, duplicate records, missing or reordered entries, changed aliases, unknown opcode arguments, excess instruction bytes or forms, unknown operator tokens or ICs, precedence drift, unavailable-type drift, option default drift, function-flag expression or transition drift, member-list flag or consumer drift, global-record flag or consumer drift, BIN record or loader-formula drift, API contract drift, and source checksum mismatches. The option, function-flag, member-list flag, global-record flag, and BIN scanners also prove that comments and literals do not create false consumers; the option tests separately confirm that `_BEQU` retains its previous-state result.

The current test binary runs 1,222 cases: 1,221 explicitly registered Alcotest cases and one property case. `test/dune` also defines 216 golden rules. A passing count describes only the implemented slices listed below; it is not a whole-compiler compatibility percentage. The reference verifier checks 80 individually audited Git blobs, and both corpus phases account for all 528 relevant blobs from the pinned Git tree.

Twenty-five function-flag source cases validate the function hash type, all shared, stored, and parser-staging flags, source expressions, record transitions, 12 source digests, and consumer anchors. Ten public API cases cover the typed registries and transition helpers. Eight semantic cases replay JIT and AOT records through ordinary and alternate bindings, joined headers, public replacement, sticky private state, varargs and `RET1`, import precedence, invalid inputs, and deterministic classification. They establish record masks and consumer intent; they do not claim header comparison, address binding, fixup or loader-record emission, call lowering, or native ABI behavior.

Fifteen global-record registry cases compare the generated hash type, nine `HTF_*` flags, six `GVF_*` flags, source digests, and consumer anchors with a fresh parse of the pinned sources. Mutation cases reject changed masks, missing or duplicate definitions, consumer drift, and source text hidden in comments or literals. Eight semantic cases cover the JIT and AOT binding matrices, ordered public/static staging, code-heap and data-heap policy, `OPTf_KEEP_PRIVATE`, imports and alternate names, alias ownership, function-pointer and array flags, per-record state, invalid-input immutability, and deterministic classification. These tests establish flags and consumer intent; they do not claim storage allocation, loader-record emission, or native execution.

Five extern-to-import cases follow the two `OPTf_EXTERNS_TO_IMPORTS` branches in `PrsStmt` and both import consumers in `PrsGlblVarLst`. They cover ordinary and alternate function and global externs, retained source and effective kinds, per-declaration snapshots, exact loader names, function identity barriers, the global immediate-alias rule, direct imports, `_intern`, an option-disabled batch, aggregate-forward bypass, deterministic replay, classifier-state disagreement, and immutable JIT rejection. These cases establish semantic normalization and record policy only; they do not execute source-positioned option changes, resolve addresses, create fixups, or emit loader records. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic extern-to-import rewriting$'
```

Six global data-heap cases follow the option checks and allocation arms in `PrsGlblVarLst`. They cover ordinary, `_intern`, grouped, and aggregate-attached definitions; every extern and import form; extern-to-import conversion; JIT initializer retention; AOT scalar and braced initializer rejection; the AOT same-name restriction; `GVF_DATA_HEAP`; value-access and publication policy; declaration-local snapshots; deterministic classification; and unchanged symbol counts after rejection. These cases establish storage selection and record intent only. They do not execute source-positioned option changes, allocate memory, evaluate an initializer, or emit `CAOTHeapGlbl` or `.BIN` records. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic global data-heap policy$'
```

Eleven member-list source cases compare all seven `MLF_*` masks, five source digests, and 23 named assignments or consumers with a fresh parse of the pinned sources. Mutation cases reject changed order, masks, definitions, and consumers, including false source text inside comments or strings. Five public registry cases cover ordering, lookup, masks, set and clear operations, provenance, and the complete behavior list. The aggregate member-type group separately proves that an ordinary member receives zero and a callback receives `MLF_FUN` (`0x8`). Function-signature tests apply the warning-suppression, callback, default, `lastclass`, string-storage, and vararg bits. Local-type tests apply `MLF_STATIC` and `MLF_FUN` independently to each declarator. Local-warning tests cover the later `MLF_NO_UNUSED_WARN` consumer for represented function-local uses. The recorded cleanup, sizing, call, address, and storage consumers remain unimplemented.

Corpus tests cover deterministic extension filtering and path order, LF and CRLF continuation trivia, a NUL-terminated source with a retained payload count, malformed-input diagnostics, file-size rejection, dirty and mismatched reference rejection, isolated parser sessions, diagnostic classification, path redaction, stable human and JSON rendering, and the complete pinned source set. A focused project-prelude test reads different direct include orders from synthetic `Compiler.PRJ` and `Kernel.PRJ` files. It proves that a header definition and type make a dependent file parse only in prelude mode, that a sibling include follows the measured source directory only in that mode, that an existing outside-root file is still rejected, and that every target receives a copy of the same frontend state. The JSON report names the `source-directory` policy and records the effective directory for every file. The validated scans read each root from its Git object rather than a converted worktree file. This keeps root byte counts stable on Windows and Unix while the pre-scan and post-scan worktree checks still detect local changes. Parser includes come from that clean checkout, stay inside its resolver roots, and omit checkout-sensitive byte offsets from the report.

The lexer report records 528 tokenized files, zero failures, 4,190,323 canonical source bytes, 719,621 tokens, 54 NUL terminators, and 1,266,852 trailing payload bytes. The reviewed AOT parser comparison records 24 standalone parses, 17 frontend failures, 487 parser failures, and 37,016 accumulated diagnostics. Its project-prelude side accepts 118 files, with 30 frontend failures, 380 parser failures, and 21,141 diagnostics. Ninety-four files parse only with the prelude. The report classifies 368 prelude first failures as unresolved names and 42 as other boundaries. Both sides have no read or internal errors and retain every file's first error. All 38 former `HCPP0003` first failures advance: 33 now reach parser diagnostics and five reach `HCPP0022` in included source. The higher accumulated diagnostic count reflects the additional source that is now read; the accepted-file count does not change. `Kernel/StrA.HC` parses in both modes, and `Kernel/KernelC.HH` retains its later prototype boundary. The seven files that formerly stopped at a discarded `$IB` command also retain their later boundaries. The 27 diagnostics encountered while reading the four prelude headers remain visible in the report; declaration publication before those unsupported forms is what makes the comparison useful.

```text
dune exec holyc -- corpus parse --mode=aot --reference-root=third_party/TempleOS
```

The command normally returns success after a completed scan, even when compatibility failures are present. Add `--require-all` to require every file to parse with the project prelude. `.github/workflows/corpus.yml` runs the lexer and parser phases in separate jobs, compares the parser JSON with `reference/parser-corpus-aot.json`, and publishes both report formats as Actions artifacts.

Preprocessor tests build isolated temporary source trees. Include cases check nested token order, caller resumption, stable source IDs, byte positions across LF and CRLF files, working-directory rather than caller-directory resolution, ordered roots, extensionless `.HC` fallback, empty includes, include-at-EOF behavior, TempleOS root mapping, and the unchanged raw lexer. Negative cases cover missing files, directory targets, canonical path escape, self and multi-file cycles, depth exhaustion, oversized sources, malformed directive arguments, unsupported directives, and unmapped TempleOS paths. A nested lexer failure is checked in both human and JSON form to ensure the complete include chain survives rendering.

Definition cases check raw text capture, empty and whitespace-only replacements, strings, character constants, retained block-comment trivia, line comments, trailing spaces, CRLF continuation, EOF capture, byte-segment maps, use-before-declaration, latest-definition lookup, keyword names, nested expansion, definition-supplied include paths, injected directives, caller resumption, and persistence across streams in one session. Boundary cases split every multi-byte operator at every possible point and also cover numbers, quoted literals, both comment openers, an exhausted intermediate definition frame, three nested include frames, and a line continuation split across an included file and its caller. They verify raw bytes, following-token order, and every source segment. Direct and indirect cycles, depth exhaustion, generated-byte exhaustion, embedded NULs, unterminated strings, and unterminated comments are negative tests. Token JSON records invocation and declaration spans; replacement diagnostics check those spans in both renderers. The exact `holyc-definition-dump-v1` output is a reviewed golden assertion. The matching native results are recorded in [`test/oracle/frame-boundaries.json`](../test/oracle/frame-boundaries.json).

Predefined-value cases cover all six standard names, exact recognition of their pinned `KernelA.HH` bodies, explicit date and time settings, root and include depth for `__CMD_LINE__`, ordinary source overrides, `#ifdef` visibility, the pinned `defined` expansion behavior, canonical file and directory values, generated-string escaping, shared depth and byte limits, declaration and invocation provenance, and byte-identical JSON across runs. The date and time formats are checked against `Kernel/StrPrint.HC`. Two CLI golden rules exercise the real command in human and JSON modes.

Mode-conditional cases run the same inputs in explicit JIT and AOT configurations. They cover each branch with and without `#else`, nested selections, unsupported openers nested inside discarded input, inactive includes and definitions, a recursive definition that must not expand in discarded text, definition-supplied directive names, boundaries that cross include and definition frames, and byte-identical JSON from repeated runs. Malformed inactive strings and comments confirm that discarded bytes use the pinned raw scan rather than ordinary tokenization. Negative cases cover embedded NULs, stray and duplicate boundaries, missing `#endif`, conditional-depth exhaustion with side-effect-free recovery, and include or definition provenance on an unterminated conditional.

Constant-expression cases cover true and false branches, adjacent lookahead boundaries, chained definitions, integer, character, multi-character, and floating literals, all supported unary and binary families, source precedence, right-associated power, mixed power and shifts, comparison chains, wrapping, signed and unsigned comparison, shift result types, masked shifts, division, modulo, signed integer-to-F64 promotion, raw floating truth and equality, F64 logical-not encoding, and logical XOR. `defined` is checked for visible functions, locals, shadows, excluded imports, missing symbols, keyword operands, and definition-expanded operands. Negative cases cover missing terms, assignments, postfix forms, runtime identifiers, unmatched parentheses, zero divisors, signed division overflow, and node-budget exhaustion. Logical tests prove that both operands are evaluated. Repeated token and diagnostic renders are byte identical, and expression failures inside include or definition frames retain their provenance.

Assertion cases cover true and false results, definition-expanded constants, `defined` symbols, retained lookahead, expression errors, node-budget exhaustion, inactive branches, include backtraces, and definition-generated directive provenance. They distinguish warnings from errors and check both `preprocess_detailed` and the existing convenience result. Two Dune rules run the actual `holyc preprocess` executable in human and JSON modes. The reviewed stdout and stderr files prove that a failed assertion returns its following tokens, renders `HCPP0024`, and exits successfully.

Help-directive cases cover current-index replacement and clearing, explicit backslash-only string continuation, ordinary adjacent strings, default and existing extensions, TempleOS root mapping, lexical path confinement, file-name sanitation, inactive input, include state, definition provenance, retained malformed input, raw-lexer stability, and per-stream isolation. Human and JSON golden rules run the actual `--dump-help-metadata` command. A pinned `Kernel/KernelC.HH` test records all 104 `#help_index` directives and all 20 `#help_file` directives, matching direct source counts without opening any target help document.

Symbol-conditional cases cover definitions without operand expansion, missing names, checked language and assembly keywords, internal types, every register family, canonical opcodes, aliases, commented-out opcode records, registered functions, classes, globals, exports, excluded imports, and the pinned local-variable shadow result. A streaming test registers a function after consuming the preceding token and before `#ifdef`, which proves that parser work can update the environment without rebuilding the stream. Include and definition frames share the environment, while inactive symbol conditionals leave invalid operands unparsed. Separate unit tests verify all 17 `HTT_*` bit assignments, all 570 static seed entries, masked lookup, local-context stack order, stable IDs, source and generated-definition provenance, call-shape structure, sorted local names, repeated byte identity, and both views of `holyc-symbol-visibility-v2`. Three CLI rules cover source-only human and JSON output plus a parser failure that retains prior declarations on stdout, reports its diagnostic on stderr, and exits with status 1.

Semantic symbol-table cases use a separate session-owned model. They fix stable scope and symbol IDs, newest-first same-scope insertion, kind-filtered first and nth matches, nth-match continuation into an explicit parent, local-only lookup, and rejection of foreign scope handles or invalid queries before mutation. Dump tests retain source segments, generation and definition origins, stable creation order, the pinned reference commit, and schema `holyc-semantic-symbol-table-v1`. The session test checks all 17 internal type spellings seeded from `Compiler/CInit.HC`.

Six semantic declaration-collection cases cover every accepted top-level aggregate, global, prototype, and function-definition shape; grouped and aggregate-attached order; stable AST positions; forward/definition lookup order; task-root internal types; direct, generated, and included provenance; repeated module scopes; deterministic table dumps; unregistered source IDs; and invalid declaration metadata. Top-level executable statements contribute no symbol. These tests establish collection identity only: they do not count a declaration as reconciled, linked, type-checked, or resolved.

Eight semantic aggregate-resolution cases exercise the complete module-local `PrsClass` identity rule. They cover a forward completed by a definition; repeated forwards where only the newest is completed; repeated definitions; a forward introduced after a definition; class/union spelling changes; unresolved forwards; JIT and AOT parser inputs; generated and included provenance; deterministic repeated resolution without table mutation; mismatched ASTs; foreign sessions; repeated facts; invalid order; and foreign scopes. The pinned routines make a native oracle unnecessary for this identity boundary. These tests do not claim parent-task lookup, named-type use resolution, unused-forward warnings, general duplicate legality, layout, or linkage.

Ten semantic aggregate-header cases exercise the publication boundary shared by `PrsType` and `PrsClass`. They distinguish public and intrinsic backing spellings, cover pointer depths zero through four, prove that a same-name backing sees the older identity, and prove that a base sees the current identity after publication. Repeated-forward cases bind through the canonical symbol completed later. Generated and included cases retain type-token and base-name provenance. JIT and AOT runs compare deterministic results and unchanged symbol counts; invalid cases cover AST and reconciliation mismatches, foreign sessions, reversed or repeated headers, wrong symbol kinds, and foreign backing targets. The pinned routines state this ordering directly, so no native oracle is required for this header boundary. The tests do not claim backing-chain evaluation, cycle checks, layout, inherited lookup, ordinary type-use resolution, or linkage.

Seven semantic member-collection cases cover direct class and union members, grouped order, nested anonymous-union flattening and retained paths, offset and empty members, ignored metadata names, repeated-name lookup, explicit inheritance boundaries, generated-source provenance, mismatched AST batches, foreign scopes, validation before mutation, and deterministic table dumps. These tests establish direct-member identity only. They do not claim layout, inherited lookup, duplicate legality, member metadata evaluation, or reference resolution.

Parser cases cover empty input, comment-only input, all 12 public primitive spellings, published class and union names, multiple globals in source order, ordered declaration prefixes, repeated and reordered modifiers, ordinary `extern` and AOT `import` bindings, alternate-name `_extern` and AOT `_import` bindings, `_intern` expression bindings in both modes, comma-separated names, arrays, bound function prototypes, unbound function definitions, local declarations, and every currently supported statement form. Named-type cases cover globals, return types, parameters, nested function pointers, automatic and static locals, arrays, and postfix casts in JIT and AOT modes. They verify symbol-sensitive recognition, local shadowing, generated-definition provenance, deterministic dumps, unresolved-name rejection without publication, and the unfiltered first 27 lines of `Compiler/CExts.HC` after the required forwards. `_intern` cases verify the exact total of 60 declarations in the pinned `Kernel/KernelB.HH` and parse representative prototypes. They also cover every core binary and prefix operator, grouping, definition and include provenance, streaming `#ifdef` visibility, deterministic dumps, malformed targets, unsupported continuations, the expression depth limit, and rejected-name publication. Array cases cover direct and pointer declarators, comma groups, multidimensional order, an unsized first dimension, the difference between `[]` and `[0]`, every audited binary operator inside brackets, grouping, definition and include provenance, pinned compiler and kernel declarations, deterministic dumps, and rejected suffixes without rejected-name publication. Prototype cases include pinned ordinary and alternate-name headers, all four calling modifiers, return pointers, empty parameter lists, named and unnamed parameters, pointer depths zero through four, commas, semicolon-separated groups, leading and repeated empty semicolon entries, terminal `...`, exact punctuation spans, AOT imports, and function visibility to a following `#ifdef`. Empty-entry tests cover prototypes, definitions, recursive callbacks, JIT and AOT modes, generated and included provenance, deterministic dumps, and rejected empty comma slots. Function-pointer cases cover separate return and declarator depths, one through four declarator stars, named and unnamed forms, empty, fixed, and variadic signatures, nested callbacks, register-qualified nested parameters, definition-generated punctuation, recursive human and JSON dumps, and the fixed 32-level hosted limit. Default-expression cases cover every literal and prefix form, every audited binary operator, every adjacent precedence boundary in both operand orders, source-defined association, grouping, the unary-minus power rule, non-trailing defaults, nested callback defaults, generated and included provenance, malformed terms, unsupported postfix continuations, no partial symbol publication, and the fixed 256-level hosted limit. `lastclass` cases check the pinned keyword and member flags, the separate `PrsVar` syntax path, ordinary and nested function-pointer parameters, non-trailing adjacency, both compilation modes, definition and include provenance, deterministic dumps, and invalid expression continuations. Parameter-register cases cover `reg` and `noreg` before and after a type, every explicit U64 register from `ST_U64_REGS`, repeated qualifiers, qualified varargs, definition provenance, and the rule that non-U64 spellings remain parameter names. Calling-modifier tests fold ordered AST nodes through the checked transition model, covering `interrupt` with implicit `noargpop`, preservation across `public`, and the `static` reset. Definition-backed tests retain invocation and definition sites on modifiers, register names, parentheses, commas, defaults, array dimensions, `_intern` targets, output markers, statement separators, block braces, and operators. Source checks read the pinned declaration dispatch, recursive block loop, statement loop and comma boundaries, literal-statement branch, implicit `Print` and `PutChars` call path, `_intern` expression and function branches, local automatic and static branches, function-pointer branch, default branch, array-dimension loop, expression loop, unary-minus power case, local publication before initialization, metadata assignment, `PrsFunJoin`, `PRS1_FUN_ARG`, both register-qualifier loops, `ST_U64_REGS`, register sentinels, parameter loop, array-parameter rejection, variadic flag and token IDs, and representative Compiler, Kernel, Adam, and Demo declarations. Negative cases reject JIT imports before symbol publication, missing parameter types or delimiters, trailing declaration commas, nonterminal varargs, missing or unsupported expression operands, empty later dimensions, missing brackets, array parameters, qualifiers after pointer stars, invalid qualifier sequences, malformed function-pointer declarators, a fifth pointer star, excessive nesting, missing prototype punctuation, a body after a bound prototype, malformed function headers and bodies, malformed local names and initializers, register qualifiers on static locals, direct initializers on function-pointer locals, automatic braced local initializers, malformed empty output markers, empty `Print` arguments, missing output semicolons, missing expression-statement terminators, unsupported declarations after a statement comma, missing block closes, unmatched closes, and statement commas immediately before a close. Existing tests continue to cover global declaration errors, include stacks, and definition traces. Human and JSON CLI rules check the `holyc-ast-v1` dumps, source provenance, mode-sensitive diagnostic text, JSON structure, named and primitive type forms, accepted ordinary and `lastclass` defaults, dimensions, `_intern` expression trees, statement, function-definition, ordinary-local, and callback-local declaration shapes, and exit status 1 after a parse error.

Function-pointer global cases reuse the recursive signature parser already exercised by function-pointer parameters, but assert a different symbol result. The retained node separates return-type pointer layers, declarator indirection, fixed and variadic parameters, nested callbacks, array suffixes, and an optional initializer. Direct cases cover singleton and comma groups, aggregate-attached declarations, defaults, definitions, includes, JIT and AOT bindings, and publication as a global variable rather than a directly callable function. Pinned-source cases parse the four definitions at `Kernel/KGlbls.HC:32-35`, the four matching `extern` declarations in `Kernel/KernelC.HH`, and representative initialized and array forms from Adam and Demo. The line-anchored audit covers ten such forms in the intended source set; it is not a structural claim about every possible declarator. `HCPARSE0131` reports malformed parentheses and `HCPARSE0132` reports a missing global name, while the shared `HCPARSE0017` guard bounds nested callback signatures. The human and JSON `parse-function-pointer-global` goldens lock the public dump shape; `parse-pointer-errors` locks the two global diagnostics.

Aggregate-forward cases read the pinned `KW_EXTERN` dispatch, the extern branch of `PrsClass`, both scoping tables, and the first 17 lines of `Kernel/KernelA.HH`. The exact pinned prefix produces 14 class forward items without filtering directives or declarations. Direct cases distinguish class from union, retain modifiers and punctuation, allow repeated names, and exercise JIT and AOT modes. A following `#ifdef` verifies that class publication happens before the next token is requested. Include and definition cases retain canonical paths plus generated and definition origins. Negative cases cover a missing name, end of input, a missing semicolon, a comma list, and a body; rejected names remain absent. Human and JSON CLI goldens cover both aggregate kinds and `HCPARSE0108`.

Aggregate-definition cases read `PrsClass`, the complete `PrsVarLst` member loop, `PrsClassNew`, the global handoff in `PrsGlblVarLst`, the recursive initializer routines, and the union and base-class notes in `Doc/HolyC.DD`. Direct fixtures cover class and union spelling, ordered modifiers, self-referential named members, comma groups, independent pointers, multidimensional arrays, recursive anonymous unions with and without a following semicolon, empty member semicolons, a prior forward declaration, attached pointer and array globals, comma-separated attached globals, scalar and nested braced initializers, trailing initializer commas, and both compilation modes. The unfiltered `Kernel/KernelA.HH:116-179` prefix parses as 12 consecutive definitions, and the exact `Compiler/CInit.HC:1-14` block parses all 17 initialized descriptor rows. Generated text verifies that aggregate names, members, attached globals, and initial values retain invocation and definition origins. A streaming conditional verifies publication after each attached name. Negative cases cover missing names, braces, member types, names, delimiters, final semicolons, nested named definitions, missing initial values, malformed or unclosed initializer lists, and the separate 256-level guards for anonymous unions and initial values. Human and JSON CLI goldens cover a complete attached-global body, recursive initial values, `HCPARSE0114`, and `HCPARSE0127`.

Aggregate-offset parser cases read the `CCF_CLASS_DOL_OFFSET` setup and repeated `$$` loop in `PrsVarLst`, the current-position term in `PrsExp.HC`, and the class-offset note in `Doc/HolyC.DD`. A syntax-specific audit finds four direct forms: one negative displacement in `Demo/Lectures/NegDisp.HC` and three literal or alignment forms in `Kernel/KernelA.HH`. Tests retain those exact expressions in JIT and AOT modes, then cover classes, named unions, anonymous unions, repeated directives, surrounding empty semicolons, ordinary top-level `$$` assignment, macro and include provenance, and deterministic dumps. Negative cases cover a missing equals sign, value, or semicolon; a malformed nested union; the expression-depth limit; and recovery to the next global. Human and JSON AST goldens fix the accepted member shape and `HCPARSE0142` output. The separate aggregate-layout tests evaluate closed offsets, track negative displacement, update union bases, and apply explicit alignment. Symbol-dependent offset expressions remain visible semantic dependencies.

Contextual member-name cases compare the `TK_IDENT` result in `Lex.HC`, the optional `PrsKeyWord` interpretation in `PrsLib.HC`, and the declarator-name path in `PrsVar.HC`. A pinned scan finds 12 direct `U0 start;` members: eight in `Kernel/KernelA.HH`, two in `Adam/Gr/Gr.HH`, and two in `Adam/Gr/SpriteMesh.HC`. Tests accept all 48 checked language-keyword spellings as ordinary members in JIT and AOT modes, then cover groups, pointers, arrays, recursive callbacks, named and anonymous unions, definitions, includes, stable dumps, and unchanged global and switch-keyword behavior. The exact `KernelA.HH:430-449` region parses through both `start` and the aligned `sys_gdt` member. Human and JSON CLI goldens fix the contextual-name shape.

Further cases extend that rule to the remaining declarator, aggregate, and member-reference slots. They read the pinned local groups at `Demo/TimeIns.HC:10` and `Kernel/BlkDev/DskFmt.HC:3`, a keyword-named global and global callback, defaulted parameters shaped like `Adam/Gr/GrMath.HC:9` and `Kernel/KExts.HC:8`, a local callback, and the `offset`, `sizeof`, and `->` member paths behind `Adam/Gr/SpriteNew.HC:41`, each in JIT and AOT modes. Aggregate fixtures declare keyword-spelled classes and unions, use them for globals and locals, inherit from a visible keyword-spelled class, retain generated provenance, and confirm that the spelling keeps its keyword role before publication. A limits case keeps `reg` and `noreg` as qualifiers rather than local names. No authored keyword-spelled aggregate was found in the intended pinned corpus.

Nine contextual operand cases read the local-first lookup in `Lex.HC`, the global-table construction in `CmpJoin`, head insertion and ordered lookup in `Kernel/KHashA.HC`, and the function and variable branches in `PrsExp.HC` and `PrsStmt.HC`. Local cases cover parameters and prior locals in arithmetic, comparisons, call arguments, indexes, and statement-leading positions in JIT and AOT modes. Global-variable cases cover top-level arithmetic, statement-leading expressions, function-body reads, and generated-definition provenance in both modes. Global-function cases cover explicit and parenthesis-free calls, default slots, address-of, pre-publication keyword behavior, and generated calls. A nested-block case confirms the parser's function-wide local visibility. In the AOT corpus, `Adam/Gr/GrMath.HC` advances from its `start` operand to an unresolved `CTask` type, while `Demo/TimeIns.HC` advances to the unsupported `#exe` directive. No authored keyword-spelled top-level variable or function occurs in the intended source roots. During incomplete recovery, however, three files publish function-local declarations at top level; the global-variable rule removes 11 later diagnostics while leaving phase counts and first errors unchanged. The global-function rule leaves the reviewed report byte-for-byte unchanged.

Aggregate member-metadata cases read the class-mode metadata loop in `PrsVar.HC`, lookup and extended-string handling in `LexLib.HC`, the `CMemberLstMeta` definition in `KernelA.HH`, and the complete two-class region at `Demo/ClassMeta.HC:16-28`. The pinned fixture contributes six members and 13 entries: strings, integers, a float, division, and address-of. Direct cases add adjacent strings, duplicate names, keyword names, callbacks, pointers, arrays, comma groups, and anonymous unions in both modes. Definition and include tests check provenance. Negative cases cover a missing value before each aggregate boundary, a malformed expression, and recovery to the next global. Human and JSON goldens fix the accepted AST and `HCPARSE0144` diagnostic. These tests do not claim metadata evaluation or runtime lookup.

Function-pointer member cases follow the `PrsType` callback branch used by `PrsVarLst` in class and union mode. Direct fixtures cover return-pointer layers outside the callback, one and two declarator stars, recursive parameters and defaults, comma groups, arrays, variadic signatures, and anonymous-union members in JIT and AOT modes. Pinned fixtures retain eight callbacks from `Kernel/KernelA.HH`: `CMathODE` at lines 251-289, `CDocEntry` at lines 1191-1222, and the key-device definitions at lines 3754-3771. The `CDocEntry` fixture omits only line 1221, `#assert !($$&7)`, because the current constant preprocessor deliberately rejects runtime `$$`; it keeps the surrounding source through the closing brace. A line-anchored scan finds 26 direct `(*name)(` or `(**name)(` member forms in that file. This count is evidence for those lines, not a complete declarator census. Generated and included cases retain provenance. Negative cases cover each punctuation boundary, a missing name, a fifth star, recovery to the following global, and the shared 32-level callback guard. Human and JSON CLI goldens fix the recursive AST and the `HCPARSE0133` diagnostic. The member-type tests resolve complete recursive signatures and retain the exact `MLF_FUN` mask, aggregate-layout tests assign pointer-sized storage and offsets, and member-index tests retain the callback marker during direct and inherited lookup. Default binding and evaluation, call checking, lowering, and execution remain pending.

Aggregate-backing cases read `PrsType`, `PrsClass`, the 17-entry internal type table in `CInit.HC`, and the whole-value and subinteger examples in `Doc/HolyC.DD`. The parser accepts all 11 intrinsic `*i` storage spellings, retains zero through four backing pointer layers, and parses the six public integer unions plus representative date, intermediate-code, counter, and color definitions from `Kernel/KernelA.HH` in JIT and AOT modes. Include and generated-definition cases verify complete backing and aggregate provenance. Negative cases reject an unknown backing, a declaration binding before `class` or `union`, and a fifth backing pointer while retaining the following global declaration. Human and JSON CLI goldens cover a successful backed union and `HCPARSE0124`.

Aggregate-inheritance cases read the complete colon branch in `PrsClass` and the one-base statement in `Doc/HolyC.DD`. Direct cases distinguish the base from a preceding backing type and exercise class plus source-permitted union forms, whitespace variants, publication before base lookup, JIT and AOT modes, definitions, includes, and deterministic dumps. The unmodified `Adam/Gr/Gr.HH:18-115` slice contributes one sprite base and 16 derived classes. Representative kernel slices cover direct and chained hash bases. Negative cases reject a missing, unknown, primitive, or intrinsic base through `HCPARSE0121`, reject a second base through `HCPARSE0126`, and verify recovery through the complete body. Human and JSON CLI goldens cover accepted base nodes and the second-base diagnostic.

Function-definition cases read the complete declaration dispatch, `PrsFun`, the shared signature path, the single `PrsStmt` body call, the explicit-semicolon branch, and the EOF exit. They parse representative definitions from Compiler, Kernel, Adam, and Demo sources in JIT and AOT modes. Shape tests cover modifiers, a return pointer, defaults, varargs, recursive calls, block, empty, unbraced, comma-linked, and absent-at-EOF bodies. Streaming tests prove that a function is visible before its body is requested. Provenance tests combine a definition-generated header with a direct body and an included body. Recovery tests retain the ordinary body diagnostic, reject a malformed header before publication, keep bound forms on the prototype path, and apply the existing block-depth guard. Human and JSON CLI goldens distinguish a present body from an absent one without changing the `holyc-ast-v1` schema. The native fixture runs one buffer through the pinned TempleOS compiler and records matching results for explicit empty, block, unbraced, recursive, pre-body `#ifdef`, and EOF-absent forms.

Parenthesis-free call cases retain fixed-parameter shape on accepted prototypes and definitions. They cover zero and all-default signatures, a required parameter between defaults, adjacent required parameters, an empty variadic tail, recursive calls, binary continuation, address-of, local and global shadowing, include and definition provenance, and deterministic dumps. Negative cases check a missing required expression and a function entry registered without parameter shape through `HCPARSE0105` and `HCPARSE0106`. Human and JSON CLI goldens distinguish this form from a parenthesized call without inventing punctuation. The native fixture records seven results from the pinned compiler, including the value 173 for `OracleMixed 7;` and a nonzero address for `&OracleZero`.

Local-declaration cases read the complete pinned `PrsVar.HC` and `PrsStmt.HC` files plus representative declarations from `Adam/Snd/SndMath.HC`, `Demo/Asm/AsmAndC1.HC`, `Demo/Asm/AsmAndC2.HC`, and `Kernel/KMisc.HC`. Shape tests cover automatic and static storage, scalar initializers, pointers, arrays, comma groups, per-declarator `reg` and `noreg`, and explicit U64 registers. Visibility tests cover named parameters, declaration-before-initializer publication, prior locals, and variadic `argc` and `argv` across nested statement contexts. Continuation tests prove that a local declaration keeps the same statement parse active until a nonlocal statement finishes or a closing brace is reached. Negative tests reject local declarations in `for` initializers and unbraced `try` bodies where the source parser continues into `catch`. Human and JSON CLI goldens lock the accepted AST and the static-register diagnostic. The native fixture records ten observable results and four rejected forms from the pinned TempleOS compiler.

Static-local initializer cases follow `PrsStaticInit` into the recursive `PrsVarInit2` path and keep automatic locals on the separate `PrsExpression` path. Exact source tests parse `Adam/WinMgr.HC:38`, `Demo/GlblVars.HC:21-25`, and `Demo/GlblVars.HC:62-66` in JIT and AOT modes. Direct cases cover named aggregates, sized and unsized arrays, multiple dimensions, nested and empty lists, trailing commas, scalar compatibility, expression elements, grouped declarators, generated-definition provenance, and deterministic dumps. A line-start scan found these three braced forms among 21 static declarations in the intended Compiler, Kernel, Adam, and Demo source set; that narrow scan is not a complete declarator census. Negative cases cover `HCPARSE0138` through `HCPARSE0141`, the 256-level guard, recovery to a following global, and the retained `HCPARSE0104` rejection for automatic braced initialization. Human and JSON CLI goldens lock one accepted tree and the missing-separator diagnostic. No native oracle was needed because the pinned static and automatic branches call different parser routines directly.

Function-pointer-local cases read the shared callback path at `Compiler/PrsVar.HC:408-524`, the automatic-storage and initializer branch at lines 590-618, and the comma restart at lines 691-714. Exact pinned-source tests parse `U0 (*fp_old_update)(CDC *dc);` from `Demo/Games/Stadium/StadiumGen.HC:5` and `U0 (*old_draw_ms)(CDC *dc,I64 x,I64 y);` from `Demo/Graphics/Grid.HC:13` in JIT and AOT modes. Direct cases cover outer return pointers, one and two callback indirections, recursive parameters and defaults, varargs, arrays, register requests, static syntax, mixed callback and ordinary comma groups, local shadowing, generated-source provenance, and deterministic dumps. Negative cases cover every punctuation boundary, a missing name, the shared nesting limit, recovery to a following global, and `HCPARSE0137` for a direct callback initializer. The human and JSON CLI goldens fix both the accepted recursive node and that initializer diagnostic. No native oracle was needed because `PrsType` and `PrsVarLst` state the syntax path directly. No static callback local was found in the pinned corpus, so the static case is focused coverage rather than a corpus claim.

Semantic function-collection cases follow `PrsFunJoin`, `PrsFun`, `PrsVarLst`, `PrsDotDotDot`, `MemberAdd`, and `MemberFind`. They preserve unnamed fixed-parameter gaps; verify `argc` then `argv` with ellipsis provenance; collect automatic and static comma groups; and traverse blocks, branches, loops, sequences, locks, switches, sub-switches, and both sides of `try`/`catch` without creating narrower local scopes. Repeated-name cases check newest-first lookup while retaining older entries. Negative cases prove that AST mismatches, foreign scopes and symbols, malformed ordering, invalid indexes, and invalid origins fail before a scope or symbol is added. A byte-stable semantic dump closes the deterministic boundary. These tests do not claim types, defaults, initializers, register allocation, storage offsets, duplicate legality, ordinary expression references, prototype reconciliation, or linkage.

Aggregate member type-reference cases follow `PrsVarLst`, `PrsType`, `PrsArrayDims`, `PrsClass`, `PrsClassNew`, `OptClassFwd`, `MemberLstDel`, `MemberLstSize`, and the `CMemberLst.fun_ptr` consumers in `PrsExp.HC`. Fourteen focused tests cover public and intrinsic spellings, comma-declarator and callback-parameter pointer depths zero through four, earlier and self types, forward completion, repeated forwards, shadowing, same-name prepublication backings versus postpublication members, recursive anonymous-union paths, array-dimension origins, callback return pointers, callback indirection, nested callbacks, named and unnamed slots, expression and `lastclass` defaults, terminal varargs, punctuation, generated and included provenance, JIT and AOT determinism, purity, low-level validation, and the exact zero-versus-`MLF_FUN` member masks. The pinned semantic fixtures resolve the complete signatures of `CMathODE.derive`, `mp_derive`, the `CDocEntry` callbacks, and the key-device callbacks. Cross-session and mismatched declaration, header, member, and AST inputs fail without adding scopes or symbols; a foreign aggregate nested in a callback signature also fails. The tests do not claim binding or evaluation inside defaults, call checking, metadata evaluation, or runtime storage. Closed array extents, byte layout, duplicate legality, and inherited lookup have separate test groups. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic aggregate member type resolution$'
```

Nine aggregate-layout cases follow the cursor, union-base, negative-offset, and array-count mutations in `PrsVarLst` and `PrsClass`. They cover packed fields, a base prefix, named and nested union overlap, multidimensional arrays, pointer and callback storage, zero-sized fields, an empty first dimension, explicit alignment, negative displacements, every supported closed expression operator, short-circuit behavior, stable typed dependencies, malformed cross-pass inputs, JIT/AOT agreement, and deterministic repeated evaluation. Metadata byte counts are overflow-checked separately from wrapping target arithmetic. These tests do not claim symbol-dependent expression evaluation, backing conversion, metadata lookup, subinteger access, runtime allocation, or native layout parity.

Eight aggregate member-index cases follow `MemberAdd`, `MemberFind`, the member tree and source list in `CMemberLst`, the single `base_class` chain, and the field consumers in `PrsExp.HC`. They cover direct and missing names, nested anonymous-union paths, one-level and chained inheritance, absolute inherited offsets, ordinary direct and inherited duplicate rejection, case-sensitive padding exceptions, first-in-source selection for repeated exempt names, retained type and layout facts, stable failure codes, cross-session and reordered batches, pure failure paths, JIT/AOT agreement, and deterministic repeat indexing. These tests do not bind AST expressions, increment use counts, evaluate metadata, or execute field access.

Four aggregate layout-dump cases fix schema `holyc-aggregate-layout-v1`, the pinned reference commit, source-order aggregate and direct-member output, stable IDs, base facts, anonymous-union paths, multidimensional arrays, callbacks, named unions, negative offsets, generated-source ancestry, empty input, target values beyond 32 bits, JIT/AOT agreement, and byte-identical repeated rendering. Six CLI rules cover human and JSON success, a semantic failure, and a parser failure. Both failure paths assert exit status 1, exact diagnostics, and empty stdout, so an incomplete semantic pipeline cannot masquerade as a usable layout report.

Seventeen semantic function type-reference cases follow `PrsGlblVarLst`, `PrsFunJoin`, `PrsType`, `PrsVarLst`, `PrsDotDotDot`, `MemberLstCmp`, and `PrsFunCall`. They cover public and intrinsic return and parameter types, pointer depths zero through four, unnamed fixed slots, source-ordered aggregate visibility and shadowing, separate prototype and definition scopes, callback indirection depths one through four, recursive callbacks, non-trailing ordinary and `lastclass` defaults, terminal ellipses, and the exact top-level `argc` and `argv` facts. Exact-mask checks cover named and unnamed slots, callbacks, ordinary and `lastclass` defaults, direct and recursively nested string defaults, and both synthetic vararg records. Register-request checks cover all four final states, all 16 canonical U64 registers, repeated before-type and after-type qualifiers, named and unnamed slots, last-request-wins source codes, recursive callbacks, and propagation from an ellipsis to both synthetic bindings. Member, global, and local callback tests exercise the same shared representation. Pinned header checks retain and resolve `Dir` from `Kernel/KernelC.HH`, `Copy` from `Adam/ABlkDev/ADskA.HC`, and `Zip` from `Adam/ABlkDev/ADskB.HC`. `argc` is checked as an intrinsic `I64` scalar; `argv` is an intrinsic `I64` array shape with no source extent and placeholder 127. Generated and included requests retain provenance. Repeated resolution compares deterministic results and unchanged symbol counts, while joined headers deliberately ignore request differences. Malformed qualifier spellings, explicit-register triples, non-U64 names, and spelling/number pairs fail through the low-level constructor. Mismatched AST, declaration, aggregate, and function batches; foreign sessions and aggregate targets; invalid recursive shapes; and invalid synthetic bindings also fail without mutation. Function identities and record flags have separate test groups. These type-reference cases do not claim complete header comparison, default evaluation or substitution, call checking, `OPTf_NO_REG_VAR`, register allocation, storage, ABI behavior, completed linkage, or other type-use sites.

Seven semantic function header-analysis cases follow `PrsFunJoin`, `MemberLstCmp`, `PrsVarLst`, and `PrsDotDotDot`. They cover independent return and argument warnings, public and intrinsic types, aggregate identity, callback class projection, exact integer and floating payloads, NUL-terminated string comparison, `lastclass`, old-header count asymmetry, zero-fixed-argument varargs, ignored register and calling-flag changes, batch and per-declaration option snapshots, generated and included warning origins, stable invalid-input diagnostics, deterministic replay, and symbol-table purity. The tests supply evaluated payloads directly. They establish comparison behavior but do not claim that arbitrary default expressions can be evaluated or substituted at calls. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic function header analysis$'
```

Ten semantic local type-reference cases follow the automatic and static branches in `PrsStmt`, local record construction and initialization in `PrsVarLst`, and local identifier handling in `PrsExp`. They cover public, intrinsic, aggregate, pointer, array, and recursive callback types; the shared checked register-request representation and explicit register number; initializer shapes; function-wide order; exact declaration-time masks `0x0`, `0x8`, `0x40`, and `0x48`; static comma groups; and independence from nested callback-parameter flags. Pinned checks retain the static declarations at `Kernel/KMisc.HC:87`, `Adam/WinMgr.HC:38`, and `Adam/Gr/SpriteMesh.HC:65`. Included and definition-generated static tokens retain provenance. Repeated JIT and AOT runs compare the masks and unchanged symbol counts, while low-level failures remain immutable. These cases do not allocate storage, evaluate extents or initializers, assign registers or offsets, lower static or frame addresses, or execute callbacks. Warning classification consumes these records in its own test group.

Eight function binding-index cases follow `MemberAdd`, `MemberFind`, `PrsVarLst`, and the local-before-global lookup branch in `Lex`. They cover named parameters, variadic `argc` and `argv`, automatic and static locals, binding ordinals and source positions, ordinary duplicates within and across categories, nested declarations, separate function namespaces, generated-source provenance, JIT/AOT equality, deterministic replay, and unchanged symbol counts. Repeated `pad`, `reserved`, and `_anon_` bindings remain in source order while lookup selects the first. Cross-pass ownership mismatches, reordered functions, repeated symbols, invalid binding scopes, and foreign parents produce stable codes `HCSEMA0014`, `HCSEMA0015`, and `HCSEMA0016`. These tests do not bind expression occurrences, model publication timing, increment use counts, assign storage, or apply ABI rules.

Eleven function expression-binding cases follow the local-first lookup in `Lex`, `MemberFind`, the insertion boundary in `PrsVarLst`, and the relevant branches in `PrsExp.HC`. They cover parameters, synthetic variadic bindings, explicit nonlocal candidates, pre-declaration misses, array dimensions before publication, self-initializers after publication, comma declarators, function-wide remainder scope, global shadowing, first-source selection for `pad`, `reserved`, and `_anon_`, every structured statement route, generated provenance, deterministic replay, and unchanged symbol counts. Ordinary uses, specialized queries, `no_warn` targets, and post-initializer count resets remain distinct in one source-ordered event stream. Query cases cover all three roles, local and nonlocal roots, member-name exclusion, non-name `defined` operands, stable indexes, and definition-generated provenance. Invalid cross-pass data and missing or mismatched publications use `HCSEMA0017` through `HCSEMA0019`; inconsistent suppression and reset events use `HCSEMA0031` and `HCSEMA0032`. The pass leaves warning classification, global or member resolution, typing, storage, and lowering to separate passes. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic function expression binding$'
```

Seven module expression-binding cases follow `HashAdd`, the newest-first hash lookup routines, the active table chain in `CMain.HC`, the aggregate, function, and global publication paths, and the identifier cases in `PrsExp.HC`. They cover prior publication visibility, recursion, local precedence, later-declaration exclusion, cross-kind shadowing, completed aggregate and joined function identities, global alias records, generated provenance, JIT/AOT equality, deterministic replay, unchanged symbol counts, and invalid semantic batches. `HCSEMA0020` and `HCSEMA0021` report validation failures. The pass leaves absent module names as outer candidates and does not count uses, search task or AOT parent tables, validate expression kinds, resolve members, type expressions, assign storage, or lower code. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic module expression binding$'
```

Five top-level expression-binding cases follow `CmpJoin`, `LexStmt2Bin`, the recursive `PrsStmt` routes, and ordinary identifier lookup in `PrsExp.HC`. They cover JIT and AOT source-order replacement, earlier global and function publications, outer candidates, recursive statement order, contiguous identities, aggregate publications through the low-level API, definition-generated provenance, deterministic replay, unchanged symbol counts, foreign ownership, and invalid statement order. `HCSEMA0052` reports validation failures. These tests establish module-prefix binding only; they do not type expressions, check calls, lower IR, execute statements, or emit `IET_MAIN`. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic top-level expression binding$'
```

Four top-level outer-expression cases consume that source-ordered batch and follow `SYS_HASH_FIND`, `CmpJoin`, and `Doc/ScopingLinkage.DD`. They cover JIT current and parent tasks, AOT enclosing compilations, assembler fallback, module precedence, newest records within a table, unchanged statement and occurrence identities, unresolved names in both modes, generated failure provenance, included success provenance, deterministic replay, unchanged symbol counts, foreign ownership, and mode mismatches. `HCSEMA0053` reports invalid semantic input, and source-positioned `HCSEMA0054` reports a name absent from the complete chain. These tests use caller-supplied immutable table snapshots; they do not construct live tasks, filter a binding by expression role, type expressions, lower code, or execute statements. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic top-level outer expression binding$'
```

Three top-level semantic-tree cases consume those exact bindings and follow `LexStmt2Bin`, `PrsStmt`, `PrsExpression`, and `PrsFunCall`. They cover every expression constructor accepted by the current parser, executable root roles, source-order IDs, parenthesized call holes, callee identity, module and outer bindings, generated provenance, JIT/AOT equality, deterministic replay, purity, mode mismatch, and foreign ownership. `HCSEMA0055` reports an inconsistent statement, binding, index, or source association. The tests do not assign value types, resolve callable signatures or defaults, lower IR, execute statements, or emit `IET_MAIN`. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic top-level expression tree$'
```

Four top-level identifier-classification cases consume those trees and follow the `HTt_GLBL_VAR`, `HTt_FUN`, `HTt_CLASS`, callback, and array routes in `PrsTerm`. They cover ordinary globals, arrays, callbacks, callback arrays, direct functions, aggregate offset bases, source-order redeclarations, all four outer record kinds, metadata-bearing outer primitive, pointer, aggregate, array, and callback globals, JIT/AOT address policy, generated provenance, deterministic replay, purity, mode mismatch, foreign ownership, and stale type batches. An outer entry without checked metadata remains explicitly unavailable. `HCSEMA0056` rejects mismatched inputs. The tests do not type operators, resolve call arguments, read storage, lower IR, execute statements, or emit `IET_MAIN`. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic top-level identifier resolution$'
```

Twenty-nine top-level expression-result cases consume those classified leaves and reuse the function expression engine. They cover JIT and AOT literal classes, object, array, callback, callback-array, and direct-function address shapes; metadata-bearing outer primitive, pointer, aggregate, array, and callback globals; direct and pointer aggregate members; aggregate offset paths; inherited lookup metadata; member arrays and callbacks; result-use roles; indexing; primitive postfix casts; comparisons; logical operators; updates; assignments; integer and `F64` conversion flags; invalid lvalues, member paths, and offset paths; generated provenance; purity; deterministic replay; foreign-session rejection; same-session stale batches; and mode mismatches. Outer results keep their exact top-level occurrence and selected table binding; metadata-free outer records remain unavailable. Offset cases retain ordered member lookups, intermediate types, and cumulative values while the completed expression remains intrinsic `I64`. Direct-call cases add parenthesized and parenthesis-free syntax, explicit middle holes, declared defaults, variadic order and count, nested calls, source-visible replacement headers, primitive, pointer, and by-value aggregate returns, included and generated origins, exact result-ID linkage, and source order in both modes. Scalar module callback cases retain the exact global and stored signature while covering the same slot shapes, nested calls, return classes, provenance, and result linkage. Fully indexed callback-array cases additionally cover one- and multidimensional bracket chains, target-integer index conversions, exact selected-callee results, and partial or excess rank diagnostics. Direct and pointer member-callback cases retain the exact current-or-inherited lookup, parenthesized callee, global base, recursive signature, slots, and selected callee result. Scalar and fully indexed outer callback cases exercise integer, `F64`, pointer, and backed aggregate returns; middle defaults; variadic tails; nested direct and outer calls; exact occurrence and binding identity; deterministic replay; symbol-table purity; and both compilation modes. The negative outer cases keep metadata-free records and ordinary objects unavailable, reject zero, partial, and excessive callback-array ranks, and create no false callback record. Other negative cases fix missing-required, extra-fixed, omitted-variadic, rank, ordinary-member, callback-member-array, and access-shape messages under `HCSEMA0057`. Partial module or outer arrays, unindexed callback-member arrays, unrelated computed callees, and metadata-free or ordinary outer callees remain explicit boundaries. Ordinary member, offset, operator, and lvalue errors outside a call retain the shared `HCSEMA0046` code. These tests do not calculate callback addresses, read outer storage, evaluate defaults or side effects, lower IR, execute statements, or emit `IET_MAIN`. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic top-level expression results$'
```

One current-position case covers direct, grouped, and scalar-binary `$$` expressions in JIT and AOT modes. Each leaf keeps the zero-depth internal `RT_PTR`/`I64` address type; the enclosing binary result remains an integer object. Assignment and postfix-update forms fix the source-positioned `HCSEMA0046` non-lvalue diagnostics. The case also checks replay determinism and symbol-table purity without choosing an address, lowering IR, or executing top-level code.

Two additional cases cover fully indexed aggregate callback-member arrays. They exercise direct, pointer, inherited, multidimensional, parenthesized, sparse-default, variadic, pointer-return, and aggregate-return calls in JIT and AOT modes. They also fix partial and excessive rank diagnostics, preserve the exact member lookup through each subscript, and extend generated-source replay coverage. These checks establish semantic selection and typing only; they do not read callback storage or calculate an address.

Two more cases cover scalar outer callbacks. Checked JIT task and AOT parent-table entries use the recursive callback metadata already retained on `CHashGlblVar`. The positive case checks slot binding, return classes, nested calls, source order, exact binding identity, replay, and purity. The negative case proves that ordinary objects and records without metadata remain unavailable. The outer call syntax is explicitly grouped in these fixtures because the parser cannot classify an otherwise unknown top-level name as executable input before outer lookup runs. A nested visible direct call still exercises the parenthesis-free parser route.

Two indexed outer callback cases reuse those JIT and AOT snapshots with one- and multidimensional callback arrays. The positive case checks exact bracket chains, every integer-target subscript, sparse defaults, a variadic tail, nested outer calls, primitive, `F64`, pointer, and backed aggregate returns, the completed computed callee, exact occurrence and binding identity, replay, and purity. The negative case fixes zero, partial, and excessive rank diagnostics plus required-slot checking under `HCSEMA0057`. Grouping remains explicit because the parser still routes an otherwise unknown top-level name through declaration disambiguation. These tests do not calculate an address, read callback storage, evaluate inputs, lower IR, or execute a call.

Seven outer expression-binding cases follow the explicit `CHashTable.next` field, single-table and chained lookup in `Kernel/KHashA.HC`, task-parent links in `Kernel/KTask.HC`, compiler-chain selection in `Compiler/CMain.HC`, and the final assembler link in `Compiler/AsmInit.HC`. They cover current and parent JIT tasks, nested and top-level AOT, assembler fallback, newest-record selection across all four supported record kinds, local and module precedence, generated-source failure provenance, deterministic replay, unchanged symbol counts, repeated identities, role and index errors, foreign sessions, and mode mismatches. `HCSEMA0022` through `HCSEMA0024` report validation and unresolved-name failures. The tests use supplied immutable table snapshots; they do not claim live task construction, addresses, imports, fixups, loader records, use counts, typing, storage, or lowering. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic outer expression binding$'
```

Five global initializer-binding cases follow the `HashAdd` call before `PrsGlblInit` in `Compiler/PrsStmt.HC`, recursive initializer parsing in `Compiler/PrsVar.HC`, ordinary identifier lookup in `Lex` and `PrsExp.HC`, and the mode-specific outer chain in `CMain.HC`. They cover self references, earlier and later comma declarators, prior and later module items, all four supported outer record kinds, nested braced paths, JIT and AOT fallback, generated-source failures, deterministic replay, unchanged symbol counts, occurrence and publication drift, invalid paths, foreign sessions, and mode mismatches. `HCSEMA0025` and `HCSEMA0026` report invalid input and unresolved ordinary names. The tests establish binding only; they do not claim initializer typing, evaluation, conversion, storage, relocation, lowering, or execution. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic global initializer binding$'
```

Five global dimension-binding cases follow the `PrsType` call before `HashAdd` in `Compiler/PrsStmt.HC`, explicit dimension evaluation in `Compiler/PrsVar.HC`, ordinary identifier lookup in `Lex` and `PrsExp.HC`, and the mode-specific outer chain in `CMain.HC`. They cover the current, earlier, and later comma declarators; prior and later module items; empty and multidimensional arrays; one occurrence order across a declarator; ordinary, callback, and aggregate-attached globals; all four supported outer record kinds; JIT and AOT fallback; generated-source failures; deterministic replay; unchanged symbol counts; occurrence, dimension, record, AST, session, and mode drift; and constructor bounds. `HCSEMA0027` and `HCSEMA0028` report invalid input and unresolved ordinary names. The tests establish binding only; they do not claim extent evaluation, constant conversion, type checking, allocation, layout, lowering, or execution. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic global dimension binding$'
```

Five function default-binding cases follow publication before `PrsVarLst` in `Compiler/PrsStmt.HC`, parameter insertion and default parsing in `Compiler/PrsVar.HC`, local-before-table lookup in `Compiler/Lex.HC`, and the JIT and AOT successor selection in `Compiler/CMain.HC`. They cover recursive defaults, the absence of parameter-local lookup during a header, prior and later module records, joined declaration identities, `lastclass`, all four supported outer record kinds, both compilation modes, generated-source failures, deterministic replay, unchanged symbol counts, reordered or incomplete batches, occurrence and parameter drift, foreign sessions, and mode mismatches. `HCSEMA0029` and `HCSEMA0030` report invalid input and unresolved ordinary names. The tests establish binding for defaults on top-level named headers only; they do not claim evaluation, substitution, conversion, string storage, header comparison, call checking, or callback-declarator defaults. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic function default binding$'
```

Sixteen call-resolution cases follow the fixed-slot loop and variadic tail in `Compiler/PrsExp.HC:PrsFunCall`. They cover explicit sparse holes, absent trailing slots, non-trailing defaults, source-visible replacement headers, canonical joined identities, parenthesis-free calls in JIT and AOT modes, fixed and variadic counts, source-visible named cast targets, generated and included provenance, deterministic replay, unchanged symbol counts, and foreign semantic inputs. Parameter, automatic, static, global, and aggregate-member function-pointer calls resolve as typed indirect calls; their recursive signatures supply defaults, varargs, and result types. Member cases cover direct and pointer access, parenthesized callees, inherited lookup, source-defined duplicate rejection, primitive, pointer, and aggregate returns, invalid access shapes, and foreign member indexes. Two cases add one- and multidimensional callback arrays stored in automatic or static locals and module globals. Exact bracket chains retain source order and signature; partial or excessive indexing and ordinary arrays produce `HCSEMA0039`. Unrelated computed callback callees remain deferred. Low-level validation rejects stale, wrong-module, or foreign aggregate identities in direct and callback signatures without adding a symbol. The parenthesis-free case also reads the checked native oracle fixture at the exact reference commit. `HCSEMA0040` reports a required fixed slot, `HCSEMA0041` reports an extra fixed argument, and `HCSEMA0042` rejects a hole in the variadic tail. These tests establish slot selection, callee identity, and retained signatures. They do not evaluate defaults or expressions, read callback storage, calculate an indexed address, lower a call, or emit ABI code. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic function call resolution$'
```

Seven direct-call conversion-policy cases follow the `OptClassFwd` and result-flag branch at `Compiler/PrsExp.HC:477-484`. They cover all public primitive spellings, intrinsic integer and `F64` storage types, `F64` pointers, callbacks returning `F64`, ordinary aggregates, unresolved forwards, primitive and intrinsic aggregate backings, pointer backings, chained aggregate backings, source-positioned header completion, JIT and AOT replacement headers, explicit and trailing defaults, parenthesis-free defaults, variadic expressions, generated and included provenance, backing cycles, foreign inputs, deterministic replay, and unchanged symbol counts. `HCSEMA0043` rejects inconsistent inputs, and `HCSEMA0044` rejects a backing cycle that `OptClassFwd` could not terminate. The result records whether a provided fixed expression targets the forwarded `F64` or integer path through headers published before its caller; a declared default and a provided variadic expression remain separate. These tests do not infer the actual expression class, select `ICF_RES_TO_F64` or `ICF_RES_TO_INT`, evaluate a value, check an indirect call, lower a call, or emit ABI code. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic function call conversion policy$'
```

Seventy-four typed call expression-result cases cover deterministic expression IDs, checked address, dereference, index, member, update, assignment, and nested direct and indirect-call transitions, public and intrinsic pointer depths one through four, primitive and aggregate base identity, source-visible aggregate, function, and callback headers, local and global arrays, partial multidimensional indexes, callbacks, nested and parenthesized bases, zero-depth integer and `U0` address classes, lvalue contexts, unavailable outer operands, and exact conversion-result identity. Direct `&Function` cases keep the replacement or recursive function publication, exact resolved declaration, canonical joined identity, internal zero-depth `RT_PTR` type, integer result class, and source-backed JIT extern-slot, JIT immediate, or AOT absolute intent without assigning an address. Ordinary AOT externs and imports, plus `_intern` functions outside the noninternal path, receive stable `HCSEMA0046` diagnostics at `&`. Separate cases prove that local storage and callback variables keep their ordinary address types, that included definitions retain invocation and definition provenance, and that forged binding shapes or mode paths are rejected. A nested resolved call keeps its exact direct or indirect record and source-visible return type; pointer returns remain addresses and by-value aggregate returns follow their visible backing. Member callback cases add direct, pointer, parenthesized, inherited, defaulted, variadic, primitive-return, pointer-return, and aggregate-return paths. Fully indexed callback-array cases retain the typed local, module, or outer base, every integer-target subscript result, the recursive signature, and the exact selected outer binding when present. Every selected default keeps the exact parameter and default-use record, checked parameter type, forwarded result class, ordinary-expression or `lastclass` kind, and immediate or AOT string-constant materialization path. Defaults do not receive expression IDs. A selected `lastclass` default also keeps the exact preceding provided result and its base spelling; an ordinary default does not replace that state, and no preceding result stays explicit. Direct, typed indirect, parenthesized, and parenthesis-free cases cover JIT and AOT behavior, included and definition-backed provenance, deterministic replay, and purity. Unrelated computed callback calls remain unresolved. Index, member, update, assignment, and provided variadic cases retain their source identities and independent conversion requirements. Proven expression errors use stable semantic diagnostics at the rejected bracket, operator, member name, or inconsistent call owner. Four return-focused cases add declared I64, U64, F64, pointer, and aggregate types; integer/F64 conversion intent; missing and unexpected value warning facts; nested direct and callback calls; source order; JIT and AOT behavior; include provenance; deterministic replay; and purity. Two condition cases cover all four loop and branch roles in both modes, integer and F64 classes, pointers, members, nested callbacks, source order, include and definition provenance, deterministic replay, purity, and the absence of an invented Boolean conversion. Two switch-selector cases cover bounded and no-bound modes, integer, F64, pointer, member, index, direct-call, and callback-call values, source order, include and definition provenance, deterministic replay, purity, and the absence of range or Boolean conversion. Two switch-case cases cover implicit, single, and ranged patterns; paired endpoints; integer, F64, pointer, member, direct-call, and callback-call values; `LexExpressionI64` conversion intent; both compilation modes; provenance; deterministic replay; purity; and constructor validation. The 34 conversion-decision cases consume this shared result; checked fixed expressions and nested calls select the integer or `F64` conversion path, while a variadic decision retains the exact typed result and actual class without a target conversion. `HCSEMA0045` reports conversion with a foreign table or policy traversal. These tests do not claim default evaluation or payload copying, AOT string allocation or fixups, index evaluation or bounds checks, update or assignment execution, pointer scaling, callback storage reads, unrelated computed callback-call checking, variadic promotion or ABI placement, member address arithmetic, stride arithmetic, executable function addresses, condition control-flow lowering, case evaluation, implicit-value assignment, switch range adjustment, duplicate detection, jump-table construction, switch IR, or general IR lowering. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic function call expression results$'
```

One current-position case covers direct and grouped statement roots, a fixed call argument, and a scalar-binary leaf in JIT and AOT modes. Each `$$` result keeps the zero-depth internal `RT_PTR`/`I64` address type. Assignment and postfix-update forms fix the `HCSEMA0046` non-lvalue diagnostics. The case also checks exact result identity, replay determinism, and symbol-table purity without choosing an address, lowering IR, or executing a function.

Separate callback-array cases cover fully indexed callback-member arrays in function bodies. They verify exact inherited lookup, every declared subscript, sparse defaults, a variadic tail, primitive, pointer, and by-value aggregate returns, both compilation modes, and included definition provenance. The call-resolution negative case also rejects partial and excessive indexing with `HCSEMA0039`.

Two cases in this group exercise ordinary function expression statements across recursive statement positions and both compilation modes. They cover integer, `F64`, pointer, member, update, assignment, and direct-call values; `ICF_RES_NOT_USED`; included definition provenance; deterministic replay; and an unchanged semantic symbol table. The call-resolution group separately rejects invalid or noncontiguous statement identities. These tests do not claim `IC_END_EXP` lowering, runtime value disposal, side-effect execution, or implicit output typing.

Two more cases exercise implicit `Print` and `PutChars` statements across recursive positions and both compilation modes. They cover marker-backed and following-expression fixed slots, ordered `Print` arguments, integer, `F64`, pointer, and nested direct-call values, `ICF_RES_NOT_USED`, included definition provenance, deterministic replay, and an unchanged symbol table. The call-resolution group validates target-specific argument shapes, punctuation provenance, and contiguous identities. Four downstream cases resolve source-ordered module headers and JIT or AOT outer entries, verify that same-name nonfunction records are skipped, retain canonical identities, reject foreign sessions, and report a source-positioned missing-header diagnostic. Nine argument-binding cases then cover standard and replacement headers; include and definition provenance; integer, `F64`, aggregate, pointer, and function-pointer targets; provided and defaulted fixed slots; variadic tails; JIT and AOT default materialization; checked and deferred outer headers; stable missing and excess argument diagnostics; deterministic replay; purity; and foreign-session rejection. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic implicit output argument binding$'
```

Five executable top-level target cases reuse the typed output roots without fabricating a function. They cover module header replacement before and after each statement, same-name object filtering, JIT task-parent and AOT enclosing-compilation lookup, assembler fallback, marker-backed and following-expression forms, recursive statements, definition-generated markers, typed fixed and trailing roots, contiguous output identities, exact module and outer records, deterministic replay, symbol-table purity, ownership, mode mismatch, and `HCSEMA0059` at a missing literal marker. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic top-level implicit output target resolution$'
```

Six downstream argument cases use the header and typed roots selected by those target cases. They cover aggregate-backed, integer, and `F64` fixed conversions; a `Print` variadic tail; marker-backed and following-expression forms; provided and defaulted slots; JIT and AOT string-default materialization; recursive and generated input; exact outer-header acceptance and deferral; wrong and duplicate outer headers; stable missing and excess diagnostics; deterministic replay; ownership; mode; symbol-table purity; and the unchanged function-body adapter after extracting shared slot rules. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic top-level implicit output argument binding$'
```

These tests do not claim format parsing, default payload evaluation, vararg promotion, call lowering, output, or top-level execution.

Three top-level condition cases collect `if`, `while`, `do while`, and `for` roots from the checked executable-statement batch. They cover integer, `F64`, pointer, member, index, direct-call, and callback-call values; zero and nonzero branch sense; JIT and AOT modes; containing-statement and result identity; included keyword provenance; definition-generated value provenance; deterministic replay; symbol-table purity; duplicate indexes; and foreign sessions. The records do not create a Boolean conversion, control-flow edge, label, branch instruction, `IET_MAIN`, or execution result. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic top-level condition results$'
```

Three top-level switch selector cases collect bounded and no-bound roots from the checked executable-statement batch. They cover integer, `F64`, pointer, member, index, direct-call, and callback-call values; JIT and AOT modes; nested source order; containing-statement and result identity; included keyword provenance; definition-generated value provenance; deterministic replay; symbol-table purity; duplicate indexes; and foreign sessions. The records do not evaluate selectors or cases, subtract a range origin, create labels or a jump table, emit switch IR, create `IET_MAIN`, or execute code. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic top-level switch selector results$'
```

Three top-level switch case cases join implicit, single-value, and ranged labels to the checked executable-statement batch. They cover integer, `F64`, pointer, member, direct-call, and callback-call values; paired range endpoints and ellipsis provenance; JIT and AOT modes; nested source order; containing-statement and exact result identity; included keyword provenance; definition-generated value provenance; deterministic replay; symbol-table purity; discontinuous metadata; missing and unexpected roots; and foreign sessions. The records do not evaluate values, assign implicit numbers, normalize or expand ranges, detect duplicates, create labels or a jump table, emit switch IR, create `IET_MAIN`, or execute code. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic top-level switch case results$'
```

Three top-level statement-validation cases reject explicit `return` at module scope in JIT and AOT modes. They cover direct returns with and without values, every recursive statement container, exact keyword spans, included and definition-generated provenance, deterministic replay, symbol-table purity, function-return separation, and the distinct final ordinary-expression path. The tests do not claim that the final expression is lowered to `IC_RETURN_VAL2` or executed. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic top-level statement validation$'
```

Thirty-four conversion-decision cases read the literal classes at `Compiler/PrsExp.HC:679-700`, local and global value construction at `Compiler/PrsExp.HC:761-807` and `830-904`, aggregate typing in `Compiler/PrsVar.HC:285-319`, the `$$`, `sizeof`, `offset`, and `defined` result paths, prefix parsing at `Compiler/PrsExp.HC:597-675` and `742-759`, update, dereference, and index construction at `Compiler/PrsExp.HC:81-154` and `1055-1112`, binary construction in `Compiler/PrsExp.HC:15-201`, unary, update, and binary class handling in `Compiler/OptLib.HC:92-220` and `Compiler/OptPass012.HC:153-897`, the postfix-cast class assignment at `Compiler/PrsExp.HC:960-1055`, and the fixed comparison at `Compiler/PrsExp.HC:477-484`. They cover integer, character, string, and float literals; all four audited integer-side primaries; named parameters; synthetic `argc` and `argv`; automatic and static locals; source-visible globals; local shadowing; comma-published and same-name replacement globals; scalar primitive, intrinsic, class, and union values; local and global arrays; local and global callbacks; primitive, intrinsic, class, and union casts; scalar and pointer cast targets; by-value backing chains; pointer-backed classes; source-positioned forward completion and same-name shadowing; outer-cast precedence; every prefix operator; checked pointer dereference and indexing; recursive unary plus, unary minus, logical not, address-of, bitwise-complement, pre-increment, and pre-decrement decisions; complement operands with integer, `F64`, backed, binary, and unavailable classes; all four updates with integer, `F64`, backed, binary, and unavailable operand classes; all 31 checked binary operators; mixed integer, `F64`, and unavailable operand classes; power, arithmetic, shift, bitwise, logical, comparison, assignment, and compound-assignment boundaries; outer identifiers, calls, and members that remain unresolved; nested prefixes, updates, indexes, binary expressions, casts, and parentheses; fixed defaults; variadic source views; deferred callbacks; JIT and AOT replacement headers; generated and included provenance; inconsistent type-reference, prefix, postfix, binary, occurrence, function-owner, and type-batch inputs; a foreign session; deterministic replay; and unchanged symbol counts. `HCSEMA0045` rejects typed results owned by another session or policy traversal, while `HCSEMA0039` rejects inconsistent bound-identifier inputs before policy analysis. The decision records no conversion, `ICF_RES_TO_F64` intent, `ICF_RES_TO_INT` intent, or an unresolved actual. It does not read identifier storage, evaluate a primary, literal, cast, operator, or operand value, apply an update or assignment, type the remaining expression families, mutate an AST, set an IC flag, lower a call, or emit ABI code. Run only this group with:

```text
dune exec test/test_main.exe -- test '^semantic function call conversion decision$'
```

Semantic label-resolution cases follow the `goto` and identifier-label branches in `PrsStmt`, `COCGoToLabelFind`, `COCDel`, and function-local label publication in `Asm.HC`. Nine focused tests cover stable IDs; first-occurrence order; forward and backward gotos; every current recursive statement route; separate function scopes; prototypes; language and structural assembly definitions in one namespace; retained same-address assembly repeats; top-level, missing, duplicate, and mixed-definition failures; mismatched ASTs; foreign sessions; include and definition provenance; and byte-stable semantic dumps. Rejected batches are checked before and after the call to prove that they add no label. The pinned functions state this identity boundary directly, so these tests do not require a native oracle. They do not claim assembly operand references, output-address validation, unused warnings, global-object collision checks, jump construction, IR lowering, or execution.

Implicit-output cases cover ordinary and variable-format strings, ordinary, multi-character, and variable characters, trailing `Print` arguments, full expression continuations beginning at a nonempty marker, JIT and AOT item order, include and definition provenance, deterministic dumps, and diagnostics for missing fixed values, empty `Print` arguments, and missing semicolons. The CLI fixtures record the distinct marker and fixed-expression fields in human and JSON output. They prove syntax and provenance only; no test claims that `Print` or `PutChars` has been resolved or executed.

Statement-sequence cases cover ordinary expression statements, empty statements, leading and repeated commas, comma terminators, semicolon-before-comma forms, trailing commas, and comma-only input. They check that `PutChars` can end at a statement comma while `Print` retains comma-led arguments until its semicolon. JIT and AOT ordering, include and definition provenance, deterministic recursive dumps, a missing terminator, and unsupported declaration or label boundaries are covered. Human and JSON CLI goldens record separator locations and the `HCPARSE0047` diagnostic.

Compound-block cases cover empty and nested blocks, mixed top-level item order, expression, empty, output, and sequence children, a block followed by a sequence comma, both compilation modes, and braces supplied by definitions or included files. Recovery tests cover a missing close, an unmatched close, a comma before a close, unsupported child syntax, an implicit-output failure whose recovery must stop at the block boundary, and the fixed 256-block hosted nesting limit. Human and JSON CLI goldens record recursive block shapes and the `HCPARSE0049` diagnostic with its opening-brace note.

Conditional-statement cases read the complete pinned `PrsIf` flow, both keyword IDs, and the documented chained-condition example. They cover JIT and AOT syntax, nearest-`if` else association, empty, expression, output, block, sequence, and nested conditional branches, mixed top-level order, and keyword or parenthesis tokens supplied by definitions and includes. Negative cases exercise missing condition parentheses, an empty condition, missing then or else branches, an unmatched `else`, recovery at a block close, and the fixed 256-conditional hosted nesting limit. Human and JSON CLI goldens record the full conditional shape and `HCPARSE0054` output.

While-statement cases read the complete pinned `PrsWhile` flow, keyword ID, and representative kernel loops. They cover JIT and AOT syntax, empty, expression, block, sequence, conditional, and nested loop bodies, plus nearest-`if` else association across loop boundaries. Provenance cases supply the keyword and parentheses through definitions and load a loop from an include. Negative cases exercise missing condition parentheses, an empty condition, a missing or comma-only body, and the shared 256-loop hosted nesting limit. Human and JSON CLI goldens record the complete loop shape and `HCPARSE0060` output.

Do-while cases read the complete pinned `PrsDoWhile` flow, both keyword IDs, and a compound loop in `Kernel/Job.HC`. They cover JIT and AOT syntax, empty, expression, block, sequence, conditional, pre-test, and post-test loop bodies, plus nearest-`if` else association on both sides of the body boundary. Provenance cases supply both keywords, both parentheses, and the final semicolon through definitions and load a loop from an include. Negative cases exercise a missing or comma-only body, every required trailing component, an empty condition, and mixed pre-test and post-test nesting at the shared 256-loop limit. Human and JSON CLI goldens record the complete post-test shape and `HCPARSE0066` output.

For-statement cases read the complete pinned `PrsFor` flow, the `PrsStmt` semicolon switch used by its update, keyword ID 8, and representative normal, empty-initializer, empty-update, and comma-update forms. They cover JIT and AOT syntax, an initializer statement, the mandatory condition, optional and comma-linked updates, empty and conditional bodies, nearest-`if` else association on both sides of a loop boundary, include and definition provenance, and mixed loop nesting. Negative cases exercise every required header boundary, the mandatory condition, a semicolon incorrectly placed after the update, a missing or comma-only body, and the shared 256-loop hosted limit. Human and JSON CLI goldens record the complete header and `HCPARSE0071` output.

Break-statement cases read the pinned `KW_BREAK` dispatch, active-target check, `IC_JMP`, common terminator path, keyword ID 19, and documented switch uses. They cover JIT and AOT syntax, top-level tooling mode, each implemented loop body, conditional branches, blocks, comma-linked statements, and a semicolon-free `for` update. Definition and include cases retain keyword and terminator provenance. Negative cases exercise end of input, a closing parenthesis, an unexpected expression, a semicolon in the `for` update boundary, and recovery that must preserve an enclosing block close. Human and JSON CLI goldens distinguish a retained semicolon from a comma boundary and record `HCPARSE0072` output.

Return-statement cases read the pinned `KW_RETURN` dispatch, function-context check, active-`try` unwind loop, optional expression, `IC_RETURN_VAL`, leave-label jump, common terminator path, keyword ID 12, and representative compiler, kernel, and documentation forms. They cover JIT and AOT syntax, valueless and value-bearing nodes, expression precedence and postfix updates, all current recursive statement contexts, statement commas, and a value-bearing semicolon-free `for` update. Definition and include cases retain keyword, value, and terminator provenance. Negative cases exercise missing values, invalid value boundaries, a valueless `for` update, and recovery that preserves an enclosing block close. Human and JSON CLI goldens distinguish absent values and semicolons and record `HCPARSE0073` output. These tests do not claim function-context validation, return-type checking, try unwinding, leave-label resolution, lowering, or execution.

Goto and label parser cases read the pinned `KW_GOTO` dispatch, label lookup and cleanup routines, keyword and record IDs, compiler and kernel call sites, and the documentation on function scope. They cover JIT and AOT syntax, forward and backward source shapes, comma and `for` update boundaries, symbol collisions, include and definition provenance, deterministic dumps, missing targets, invalid terminators, and block-safe recovery. Human and JSON CLI goldens preserve both node kinds and record `HCPARSE0075` output. The separate semantic cases cover function-local identity and binding; neither group claims jump construction or control-flow lowering.

Lock-statement cases read the pinned `KW_LOCK` dispatch, `lock_cnt` lifetime, `ICF_LOCK` handoff, keyword ID 42, the language guide, and braced and unbraced multicore demos. They cover JIT and AOT syntax, blocks, ordinary and empty statements, nested locks, comma-linked bodies, conditionals, loops, definitions, includes, and deterministic dumps. Negative cases exercise missing and comma-only bodies, malformed children, and the independent 256-lock hosted nesting limit. Human and JSON CLI goldens record the wrapper and `HCPARSE0077` output. These tests prove syntax and provenance only; they do not claim IR flag propagation, x86 prefix selection, or execution.

Try/catch cases read the complete pinned `PrsTryBlk` flow, keyword IDs 5 and 3, unbraced compiler and kernel uses, braced and nested demo uses, and the language guide. They cover JIT and AOT syntax, both body shapes, empty and nested pairs, comma-linked bodies, conditionals, loops, locks, `for` updates, definitions, includes, and deterministic dumps. Negative cases exercise a missing body, keyword, or handler, a standalone `catch`, malformed children, block-safe recovery, and the independent 256-try hosted nesting limit. Human and JSON CLI goldens record the complete pair and `HCPARSE0080` output. The tests prove syntax and provenance only; they do not claim `SysTry` or `SysUntry` calls, handler labels, exception transfer, return unwinding, lowering, or execution.

Assembly-block cases read the `KW_ASM` dispatch, both `PrsAsmBlk` entry paths, the directive and opcode hash branches, label handling, assembly expression support, listing behavior, and the relevant `Doc/Asm.DD` definitions. The checked inventory is not based on a line-prefix search: tests lex every committed `.HC` and `.HH` file under `Compiler`, `Kernel`, `Adam`, and `Demo`, identify adjacent `asm` and `{` raw tokens, track surrounding brace depth, and compare the resulting path, line, and context tuples with `reference/assembly-blocks.json`. The pinned result is 45 blocks in 40 files, with six nested blocks. Focused cases parse four exact top-level and function-local regions from `AsmAndC1.HC` and `AsmAndC2.HC` in both compilation modes. Exhaustive classification covers all 325 canonical opcodes, 49 aliases, 25 directives, and 106 registers. Other cases cover global, exported, and local labels, multiple operations on one physical line, includes, definition-generated tokens, deterministic dumps, empty blocks, recovery after a missing opener, and an unterminated close. Human and JSON goldens record the accepted structure and `HCPARSE0145`. Separate semantic tests give function-local structural labels stable identities. These parser cases do not claim operand parsing, directive execution, output-address validation, encoding, or assembly execution.

Direct-assembly cases read both hash branches in `PrsStmt`, the `CMPF_ONE_ASM_INS` flag, `PrsAsmArg`, first-form operand checks, `PrsAsmDefine`, `PrsBinFile`, and the assembler-keyword dispatch in `PrsAsmBlk`. Exact instruction tests cover operand-free uses in `Adam/AMem.HC`, `Kernel/Job.HC`, and `Demo/Graphics/Balloon.HC`, plus operand-bearing uses in `Demo/Asm/AsmAndC3.HC`, `Demo/Lectures/Ring3.HC`, and `Kernel/KInts.HC`. JIT and AOT cases retain adjacent instructions and directives; aliases; register, immediate, sized-memory, and segment-prefixed memory forms; import names; `DU*` values and postfix `DUP`; file strings; listing and mode changes; optional separators; semicolon distinctions; local shadows; includes; definitions; and deterministic dumps. Negative cases assert `HCPARSE0147` and `HCPARSE0149` through `HCPARSE0156`. The issue #157 corpus comparison added `AsmAndC3.HC` to the parsed set and removed four `HCPARSE0148` diagnostics. The issue #159 comparison leaves all phase and diagnostic totals unchanged; `Kernel/KStart16.HC` and `Kernel/KStart32.HC` retain their first `HCPP0008` failure but classify 19 recovered direct items as `HCPARSE0147` instead of `HCPARSE0001`. These tests do not claim operand validity, directive evaluation or state changes, form selection, lowering, encoding, or execution.

Switch cases read the complete pinned `PrsSwitch` flow, keyword IDs 20 through 24, the checked `default` spelling, and representative forms in `Compiler/UAsm.HC`, `Kernel/Compress.HC`, `Kernel/StrPrint.HC`, `Demo/NullCase.HC`, and `Demo/SubSwitch.HC`. They cover bounded and no-bound headers in JIT and AOT modes, empty bodies, ordinary statements before labels, stacked labels, implicit, single, and ranged cases, default labels, nested switches, nested and empty sub-switch front porches, all current recursive statement contexts, definitions, includes, and deterministic dumps. Negative cases exercise every header and body delimiter, label colons, a missing range endpoint, unmatched structure, malformed children, recovery at the next case, and independent 256-level switch and sub-switch hosted limits. Human and JSON CLI goldens record a complete structural switch and `HCPARSE0091` output. These tests prove syntax and provenance only; they do not claim case evaluation, duplicate checks, break binding, front-porch validation, jump-table construction, lowering, or execution.

Call-expression tests cover empty calls, explicit zero, leading, middle, consecutive, and trailing holes, nested calls, parenthesized callees, every binary precedence boundary, JIT and AOT syntax, definition and include provenance, and the 256-level expression guard. Source assertions read `PrsFunCall`, the documented `Test(,3);` example, and three compiler call sites with omitted arguments. Negative cases check a missing separator, missing closing parenthesis, missing prefix operand, and a call after a terminal postfix increment without publishing the rejected declaration. Human and JSON CLI goldens preserve each slot and comma and check `HCPARSE0024` output in both formats.

Index-expression tests cover simple and zero subscripts, binary and grouped indexes, repeated indexes, calls before and after an index, omitted call slots inside brackets, parenthesized bases, every binary precedence boundary, JIT and AOT syntax, all existing declaration expression contexts, definition and include provenance, and the 256-level expression guard. Source assertions read the bracket branch in `PrsUnaryModifier`, the modifier loop, the `argv[i]` documentation example, and representative compiler and kernel uses. Negative cases check an empty index, a missing closing bracket, a missing binary operand, an index after a terminal postfix increment, and rejected-name publication. Human and JSON CLI goldens preserve both brackets, nested postfix order, and `HCPARSE0026` output.

Member-expression tests cover direct access, pointer access, repeated members, and chains that mix calls and indexes before or after a member. They exercise every binary precedence boundary, JIT and AOT modes, each existing declaration expression context, include and definition provenance, deterministic dumps, and rejected-name publication after malformed input. Source assertions read both member cases in `PrsUnaryModifier`, the `TK_DEREFERENCE` construction and ID, direct and pointer examples in `Doc/HolyC.DD`, and a mixed chain in the compiler. Human and JSON CLI goldens preserve the access kind, operator, member name, full postfix nesting, and `HCPARSE0027` output.

Postfix-update tests cover `++` and `--`, compound operands, interaction with prefix dereference, every binary precedence boundary, all existing declaration expression contexts, JIT and AOT modes, include and definition provenance, deterministic dumps, and rejected-name publication. Source assertions read both update branches in `PrsUnaryModifier`, their flags and precedence, the `IC__PP` and `IC__MM` selection in `PrsExpression2`, and representative compiler, kernel, Adam, and demo uses. Negative cases prove that a call, index, member access, or second update cannot continue the postfix chain after an update. Human and JSON CLI goldens preserve the operand, update kind, operator span, and `HCPARSE0028` output.

Postfix-cast tests cover all 12 checked primitive spellings, published class and union names, pointer depths zero through four, casts after every existing postfix base, repeated casts, casts followed by each modifier form, every binary precedence boundary, all existing declaration expression contexts, JIT and AOT modes, include and definition provenance, and rejected-name publication. Source assertions read the cast and prefix-rejection branches in `PrsExp.HC`, `PrsType` pointer handling, `IC_HOLYC_TYPECAST` metadata, the language guide, and representative compiler, kernel, Adam, and demo uses. Negative cases cover C-style prefix casts, unresolved names, an unclosed target, a token before the closing parenthesis, a fifth pointer star, and a cast after a terminal update. Human and JSON CLI goldens keep the operand, both parentheses, type target, pointer layers, and `HCPARSE0029` diagnostic.

`sizeof` tests cover every primitive spelling, unresolved class and variable names, no wrapper, one wrapper pair, repeated pairs, repeated member segments, and zero through five target stars. They distinguish a target star in `sizeof U8 *` from multiplication after `sizeof(U8)`, exercise every binary precedence boundary, and cover all existing declaration expression contexts in JIT and AOT modes. Modifier tests accept a recognized postfix cast immediately after the raw term and accept calls, indexes, members, and updates only after that cast. Negative cases reject empty and literal targets, missing member names, unmatched wrappers, arbitrary expression operands, every direct postfix suffix, and unrecognized cast targets without publishing a rejected declaration. Source assertions read the complete `KW_SIZEOF` and `PrsSizeOf` token flow, the keyword and IC identities, metadata, the language-guide conflict, and representative compiler, kernel, Adam, and demo uses. Include and definition cases preserve every token origin. Human and JSON CLI goldens retain the keyword, wrappers, target, member path, pointer layers, and `HCPARSE0033` diagnostic.

`offset` tests cover bare and wrapped forms, the required first member, repeated member segments, every binary precedence boundary, and every existing declaration expression context in JIT and AOT modes. A star after the member path remains multiplication rather than a target pointer layer. Modifier tests enforce the same recognized-cast boundary as `PE_MAYBE_MODIFIERS`, including ordinary postfix suffixes after the cast. Negative cases reject missing roots, missing first dots, missing member names, pointer stars inside wrappers, unmatched wrappers, direct postfix suffixes, and unrecognized cast targets without publishing a rejected declaration. Source assertions read `PrsOffsetOf`, the `KW_OFFSET` dispatch and identity, the documentation conflict, and representative compiler, kernel, and Adam uses. Include and definition cases preserve every token origin. Human and JSON CLI goldens retain the keyword, wrapper pairs, root, complete member path, and `HCPARSE0036` diagnostic.

Opcode-database tests parse the pinned source and check all 106 registers, 325 canonical opcodes, 49 aliases, and 924 instruction forms. They cover each register kind, every instruction field family, argument-size derivation, aliases, nested comments, deterministic output, source lines, and representative `MOV`, `ENTER`, and x87 forms. A regression assertion keeps the empty first `REP_OUTSB` form created by the pinned loader's leading-comma behavior.

Operator API tests cover prefix boundaries, source provenance, all precedence bands, raw association flags, and binary mappings. Semantic type tests cover every supported spelling, raw ID, size, signedness, declaration form, `Bool`, zero-sized types, and the pointer raw alias. Compiler-option API tests cover all names and bit indices, intentional gaps, the initial mask, immutable updates, consumer lines, phase classifications, and the source-marked incomplete state of `OPTf_USE_IMM64`.

Intermediate-code tests cover all 185 constructors and numeric slots. They compare every generated metadata field with a fresh parse of the pinned source, check all lookup directions, retain the 13 constant/display-name differences, and reject malformed shapes, counts, Booleans, padding, and records. Golden tests compare deterministic token dumps. Negative fixtures check malformed comments, strings, characters, and bytes. QCheck generates source buffers to verify offset and line-column invariants.

Function-flag tests cover the two shared flag positions, all eight stored function bits, all eight parser-staging masks, both source groups, and every modifier transition. The transition test checks all 4,096 possible staging masks. Boundary and truth-table tests cover automatic `RET1`, varargs, caller cleanup, interrupt error-code handling, internal functions, and the distinction between public hash state and stored function flags.

BIN source tests cover the exact header layout and signature, entry order and numeric gaps, source status comments, import displacement biases, record payloads, loader-pass actions, AOT adjustments, checksum normalization, and source provenance. Mutation tests reject changed, duplicate, missing, or reordered entries, changed reserved gaps, altered import formulas, altered adjustment operations, unknown consumers, and incomplete source sets. Public API tests distinguish known, reserved, and unknown codes and check every name and numeric round trip. These are specification tests; loader and actual-TempleOS oracle tests remain pending with the serializer.

`defined` tests cover bare and repeatedly wrapped forms, ordinary identifiers, language keywords, integers, and punctuation as the single operand token. They verify that keywords retain the pinned `TK_IDENT` shape while other tokens stay explicit non-name operands, without assigning a truth value during parsing. The cases cover every binary precedence boundary, each existing declaration expression context in both compilation modes, the recognized-cast boundary, suffixes after that cast, and include and definition provenance. Negative cases cover EOF before the operand, empty and unmatched wrappers, expression-shaped input, every forbidden direct suffix, and an unrecognized cast target. Source assertions read the `KW_DEFINED` branch and identity, `PrsKeyWord`, lexer identifier construction, and the separate preprocessor note. Human and JSON CLI goldens retain both operand classes and the `HCPARSE0041` diagnostic.

`no_warn` parser cases read `PrsNoWarn`, its statement dispatch and common terminator, local-first lookup, `MemberFind`, the member-list flag, the language guide, six live corpus files, and the Adam generated-source string. Seven focused cases cover JIT and AOT shapes, empty and trailing-comma forms, the distinction between an internal target comma and an outer statement comma, parameters, `argc`, `argv`, prior locals, nesting, semicolon-free `for` updates, definitions, includes, deterministic dumps, and stable failures. Human and JSON CLI rules retain the accepted AST and `HCPARSE0157` output. The AOT corpus records 21 parses, 17 frontend failures, and 490 parser failures; `Demo/MultiCore/Palindrome.HC` loses one recovered `HCPARSE0048`.

Eight semantic local-warning cases read the exact flag, count threshold, `_anon_`, option, lookup increment, specialized-expression branches, member-name decrement, and initializer-reset branches. They cover named parameters, synthetic `argc` and `argv`, automatic and static locals, declaration-time flag preservation, used and unused bindings, one and repeated suppressions, prototypes, the disabled unused-variable option, ordinary and query self-initializer resets, uses of earlier bindings in later initializers, all three query roles, member and non-name exclusions, JIT/AOT equality, definition-generated and included origins, deterministic replay, unchanged symbol counts, unknown option bits, and foreign semantic inputs. `HCSEMA0033` reports invalid analysis input; structured findings use `HCSEMA0034` and `HCSEMA0035`. The source count includes ordinary uses, locally resolved specialized queries, and suppression lookups. Source-positioned option execution, type/member resolution, query evaluation, and CLI warning rendering remain pending.

The function expression-result group contains 77 cases. Six outer-global cases cover JIT task-parent and AOT enclosing-compilation records with primitive, `F64`, pointer, aggregate, array, and recursive callback shapes. Scalar and fully indexed callback-call cases bind provided values, a middle default, and a variadic tail; type integer, `F64`, pointer, and backed aggregate returns; retain nested call order and exact result identity; and keep metadata-free records and ordinary objects unavailable. The three indexed-array cases cover automatic and static locals, module globals, checked JIT and AOT outer globals, one- and multidimensional bracket chains, typed bases, integer-target subscript results, exact outer bindings, partial and excessive ranks, and unindexed array calls. The group also checks exact occurrence and binding identity, negative metadata ranks, symbol-kind mismatches, aggregate ownership, stale same-session expression batches, deterministic replay, and symbol-table purity. These tests do not read outer storage, calculate indexed addresses, invoke a callback, evaluate defaults, lower IR, or execute code.

Golden changes require a reviewed fixture update. Tests must not rewrite expected files automatically.

Corpus, differential, fuzz, loader, and bootstrap suites will be added with the stages they exercise. A suite is not marked passing until its command runs in CI and publishes its exact reference commit.
