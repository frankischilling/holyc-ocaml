# holyc-ocaml compatibility status

Reference commit: `c26482bb6ad3f80106d28504ec5db3c6a360732c`.

| Phase | Current result | Evidence |
| --- | --- | --- |
| Top-level direct-call IR fragments | Checked executable top-level direct calls use the same composer as function-scope calls. The top-level adapter verifies the exact retained call result and target, then supplies its fixed arguments, optional hidden variadic count, variadic arguments, canonical symbol, record, and access policy. The composer emits `IC_CALL_START`, canonically lowered argument trees, `IC_CALL`, `IC_CALL_INDIRECT2`, `IC_CALL_IMPORT`, or `IC_CALL_EXTERN`, byte-counted cleanup, and `IC_CALL_END` with caller-owned identities. Defaults and internal operations remain unsupported without partial IR. Complete statement bodies, callback calls, `IET_MAIN`, and execution remain unimplemented. | [Issue #544](https://github.com/frankischilling/holyc-ocaml/issues/544), three added cases within the seventeen-case direct-call group, JIT/AOT modes, all four emitted opcodes, fixed and variadic arguments, hidden count, push flags, cleanup, identities, deterministic output, exact cross-call and cross-session ownership, defaults, and internal operations |
| Top-level direct-call target classification | A typed direct call in an executable module statement joins only to the exact declaration-order function record owned by its semantic resolution. The immutable target retains the top-level call, resolved declaration, classified declaration, record, and the same total access policy used by function-scope lowering. Replacement headers remain distinct; no spelling or canonical-symbol fallback is permitted. `HCSEMA0068` rejects foreign, replayed, or different-mode evidence at the call origin without changing symbols. The separate top-level direct-call IR adapter now consumes this record; callback lowering, default evaluation, complete top-level bodies, `IET_MAIN`, and execution remain unimplemented. | [Issue #542](https://github.com/frankischilling/holyc-ocaml/issues/542), three focused cases, JIT and AOT modes, all five access paths, exact identities, replacement snapshots, deterministic reclassification, ownership and mode rejection, provenance, and purity |
| Direct extern and import call opcodes | Implemented for checked function-scope targets classified as JIT extern address slots, AOT imports, or AOT externs. The lowerer selects `IC_CALL_INDIRECT2`, `IC_CALL_IMPORT`, or `IC_CALL_EXTERN` directly from the exact declaration-order access record and carries the canonical function symbol as address-slot or relocation intent. All three paths reuse the verified fixed/variadic argument order, caller cleanup, return type, call span, and identity allocation. Internal operations remain unsupported. | [Issue #540](https://github.com/frankischilling/holyc-ocaml/issues/540), one matrix case within the seventeen-case direct-call IR group, all three access classes, fixed plus variadic arguments, exact selected opcodes, canonical symbols, 24-byte cleanup, identities, and deterministic replay |
| Supplied variadic direct-call arguments | Implemented for ordinary direct executable calls when every fixed slot is provided and all fixed and variadic expression trees are supported by canonical lowering. The lowerer validates the resolved I64 count against the retained typed-result list, emits fixed trees, the synthesized `argc` immediate, and variadic trees in source order, then marks every unique logical argument root with `ICF_PUSH_RES`. The hidden payload is the supplied-value count; cleanup includes fixed, hidden, and supplied slots. Variadic actuals retain their own expression flags without invented fixed-target conversions. An unsupported supplied expression returns unsupported without partial IR. | [Issue #538](https://github.com/frankischilling/holyc-ocaml/issues/538), two added focused cases within the thirteen-case direct-call IR group, one and multiple supplied values, fixed-prefix composition, JIT/AOT modes, exact order, payload, type, flags, spans, identities, cleanup, and deterministic replay |
| Empty variadic-tail direct-call IR | Implemented for ordinary direct executable calls with an active variadic header, only supported provided fixed arguments, and no supplied variadic values. After the fixed trees, the lowerer emits the hidden `IC_IMM_I64 0` required by `PrsFunCall`, types it from the synthesized internal `argc` binding, marks it `ICF_PUSH_RES`, and then emits the call. The hidden slot consumes one instruction and value identity and contributes eight cleanup bytes. Variadic calls use caller cleanup. The separate supplied-value slice extends this boundary to nonempty tails. | [Issue #536](https://github.com/frankischilling/holyc-ocaml/issues/536), two added focused cases within the eleven-case direct-call IR group, zero and multiple fixed arguments, JIT/AOT modes, exact opcode order, payload, type, flags, spans, identities, cleanup, deterministic replay, and nonempty-tail fallback |
| Multiple provided fixed direct-call arguments | Implemented for ordinary direct executable calls when every fixed slot is provided, every typed expression is supported by canonical lowering, and no variadic binding or actual exists. The lowerer validates exact agreement among resolution slots, typed fixed results, and active parameters, then threads instruction and value identities through argument trees in parameter order. Each unique argument root gains `ICF_PUSH_RES` without losing conversion flags. Cleanup carries eight bytes per fixed slot, and the call result follows every argument value. A default or unsupported expression in any position returns unsupported without partial IR. | [Issue #534](https://github.com/frankischilling/holyc-ocaml/issues/534), one added focused case within the nine-case direct-call IR group, three ordered argument trees, mixed integer and floating targets, JIT/AOT modes, exact flags and identities, 24-byte cleanup, deterministic replay, and unsupported middle-argument coverage |
| One provided fixed direct-call argument IR | Implemented for an ordinary direct executable call with exactly one fixed parameter, one provided typed argument, and no variadic binding or actuals. `IC_CALL_START` precedes the canonical checked argument expression; only its final producer gains `ICF_PUSH_RES`, preserving `ICF_RES_TO_F64` or `ICF_RES_TO_INT`. The call result follows all argument values, cleanup carries eight bytes, and the exact classified record selects `IC_ADD_RSP1` or `IC_ADD_RSP`. Unsupported argument expressions expose no partial sequence. The separate multiple-provided-argument slice extends this boundary; defaults, variadics, other access paths, stack placement, optimization, and machine emission remain outside it. | [Issue #532](https://github.com/frankischilling/holyc-ocaml/issues/532), three added focused cases within the eight-case direct-call IR group, JIT/AOT integer and floating targets, expression order and flags, identities, cleanup policy, deterministic replay, and unsupported-expression fallback |
| Zero-parameter direct executable call IR | Implemented for checked function-scope direct calls whose exact declaration record classifies as an ordinary executable target, whose active header has no fixed parameters or variadic binding, and whose typed call has no fixed or variadic actual results. The immutable lowerer emits `IC_CALL_START`, `IC_CALL`, `IC_ADD_RSP` or `IC_ADD_RSP1`, and `IC_CALL_END` with consecutive caller-seeded identities, one result, the canonical target symbol, zero-byte cleanup, exact call span, and the source-selected cleanup predicate. `HCIRL0004` rejects inconsistent semantic ownership, and `HCIRL0005` rejects identity exhaustion. The separate one-provided-argument slice extends this boundary; defaults, multiple or variadic arguments, internal operations, externs, imports, indirect targets, address selection, optimization, and machine emission remain outside it. | [Issue #530](https://github.com/frankischilling/holyc-ocaml/issues/530), five focused cases, JIT/AOT modes, both cleanup opcodes, exact sequence shape, payload, type, span, identity, deterministic replay, unsupported boundaries, and stable failures |
| Function-scope direct-call target classification | A typed direct call joins only to the exact declaration-order function record owned by its semantic resolution. The immutable target retains the typed call, resolved declaration, classified declaration, record, and one total access path: internal operation, ordinary executable call, JIT extern address slot, AOT import, or AOT extern. Replacement headers select their own snapshots instead of matching by name or canonical identity. `HCSEMA0067` rejects foreign or separately resolved evidence at the call origin without mutating symbols. The classifier itself emits no opcode or address; the separate zero-parameter lowerer consumes its ordinary executable path. Internal-opcode decoding, argument lowering, extern and import addresses, and other call IR remain unimplemented. | [Issue #528](https://github.com/frankischilling/holyc-ocaml/issues/528), three focused cases, both compilation modes, all five access paths, exact-identity checks, replacement snapshots, deterministic reclassification, foreign-resolution rejection, provenance, and purity |
| Function-scope direct-call declaration evidence | Every resolved direct call retains the exact source-visible `Function_resolution.resolved_declaration` alongside its active header and canonical JIT or AOT identity. Replacement headers keep distinct declaration records even when the join rule gives them one canonical identity. Typed call results expose that same immutable declaration as the join key for function-record call-access classification and later verified IR lowering. This boundary does not emit a call sequence, choose an executable address, or change argument conversion. | [Issue #526](https://github.com/frankischilling/holyc-ocaml/issues/526), source-order replacement tests in JIT and AOT modes, distinct source-symbol and shared-identity checks, and exact typed-result identity checks |
| Semantic literal payloads | Implemented in the shared function-body and executable top-level expression representation. Integer and character nodes retain target `I64` values, including wrapped and multi-character forms. `F64` nodes retain raw IEEE-754 bits, and string nodes retain decoded bytes without choosing storage, an address, or a terminator. Grouping and prefix nodes keep the same literal leaf. Typed results expose that exact semantic source alongside the existing type, value category, result class, array rank, identity, and generated-source provenance. Direct literal results now feed checked IR with that retained payload, semantic type, and source location. AST adapters reject a literal node whose frontend payload has the wrong family. This boundary does not allocate constants, lower semantic wrappers, or execute an expression. | [Issues #429](https://github.com/frankischilling/holyc-ocaml/issues/429) and [#431](https://github.com/frankischilling/holyc-ocaml/issues/431), eight focused cases across constructor, lowering, function-body, top-level, JIT, AOT, wrapping, raw-bit, decoded-byte, provenance, caller-owned identity, replay, and purity checks |
| Typed semantic literal grouping | Implemented for any parenthesis depth around integer, character, F64, and string leaves. The lowering copies the retained leaf payload and span with the outer checked semantic type, consumes no identity for grouping, and returns `Not_literal` for grouped nonliterals. Function-body and executable top-level results use the same iterative path in JIT and AOT modes. Semantic emitting prefixes, string storage, and execution remain unavailable. | [Issue #433](https://github.com/frankischilling/holyc-ocaml/issues/433), three focused cases inside the IR literal-lowering group, all four payload families, both result owners, both compilation modes, exact type, span, source-kind, identity, deterministic replay, and grouped-nonliteral checks |
| Typed semantic literal unary plus | Implemented for unary plus around integer, character, F64, and string leaves, including mixed parentheses. The lowering copies the retained leaf payload and span with the outer checked semantic type, consumes no identity for unary plus, and returns `Not_literal` for unary plus around a nonliteral. Function-body and executable top-level results use the same iterative path in JIT and AOT modes. Emitting semantic prefixes, string storage, and execution remain unavailable. | [Issue #435](https://github.com/frankischilling/holyc-ocaml/issues/435), three focused cases inside the 53-case IR literal-lowering group, all four payload families, both result owners, both compilation modes, exact type, span, source-kind, identity, deterministic replay, and unary-plus-nonliteral checks |
| Typed semantic literal unary minus | Implemented for unary minus around integer, character, F64, and string leaves, including mixed parentheses and unary plus. The lowering emits the literal instruction first, then one unfused `IC_UNARY_MINUS` per minus prefix from innermost to outermost. It copies the retained leaf payload and span, forwards the outer checked type, keeps caller-owned identities and operator spans, and returns `HCIRL0001` when the identity count does not match. Unary minus around a nonliteral returns `Not_literal`. Function-body and executable top-level results use the same path in JIT and AOT modes. Dereference, address-of, string storage, folding, and execution remain unavailable. | [Issue #438](https://github.com/frankischilling/holyc-ocaml/issues/438), five focused cases inside the 53-case IR literal-lowering group, all four payload families, both result owners, both compilation modes, nested inner-to-outer order, exact type, span, source-kind, identity, deterministic replay, nonliteral, and identity-count checks |
| Typed semantic literal logical not | Implemented for logical not around integer, character, F64, and string leaves, including mixed parentheses, unary plus, and unary minus. The lowering emits the literal instruction first, then one unfused `IC_NOT` or `IC_UNARY_MINUS` per emitting prefix from innermost to outermost. It copies the retained leaf payload and span, forwards the outer checked type, keeps caller-owned identities and operator spans, and returns `HCIRL0001` when the identity count does not match. Logical not around a nonliteral returns `Not_literal`. Function-body and executable top-level results use the same path in JIT and AOT modes. Dereference, address-of, string storage, folding, branch inversion, and execution remain unavailable. | [Issue #440](https://github.com/frankischilling/holyc-ocaml/issues/440), five focused cases inside the 53-case IR literal-lowering group, all four payload families, both result owners, both compilation modes, nested inner-to-outer order, mixed minus-then-not chains, exact type, span, source-kind, identity, deterministic replay, nonliteral, and identity-count checks |
| Typed semantic literal bitwise complement | Implemented for bitwise complement around integer, character, F64, and string leaves, including mixed parentheses, unary plus, unary minus, and logical not. The lowering emits the literal instruction first, then one unfused `IC_COM`, `IC_NOT`, or `IC_UNARY_MINUS` per emitting prefix from innermost to outermost. Each complement uses internal `I64`. It copies the retained leaf payload and span, keeps caller-owned identities and operator spans, and returns `HCIRL0001` when the identity count does not match. Complement around a nonliteral returns `Not_literal`. Function-body and executable top-level results use the same path in JIT and AOT modes. Dereference, address-of, string storage, folding, and execution remain unavailable. | [Issue #442](https://github.com/frankischilling/holyc-ocaml/issues/442), five focused cases inside the 53-case IR literal-lowering group, all four payload families, both result owners, both compilation modes, nested inner-to-outer order, mixed minus/not/complement chains, internal `I64` result, exact span, source-kind, identity, deterministic replay, nonliteral, and identity-count checks |
| Closed aggregate byte layout | Implemented for class and union definitions whose dimensions, explicit offsets, bases, and by-value member sizes are available without general semantic execution. The pass adds no implicit padding: classes advance by exact storage size, named unions overlap at base zero, and anonymous unions begin at the containing aggregate's current end. A base remains at offset zero; primitive fields use the checked type registry; pointers and callbacks use eight bytes; arrays retain every evaluated dimension; and negative `$$` positions contribute the source `neg_offset` adjustment. Literal, character, `$$`, unary, and non-assignment binary expressions use 64-bit target arithmetic, while byte counts and final metadata are overflow-checked. Stable `HCSEMA0001` through `HCSEMA0009` failures expose invalid input, unresolved dependencies, bad dimensions, division faults, non-finite values, conversions, metadata overflow, and unsupported expressions. Identifiers, calls, `sizeof`, `offset`, `defined`, later by-value definitions, cycles, metadata, subinteger access, and runtime allocation remain outside this pass. Duplicate validation and inherited lookup are supplied by the member index below. | [Issue #194](https://github.com/frankischilling/holyc-ocaml/issues/194), [class and layout notes](classes-and-layout.md), focused semantic tests, JIT/AOT comparisons, and deterministic repeat checks |
| Aggregate member index | Implemented for aggregate definitions with completed closed layouts. The index reproduces `MemberFind` by searching the current aggregate before its single base chain and keeps the queried aggregate, declaring aggregate, inheritance depth, stable member identity, resolved type, callback marker, byte layout, anonymous-union path, and provenance. Duplicate validation reproduces `MemberAdd`: ordinary direct and inherited duplicates fail, while exact `pad`, `reserved`, and `_anon_` spellings remain legal. Repeated exempt names resolve to the first direct source occurrence rather than the general symbol table's newest entry. Stable `HCSEMA0010` through `HCSEMA0013` failures cover invalid cross-pass inputs, a missing earlier base, an ordinary duplicate, and an unindexed query. Lookup is pure; AST expression binding, use counts, metadata, subinteger access, and runtime field access remain separate. | [Issue #196](https://github.com/frankischilling/holyc-ocaml/issues/196), [class and layout notes](classes-and-layout.md), focused semantic tests, JIT/AOT comparisons, invalid-input tests, and deterministic repeat checks |
| Aggregate layout inspection | `holyc dump-layout` runs the checked declaration, aggregate, header, direct-member type, closed-layout, and member-index passes before writing any report. Schema `holyc-aggregate-layout-v1` records the exact reference commit, stable aggregate and member IDs, source-order direct members, base facts, byte sizes and offsets, array shape, signedness, resolved type identity, callback markers, and source or generated provenance. Human and JSON results are deterministic across repeated runs and JIT/AOT parsing for mode-independent input. Parser and semantic failures exit with status 1 and leave stdout empty; JSON semantic failures use `holyc-command-error-v1`. The report does not flatten inherited members or claim metadata, subinteger access, allocation, expression binding, or native layout validation. | [Issue #198](https://github.com/frankischilling/holyc-ocaml/issues/198), [class and layout notes](classes-and-layout.md), renderer unit tests, and human, JSON, parser-failure, and semantic-failure CLI goldens |
| Source loading | Implemented for bounded local files and in-memory buffers | Source and span unit tests |
| Raw lexing | Implemented for identifiers, keywords, literals, comments, line continuations, and operators. At the pinned commit, all 528 `.HC`, `.HH`, and `.PRJ` Git blobs tokenize without a diagnostic or crash. The report accounts for 54 NUL terminators and 1,266,852 trailing payload bytes. | Unit, golden, negative, property, full-corpus, and CLI golden tests; [issue #42](https://github.com/frankischilling/holyc-ocaml/issues/42) |
| Operator specification | Implemented for compound recognition, precedence constants, association flags, and binary IC mappings | Generated-table and public API tests |
| Primitive type facts | Implemented for supported names, raw IDs, sizes, signedness, `Bool`, public integer union backing, and `RT_PTR` | Generated-table and semantic API tests |
| Semantic symbols and declaration collection | Each session owns stable symbol and scope IDs; task, module, function, block, aggregate, and assembler-block contexts; newest-first kind-filtered lookup; nth-match traversal through explicit parents; 17 pinned internal-type entries; checked scope and symbol ownership; source provenance; and deterministic human and JSON dumps. Accepted top-level aggregate, global, prototype, and function-definition AST items enter one module scope in lexical order. Every accepted aggregate definition receives a child scope containing its named direct members; nested anonymous-union fields share that scope. Every accepted function prototype and definition receives a child scope containing named fixed parameters at their original signature indexes, variadic `argc` and `argv`, and automatic or static locals from nested statements in parser order. Function-local language gotos bind forward or backward to language and structural assembly-block definitions through stable label IDs. Top-level language occurrences, missing targets, duplicate language definitions, and mixed language/assembly definitions fail before mutation. Later checked passes calculate closed aggregate layouts, build source-compatible direct and inherited member indexes, validate completed function binding namespaces, and bind ordinary function-body identifiers first to visible parameters or locals and then to source-visible compilation-unit publications. Type uses outside aggregate member heads, global declarations, top-level function signatures, and local declarations; default evaluation; initializers; runtime storage; outer-environment, aggregate-member, type, and assembly expression references; expression-kind checking; automatic header-analysis pipeline integration; top-level duplicate rules; task-level linkage; addresses; fixups; loader-record emission; repeated assembly-definition address checks; unused-label warnings; control-flow lowering; and execution are not implemented. | Semantic API and AST-collection tests; [issue #164](https://github.com/frankischilling/holyc-ocaml/issues/164), [issue #166](https://github.com/frankischilling/holyc-ocaml/issues/166), [issue #168](https://github.com/frankischilling/holyc-ocaml/issues/168), [issue #170](https://github.com/frankischilling/holyc-ocaml/issues/170), [issue #172](https://github.com/frankischilling/holyc-ocaml/issues/172), [issue #174](https://github.com/frankischilling/holyc-ocaml/issues/174), [issue #202](https://github.com/frankischilling/holyc-ocaml/issues/202), [issue #204](https://github.com/frankischilling/holyc-ocaml/issues/204), and [issue #206](https://github.com/frankischilling/holyc-ocaml/issues/206) |
| Aggregate forward reconciliation | Implemented for the module-local source-order rule in `PrsClass`. Every `extern class` or `extern union` starts a distinct identity. A definition completes only the newest same-name identity when that identity remains unresolved; otherwise it creates a shadowing identity. Completed identities retain both sites, use the definition symbol associated with direct-member collection, and take their effective class or union kind from the definition. A class/union spelling change is retained without an invented mismatch diagnostic because both source forms use `HTT_CLASS`. Ownership, module scope, ordering, and AST association are checked before a result is returned, and resolution does not mutate the semantic table. Closed layout and member indexing are separate later passes. Reconciliation does not search parent tasks, resolve other named-type uses, warn about unused forwards, store runtime flags, or apply linkage. | [Issue #175](https://github.com/frankischilling/holyc-ocaml/issues/175), aggregate-resolution API tests, JIT and AOT cases, provenance cases, and invalid-input tests |
| Aggregate header types | Implemented for class and union definition headers in one checked module. A backing may resolve to a public primitive spelling, an intrinsic storage spelling, or the newest visible aggregate identity with pointer depth zero through four. Named backing lookup occurs before the current identity is published; base lookup occurs afterward. Forward targets use the canonical identity selected by aggregate reconciliation, including a definition that appears later. Definition, keyword, backing, pointer, colon, and base-name provenance remains available. AST, reconciliation, table ownership, module scope, source order, symbol kind, and pointer provenance are validated without mutation. Later passes calculate closed layouts and inherited member indexes from these headers. This pass does not traverse backing chains, reject every inheritance cycle, resolve ordinary type uses, search parent tasks, or apply linkage. | [Issue #177](https://github.com/frankischilling/holyc-ocaml/issues/177), aggregate-header API tests, JIT and AOT cases, source-order cases, provenance cases, and invalid-input tests |
| Aggregate member type references | Implemented for every collected direct member in one checked module. Public primitive, intrinsic storage, and source-visible aggregate bases remain distinct at pointer depths zero through four. The current canonical aggregate is published before its members; earlier definitions, completed forwards, repeated forwards, and shadowing names retain source-order binding. Ordinary object types and callback return types are separate from callback indirection. Array rank and dimension origins remain present without invented extent values. The pass validates declarations, aggregate reconciliation, headers, member scopes, AST positions, table ownership, and provenance without mutation. Later passes evaluate closed array extents, calculate byte layouts, validate member duplicates, and provide inherited lookup. Recursive parameter types inside member callbacks, metadata evaluation, and runtime storage remain unimplemented. Top-level function and nested parameter callback signatures are covered separately below. | [Issue #179](https://github.com/frankischilling/holyc-ocaml/issues/179), member-type API tests, JIT and AOT cases, source-order cases, provenance cases, purity checks, and invalid-input tests |
| Function signature type references | Implemented for each collected top-level function prototype and definition in one checked module. Return and fixed-parameter types distinguish public primitive spellings, intrinsic storage spellings, and the canonical aggregate identity visible at that declaration, with zero through four ordinary pointer layers. Every fixed slot remains present, including unnamed slots; named top-level parameters map back to their collected symbols. Callback parameters retain one through four indirection layers and recursively resolved signatures. Each fixed slot has a derived `CMemberLst` mask: unnamed slots use `MLF_NO_UNUSED_WARN`, callbacks use `MLF_FUN`, and defaults use `MLF_DFT_AVAILABLE` plus the applicable `MLF_LASTCLASS` or `MLF_STR_DFT_AVAILABLE` bit. Recursive callback slots derive their masks independently. Ordered `reg` and `noreg` requests also survive on every fixed slot reached through the function, member, global, and local type passes. Their checked final value is `-128` when unspecified, `32` for `noreg`, `33` for allocation, or `0` through `15` for an explicit canonical U64 register; the last request wins without losing earlier provenance. Ordinary expression defaults and `lastclass` defaults remain distinct by source provenance without evaluation. A top-level ellipsis maps to source-linked synthetic `argc` and `argv` bindings: both use intrinsic `I64`, carry exactly `MLF_DOT_DOT_DOT`, and receive the ellipsis request list, while `argv` retains an array shape with no source extent and the compiler's placeholder extent 127. String-storage classification covers accepted expression trees; the unavailable `TK_INS_BIN` expression path is not claimed. The pass validates its AST, declaration, aggregate, scope, symbol, source-order, recursive type, and explicit-register associations without mutation. Function identity and record flags are covered separately below; register requests do not split joined identities, matching `MemberLstCmp`. Default evaluation or substitution, call checking, `OPTf_NO_REG_VAR`, register allocation, storage, ABI rules, completed linkage, and other type-use sites remain unimplemented. | [Issue #181](https://github.com/frankischilling/holyc-ocaml/issues/181), [issue #220](https://github.com/frankischilling/holyc-ocaml/issues/220), [issue #234](https://github.com/frankischilling/holyc-ocaml/issues/234), 17 focused function-type tests, member/global/local callback coverage, JIT and AOT comparisons, definition and include provenance cases, purity checks, and invalid-input tests |
| Local declaration type references | Implemented for accepted automatic and static declarations in checked function scopes. Each record retains its public, intrinsic, aggregate, pointer, array, recursive callback, storage, register-request, initializer-shape, delimiter, and provenance facts. The declaration-time `CMemberLst` mask is derived independently for every comma declarator: an automatic object uses `0x0`, an automatic callback uses `MLF_FUN` (`0x8`), a static object uses `MLF_STATIC` (`0x40`), and a static callback uses both (`0x48`). Nested callback parameters keep their own masks. Pinned checks retain static declarations at `Kernel/KMisc.HC:87`, `Adam/WinMgr.HC:38`, and `Adam/Gr/SpriteMesh.HC:65`; no static callback local was found in the pinned corpus, so `0x48` is direct source-path coverage rather than a frequency claim. Included and definition-generated declarations keep their origins, and JIT and AOT runs produce the same semantic records. A separate immutable pass applies the later warning bit and counts. Extent and initializer evaluation, storage allocation, register or offset assignment, static-address lowering, callback cleanup, ABI behavior, and execution remain unimplemented. The frame-bound identifier row records the separate non-static object address fragment. | [Issue #200](https://github.com/frankischilling/holyc-ocaml/issues/200), [issue #222](https://github.com/frankischilling/holyc-ocaml/issues/222), [issue #226](https://github.com/frankischilling/holyc-ocaml/issues/226), ten focused local-type tests, pinned-source checks, provenance cases, JIT/AOT comparisons, purity checks, and invalid-input tests |
| Function binding indexes | Implemented for each collected prototype or definition after function signature and local declaration type resolution. The immutable index keeps named fixed parameters, synthetic `argc` and `argv`, automatic locals, and static locals in source order with stable symbols, binding kinds, ordinals, and source positions. One namespace spans nested statements. Ordinary repeated names fail with `HCSEMA0015`; only `pad`, `reserved`, and `_anon_` may repeat, and lookup selects their first source occurrence. Ownership, function order, parent scope, binding order, type-batch association, and symbol identity are checked without mutation. Expression publication consumes this index through the separate row below. Use counts, storage, register allocation, and ABI behavior remain unimplemented. | [Issue #202](https://github.com/frankischilling/holyc-ocaml/issues/202), eight focused binding-index tests, generated-source provenance, JIT/AOT comparisons, deterministic replay, purity checks, and invalid-input tests |
| Function-local identifier expressions | Implemented for ordinary identifier expression nodes in checked function bodies. Named parameters plus synthetic `argc` and `argv` are visible at body entry. Each local becomes visible after its type, callback, pointer, and array declarator syntax and before its initializer, then remains visible through later nested and sibling statements. Visible function bindings take precedence over compilation-unit lookup. A miss remains an explicit nonlocal candidate for the separate module pass rather than a false binding. The permitted repeated names continue to select their first source binding. Member suffix names, type syntax, specialized query nodes, labels, and assembly tokens are excluded from the ordinary occurrence list. Occurrences retain source and generated provenance in deterministic order. `HCSEMA0017` through `HCSEMA0019` cover invalid cross-pass inputs and publication drift. Resolution is read-only; the separate warning pass counts ordinary and query events. | [Issue #204](https://github.com/frankischilling/holyc-ocaml/issues/204), [issue #228](https://github.com/frankischilling/holyc-ocaml/issues/228), eleven focused expression-binding tests, generated-source provenance, deterministic replay, purity checks, and invalid-input tests |
| Compilation-unit identifier expressions | Implemented for nonlocal candidates and specialized name queries from checked function bodies. One mixed stream publishes aggregate, function, and global records in source order through each current function header. The current function is available for recursion, a later declaration does not leak backward, and the newest visible spelling wins across record kinds. Local bindings retain precedence. Aggregate and function publication sites retain their source symbol and reconciled canonical identity; globals retain their individual record identity and alias edge. Names absent from the current module remain explicit outer candidates. `HCSEMA0020` and `HCSEMA0021` cover invalid cross-pass data and a missing function publication. Resolution is deterministic and read-only. The next row covers lookup through supplied JIT and AOT outer snapshots. Member and type binding, expression-kind validation, use counts, typing, storage, general lowering, and execution remain unimplemented. | [Issues #206](https://github.com/frankischilling/holyc-ocaml/issues/206) and [#472](https://github.com/frankischilling/holyc-ocaml/issues/472), eight focused module-expression tests, ordinary and specialized-query replay, generated-source provenance, JIT/AOT comparisons, deterministic replay, purity checks, and invalid-input tests |
| Top-level executable expressions | Implemented for every expression under an accepted executable module statement. Module publications replay to each statement's item position, and missing names continue through a supplied JIT or AOT outer chain and assembler table. The immutable tree retains each accepted source shape, call hole, and exact binding. The shared expression engine types literals, module values, metadata-bearing outer globals, direct and pointer members, aggregate offsets, audited operators, updates, assignments, and calls. Typed outer results retain their primitive, pointer, aggregate, array, or callback shape, exact top-level occurrence, and selected outer entry. Metadata-free outer records remain explicitly unavailable. Direct functions, scalar module callbacks, fully indexed module callback arrays, direct or pointer aggregate-member callbacks, and scalar or fully indexed metadata-bearing outer callbacks have separate records linked to exact result identities. Outer callback records retain the exact occurrence, table binding, and supplied recursive signature; indexed forms also keep the completed callee and integer-target subscripts. Member callbacks retain the computed callee result, current-or-inherited lookup, global base, and stored recursive signature; parenthesized member callees keep the same lookup. Fixed values, holes, defaults, variadic values, nested calls, and primitive, pointer, or backed aggregate returns remain distinct. Ordinary outer objects, metadata-free records, partial outer or module callback arrays, unindexed callback-member arrays, and unrelated computed callees remain explicit boundaries. `HCSEMA0052` through `HCSEMA0057` reject inconsistent source binding, outer lookup, unresolved names, tree, leaf, result, rank, member-call, or slot input. The passes are deterministic and read-only. Runtime addresses, outer storage, default evaluation, side effects, control flow, IR, execution, and AOT `IET_MAIN` records remain unimplemented. | [Issues #308](https://github.com/frankischilling/holyc-ocaml/issues/308) through [#344](https://github.com/frankischilling/holyc-ocaml/issues/344), five module-prefix tests, four outer-binding tests, three semantic-tree tests, four identifier-classification tests, twenty-nine expression-result tests, JIT/AOT comparisons, source-shape coverage, provenance, deterministic replay, purity, stale-batch and mode rejection, and negative diagnostics |
| Outer-table identifier expressions | Implemented for explicit candidates left by compilation-unit binding. A validated JIT snapshot searches the current task, its parent tasks in depth order, and one final assembler table. A validated AOT snapshot searches zero or more enclosing compilations in depth order and the assembler table; top-level AOT accepts only the assembler table. Each table selects its newest matching aggregate, function, global, or export-system record before lookup advances. Local and compilation-unit bindings retain precedence, and successful outer results retain stable symbol, record, table, and occurrence provenance. `HCSEMA0022` covers malformed chains, `HCSEMA0023` covers mismatched semantic inputs, and source-positioned `HCSEMA0024` rejects an ordinary name absent from the complete chain instead of guessing an import. Resolution is deterministic and read-only. Live task and compiler table construction, addresses, alternate binding targets, imports, fixups, loader records, member and type binding, expression-kind validation, use counts, typing, storage, lowering, and execution remain unimplemented. | [Issue #208](https://github.com/frankischilling/holyc-ocaml/issues/208), seven focused outer-expression tests, generated-source failure provenance, JIT and AOT chain-order cases, deterministic replay, purity checks, and invalid-input tests |
| Global initializer identifier expressions | Implemented for ordinary identifiers in scalar and recursively braced global initializers. The owning `CHashGlblVar` publication is visible before its initializer, so a declarator can refer to itself or an earlier member of its comma group. Later declarators and module items remain unavailable. A missing module spelling continues through the supplied JIT or AOT outer snapshot, and every result retains the owning record, source publication, occurrence order, nested initializer path, and generated-source provenance. `HCSEMA0025` rejects inconsistent cross-pass input; `HCSEMA0026` rejects a name absent from both visible module records and the complete outer chain. Resolution is deterministic and read-only. Member suffix names, initializer typing, evaluation, conversion, storage, relocations, lowering, and execution remain unimplemented. | [Issue #210](https://github.com/frankischilling/holyc-ocaml/issues/210), five focused global-initializer tests, JIT and AOT cases, source-order cases, generated-source failure provenance, deterministic replay, purity checks, and invalid-input tests |
| Global array-extent identifier expressions | Implemented for ordinary identifiers in every array suffix on object, function-pointer, and aggregate-attached globals. `PrsType` reads dimensions before `PrsGlblVarLst` publishes the owning `CHashGlblVar`, so an extent sees earlier module records but not its current owner or a later declaration. A missing spelling continues through the supplied JIT or AOT outer snapshot. Empty first dimensions remain explicit without false occurrences, and a multidimensional declarator uses one stable occurrence order while retaining each dimension index and full generated-source provenance. `HCSEMA0027` rejects inconsistent cross-pass input; `HCSEMA0028` rejects a name absent from both visible module records and the complete outer chain. Resolution is deterministic and read-only. Extent evaluation, 64-bit constant conversion, type checking, allocation, layout changes, lowering, and execution remain unimplemented. | [Issue #212](https://github.com/frankischilling/holyc-ocaml/issues/212), five focused global-dimension tests, JIT and AOT cases, declarator-shape and source-order cases, generated-source failure provenance, deterministic replay, purity checks, and invalid-input tests |
| Function identities and record flags | Implemented for named top-level prototypes and definitions in one checked module. JIT reuses only the newest same-name record while `Cf_EXTERN` remains set; AOT reuses the newest record unless `HTF_IMPORT` makes it a barrier. Each declaration retains its source kind, effective kind, and compiler-option snapshot. `OPTf_EXTERNS_TO_IMPORTS` converts both extern spellings before selection, so converted records use the import barrier, exact local or alternate loader name, import flags, and the normal AOT-only guard. Classification then replays new-record staging, joined-header accumulation, public replacement, private accumulation, varargs, `RET1`, `_intern`, `_extern`, import, and definition transitions. It retains separate shared, stored, hash, and function-type masks plus the selected call, hash-value, runtime-lookup, map, AOT resolution, and publication policy. The low-level API accepts one compiler-option snapshot per declaration; the driver accepts one batch mask until source-positioned option execution is available. Header comparison is covered by the next row. Default evaluation, parent-task lookup, alternate address resolution, fixups, record emission, call lowering, and ABI behavior remain unimplemented. | [Issue #183](https://github.com/frankischilling/holyc-ocaml/issues/183), [issue #191](https://github.com/frankischilling/holyc-ocaml/issues/191), [issue #230](https://github.com/frankischilling/holyc-ocaml/issues/230), function identity and record-classification tests, five focused option-rewrite tests, source-contract tests, purity checks, and JIT/AOT cases |
| Joined function header comparison | Implemented as an immutable semantic analysis for resolved replacement pairs with supplied compile-time default payloads. Return types compare by exact public, intrinsic, aggregate, and pointer identity. Argument records reproduce `MemberLstCmp`: `_anon_` names, callback normalization to internal `I64i` pointer classes, exact 64-bit values, `StrCmp`-style string bytes, zero-valued `lastclass`, the replaced header's fixed argument-count limit, and synthetic vararg list edges. Register requests, calling flags, callback nested signatures, and fields omitted by the source comparison do not create warnings. The current declaration's `OPTf_WARN_HEADER_MISMATCH` snapshot controls the check. `HCSEMA0037` and `HCSEMA0038` report return and argument mismatches independently; `HCSEMA0036` rejects missing evaluated values or inconsistent inputs. The result retains both declaration sites and generated or included source provenance without mutating symbols. Arbitrary default evaluation, automatic pipeline integration, call substitution, and source-positioned option execution remain unimplemented. | [Issue #236](https://github.com/frankischilling/holyc-ocaml/issues/236), seven focused semantic tests, source-contract edge cases, per-site option snapshots, provenance cases, deterministic replay, purity checks, and invalid-input tests |
| Direct and typed indirect function-call slot resolution | Implemented for direct function-body and top-level calls, plus calls through typed function-pointer parameters, automatic or static locals, module globals, aggregate members, and metadata-bearing outer globals. Function bodies accept fully indexed callback arrays stored in automatic or static locals, module globals, and checked outer globals. Top-level module forms include scalar callbacks, fully indexed callback arrays, and direct or pointer aggregate-member callbacks; checked outer globals support scalar callbacks and fully indexed callback arrays. A direct call retains the source-visible header and canonical JIT or AOT identity. An indirect call retains its selected value or exact member or outer binding, source callee form, recursive callback signature, and distinct result identity. Indexed function-body and top-level outer calls also retain the typed base and each integer-target subscript result. Top-level member calls retain the global base and computed callee result; grouping preserves the selected lookup. Outer calls retain the exact occurrence and selected JIT task or AOT parent-table binding. All paths distinguish provided expressions, explicit holes, absent trailing slots, non-trailing defaults, and provided variadic values. Member selection requires `MLF_FUN` metadata and a completed aggregate before the caller. A callback array must use exactly its declared rank; partial or excessive indexing produces `HCSEMA0039` for function-body module values, `HCSEMA0046` for function-body outer values, or `HCSEMA0057` at executable top level. Unindexed callback-member arrays, unrelated computed callees, aggregate spellings, ordinary outer objects, metadata-free outer records, and function-parameter array declarators remain explicit boundaries. Other function-body slot failures use `HCSEMA0040` through `HCSEMA0042`; top-level member, slot, rank, and outer-shape failures use `HCSEMA0057` or stay unavailable. The passes preserve included and generated origins without mutating symbols. Runtime callback address calculation, default and expression evaluation, callback storage reads, lowering, execution, and ABI emission remain unimplemented. | [Issues #238](https://github.com/frankischilling/holyc-ocaml/issues/238), [#280](https://github.com/frankischilling/holyc-ocaml/issues/280), [#290](https://github.com/frankischilling/holyc-ocaml/issues/290), [#322](https://github.com/frankischilling/holyc-ocaml/issues/322), [#324](https://github.com/frankischilling/holyc-ocaml/issues/324), [#326](https://github.com/frankischilling/holyc-ocaml/issues/326), [#328](https://github.com/frankischilling/holyc-ocaml/issues/328), [#336](https://github.com/frankischilling/holyc-ocaml/issues/336), [#340](https://github.com/frankischilling/holyc-ocaml/issues/340), and [#344](https://github.com/frankischilling/holyc-ocaml/issues/344); sixteen focused call-resolution tests; 77 focused function expression-result tests; twenty-nine focused top-level expression-result tests; the pinned parenthesis-free oracle; exact `QSort.HC`, callback-array, and member-call source evidence; JIT/AOT cases; provenance, replay, purity, ownership, and negative checks |
| Call conversion policy | Implemented for provided fixed arguments after direct or typed indirect slot resolution. The pass follows aggregate backing chains like `OptClassFwd`, stops at pointers and callbacks, and classifies the source-visible target as the `F64` result path or the integer result path. Only headers published before the caller participate. Public and intrinsic `F64` scalars use the floating path; other primitives, pointers, callbacks, ordinary aggregates, unresolved forwards, and pointer-backed aggregates use the integer path. A declared default and each provided variadic expression remain distinct. Replacement direct headers can change the target path without changing the joined function identity; typed indirect calls use the retained callback parameter records. `HCSEMA0043` rejects inconsistent batches, and `HCSEMA0044` rejects aggregate backing cycles. The pass does not infer the actual expression class, evaluate a value, read a callback pointer, lower a call, or emit ABI code. | [Issue #240](https://github.com/frankischilling/holyc-ocaml/issues/240), [issue #280](https://github.com/frankischilling/holyc-ocaml/issues/280), seven focused semantic tests, every retained target category, source-position and JIT/AOT replacement cases, provenance checks, deterministic replay, purity checks, and stable cycle and input diagnostics |
| Typed call expression results | Implemented for every provided fixed and variadic argument on an already resolved direct or typed indirect function-body call. The immutable expression result records a deterministic traversal ID, exact source expression and origin, known semantic type, value category, remaining array rank, intrinsic conversion requirement, forwarded integer, `F64`, or unresolved class, and a distinct execution class where compound assignment needs one. A nested resolved call retains its direct or indirect resolution record and receives the return type from the source-visible function header or callback signature. This includes callbacks selected through direct, pointer, parenthesized, and inherited member access, plus fully indexed automatic, static, module-global, and checked outer callback arrays. An indexed callee retains its typed base and every integer-target subscript result. Pointer returns are address values; zero-depth returns are object values; by-value aggregate returns follow the same source-visible backing rules as other values. Prefix dereference, indexing, member access, updates, and assignments retain their previously documented checked results. A selected declared default has a separate immutable result with the exact parameter and default-use record, checked parameter type, forwarded class, ordinary-expression or `lastclass` kind, and immediate or AOT string-constant path. It has no expression ID. A selected `lastclass` default also retains the preceding provided result and its source-compatible base spelling; ordinary defaults do not replace that state, and the absence of a previous typed expression remains explicit. Variadic results remain in source order and expose their actual class without a target conversion. Known invalid bases, callback-array ranks, premature aggregate use, missing members, invalid update operands, invalid assignment destinations, and inconsistent nested-call ownership report stable semantic diagnostics; an unavailable nonassignment outer type stays unresolved. Update effects, stores, pointer scaling, unrelated computed callback-call checking, callback storage reads, default evaluation and payload copying, AOT string allocation and fixups, variadic promotion, and ABI placement remain unimplemented. | [Issue #264](https://github.com/frankischilling/holyc-ocaml/issues/264), [issue #266](https://github.com/frankischilling/holyc-ocaml/issues/266), [issue #268](https://github.com/frankischilling/holyc-ocaml/issues/268), [issue #270](https://github.com/frankischilling/holyc-ocaml/issues/270), [issue #272](https://github.com/frankischilling/holyc-ocaml/issues/272), [issue #274](https://github.com/frankischilling/holyc-ocaml/issues/274), [issue #276](https://github.com/frankischilling/holyc-ocaml/issues/276), [issue #278](https://github.com/frankischilling/holyc-ocaml/issues/278), [issue #280](https://github.com/frankischilling/holyc-ocaml/issues/280), [issue #282](https://github.com/frankischilling/holyc-ocaml/issues/282), [issue #284](https://github.com/frankischilling/holyc-ocaml/issues/284), [issue #290](https://github.com/frankischilling/holyc-ocaml/issues/290), and [issue #340](https://github.com/frankischilling/holyc-ocaml/issues/340), 77 focused expression-result tests, all 34 conversion-decision tests, direct and indirect nested-return values, selected defaults in both modes and call forms, primitive, pointer, and aggregate member returns, one through four pointer layers, local, global, and outer callback arrays, inherited members, callbacks, source-ordered function and aggregate headers, lvalue contexts, typed variadic order, unavailable values, provenance, deterministic replay, purity, and ownership cases |
| Function return expression results | Implemented for every return statement in a checked function definition. Each source-ordered result keeps the return keyword and statement origins, optional immutable expression result, exact declared type, forwarded integer or `F64` class, and `ICF_RES_TO_F64` or `ICF_RES_TO_INT` intent from the pinned optimizer rule. A missing value from a nonzero-sized result and a value from a zero-sized result remain warning facts because the source compiler accepts both forms with warnings. Nested direct, named callback, and member callback calls keep their exact call identity. Accepted numeric values now feed the separate checked return fragment; calls, identifiers, and other unsupported values do not expose partial IR. This semantic pass does not create `IC_RETURN_VAL`, `IC_RETURN_VAL2`, `IC_JMP`, `IC_LEAVE`, try unwinding, register placement, an epilogue, or executable behavior. | [Issue #292](https://github.com/frankischilling/holyc-ocaml/issues/292), four return-focused cases inside the 77-case expression-result group, I64, U64, F64, pointer and aggregate returns, value-presence mismatches, JIT and AOT modes, include provenance, deterministic replay, purity, and stable ownership checks |
| Function control-flow condition results | Implemented for `if`, `while`, `do while`, and `for` conditions in checked function definitions. Each result keeps a contiguous source identity, statement role, keyword and statement origins, immutable expression result, integer or `F64` class, and nested call identity. The pinned statement parser sends these results directly to `IC_BR_ZERO` or `IC_BR_NOT_ZERO`; this pass does not insert `IC_TO_BOOL`. Constant folding, control-flow edges, labels, and branch emission remain unimplemented. | [Issue #294](https://github.com/frankischilling/holyc-ocaml/issues/294), two focused cases inside the 77-case expression-result group, all four statement roles, integer, F64, pointer, member, and callback conditions, JIT and AOT modes, include and definition provenance, deterministic replay, source order, purity, and conversion checks |
| Executable top-level control-flow condition results | Implemented for `if`, `while`, `do while`, and `for` conditions in executable module statements. Each record reuses the exact typed root and keeps its containing top-level statement, contiguous identity, role, keyword origin, result class, and nested call identity. `if`, `while`, and `for` select a zero branch; `do while` selects a nonzero back edge. `HCSEMA0063` rejects foreign and discontinuous input. Boolean conversion, evaluation, blocks, labels, branch IR, `IET_MAIN`, and execution remain unimplemented. | [Issue #350](https://github.com/frankischilling/holyc-ocaml/issues/350), three focused cases, JIT and AOT comparisons, all four roles, integer, F64, pointer, member, index, direct-call, and callback-call values, include and definition provenance, deterministic replay, purity, exact identity checks, duplicate indexes, and foreign-session rejection |
| Executable top-level switch selector results | Implemented for bounded `switch(expr)` and no-bound `switch[expr]` selectors in executable module statements. Each record reuses the exact typed root and keeps its containing top-level statement, contiguous identity, explicit mode, switch keyword origin, result class, and nested call identity. `HCSEMA0064` rejects foreign and discontinuous input. Selector and case evaluation, range-origin subtraction, labels, jump tables, switch IR, `IET_MAIN`, and execution remain unimplemented. | [Issue #352](https://github.com/frankischilling/holyc-ocaml/issues/352), three focused cases, JIT and AOT comparisons, both modes, integer, F64, pointer, member, index, direct-call, and callback-call values, nested source order, include and definition provenance, deterministic replay, purity, exact identity checks, duplicate indexes, and foreign-session rejection |
| Executable top-level switch case results | Implemented for implicit, single-value, and ranged `case` labels in executable module statements. Each source-ordered record keeps a contiguous identity, containing statement, keyword and full-label origins, and one explicit pattern. Ranged endpoints and ellipsis provenance stay together. Every explicit value reuses its exact typed expression root; only `F64` carries `ICF_RES_TO_INT`, matching `LexExpressionI64`. `HCSEMA0065` rejects foreign ownership, discontinuous identities, and missing or excess roots. Evaluation, implicit numbering, reversed-range normalization, expansion, duplicate checks, labels, jump tables, switch IR, `IET_MAIN`, and execution remain unimplemented. | [Issue #354](https://github.com/frankischilling/holyc-ocaml/issues/354), three focused cases, JIT and AOT comparisons, implicit, single, and ranged patterns, integer, F64, pointer, member, direct-call, and callback-call values, nested source order, include and definition provenance, deterministic replay, purity, exact identity checks, discontinuous metadata, missing and unexpected roots, and foreign-session rejection |
| Executable top-level return validation | Implemented for every explicit `return` nested under an executable module statement. The validator stops at function definitions, so ordinary function returns retain their checked path. `HCSEMA0066` reports the exact `return` keyword and preserves include and definition provenance. TempleOS's separate implicit return of the final ordinary top-level expression remains unimplemented. | [Issue #356](https://github.com/frankischilling/holyc-ocaml/issues/356), three focused cases, JIT and AOT comparisons, direct and recursively nested returns, function-return separation, include and definition provenance, deterministic replay, symbol-table purity, and ordinary final-expression coverage |
| Function switch selector results | Implemented for bounded `switch(expr)` and no-bound `switch[expr]` selectors in checked function definitions. Each source-ordered selector keeps a contiguous identity, explicit mode, keyword and statement origins, immutable expression result, source-visible type, integer or `F64` class, and nested direct or callback call identity. The pass does not subtract the lowest case value, assign case values, validate duplicates, create labels, build a jump table, or emit `IC_SWITCH` or `IC_NOBOUND_SWITCH`. | [Issue #296](https://github.com/frankischilling/holyc-ocaml/issues/296), two focused cases inside the 77-case expression-result group, bounded and no-bound syntax, integer, F64, pointer, member, index, direct-call, and callback-call selectors, JIT and AOT modes, include and definition provenance, deterministic replay, source order, purity, constructor validation, and conversion checks |
| Function switch case results | Implemented for implicit, single-value, and ranged `case` labels in checked function definitions. Each source-ordered case keeps a contiguous identity, keyword and label origins, and one immutable pattern. Ranged endpoints remain paired. Every explicit value receives the shared checked expression result and nested call identity; only `F64` carries `ICF_RES_TO_INT`, matching `LexExpressionI64`. The pass does not evaluate values, assign implicit cases, swap reversed endpoints, calculate the switch range, reject duplicates, create labels, build a jump table, or emit switch IR. | [Issue #298](https://github.com/frankischilling/holyc-ocaml/issues/298), two focused cases inside the 77-case expression-result group, implicit, single, and ranged patterns, integer, F64, pointer, member, direct-call, and callback-call values, JIT and AOT modes, include and definition provenance, deterministic replay, source order, purity, constructor validation, and conversion checks |
| Literal direct-call conversion decisions | Implemented for literal actuals on provided fixed slots. Every provided fixed or variadic argument keeps a semantic source view with its literal or unresolved top-level kind and provenance. Integer, character, and string literals use the integer result path; float literals use the `F64` path; ordinary parentheses preserve the class. Combining a fixed actual class with the forwarded target records no conversion, `ICF_RES_TO_F64` intent, or `ICF_RES_TO_INT` intent exactly as the pinned branch does. A variadic literal retains the same typed actual class but has no fixed target or conversion field. Defaults remain outside expression typing. Bound identifier categories and audited prefix, postfix-update, binary, and resolved nested direct-call results have the separate classifications recorded below. Outer identifiers and untyped indirect-call shapes stay explicitly unresolved. `HCSEMA0045` rejects typed results from another session or policy traversal. The pass records intent but does not mutate the AST, set an IC flag, evaluate a value, lower a call, or emit ABI code. | [Issue #242](https://github.com/frankischilling/holyc-ocaml/issues/242), [issue #276](https://github.com/frankischilling/holyc-ocaml/issues/276), [issue #278](https://github.com/frankischilling/holyc-ocaml/issues/278), six focused literal tests plus typed variadic and nested-call coverage, all four literal families, parenthesis cases, target categories, JIT/AOT replacement headers, unresolved shapes, provenance checks, deterministic replay, purity checks, and stable input diagnostics |
| Bound-identifier direct-call conversion decisions | Implemented for named fixed parameters, synthetic `argc` and `argv`, automatic and static locals, and source-visible globals. Each supported argument retains its exact expression occurrence, stable binding identity, checked type, and object, array, or callback shape. Scalar public and intrinsic `F64` values use the floating path; other primitives use the integer path; by-value aggregates follow backing headers visible before the caller. Arrays, callbacks, and `argv` use the pointer-side integer path. Outer candidates and unsupported expression bindings remain unresolved. Foreign type batches, mismatched function owners, and occurrences from another traversal are rejected. The pass does not access storage, evaluate a value, validate an lvalue, lower a call, or emit ABI code. | [Issue #259](https://github.com/frankischilling/holyc-ocaml/issues/259), focused JIT and AOT tests for every supported binding category and value shape, generated provenance, outer candidates, deterministic replay, and foreign-input rejection |
| Integer-primary direct-call conversion decisions | Implemented for `$$`, `sizeof`, `offset`, and `defined` actuals on provided fixed slots. The pinned parser gives each form a non-`F64` result before the call comparison, so the semantic decision uses the integer side of that split. A scalar `F64` target records `ICF_RES_TO_F64` intent; integer-side targets record no conversion. Parentheses, JIT and AOT modes, included files, and generated definitions preserve the same decision and source origin. The pass does not evaluate a size, offset, definition query, or current position, and it does not type other expression families or emit an IC flag. | [Issue #244](https://github.com/frankischilling/holyc-ocaml/issues/244), four focused semantic tests, all four primary families, both target paths, nested parentheses, JIT/AOT cases, included and generated provenance, deterministic replay, and purity checks |
| Ordinary `defined` semantic operands and source-visible lowering | Implemented for ordinary language expressions in checked function bodies and executable top-level trees. The shared source record distinguishes a name-shaped operand from another token, retains the exact spelling and operand origin, and keeps the complete expression origin through grouping, unary and binary operators, postfix casts, call arguments, statements, conditions, and returns. Function-body lookup checks parameters, synthetic `argc` and `argv`, published locals, and the exact aggregate, function, or global publication visible at the containing function header. Executable top-level lookup starts with the module publications visible before its statement. A remaining candidate continues through caller-supplied JIT task-parent or AOT enclosing-compilation tables and the final assembler table. A supported record produces one; a non-name operand or complete-chain miss produces zero. An ordinary missing top-level identifier still reports `HCSEMA0054`. Without the required outer snapshot, a remaining function candidate stays deferred. Known results lower to zero-operand `IC_IMM_I64` with an internal `I64` result, exact span, deterministic identity, and `ICF_RES_TO_F64` when mixed arithmetic requires it. JIT and AOT paths use the same record, and generated definitions retain invocation and definition provenance. Validation rejects mismatched operand shape, query role, spelling, origin, statement or function identity, a missing statement query, a publication outside the resolved module batch, or an outer query outside the exact supplied batch. Unrepresented hash-record families, interpretation, and backend execution are not implemented. | [Issues #468](https://github.com/frankischilling/holyc-ocaml/issues/468), [#470](https://github.com/frankischilling/holyc-ocaml/issues/470), [#472](https://github.com/frankischilling/holyc-ocaml/issues/472), [#474](https://github.com/frankischilling/holyc-ocaml/issues/474), and [#476](https://github.com/frankischilling/holyc-ocaml/issues/476), semantic retention, local, module, outer-chain, and executable top-level selection, forged-query rejection, publication and query ownership, expression- and statement-lowering cases, both result owners, both modes, exact payloads and spans, composition, deterministic dumps, generated provenance, and symbol-table purity |
| Ordinary current-position expression results | Implemented for `$$` in checked function bodies and executable top-level statements outside aggregate-offset and assembler-expression modes. `Compiler/PrsExp.HC` assigns ordinary `$$` the internal `RT_PTR` class; the semantic result retains that zero-depth `I64` storage form, address value category, integer result class, and exact source identity. The checked expression lowerer emits zero-operand `IC_RIP` with one result, the exact checked type and span, no payload, and consecutive module-owned identities. Grouping, integer operations, primitive postfix casts, and expression-statement termination compose through the same JIT and AOT path. Mixed `F64` arithmetic places `ICF_RES_TO_F64` on the `IC_RIP` producer. Assignment and update uses fail the existing lvalue rule with `HCSEMA0046`. Numeric RIP selection, AOT adjustment, class-offset evaluation, assembler fixups, relocation, interpretation, and code generation are not implemented. | [Issues #342](https://github.com/frankischilling/holyc-ocaml/issues/342) and [#466](https://github.com/frankischilling/holyc-ocaml/issues/466), semantic owner and lvalue cases plus three expression-lowering cases and one expression-statement case, JIT/AOT comparisons, exact instruction shape, composition, deterministic dumps, and identity checks |
| Primitive postfix-cast direct-call decisions | Implemented for public primitive and intrinsic-storage postfix casts on provided fixed slots. The semantic source view retains the operand, target spelling, public or storage form, pointer depth, and each source origin. A depth-zero `F64` or `F64i` target uses the floating result path. Other primitive targets and every pointer target use the non-`F64` path. The outermost cast decides the class even when its operand remains otherwise unresolved. Parentheses, JIT and AOT modes, included files, and generated definitions preserve the result. The pass does not evaluate a cast or operand, change the AST, or emit an IC flag. | [Issue #246](https://github.com/frankischilling/holyc-ocaml/issues/246), four focused semantic tests, both type forms, scalar and pointer targets, all three conversion outcomes, outer-cast precedence, JIT/AOT cases, included and generated provenance, deterministic replay, purity checks, and constructor validation |
| Named aggregate postfix-cast direct-call decisions | Implemented for source-visible class and union postfix casts on provided fixed slots. The semantic source view retains the canonical aggregate identity, source spelling, pointer depth, operand view, and every source origin. A by-value target follows backing headers already published before the caller. A chain ending in `F64` or `F64i` uses the floating result path; unbacked aggregates, pointer-backed aggregates, and every aggregate pointer use the non-`F64` path. Later forward completion and same-name shadowing do not leak backward. The outermost cast decides the class, and parentheses preserve it. `HCSEMA0039` rejects a stale same-name identity, an identity from the wrong module, or one from another session without mutating the symbol table. The pass does not evaluate a cast or operand, change the AST, or emit an IC flag. | [Issue #248](https://github.com/frankischilling/holyc-ocaml/issues/248), three focused decision tests and one resolution-validation test, class and union targets, backing chains and pointer stops, all three conversion outcomes, source-order cases, outer-cast precedence, JIT/AOT cases, included and generated provenance, deterministic replay, purity checks, and stable input diagnostics |
| Checked postfix-cast operand edges | Implemented for primitive, intrinsic, pointer, and resolved aggregate postfix casts that reach checked function-body or executable top-level results. Each cast exposes the exact checked operand produced by the same traversal. Nested casts point to their immediate cast child; a grouped or binary operand remains a separate checked node. Target type, result class, source provenance, and deterministic expression identity are unchanged. This boundary does not emit `IC_HOLYC_TYPECAST`, choose its `was_paren` payload, convert a value, fold an immediate, optimize, or execute the cast. | [Issue #460](https://github.com/frankischilling/holyc-ocaml/issues/460), assertions in the function conversion-identity and top-level cast cases covering JIT and AOT ownership, nested and grouped operands, exact identity, types, classes, provenance, and deterministic replay |
| Prefix-expression direct-call decisions | Implemented for audited prefix actuals on provided fixed slots. The semantic source view retains the exact operator, operator origin, recursive operand, and nested cast identity. Unary plus, unary minus, logical not, pre-increment, and pre-decrement keep the operand class through the checked source paths. Address-of uses the integer result path. Bitwise complement uses the internal `I64` result assigned by `OptPass012`, independently of its operand class. JIT and AOT runs preserve source-order aggregate backing, includes retain file provenance, and generated definitions retain both invocation and definition origins. Dereference remains an explicit unresolved actual. The pass does not validate lvalues, apply an update, evaluate an operand, change the AST, or emit an IC flag. | [Issue #250](https://github.com/frankischilling/holyc-ocaml/issues/250), [issue #257](https://github.com/frankischilling/holyc-ocaml/issues/257), [issue #261](https://github.com/frankischilling/holyc-ocaml/issues/261), five focused semantic tests, all eight prefix operators, nested casts and prefixes, both conversion directions, no-conversion and unresolved cases, source-order type visibility, JIT/AOT cases, included and generated provenance, deterministic replay, purity checks, and constructor validation |
| Binary-expression direct-call decisions | Implemented for all 31 checked binary operators on provided fixed slots. Each semantic source view retains the generated IC identity, operator origin, and two recursive operands. Power produces `F64`. Logical and comparison operators produce an integer result. Arithmetic, shifts, and bitwise operators produce `F64` when either audited operand is `F64`, produce an integer result when both use the integer path, and remain unresolved when neither result is forced and an operand class is unavailable. Assignment and compound-assignment families remain unresolved until lvalue typing is implemented. Source-order aggregate casts, JIT and AOT modes, includes, definitions, and nested prefixes preserve their facts and provenance. The pass does not evaluate operands, validate their legality, apply side effects, change the AST, or emit an IC flag. | [Issue #252](https://github.com/frankischilling/holyc-ocaml/issues/252), four focused semantic tests, the complete generated operator table, mixed operand classes, nested expressions, source-order backing, both modes, provenance, deterministic replay, purity, and constructor validation |
| Checked binary operand edges | Implemented for accepted binary expressions in function bodies and executable top-level statements. Every completed result exposes the exact checked left and right children in source order. Nested binary expressions keep their immediate edges. Simple and compound assignments retain the lvalue result on the left and the converted result from the same analysis pass on the right. Nonbinary results expose no pair. This boundary does not emit a binary instruction, choose pointer scaling or short-circuit control flow, evaluate a value, or apply a store. | [Issue #448](https://github.com/frankischilling/holyc-ocaml/issues/448), three focused tests covering arithmetic, comparisons, logical operators, assignments, nesting, identity, JIT and AOT function bodies, and executable top-level statements |
| Postfix-update direct-call decisions | Implemented for post-increment and post-decrement on provided fixed slots. Each semantic source view retains the operator, operator origin, and recursive operand. `PrsExp.HC` constructs `IC__PP` or `IC__MM`, and `OptPass012.HC` keeps the operand class for both forms. The conversion decision therefore forwards an audited integer or `F64` operand class and remains unresolved when that class is unavailable. JIT and AOT modes, includes, generated definitions, nested casts, prefixes, binary expressions, and parentheses preserve their facts and provenance. The pass does not validate a modifiable lvalue, apply an update, produce the old value, change the AST, or emit IC. | [Issue #255](https://github.com/frankischilling/holyc-ocaml/issues/255), three focused semantic tests, both operators, recursive operand classes, both conversion directions, JIT/AOT cases, included and generated provenance, deterministic replay, and constructor validation |
| Global types, records, and flags | Implemented for ordinary, grouped, bound, and aggregate-attached globals in one checked module. Types retain primitive, intrinsic, or canonical aggregate identity, pointers, recursive function-pointer signatures, arrays, initializer shape, and provenance. Every declarator remains a separate record. Each declaration retains its source kind, effective kind, compiler-option snapshot, and derived storage policy. `OPTf_EXTERNS_TO_IMPORTS` converts ordinary and alternate externs before mode validation, with exact loader names and import-facing record policy. `OPTf_GLBLS_ON_DATA_HEAP` selects data-heap policy for ordinary and `_intern` definitions while binding-specific extern and import records remain on the code-heap path. JIT data-heap initializers remain represented; AOT data-heap initializers and a preceding same-name global are rejected before reconciliation returns. AOT code-heap definitions may still redirect a converted import. Classification retains `GVF_DATA_HEAP`, hash and value masks, access and ownership policy, map eligibility, and the source omission of an AOT export for a data-heap definition. Address evaluation, allocation, initializer and extent evaluation, alternate target resolution, parent-task lookup, actual fixups, map writing, and loader-record emission remain unimplemented. | [Issue #185](https://github.com/frankischilling/holyc-ocaml/issues/185), [issue #187](https://github.com/frankischilling/holyc-ocaml/issues/187), [issue #189](https://github.com/frankischilling/holyc-ocaml/issues/189), [issue #230](https://github.com/frankischilling/holyc-ocaml/issues/230), [issue #232](https://github.com/frankischilling/holyc-ocaml/issues/232), focused type, reconciliation, flag, option-policy, source-contract, purity, and JIT/AOT tests |
| Top-level function-default identifier expressions | Implemented for ordinary identifier expressions in fixed-parameter defaults on named top-level prototypes and definitions. `PrsFunJoin` has already published the current header, so a recursive default selects that module record. The function member list is not yet the active local environment, so a parameter spelling falls through to the visible module prefix and then the supplied JIT or AOT outer snapshot. Joined headers retain distinct source symbols and share the canonical identity selected by function reconciliation. `lastclass` remains a separate default kind and creates no ordinary occurrence. Member suffixes, type syntax, `sizeof`, `offset`, and `defined` are excluded. `HCSEMA0029` rejects inconsistent cross-pass input, and `HCSEMA0030` reports a name absent from both lookup stages. Resolution is deterministic and read-only. Header comparison consumes this pass's retained facts through the separate evaluated-input analysis. Default evaluation, conversion, automatic string storage, substitution, call checking, and defaults nested inside callback, member, global, or local declarators remain unimplemented. | [Issue #214](https://github.com/frankischilling/holyc-ocaml/issues/214), five focused function-default tests, JIT and AOT cases, joined-identity and source-order cases, `lastclass`, generated-source failure provenance, deterministic replay, purity checks, and invalid-input tests |
| Compiler option specification | Implemented for all 12 bit indices, initial state, gaps, core consumers, and the `Option`/`GetOption` contract. `OPTf_EXTERNS_TO_IMPORTS` affects function and global semantic resolution, `OPTf_GLBLS_ON_DATA_HEAP` selects global storage policy and enforces the AOT initializer restriction, record classification consumes `OPTf_KEEP_PRIVATE`, local warning analysis consumes `OPTf_WARN_UNUSED_VAR`, and function header analysis consumes `OPTf_WARN_HEADER_MISMATCH`. Executing source-positioned option changes is not implemented. | Generated-table, semantic API, record-classification, focused option-policy tests, and per-declaration header snapshots |
| Function-flag specification | Implemented for the function hash type, inherited and function-only bits, parser staging masks, modifier transitions, `RET1`, cleanup selection, record mutations, and pinned consumers. Prototype syntax retains ordered `interrupt`, `haserrcode`, `argpop`, and `noargpop` nodes plus a terminal variadic marker. The semantic record pass applies the source-backed stored and hash flags to reconciled identities; ABI validation and machine emission remain unavailable. | Generated-table, semantic API, parser, provenance, CLI golden, and record-classification tests; [calling-modifier issue #63](https://github.com/frankischilling/holyc-ocaml/issues/63), [issue #191](https://github.com/frankischilling/holyc-ocaml/issues/191) |
| Intermediate representation | Implemented for all 185 numeric identities, argument and result metadata, structural types, table flags, and source/display names. A checked immutable sequence adds stable instruction and value IDs, independent target types, ordered value operands, exact immediate and reference payloads, source spans, audited `ICF_*` flags, stable `HCIR0001` through `HCIR0010` failures, and a versioned deterministic text form. A total control-flow table distinguishes ordinary fallthrough, labels, one unconditional jump, all 32 conditional branches, both switches, the return, and the zero-byte end marker. A separate total effect table records conservative runtime effects independently from the source `not_const` field. The x87 verifier checks depth through edges and loops. A named-function wrapper retains its signature, scopes, members, compiler state, spans, and verified graph. A separate top-level wrapper retains stream identity, source-item position, compiler options, span, and the verified graph without fabricated function metadata. Literal lowering accepts explicit payloads or integer, character, F64, and string parser nodes through nested grouping and the audited prefix operators. It constructs source-backed immediate and string instructions and preserves emitting prefixes with checked operand links, operator spans, and caller-owned identities. Direct typed semantic literal results use the same boundary, copying their retained payload, checked result type, and source location without consulting a parser AST. Parentheses and unary plus are transparent on that typed path as well. Typed unary minus emits inner-to-outer `IC_UNARY_MINUS` chains on the same path. `HCIRL0001` through `HCIRL0003` cover identity-count, pointer-depth, and typed-metadata failures; supported parser wrappers and other semantic shapes return `Not_literal` without fabricated IR. `HCIR0021` through `HCIR0037` cover x87 and wrapper failures. Remaining prefix operations and expression or statement lowering beyond the documented integer unary and binary slice, top-level result rewriting, string storage, memory execution, floating evaluation, instruction-specific alias refinement, exception and lock effects, relocation intent, optimization, interpretation, and code generation are not implemented. | [Issues #397](https://github.com/frankischilling/holyc-ocaml/issues/397), [#399](https://github.com/frankischilling/holyc-ocaml/issues/399), [#403](https://github.com/frankischilling/holyc-ocaml/issues/403), [#405](https://github.com/frankischilling/holyc-ocaml/issues/405), [#407](https://github.com/frankischilling/holyc-ocaml/issues/407), [#409](https://github.com/frankischilling/holyc-ocaml/issues/409), [#411](https://github.com/frankischilling/holyc-ocaml/issues/411), [#413](https://github.com/frankischilling/holyc-ocaml/issues/413), [#415](https://github.com/frankischilling/holyc-ocaml/issues/415), [#417](https://github.com/frankischilling/holyc-ocaml/issues/417), [#419](https://github.com/frankischilling/holyc-ocaml/issues/419), [#421](https://github.com/frankischilling/holyc-ocaml/issues/421), [#423](https://github.com/frankischilling/holyc-ocaml/issues/423), [#425](https://github.com/frankischilling/holyc-ocaml/issues/425), [#427](https://github.com/frankischilling/holyc-ocaml/issues/427), [#431](https://github.com/frankischilling/holyc-ocaml/issues/431), [#433](https://github.com/frankischilling/holyc-ocaml/issues/433), [#435](https://github.com/frankischilling/holyc-ocaml/issues/435), and [#438](https://github.com/frankischilling/holyc-ocaml/issues/438), all-opcode shape, control-flow, effect, x87, wrapper, literal-lowering, parser-boundary, semantic-boundary, grouping, unary-prefix, deterministic text, negative verifier, and identity property tests |
| TempleOS BIN specification | Implemented for the 32-byte header, all 21 defined entries, five reserved codes, relocation formulas, record payloads, two loader passes, eight AOT adjustments, and source-marked limitations; parsing and emission are not implemented | Generated-table and public API tests; [format notes](templeos-bin-format.md) |
| Integrated preprocessing | Partially implemented: quoted `#include`; TempleOS text `#define`; deterministic `#if` and `#assert` over literals, definition-expanded constants, `defined`, and the audited HolyC operator table; nonfatal failed-assertion warnings; explicit JIT/AOT mode branches; `#ifdef` and `#ifndef` over definitions and a typed session symbol environment; source-ordered `#help_index` and `#help_file` metadata; and deterministic expansion of the six standard predefined values. New sessions contain all 570 checked static compiler symbols, including every register, canonical opcode, and alias from `OpCodes.DD`; imports remain excluded and local symbols retain their source shadow behavior. Includes, definitions, predefined values, conditional expressions, and help records keep their provenance and hosted resource limits. A lexical item may cross nested frames, and tokens, trivia, and diagnostics retain each contributing source segment. The boundary behavior matches a native oracle run that covers token identity, caller position, operators, numbers, strings, characters, comments, nested definitions, includes, and line continuations. The pinned `Kernel/KernelC.HH` check records all 104 index directives and 20 help-file directives without a diagnostic. Date and time are explicit build inputs, while file and directory values use canonical hosted paths rather than TempleOS drive paths. The standalone command still cannot discover declarations before the parser exists. Unmatched-boundary diagnostics remain stricter than TempleOS under #27. Native nonconstant directive expressions await #33. General `#exe` execution and arbitrary generated source remain unavailable. | [Preprocessor notes](preprocessor.md), [oracle fixture](oracle-fixtures.md), [assembler notes](assembler.md), [predefined-value issue #40](https://github.com/frankischilling/holyc-ocaml/issues/40), [help metadata issue #37](https://github.com/frankischilling/holyc-ocaml/issues/37), [constant expression issue #32](https://github.com/frankischilling/holyc-ocaml/issues/32), [assertion issue #35](https://github.com/frankischilling/holyc-ocaml/issues/35), [VM expression issue #33](https://github.com/frankischilling/holyc-ocaml/issues/33), [include issue #21](https://github.com/frankischilling/holyc-ocaml/issues/21), [definition issue #23](https://github.com/frankischilling/holyc-ocaml/issues/23), [mode issue #26](https://github.com/frankischilling/holyc-ocaml/issues/26), [symbol issue #29](https://github.com/frankischilling/holyc-ocaml/issues/29), [opcode seed issue #30](https://github.com/frankischilling/holyc-ocaml/issues/30), [boundary issue #24](https://github.com/frankischilling/holyc-ocaml/issues/24), [diagnostic issue #27](https://github.com/frankischilling/holyc-ocaml/issues/27), and [epic #3](https://github.com/frankischilling/holyc-ocaml/issues/3) |
| Parsing | Implemented for empty input; extern class and union forward declarations; ordinary, type-backed, and single-base class and union definitions with grouped pointer, array, and recursive function-pointer members, source-ordered member metadata, anonymous unions, empty member semicolons, source-positioned `$$ = expression;` offset directives, and attached global declarators; primitive, intrinsic storage, and published aggregate names in globals, arrays, prototypes, function definitions, parameters, nested function pointers, automatic and static locals, members, and postfix casts; scalar and recursive braced global initializer syntax; ordinary and `lastclass` parameter defaults; the audited core expression forms; and empty, expression, output, comma-linked, compound-block, `if`, `while`, `do ... while`, `for`, `goto`, label, `lock`, `switch`, paired `try`/`catch`, `break`, `return`, structural brace-delimited assembly, and function-local direct assembly statements. Declaration groups retain prefixes, bindings, pointer layers, array dimensions, optional initial values, and delimiters. Local groups retain storage, per-declarator register requests, pointer layers, optional recursive callback signatures, array dimensions, scalar initializers on ordinary declarators, recursive braced initializers on static ordinary declarators, and delimiters. Direct assembly retains source-ordered instructions and supported directives, first-form operand shape, directive arguments, separators, and provenance. Named type recognition follows visible class symbols and local shadowing, never capitalization. Aggregate definitions publish their symbol before reading an optional base or body, and each attached global is published before the next token is requested. Function definitions publish before reading their body. Named parameters, prior locals, and variadic `argc` and `argv` participate in the function-wide parser lookup context. Calls retain omitted slots, statement sequences retain their separators, and recursive statements retain their source-shaped boundaries. Controlled native fixtures confirm the implemented function-body and ordinary local-declaration behavior. Errors suppress the public AST instead of falling back to raw tokens. This remains syntax support apart from the separately listed aggregate and function-signature semantic passes: type identity and name resolution outside aggregate headers, direct member heads, and top-level function signatures; block scopes; storage and layout; whole-value and subinteger aggregate access; call checking; default substitution; initializer and metadata evaluation; direct local callback initialization; register allocation; assembly-address label validation; lock-aware IR; switch evaluation; exception semantics; executable control flow; return checks; automatic local braced initializers; inherited layout and member lookup; metadata lookup; aggregate-offset evaluation and layout effects; assembly operand and directive validation; assembler state and output effects; and complete corpus parsing are not implemented. | [Direct-directive issue #159](https://github.com/frankischilling/holyc-ocaml/issues/159), [direct-operand issue #157](https://github.com/frankischilling/holyc-ocaml/issues/157), [assembly-block issue #151](https://github.com/frankischilling/holyc-ocaml/issues/151), [member-metadata issue #149](https://github.com/frankischilling/holyc-ocaml/issues/149), [aggregate-offset issue #145](https://github.com/frankischilling/holyc-ocaml/issues/145), [parser epic #48](https://github.com/frankischilling/holyc-ocaml/issues/48), [static-local initializer issue #143](https://github.com/frankischilling/holyc-ocaml/issues/143), [function-pointer local issue #141](https://github.com/frankischilling/holyc-ocaml/issues/141), [function-pointer member issue #139](https://github.com/frankischilling/holyc-ocaml/issues/139), [aggregate global issue #135](https://github.com/frankischilling/holyc-ocaml/issues/135), [aggregate inheritance issue #133](https://github.com/frankischilling/holyc-ocaml/issues/133), [aggregate backing issue #131](https://github.com/frankischilling/holyc-ocaml/issues/131), [aggregate definition issue #129](https://github.com/frankischilling/holyc-ocaml/issues/129), [named type issue #127](https://github.com/frankischilling/holyc-ocaml/issues/127), [function-definition issue #119](https://github.com/frankischilling/holyc-ocaml/issues/119), [local-declaration issue #121](https://github.com/frankischilling/holyc-ocaml/issues/121), [native function oracle fixture](../test/oracle/function-definitions.json), [native local-declaration oracle fixture](../test/oracle/local-declarations.json), [statement issue #95](https://github.com/frankischilling/holyc-ocaml/issues/95), [block issue #97](https://github.com/frankischilling/holyc-ocaml/issues/97), [conditional issue #99](https://github.com/frankischilling/holyc-ocaml/issues/99), [while issue #101](https://github.com/frankischilling/holyc-ocaml/issues/101), [do-while issue #103](https://github.com/frankischilling/holyc-ocaml/issues/103), [for issue #105](https://github.com/frankischilling/holyc-ocaml/issues/105), [break issue #107](https://github.com/frankischilling/holyc-ocaml/issues/107), [return issue #109](https://github.com/frankischilling/holyc-ocaml/issues/109), [goto and label issue #111](https://github.com/frankischilling/holyc-ocaml/issues/111), [lock issue #113](https://github.com/frankischilling/holyc-ocaml/issues/113), [try/catch issue #115](https://github.com/frankischilling/holyc-ocaml/issues/115), [switch issue #117](https://github.com/frankischilling/holyc-ocaml/issues/117), parser unit and negative tests, provenance tests, and human and JSON CLI goldens |
| Parser visibility inspection | `holyc dump-symbols` emits schema `holyc-symbol-visibility-v2` in stable ID order as human text or JSON. It records the exact reference commit, hash kind, source segments, generated-source invocation and definition sites, direct-function call shapes, and active local contexts. The default view includes all 570 pinned compiler entries; `--source-only` keeps source-published declarations. A parser error retains the state accumulated before failure but still reports diagnostics and exits with status 1. This is parser routing state, not semantic type identity, binding resolution, storage, linkage, imports, exports, task-parent lookup, or loader symbols. | [Issue #162](https://github.com/frankischilling/holyc-ocaml/issues/162), [scoping notes](scoping-and-linkage.md), unit tests, and human, JSON, and failure-path CLI goldens |
| Parser corpus | The deterministic AOT comparison covers all 528 committed `.HC`, `.HH`, and `.PRJ` objects at the pinned commit. The standalone side has 25 parses, 17 lexer or preprocessor first errors, 486 parser first errors, and 36,999 accumulated diagnostics. The project-prelude side reads both project include orders, copies the four direct compiler-project headers into each isolated target session, sets that session's working directory to the measured source directory, and accepts 126 files. One hundred and one parse only with that prelude. The remaining prelude results are 30 frontend first errors and 372 parser first errors, split into 370 unresolved-name failures and 32 other boundaries, with 20,981 accumulated diagnostics. All 38 former `HCPP0003` first failures advance: 33 reach parser diagnostics and five reach `HCPP0022`. Neither side excludes a file or reports a read or internal error. Root bytes come from Git; includes come from the clean checkout after revision checks and remain confined to that root; physical NUL ends those source buffers before trailing payload bytes. The v2 report records the `source-directory` policy, project directories, and every target's effective directory. Corpus mode decodes `$IB` and `$IS` commands from those trailers and marks archive-shortened or missing payloads incomplete. Seven former `HCPARSE0127` first failures retain their later parser boundaries. Semicolon-separated function parameters make `Kernel/StrA.HC` parse in both modes. Optional prototype terminators make `Kernel/KernelC.HH`, `Kernel/KMisc.HC`, and `Demo/AcctExample/TOS/TOSExt.HC` parse with the project prelude; `MakeTOS.HC` advances into `TOSMisc.HC`. Unbraced fixed-array initialization makes `Adam/DolDoc/DocHighlight.HC` parse with the project prelude. Complete single-dollar DolDoc commands inside strings make `Adam/Opt/Utils/MemRep.HC`, `Demo/DolDoc/TextDemo.HC`, and `Demo/MsgLoop.HC` parse with the prelude; `TextDemo.HC` also parses standalone and standalone `MsgLoop.HC` advances to unresolved `Fs` at line 10. The assembler-size fallback from a visible intrinsic class to `cmp.asm_hash` makes `Kernel/KInts.HC` parse with the prelude and removes the same recovered diagnostic from the `Kernel.PRJ` run. The two unnamed string definition headings in `KernelA.HH` remove `HCPP0010` without changing a phase classification. Terminal global commas move `Demo/Graphics/Life.HC` to unresolved `GrBlot` at line 10. Optional adjacent-aggregate terminators move `Demo/Graphics/Pick3D.HC` from `HCPARSE0115` at line 37 to unresolved `DCDepthBufAlloc` at line 83. The prelude's own 23 diagnostics are recorded rather than hidden. A completed comparison exits successfully so the report can be published; `--require-all` gates the project-prelude side. Arbitrary `Cd` execution and intact binary-payload oracle validation remain separate work. No semantic, lowering, or runtime compatibility follows from this parser-only result. | [Reviewed AOT comparison](../reference/parser-corpus-aot.json), [issue #153](https://github.com/frankischilling/holyc-ocaml/issues/153), [issue #359](https://github.com/frankischilling/holyc-ocaml/issues/359), [issue #369](https://github.com/frankischilling/holyc-ocaml/issues/369), [issue #371](https://github.com/frankischilling/holyc-ocaml/issues/371), [issue #372](https://github.com/frankischilling/holyc-ocaml/issues/372), [issue #382](https://github.com/frankischilling/holyc-ocaml/issues/382), [issue #384](https://github.com/frankischilling/holyc-ocaml/issues/384), [issue #386](https://github.com/frankischilling/holyc-ocaml/issues/386), [issue #388](https://github.com/frankischilling/holyc-ocaml/issues/388), [issue #390](https://github.com/frankischilling/holyc-ocaml/issues/390), [issue #392](https://github.com/frankischilling/holyc-ocaml/issues/392), [issue #394](https://github.com/frankischilling/holyc-ocaml/issues/394), [oracle issue #375](https://github.com/frankischilling/holyc-ocaml/issues/375), corpus unit tests, and the parser corpus Actions job |
| Semicolon-separated function parameters | Syntax implemented for semicolons after fixed parameters and for leading or repeated empty semicolon entries in prototypes, definitions, and recursive function-pointer signatures. Ordinary separators remain attached to concrete parameters. Each empty entry records its source-spanned delimiter and the number of preceding concrete parameters, which retains source order without changing semantic parameter lists. A comma cannot create an empty entry. The exact `StrPrintHex(U8 *dst,I64 num;I64 width)` header and definition parse in JIT and AOT modes. This is syntax retention only and does not change parameter storage or the ABI. | [Issue #372](https://github.com/frankischilling/holyc-ocaml/issues/372), [empty-entry issue #377](https://github.com/frankischilling/holyc-ocaml/issues/377), exact pinned-source tests, negative boundary tests, and deterministic AST dumps |
| Optional prototype terminators | Bound prototypes may omit the semicolon after their closing parenthesis. A written semicolon retains its source location; an omission stays absent and consumes no token from the next top-level item. All five binding spellings are covered, with `import` forms restricted to AOT as before. Exact tests use `Kernel/KernelC.HH:179-180` and `Demo/AcctExample/TOS/TOSExt.HC:306-307`. A body after a bound prototype remains a separate top-level statement. This is parser compatibility only; linkage and ABI behavior are unchanged. | [Issue #384](https://github.com/frankischilling/holyc-ocaml/issues/384), [source map](reference-source-map.md), parser tests, and the reviewed corpus report |
| Unbraced fixed-array initializers | A fixed-size global array may start its element list directly after `=`. The AST retains the absent opening brace, fixed-dimension grouping, every element comma, and an optional closing brace. The hosted parser accepts positive literal bounds after definition expansion, with a one-million-element limit per dimension; `HCPARSE0159` rejects other bounds until general constant evaluation is available. The exact `highlight_hash_type_colors[HTt_TYPES_NUM]` table parses in both modes. Initializer expression binding follows the retained element paths. Storage bytes and runtime initialization are not yet tested. | [Issue #386](https://github.com/frankischilling/holyc-ocaml/issues/386), [source map](reference-source-map.md), parser and semantic-binding tests, and the reviewed corpus report |
| Aggregate forward declarations | Syntax implemented for ordinary `extern class Name;` and `extern union Name;` in JIT and AOT modes. The AST retains modifiers, binding and aggregate spelling, name, semicolon, and source provenance. Accepted names enter the streaming environment as class symbols before later preprocessing, which makes a following `#ifdef` observe them and permits later named-type syntax. The exact first 17 lines of `Kernel/KernelA.HH` produce all 14 forward items. `HCPARSE0107` reports a missing name, and `HCPARSE0108` rejects a missing semicolon, comma list, or body without publishing the name. A later ordinary definition is accepted through the aggregate-definition path. Separate semantic passes reconcile identities, calculate closed definition layouts, and index completed members. The parser does not assign concrete flags, validate unrelated duplicates, or apply linkage. | [Issue #125](https://github.com/frankischilling/holyc-ocaml/issues/125), parser unit and negative tests, provenance tests, and human and JSON CLI goldens |
| Class and union definitions | Syntax implemented for named class and union bodies in JIT and AOT modes. The AST retains ordered modifiers, aggregate spelling, name, an optional single base, braces, ordinary member groups, independent return-pointer layers, recursive function-pointer declarators, array dimensions, recursive anonymous unions, empty member semicolons, explicit offset directives, member metadata, and either a standalone semicolon or attached global declarators. The aggregate name is published before base and body parsing, so source-shaped self references and a prior forward declaration work. Tests parse the 12 consecutive ordinary definitions at `Kernel/KernelA.HH:116-179` without filtering. Callback-member tests cover eight declarations across `CMathODE`, `CDocEntry`, `CKeyDevEntry`, and `CKeyDevGlbls` in both compilation modes. Four direct pinned offset forms cover negative, literal, and alignment expressions. Semantic passes reconcile aggregate identities, resolve definition headers, direct member heads, and recursive callback signatures, retain `MLF_FUN` on callback members, calculate closed class and union layouts, and build direct and inherited member indexes with source-compatible duplicate checks. Backing-chain and cycle validation beyond closed inputs, metadata evaluation and lookup, general `offset` expression binding, nested named definitions, callback-default binding and evaluation, subinteger and runtime field access, and linkage remain unimplemented. | [Member-metadata issue #149](https://github.com/frankischilling/holyc-ocaml/issues/149), [aggregate-offset issue #145](https://github.com/frankischilling/holyc-ocaml/issues/145), [issue #129](https://github.com/frankischilling/holyc-ocaml/issues/129), [function-pointer member issue #139](https://github.com/frankischilling/holyc-ocaml/issues/139), [aggregate global issue #135](https://github.com/frankischilling/holyc-ocaml/issues/135), [type-backed definition issue #131](https://github.com/frankischilling/holyc-ocaml/issues/131), [single-base issue #133](https://github.com/frankischilling/holyc-ocaml/issues/133), [member-type issue #179](https://github.com/frankischilling/holyc-ocaml/issues/179), [callback-signature issue #216](https://github.com/frankischilling/holyc-ocaml/issues/216), [member-list flag issue #218](https://github.com/frankischilling/holyc-ocaml/issues/218), parser and semantic unit tests, negative tests, generated-source provenance tests, and human and JSON CLI goldens |
| Explicit aggregate-offset directives | Syntax implemented for repeated `$$ = expression;` members in classes, named unions, and anonymous unions in JIT and AOT modes. The AST retains the marker, equals sign, ordinary expression tree, semicolon, complete span, and generated-source provenance. A syntax-specific audit finds four direct forms: `Demo/Lectures/NegDisp.HC:35` and `Kernel/KernelA.HH:447`, `3422`, and `3806`. Outside an aggregate body, the same tokens remain an ordinary current-position assignment expression. `HCPARSE0142` and `HCPARSE0143` report missing punctuation; the shared expression diagnostics cover a missing value and excessive nesting. The closed-layout pass evaluates source-position, literal, character, unary, and non-assignment binary forms; it applies negative displacements, union-base updates, and explicit alignment expressions. Symbol-dependent expressions such as identifiers, calls, `sizeof`, and `offset` remain typed dependencies. | [Issue #145](https://github.com/frankischilling/holyc-ocaml/issues/145), [issue #194](https://github.com/frankischilling/holyc-ocaml/issues/194), exact pinned-source tests, provenance and recovery tests, and human and JSON CLI goldens |
| Contextual aggregate member names | Syntax implemented for all 48 checked language-keyword spellings when the class, named-union, or anonymous-union grammar is specifically expecting an ordinary or recursive function-pointer member name. TempleOS retains these words as `TK_IDENT` and interprets them through `PrsKeyWord` only in keyword positions; the OCaml lexer keeps explicit keyword token kinds and applies the compatibility rule at the member-name boundary. The intended source set contains 12 direct `U0 start;` members across `Kernel/KernelA.HH`, `Adam/Gr/Gr.HH`, and `Adam/Gr/SpriteMesh.HC`. Tests parse the complete `CKernel` region through its aligned `sys_gdt` member in JIT and AOT modes. | [Issue #147](https://github.com/frankischilling/holyc-ocaml/issues/147), exact pinned-source tests, all-keyword tests, provenance and scope controls, and human and JSON CLI goldens |
| Contextual declarator, aggregate, and operand names | Declarator slots accept each checked keyword spelling for globals, functions, parameters, locals, members, and recursive function pointers. Class and union declarations accept the same spellings, and a visible aggregate with that name wins before keyword dispatch in later type and base positions. During expression and statement parsing, a visible parameter, earlier local, earlier global variable, or earlier global function also wins before keyword dispatch. The global rule follows `CmpJoin`: the compilation global table precedes the keyword-bearing table in both JIT and AOT modes, while both hash entries remain intact. Tests cover global and local declarations using keyword-spelled aggregate types, inheritance, ordinary keyword behavior without a matching aggregate, calls, default slots, address-of, publication order, and generated provenance. Labels and unrelated symbol kinds remain outside this claim. | [Issue #358](https://github.com/frankischilling/holyc-ocaml/issues/358), [issue #361](https://github.com/frankischilling/holyc-ocaml/issues/361), [issue #362](https://github.com/frankischilling/holyc-ocaml/issues/362), [issue #363](https://github.com/frankischilling/holyc-ocaml/issues/363), [issue #366](https://github.com/frankischilling/holyc-ocaml/issues/366), pinned lookup-order tests, provenance tests, and JIT/AOT parser cases |
| Aggregate member metadata | Syntax implemented for repeated metadata pairs on class, named-union, and anonymous-union declarators. String values retain every adjacent literal and their combined decoded bytes; other values retain the ordinary expression tree. Entries and duplicates stay in source order so later semantic work can reproduce TempleOS's prepended lookup list. The exact `Demo/ClassMeta.HC:16-28` region contributes six members and 13 entries and parses in JIT and AOT modes. `HCPARSE0144` reports a name without a value. Evaluation, `MLMF_IS_STR`, `MemberMetaData`, and `MemberMetaFind` remain unimplemented. | [Issue #149](https://github.com/frankischilling/holyc-ocaml/issues/149), pinned-source tests, shape and provenance tests, negative recovery tests, and human and JSON CLI goldens |
| Aggregate-attached globals and global initializers | Syntax implemented in JIT and AOT modes. After a class or union body, the AST retains pointer, array, and comma-separated global declarators. Ordinary and attached globals retain scalar expressions or recursive braced initializer trees, including trailing commas and complete source provenance. A byte-preserving structural scan finds 40 valid aggregate definitions with attached globals in the pinned Compiler, Kernel, Adam, and Demo `.HC` and `.HH` set: six pointer forms, 18 array forms, eight comma groups, and four initialized forms. The exact 17 initialized descriptor rows at `Compiler/CInit.HC:1-14` parse in both modes. Initializer typing, evaluation, storage, and relocations remain unimplemented; automatic local braced initializers stay on their separate source rejection path. `HCPARSE0127` through `HCPARSE0130` cover malformed values and the 256-level list guard. | [Issue #135](https://github.com/frankischilling/holyc-ocaml/issues/135), parser unit and negative tests, pinned source tests, generated-source provenance tests, and human and JSON CLI goldens |
| Function-pointer globals | Syntax implemented for top-level and aggregate-attached declarations in JIT and AOT modes. The AST keeps return-type pointer layers, one through four callback indirections, recursive fixed or variadic parameters, nested defaults, array suffixes, an optional initializer, binding, and provenance. Publication uses global-variable identity rather than direct-function identity. Tests parse the four definitions at `Kernel/KGlbls.HC:32-35`, the four corresponding `extern` declarations in `Kernel/KernelC.HH`, and representative initialized and array forms from Adam and Demo in both modes. A line-anchored audit found ten matching forms in the intended source set; this is evidence for those locations, not a complete declarator census. `HCPARSE0131` and `HCPARSE0132` report malformed punctuation and a missing name, and `HCPARSE0017` retains the recursive signature limit. The global type pass resolves recursive callback signatures, record classification sets `GVF_FUN`, and identifier or explicit-dereference calls use that signature for checked slots and result typing. Storage, completed linkage, callback value reads, lowering, and execution remain unimplemented. | [Issue #137](https://github.com/frankischilling/holyc-ocaml/issues/137), [issue #185](https://github.com/frankischilling/holyc-ocaml/issues/185), [issue #189](https://github.com/frankischilling/holyc-ocaml/issues/189), [issue #280](https://github.com/frankischilling/holyc-ocaml/issues/280), parser and semantic unit tests, pinned source tests, provenance tests, and human and JSON CLI goldens |
| Function-pointer aggregate members | Syntax implemented for class, union, and anonymous-union members in JIT and AOT modes. Each member declarator keeps return-pointer layers outside a recursive callback node, one through four callback indirections, nested parameters and defaults, a variadic marker, array suffixes, and its comma or semicolon. Members stay out of the top-level symbol environment. Pinned tests parse eight callbacks across `Kernel/KernelA.HH:251-289`, `1191-1222`, and `3754-3771`. The `CDocEntry` fixture omits only line 1221, `#assert !($$&7)`, because runtime `$$` is outside the constant preprocessor; it retains the closing brace at line 1222. A line-anchored audit found 26 exact `(*name)(` or `(**name)(` forms in `KernelA.HH`, which is evidence for those locations rather than a complete declarator census. `HCPARSE0133` and `HCPARSE0134` report malformed punctuation and a missing member name. The member-type pass resolves the return head and complete recursive signature against the aggregate identities visible after current-definition publication. It retains unnamed slots, defaults, varargs, nested callbacks, punctuation, and generated-source provenance. A checked registry covers all seven member-list masks and 23 assignments or consumers from five pinned sources. The semantic result assigns mask zero to an ordinary object and `MLF_FUN` (`0x8`) to a callback. Closed layout assigns pointer-sized storage and an offset, and the member index retains the callback marker for direct or inherited lookup. Default binding and evaluation, call checking, recorded cleanup and sizing behavior, lowering, and execution remain unimplemented. | [Issue #139](https://github.com/frankischilling/holyc-ocaml/issues/139), [issue #216](https://github.com/frankischilling/holyc-ocaml/issues/216), [issue #218](https://github.com/frankischilling/holyc-ocaml/issues/218), parser and semantic unit tests, pinned source tests, provenance tests, and human and JSON CLI goldens |
| Function-pointer locals | Syntax implemented for automatic and static locals in JIT and AOT modes. A local declarator keeps return-pointer layers outside the recursive callback node, one through four callback indirections, nested fixed or variadic parameters and defaults, array suffixes, register requests where automatic storage permits them, and its comma or semicolon. It enters the function-wide parser environment as a local variable, so it shadows direct-function and class syntax without enabling parenthesis-free direct calls. Tests parse the exact automatic declarations at `Demo/Games/Stadium/StadiumGen.HC:5` and `Demo/Graphics/Grid.HC:13`; these two directly audited forms are not a whole-corpus declarator count. No static callback local was found in the pinned corpus, so static syntax has focused coverage through the shared `PrsVarLst` path but is not a corpus claim. `HCPARSE0135` and `HCPARSE0136` report malformed punctuation and a missing local name. `HCPARSE0137` rejects a direct callback initializer at the boundary noted by the `Grid.HC` source; both pinned examples assign the callback later. Recursive semantic callback types and declaration-time `MLF_FUN` and `MLF_STATIC` masks are implemented. Parameter, automatic, and static callback calls use these signatures for checked slots and results. Storage allocation, initializer checking, callback value reads, lowering, cleanup, and execution remain unimplemented. | [Issue #141](https://github.com/frankischilling/holyc-ocaml/issues/141), [issue #200](https://github.com/frankischilling/holyc-ocaml/issues/200), [issue #222](https://github.com/frankischilling/holyc-ocaml/issues/222), [issue #280](https://github.com/frankischilling/holyc-ocaml/issues/280), parser and semantic unit tests, exact pinned-source tests, provenance tests, and human and JSON CLI goldens |
| Static local braced initializers | Syntax implemented for ordinary `static` locals in JIT and AOT modes. The AST shares the recursive scalar-or-braced initializer tree used by globals while preserving the existing scalar local dump shape. Exact tests parse `Adam/WinMgr.HC:38`, `Demo/GlblVars.HC:21-25`, and `Demo/GlblVars.HC:62-66`. A line-start scan found those three braced forms among 21 static declarations in the intended Compiler, Kernel, Adam, and Demo `.HC` and `.HH` set; this is a narrow source audit, not a complete declarator census. Automatic braced locals remain rejected with `HCPARSE0104`. `HCPARSE0138` through `HCPARSE0141` cover missing values, missing separators, unclosed lists, and the fixed 256-level guard. Initializer typing, evaluation, dimension inference, storage, lowering, and emission remain unimplemented. | [Issue #143](https://github.com/frankischilling/holyc-ocaml/issues/143), parser unit and negative tests, exact pinned-source tests, provenance tests, and human and JSON CLI goldens |
| Type-backed class and union definitions | Syntax implemented for the 11 internal storage spellings from `CInit.HC` and for an optional backing type with zero through four pointer layers before `class` or `union`. Internal storage types have a distinct AST form and retain their original spelling and provenance. Tests parse all six public integer unions and seven representative backed classes from `Kernel/KernelA.HH` in JIT and AOT modes. `HCPARSE0124` rejects an unknown backing, `HCPARSE0125` rejects a declaration binding before the aggregate keyword, and the existing `HCPARSE0004` pointer-depth diagnostic recovers at the aggregate boundary. Closed body layouts are available when every dependency is known; applying the backing type to whole-value and subinteger access or conversions remains unimplemented. | [Issue #131](https://github.com/frankischilling/holyc-ocaml/issues/131), [issue #194](https://github.com/frankischilling/holyc-ocaml/issues/194), parser and primitive-type unit tests, provenance and recovery tests, and human and JSON CLI goldens |
| Single-base aggregate inheritance | Syntax implemented for one visible class or union symbol after `:` on class or union definitions in JIT and AOT modes. The AST retains the colon and base identifier separately from an optional backing type. Publication happens before base lookup, matching `PrsClass`. The complete `Adam/Gr/Gr.HH:18-115` sprite hierarchy and representative kernel hash chains parse unchanged. A byte-preserving scan of the intended Compiler, Kernel, Adam, and Demo source set finds 37 inherited class headers and no inherited union header; union support follows the shared source path and is not a corpus claim. `HCPARSE0121` rejects an invalid base and `HCPARSE0126` rejects a second base. For closed dependencies, semantic layout places the base at offset zero and starts direct fields after its size; the member index then follows the single base chain. General cycle diagnostics, metadata inheritance, and runtime member access remain unimplemented. | [Issue #133](https://github.com/frankischilling/holyc-ocaml/issues/133), parser unit and negative tests, source and provenance checks, and human and JSON CLI goldens |
| Named class and union type syntax | Syntax implemented for a visible class or union name in global declaration groups, return types, parameters, nested function pointers, automatic and static locals, arrays, postfix casts, ordinary aggregate members, and base clauses. The AST distinguishes named from primitive type specifiers while retaining spelling and source provenance. JIT and AOT modes share the rule, local bindings can shadow a class name in function syntax, and unresolved names receive `HCPARSE0001` without publishing a declaration. The unfiltered first 27 lines of `Compiler/CExts.HC` parse after their required forward names are supplied. Aggregate declarations receive reconciled identities; direct member heads, local and global types, top-level function returns, recursive fixed-parameter signatures, aggregate callback parameters, and named postfix-cast arguments on resolved direct calls bind to the identity visible at their source position. Closed aggregate definitions also receive byte layouts and direct or inherited member indexes. Other named type-use sites remain unresolved, as do metadata, value conversion, and lowering. | [Issue #127](https://github.com/frankischilling/holyc-ocaml/issues/127), [issue #129](https://github.com/frankischilling/holyc-ocaml/issues/129), [issue #133](https://github.com/frankischilling/holyc-ocaml/issues/133), [issue #179](https://github.com/frankischilling/holyc-ocaml/issues/179), [issue #181](https://github.com/frankischilling/holyc-ocaml/issues/181), [issue #185](https://github.com/frankischilling/holyc-ocaml/issues/185), [issue #194](https://github.com/frankischilling/holyc-ocaml/issues/194), [issue #196](https://github.com/frankischilling/holyc-ocaml/issues/196), [issue #200](https://github.com/frankischilling/holyc-ocaml/issues/200), [issue #216](https://github.com/frankischilling/holyc-ocaml/issues/216), and [issue #248](https://github.com/frankischilling/holyc-ocaml/issues/248), parser and semantic unit tests, negative tests, provenance tests, and human and JSON CLI goldens |
| Parenthesis-free direct calls | Syntax implemented for a visible direct function with a retained signature shape. Defaulted fixed positions become omitted slots, required positions consume adjacent expressions, and a variadic signature adds no extras without parentheses. Definitions publish the shape before their body, so recursive calls work. Address-of and a local or newer nonfunction entry suppress this form. `HCPARSE0105` reports a missing required expression; `HCPARSE0106` rejects an entry that lacks retained shape. The direct-call semantic pass resolves these slots against the source-visible header and canonical function identity. Literal, audited integer-primary, postfix-cast, prefix, update, and binary conversion intent is available when a parenthesis-free call supplies a required expression from those classes. A named by-value cast follows only aggregate backing headers visible before its caller; pointer targets stay on the integer path. The typed-result pass records `lastclass` state for this call form. Ordinary default evaluation, lastclass string allocation, remaining expression classes, lowering, and execution remain unimplemented. | [Issue #123](https://github.com/frankischilling/holyc-ocaml/issues/123), [issue #238](https://github.com/frankischilling/holyc-ocaml/issues/238), [issue #242](https://github.com/frankischilling/holyc-ocaml/issues/242), [issue #244](https://github.com/frankischilling/holyc-ocaml/issues/244), [issue #246](https://github.com/frankischilling/holyc-ocaml/issues/246), [issue #248](https://github.com/frankischilling/holyc-ocaml/issues/248), [issue #250](https://github.com/frankischilling/holyc-ocaml/issues/250), [issue #252](https://github.com/frankischilling/holyc-ocaml/issues/252), [issue #255](https://github.com/frankischilling/holyc-ocaml/issues/255), [issue #257](https://github.com/frankischilling/holyc-ocaml/issues/257), [issue #282](https://github.com/frankischilling/holyc-ocaml/issues/282), [native fixture](../test/oracle/parenthesis-free-calls.json), parser and semantic tests, and human and JSON CLI goldens |
| Statement-position output literals | Syntax implemented for top-level items, compound-block children, and parsed function bodies. String markers select `Print`, character markers select `PutChars`, and empty markers retain the following fixed expression. Consecutive string tokens combine before that empty-marker check, so `"" "ready";` uses the nonempty combined format. The literal keeps each physical spelling, decoded bytes, and source location. Nonempty markers remain the start of the full expression. Additional `Print` arguments keep their leading commas until the required semicolon. A `PutChars` statement may instead end at a sequence comma. Declarations, prototypes, definitions, and statements retain source order in JIT and AOT modes, with include and definition provenance. Runtime symbol resolution, argument checking, lowering, and execution remain unimplemented. | [Issue #370](https://github.com/frankischilling/holyc-ocaml/issues/370), [issue #93](https://github.com/frankischilling/holyc-ocaml/issues/93), [issue #119](https://github.com/frankischilling/holyc-ocaml/issues/119), parser unit and negative tests, exact pinned-source slices, and human and JSON CLI goldens |
| Statement sequences | Syntax implemented at top level, recursively inside blocks, and in parsed function bodies for ordinary expressions, empty statements, primitive local declarations, `goto`, labels, leading and repeated commas, comma terminators, semicolon-before-comma forms, trailing commas, and comma-only groups. The AST retains each separator and whether a semicolon was present. Known globals, functions, parameters, and locals keep expression routing; only an absent name immediately followed by `:` becomes a label. A primitive local declaration may follow a sequence comma inside a function. It also continues directly into the next statement without a comma unless `}` closes the block, matching the pinned `PrsStmt` branch and native output. Semantic control flow, local storage, function scope, lowering, and execution remain unimplemented. | [Issue #95](https://github.com/frankischilling/holyc-ocaml/issues/95), [issue #111](https://github.com/frankischilling/holyc-ocaml/issues/111), [issue #119](https://github.com/frankischilling/holyc-ocaml/issues/119), [issue #121](https://github.com/frankischilling/holyc-ocaml/issues/121), [native local-declaration oracle fixture](../test/oracle/local-declarations.json), parser unit and negative tests, and human and JSON CLI goldens |
| Compound statement blocks | Syntax implemented for empty and nested blocks at top level, inside other blocks, and as parsed function bodies. The AST retains both braces, ordered child statements, recursive sequences, primitive local declarations in function contexts, `goto` and label nodes, and include or definition provenance. A comma after `}` continues the enclosing sequence; a comma immediately before `}` is not treated as a trailing delimiter. `HCPARSE0049` reports a missing close with the opener as a secondary location, and `HCPARSE0050` reports an unmatched top-level close. `HCPARSE0051` enforces a hosted safety limit of 256 nested blocks. Blocks do not create semantic scopes; accepted locals enter the current function-wide parser context. Local storage and lifetime, semantic checks, lowering, and execution remain unimplemented. | [Issue #97](https://github.com/frankischilling/holyc-ocaml/issues/97), [issue #111](https://github.com/frankischilling/holyc-ocaml/issues/111), [issue #119](https://github.com/frankischilling/holyc-ocaml/issues/119), [issue #121](https://github.com/frankischilling/holyc-ocaml/issues/121), parser unit and negative tests, and human and JSON CLI goldens |
| `if` and `else` statements | Syntax implemented at top level and recursively in every supported statement context. Conditions require parentheses and use the audited expression parser. Each branch reuses the statement parser, including blocks and comma-linked sequences, and an `else` binds to the nearest unmatched `if`. Keyword, parenthesis, condition, branch, include, and definition provenance appears in both dump formats. `HCPARSE0052` through `HCPARSE0056` cover malformed delimiters or branches, and `HCPARSE0057` enforces a hosted safety limit of 256 nested conditionals. Truth conversion, semantic checks, control-flow construction, IR lowering, and execution remain unimplemented. | [Issue #99](https://github.com/frankischilling/holyc-ocaml/issues/99), parser unit and negative tests, and human and JSON CLI goldens |
| `while` statements | Syntax implemented at top level and recursively in every supported statement context. Conditions require parentheses and use the audited expression parser. Bodies reuse the statement parser, including blocks, comma-linked sequences, nested loops, conditionals, and `break`. Keyword, parenthesis, condition, body, include, and definition provenance appears in both dump formats. Function-body semantic resolution now gives each loop a stable break-region identity and binds nested breaks to the nearest active region. `HCPARSE0058` through `HCPARSE0060` cover malformed delimiters or a missing body, and `HCPARSE0061` enforces a hosted safety limit of 256 nested loop statements shared with `do ... while`. Truth conversion, control-flow construction, IR lowering, and execution remain unimplemented. | [Issues #101](https://github.com/frankischilling/holyc-ocaml/issues/101) and [#516](https://github.com/frankischilling/holyc-ocaml/issues/516), parser, semantic, negative, and provenance tests, and human and JSON CLI goldens |
| `do ... while` statements | Syntax implemented at top level and recursively in every supported statement context. The AST keeps the `do` keyword, body, required trailing `while`, condition parentheses, condition, and final semicolon. Bodies reuse the common statement parser, including blocks, comma-linked sequences, both loop forms, conditionals, and `break`. Function-body semantic resolution gives each loop a stable break-region identity. Tests cover nearest-`if` else association, JIT and AOT modes, include and definition provenance, and mixed loop nesting. `HCPARSE0062` through `HCPARSE0066` cover a missing body or trailing clause; the shared `HCPARSE0061` guard caps mixed loop nesting at 256. Truth conversion, `IC_BR_NOT_ZERO` control flow, IR lowering, and execution remain unimplemented. | [Issues #103](https://github.com/frankischilling/holyc-ocaml/issues/103) and [#516](https://github.com/frankischilling/holyc-ocaml/issues/516), parser, semantic, negative, and provenance tests, and human and JSON CLI goldens |
| `for` statements | Syntax implemented at top level and recursively in every supported statement context. The AST preserves an initializer statement, required condition and separator, optional update statement, both parentheses, and the body. Empty initializers and updates remain distinct, and comma-linked updates use the ordinary statement-sequence node without a fabricated semicolon. Function-body semantic resolution gives only the body a break-region identity; direct breaks in the initializer or update are rejected as targetless, matching the pinned parser calls. A primitive local declaration cannot occupy the initializer because the source statement loop continues past its semicolon; the native compiler reports `Missing )`, and the hosted parser reports `HCPARSE0069`. Other `HCPARSE0067` through `HCPARSE0071` diagnostics cover malformed headers and bodies. Truth conversion, deferred update control flow, IR lowering, and execution remain unimplemented. | [Issues #105](https://github.com/frankischilling/holyc-ocaml/issues/105) and [#516](https://github.com/frankischilling/holyc-ocaml/issues/516), [native local-declaration oracle fixture](../test/oracle/local-declarations.json), parser, semantic, negative, and provenance tests, and human and JSON CLI goldens |
| `break` statements | Syntax implemented at top level and in every recursive statement context. The AST retains the keyword and an optional semicolon. The function-body semantic pass assigns stable identities to `while`, `do while`, `for`-body, switch, and subswitch regions, then binds every break to the nearest active region through blocks, sequences, `if`, lock, and try/catch statements. Direct targetless breaks, including `for` initializer and update forms, are rejected. The checked IR fragment gives every resolved region a caller-seeded block and emits one source-ordered `IC_JMP` per break. Complete include and definition provenance and deterministic semantic and IR dumps are covered. `HCPARSE0072` reports an invalid active terminator. Block placement, complete loop and switch control flow, and execution remain unimplemented. | [Issues #107](https://github.com/frankischilling/holyc-ocaml/issues/107), [#516](https://github.com/frankischilling/holyc-ocaml/issues/516), and [#518](https://github.com/frankischilling/holyc-ocaml/issues/518), parser, semantic, IR fragment, negative, and provenance tests, and human and JSON CLI goldens |
| `return` statements | Syntax implemented at top level for tooling and in every recursive statement context. The AST retains the keyword, optional value expression, optional semicolon, and complete location. An immediate semicolon is the only valueless form; a value may end at a semicolon, statement comma, or semicolon-free `for` update boundary. JIT and AOT parsing, expression precedence, postfix updates, includes, definitions, deterministic dumps, and block-safe recovery are covered. `HCPARSE0073` reports an invalid boundary after a value, and `HCPARSE0074` reports a missing value. Function-context and return-type checks, active-`try` unwinding, function metadata, leave-label resolution, IR lowering, and execution remain unimplemented. | [Issue #109](https://github.com/frankischilling/holyc-ocaml/issues/109), parser unit and negative tests, and human and JSON CLI goldens |
| `goto` statements and function labels | Syntax implemented at top level for tooling and in every recursive statement context. `goto` retains its keyword, required identifier target, optional semicolon, and complete location. Labels retain the unresolved name and colon. Comma-linked forms and semicolon-free `for` updates use the shared statement boundary. Known globals and functions are not silently reinterpreted as labels. JIT and AOT parsing, forward and backward source shapes, includes, definitions, deterministic dumps, malformed targets, and block-safe recovery are covered. `HCPARSE0075` reports a missing target and `HCPARSE0076` reports an invalid terminator. The semantic pass supplies stable function-scoped identities and resolves language gotos; uniquely defined language forms now lower through the separate checked fragment. Unused-label warnings, repeated assembly-address checks, block partitioning, full graph construction, and execution remain unimplemented. | [Issues #111](https://github.com/frankischilling/holyc-ocaml/issues/111) and [#514](https://github.com/frankischilling/holyc-ocaml/issues/514), parser and semantic tests, IR fragment tests, provenance tests, and human and JSON CLI goldens |
| `lock` statements | Syntax implemented at top level and in every recursive statement context. The AST retains the keyword, exactly one recursively parsed statement, and the complete region. That nested statement may be a block, an unbraced statement, another lock, or a comma-linked sequence, matching `PrsStmt`. JIT and AOT parsing, loop and conditional bodies, includes, definitions, deterministic dumps, missing bodies, and the independent 256-level hosted nesting guard are covered. `HCPARSE0077` reports a missing body and `HCPARSE0078` reports excessive lock nesting. `ICF_LOCK` propagation, operation checks, x86 prefix selection, and execution remain unimplemented. | [Issue #113](https://github.com/frankischilling/holyc-ocaml/issues/113), parser unit and negative tests, and human and JSON CLI goldens |
| `try` and `catch` statements | Syntax implemented at top level for tooling and in every recursive statement context. The AST retains both keywords, the ordinary statement after `try`, the required ordinary statement after `catch`, and the complete paired region. Either side may be braced, unbraced, empty, nested, or comma-linked. JIT and AOT parsing, conditionals, loops, locks, includes, definitions, deterministic dumps, malformed pairs, and an independent 256-level hosted nesting guard are covered. `HCPARSE0079` through `HCPARSE0081` report a missing body, keyword, or handler; `HCPARSE0082` gives a specific hosted diagnostic for a standalone `catch`, where the pinned parser reaches its generic `Missing expression` error; and `HCPARSE0083` reports excessive nesting. `SysTry` and `SysUntry` calls, handler labels, exception transfer, return unwinding, lowering, and execution remain unimplemented. | [Issue #115](https://github.com/frankischilling/holyc-ocaml/issues/115), parser unit and negative tests, and human and JSON CLI goldens |
| Brace-delimited assembly statements | Structural syntax implemented at top level and inside functions in JIT and AOT modes. The AST retains both braces, every body token, physical source-line groups, and ordinary global, exported global, and `@@` local labels. All 325 canonical opcodes, 49 aliases, 25 directives, and 106 registers are classified from the checked pinned table without rewriting source spellings. A raw-token inventory covers all 45 blocks in 40 files under the pinned Compiler, Kernel, Adam, and Demo trees, including six nested blocks. Includes, definitions, deterministic dumps, and `HCPARSE0145` and `HCPARSE0146` malformed-brace diagnostics are covered. Physical lines are display groups, not instruction boundaries. Function-local structural labels receive stable semantic identities. Operand parsing, directive validation and execution, assembly operand references, output-address validation, encoding, and execution remain unimplemented. | [Issue #151](https://github.com/frankischilling/holyc-ocaml/issues/151), [issue #172](https://github.com/frankischilling/holyc-ocaml/issues/172), [issue #174](https://github.com/frankischilling/holyc-ocaml/issues/174), [assembly notes](assembler.md), [inventory](../reference/assembly-blocks.json), parser unit and negative tests, and human and JSON CLI goldens |
| Function-local direct assembly statements | Structural syntax implemented in JIT and AOT modes as a source-ordered stream of instruction and directive items. Instructions use zero-, one-, or two-operand forms selected by the first checked opcode record. The AST retains register, immediate, and memory operands, size and segment prefixes, commas, optional semicolons, source spellings, canonical alias identities, and include or definition provenance. Directive items retain `IMPORT`, all four `DU*` widths and postfix `DUP`, `BINFILE`, listing changes, and mode changes, including optional separators and their source-specific semicolon rules. Parsing `BINFILE` never opens its path. `ORG` and `ALIGN` are rejected in direct function assembly; type-sized names remain operand or declaration prefixes; and `DUP` is suffix-only. Visible compiler symbols can shadow opcode, register, and directive names. Exact pinned instruction excerpts cover Adam, Kernel, and Demo sources. `HCPARSE0147` rejects supported direct items at top level; `HCPARSE0149` through `HCPARSE0156` report malformed operands, forbidden directives, and malformed directive arguments. Direct labels, operand and directive validation, directive effects, form selection, lowering, encoding, and execution remain unimplemented. | [Issue #159](https://github.com/frankischilling/holyc-ocaml/issues/159), [issue #157](https://github.com/frankischilling/holyc-ocaml/issues/157), [issue #155](https://github.com/frankischilling/holyc-ocaml/issues/155), [assembly notes](assembler.md), exact pinned-source tests, provenance tests, corpus comparison, and human and JSON AST goldens |
| `sizeof` expressions | Syntax, source-ordered root binding, public primitive evaluation, checked function-bound scalar evaluation, and completed aggregate evaluation are implemented. The AST records zero or more matched wrapper pairs, a named root, zero or more member segments, and pointer layers. The semantic leaf retains that complete source evidence and the exact local, module, supplied outer, or complete-miss query. A bound function root also retains its checked type, object, array, callback, or direct-function shape, array rank, and any completed aggregate size. A source binding wins even when its spelling is a primitive name. An otherwise unbound `I0`, `I8`, `I16`, `I32`, `I64`, `U0`, `U8`, `U16`, `U32`, `U64`, `F64`, or `Bool` receives the audited `Compiler/CInit.HC` byte size. Inside a function, zero-rank primitive parameters, automatic and static locals, module globals, and metadata-bearing outer globals receive the size of their checked type. Completed class and union names plus zero-rank by-value aggregate parameters, automatic locals, static locals, and module globals use the source-visible aggregate layout. Each member segment retains the exact current-or-inherited lookup and can continue only through a completed by-value aggregate member. The final member uses its full layout size, including checked member-array dimensions. Object pointers, aggregate pointers, scalar function pointers, and trailing pointer layers after a valid target use the pinned eight-byte `RT_PTR` representation. Checked values lower to one internal-`I64` `IC_IMM_I64` in JIT and AOT function bodies; unbound primitive values also lower in executable top-level statements. A root that resolves to a visible parameter or local still contributes one specialized warning-count use. Root array multiplication, pointer traversal, executable top-level aggregate bindings, outer aggregate records without a completed module identity, direct function debug ranges, interpretation, and execution remain unimplemented and return no partial IR sequence. | [Issues #85](https://github.com/frankischilling/holyc-ocaml/issues/85), [#228](https://github.com/frankischilling/holyc-ocaml/issues/228), [#482](https://github.com/frankischilling/holyc-ocaml/issues/482), [#484](https://github.com/frankischilling/holyc-ocaml/issues/484), [#486](https://github.com/frankischilling/holyc-ocaml/issues/486), and [#488](https://github.com/frankischilling/holyc-ocaml/issues/488), parser, semantic, and IR tests, and human and JSON CLI goldens |
| `offset` expressions | Syntax, function-local root counting, and completed source-visible aggregate roots are implemented. The AST records zero or more matched wrapper pairs, an unresolved named root, and one or more member segments. It accepts the repeated member path in `PrsOffsetOf`, despite the one-level statement in `Doc/HolyC.DD`, and applies the source `PE_MAYBE_MODIFIERS` boundary. A root that resolves to a visible parameter or local contributes one specialized warning-count use. In function bodies and executable top-level statements, semantic analysis retains the keyword, wrappers, root and member origins, exact aggregate publication, checked current-before-base lookups, intermediate types, cumulative offsets, and internal-`I64` value. Unknown or incomplete roots and invalid member continuations report `HCSEMA0046` without a partial result. Local and global object-root semantics and `IC_IMM_I64` lowering remain unimplemented. | [Issues #87](https://github.com/frankischilling/holyc-ocaml/issues/87), [#228](https://github.com/frankischilling/holyc-ocaml/issues/228), and [#520](https://github.com/frankischilling/holyc-ocaml/issues/520), parser, binding, and expression-result tests, and human and JSON CLI goldens |
| Function-body aggregate offset paths | Implemented for a source-visible aggregate name and for zero-rank by-value aggregate parameters, automatic locals, static locals, and checked globals. A direct member chain follows `PrsOffsetOf`: it starts at zero, searches each aggregate before its base chain, adds the selected member offset, advances through a by-value aggregate member when another segment follows, and returns an internal-`I64` offset value. The standalone `offset(...)` form retains its exact root query, checked object value when present, aggregate publication, every lookup, intermediate type, segment offset, and cumulative value. Grouping and numeric composition preserve the path in JIT and AOT mode. The canonical expression planner lowers a completed path to one internal-`I64` immediate and composes it with mixed `F64` arithmetic and expression-statement termination. A bare aggregate, pointer or array object, callback, primitive object, pointer access, missing member, incomplete aggregate, pointer continuation, or nonaggregate continuation reports `HCSEMA0046` without a partial result. Runtime addresses, storage reads, interpretation, and execution remain unavailable. | [Issues #494](https://github.com/frankischilling/holyc-ocaml/issues/494), [#496](https://github.com/frankischilling/holyc-ocaml/issues/496), [#522](https://github.com/frankischilling/holyc-ocaml/issues/522), and [#524](https://github.com/frankischilling/holyc-ocaml/issues/524), semantic and IR cases covering direct, inherited, nested, grouped, composed, and object-root paths, exact identity and provenance, both modes, stable diagnostics, deterministic replay, purity, instruction metadata, conversion flags, and `IC_END_EXP` |
| `defined` expressions | Syntax and function-local operand counting are implemented. The AST records zero or more matched wrapper pairs and exactly one operand token. Ordinary identifiers and language keywords retain the pinned `TK_IDENT` shape; every other token remains an explicit non-name operand. The term follows `PE_MAYBE_MODIFIERS`, so a recognized postfix cast may follow directly and other suffixes require that cast first. A name-shaped operand that resolves to a visible parameter or local contributes one specialized warning-count use; a non-name operand contributes none. The semantic and IR row above records the implemented local, module, and supplied outer-chain Boolean paths. No language-expression use was found in the pinned Compiler, Kernel, Adam, or Demo sources, so focused fixtures provide this gate. | [Issue #89](https://github.com/frankischilling/holyc-ocaml/issues/89), [issue #228](https://github.com/frankischilling/holyc-ocaml/issues/228), parser and semantic tests, and human and JSON CLI goldens |
| `no_warn` statements and local warnings | Syntax implemented in JIT and AOT modes. The AST retains the keyword, ordered targets, each internal comma, an optional semicolon, and source provenance. A target must be a fixed parameter, synthetic `argc` or `argv`, or a prior local in the active function-wide parser context. Empty forms, trailing target commas, outer statement commas, nested statements, and semicolon-free `for` updates follow the pinned statement boundaries. `HCPARSE0157` rejects a nonlocal or malformed target; `HCPARSE0158` rejects a missing delimiter or boundary. Semantic binding resolves each target to a stable identity and separately retains ordinary uses, specialized name queries, suppression lookups, and local-initializer resets. The warning pass derives effective `MLF_NO_UNUSED_WARN` state, preserves declaration-time flags, honors a supplied `OPTf_WARN_UNUSED_VAR` batch mask, exempts `_anon_`, and returns deterministic `HCSEMA0034` unused-variable or `HCSEMA0035` unneeded-suppression findings for definitions. `HCSEMA0031` through `HCSEMA0033` reject mismatched inputs. Counts include ordinary identifiers, locally resolved `sizeof` and `offset` roots, name-shaped `defined` operands, and suppression lookups. Type roles, member-path names, labels, assembly tokens, non-name `defined` operands, query values, source-executed option changes, and CLI rendering remain unimplemented. | [Issue #224](https://github.com/frankischilling/holyc-ocaml/issues/224), [issue #226](https://github.com/frankischilling/holyc-ocaml/issues/226), [issue #228](https://github.com/frankischilling/holyc-ocaml/issues/228), [language notes](holyc-language.md), [scoping notes](scoping-and-linkage.md), exact pinned-source tests, semantic threshold tests, provenance tests, corpus comparison, and human and JSON AST goldens |
| Typed outer global expression values | Implemented for caller-supplied JIT task-parent and AOT enclosing-compilation global records. An optional checked payload retains the source type, object or recursive callback kind, and array rank represented by `CHashGlblVar`. Function and executable top-level expression typing consume the exact outer-expression batch and produce primitive, `F64`, pointer, aggregate, array, or callback leaves without losing the selected occurrence, table, or entry. Scalar and fully indexed top-level callback calls reuse the checked indirect binder and retain the supplied header, fixed and defaulted slots, variadic tail, return type, and result identity. Function bodies and executable top-level statements accept fully indexed outer callback arrays and retain their typed base and ordered subscript results. Ordinary object call targets and metadata-free records remain unavailable. `HCSEMA0022` rejects negative ranks, metadata attached through a non-global semantic symbol, and foreign aggregate identities; `HCSEMA0046` and `HCSEMA0057` reject stale expression batches, mode mismatches, inconsistent traversals, and invalid callback-array ranks. Storage reads, runtime addresses, lowering, and execution remain unimplemented. | [Issues #332](https://github.com/frankischilling/holyc-ocaml/issues/332), [#334](https://github.com/frankischilling/holyc-ocaml/issues/334), [#336](https://github.com/frankischilling/holyc-ocaml/issues/336), [#338](https://github.com/frankischilling/holyc-ocaml/issues/338), [#340](https://github.com/frankischilling/holyc-ocaml/issues/340), and [#344](https://github.com/frankischilling/holyc-ocaml/issues/344), indexed and scalar outer cases inside the 77-case function expression-result group, four outer-call cases inside the 29-case top-level expression-result group, JIT and AOT shapes, recursive callback metadata, retained-identity checks, stale-batch checks, deterministic replay, purity checks, and foreign-session rejection |
| Function-body outer callback calls | Implemented for scalar and fully indexed callback globals selected from an exact caller-supplied JIT task-parent or AOT enclosing-compilation snapshot. The checked `CHashGlblVar` payload supplies the recursive signature and declared rank used by the shared indirect slot binder. Each source-ordered record retains provided values, explicit holes, defaults, the variadic tail and count, integer, `F64`, pointer, or backed aggregate return shape, its result identity, and the exact outer occurrence and table binding. Indexed calls also retain the typed outer base and each integer-target subscript result. Partial, excessive, or absent indexing of an array callback produces `HCSEMA0046`; metadata-free records and ordinary objects stay unavailable and create no callback record. Unknown callees at the start of a hosted statement currently require explicit grouping to avoid the parser's unresolved declaration branch. Runtime storage reads, address selection, default evaluation, lowering, and invocation remain unimplemented. | [Issues #338](https://github.com/frankischilling/holyc-ocaml/issues/338) and [#340](https://github.com/frankischilling/holyc-ocaml/issues/340), scalar and indexed cases inside the 77-case function expression-result group, JIT and AOT comparisons, nested direct and outer calls, deterministic replay, purity checks, and negative rank and shape coverage |
| Executable top-level outer callback-array calls | Implemented for callback globals selected from an exact caller-supplied JIT task-parent or AOT enclosing-compilation snapshot. A grouped computed callee must consume exactly the array rank stored in the checked `CHashGlblVar` metadata. The result keeps the typed bracket chain, every integer-target subscript, recursive callback signature, fixed and defaulted slots, variadic tail and count, primitive, pointer, or backed aggregate return type, exact outer occurrence and binding, and call-result identity. Zero, partial, and excessive ranks report `HCSEMA0057`. Ordinary objects and metadata-free records remain unavailable. The pass does not calculate an address, read callback storage, evaluate an index or default, lower IR, place ABI arguments, or execute the call. | [Issue #344](https://github.com/frankischilling/holyc-ocaml/issues/344), one positive and one negative focused case inside the 29-case top-level expression-result group, JIT/AOT comparisons, one- and multidimensional ranks, defaults, varargs, nested calls, all supported return classes, exact identity checks, deterministic replay, purity, and stable diagnostics |
| Terminal global commas | Syntax implemented for ordinary and aggregate-attached global lists in JIT and AOT modes. The AST keeps the comma on the final concrete declarator and a separate semicolon on the declaration. Global type resolution uses that semicolon as the final variable's effective terminator and does not invent an empty variable. Tests cover initialized and bound globals, recursive function pointers, definition and include provenance, deterministic human and JSON dumps, and rejection when the comma is not followed by a semicolon. The intended corpus contains one authored form, `I64 cur_dc,;` at `Demo/Graphics/Life.HC:4`. | [Issue #382](https://github.com/frankischilling/holyc-ocaml/issues/382), exact pinned-source parser and semantic tests, and the reviewed AOT corpus comparison |
| Full semantic analysis | Not implemented | M4 |
| Executable IR and interpretation | The complete opcode registry, checked instruction sequences, control-flow and effect classifications, checked block graphs, structural x87 verification, immutable named-function bodies, and distinct executable top-level streams are implemented. Literal lowering accepts explicit payloads, parser nodes, and direct typed semantic results. The multi-node lowerer emits source-ordered integer trees, pure and mixed `F64` arithmetic for five operations, two floating-domain shifts, three floating-domain bitwise operations, three floating-domain logical operations, primitive postfix casts, ordinary current-position `IC_RIP` leaves, checked direct or standalone aggregate offset constants including checked object roots, and symbolic direct-function address fragments selected from exact JIT or AOT paths. Checked function-scope and top-level direct calls emit fixed and variadic argument trees, four noninternal call forms, cleanup, and a result. Accepted ordinary expression statements append a checked `IC_END_EXP` with the final expression or standalone top-level call value and unused-result flag. Retained fixed-argument root intent marks the final integer producer with `ICF_RES_TO_F64` or the final `F64` producer with `ICF_RES_TO_INT`. Every deterministic dump includes the pinned reference commit. Ordinary object identifier values, bare function values, assignments, defaults, internal and indirect calls, numerical conversion, storage layout and reads, implicit output, whole-body placement, exceptions, locks, relocation records, optimization, interpretation, and code generation are not implemented. | [IR epic #396](https://github.com/frankischilling/holyc-ocaml/issues/396), [issues #401](https://github.com/frankischilling/holyc-ocaml/issues/401), [#405](https://github.com/frankischilling/holyc-ocaml/issues/405), [#407](https://github.com/frankischilling/holyc-ocaml/issues/407), [#409](https://github.com/frankischilling/holyc-ocaml/issues/409), [#411](https://github.com/frankischilling/holyc-ocaml/issues/411), [#448](https://github.com/frankischilling/holyc-ocaml/issues/448), [#450](https://github.com/frankischilling/holyc-ocaml/issues/450), [#452](https://github.com/frankischilling/holyc-ocaml/issues/452), [#454](https://github.com/frankischilling/holyc-ocaml/issues/454), [#456](https://github.com/frankischilling/holyc-ocaml/issues/456), [#458](https://github.com/frankischilling/holyc-ocaml/issues/458), [#462](https://github.com/frankischilling/holyc-ocaml/issues/462), [#464](https://github.com/frankischilling/holyc-ocaml/issues/464), [#466](https://github.com/frankischilling/holyc-ocaml/issues/466), [#498](https://github.com/frankischilling/holyc-ocaml/issues/498), [#500](https://github.com/frankischilling/holyc-ocaml/issues/500), [#502](https://github.com/frankischilling/holyc-ocaml/issues/502), [#504](https://github.com/frankischilling/holyc-ocaml/issues/504), [#506](https://github.com/frankischilling/holyc-ocaml/issues/506), [#522](https://github.com/frankischilling/holyc-ocaml/issues/522), [#524](https://github.com/frankischilling/holyc-ocaml/issues/524), [#530](https://github.com/frankischilling/holyc-ocaml/issues/530) through [#540](https://github.com/frankischilling/holyc-ocaml/issues/540), [#544](https://github.com/frankischilling/holyc-ocaml/issues/544), [#546](https://github.com/frankischilling/holyc-ocaml/issues/546), and [#562](https://github.com/frankischilling/holyc-ocaml/issues/562), 76 expression-lowering cases, eighteen direct-call cases, fourteen expression-statement cases, checked graph/effect/x87 tests, and M5 |
| Frame-bound identifier address lowering | Implemented for direct identifier expressions that resolve to an exact non-static object binding with a checked frame slot. Named parameters, synthetic `argc` and `argv`, and supported automatic scalar, pointer, array, by-value aggregate, and zero-sized object locations emit `IC_RBP`, signed-displacement `IC_IMM_I64`, and `IC_ADD`. The three producers share one pointer layer over the checked location type, retain the exact identifier span, carry zero flags, and use consecutive caller-owned identities. Static locals, callback declarators, module and outer bindings, direct functions, unsupported positive parameter arrays, and maximum-depth pointer values return `Unsupported_location` without a partial sequence. Named array parameters currently stop at `HCPARSE0011`, so that lowerer branch is defensive. Loads, stores, index scaling, expression-planner integration, execution, and machine emission remain unimplemented. | [Issue #560](https://github.com/frankischilling/holyc-ocaml/issues/560), six focused cases covering JIT and AOT, fixed and variadic parameters, automatic scalar, pointer, array, aggregate, and zero-sized objects, positive, negative, and zero displacements, exact binding and frame ownership, module and selected outer boundaries, aggregate type identity, spans, identities, deterministic output, unsupported paths, and stable diagnostics |
| Canonical string-literal expression lowering | Implemented for checked direct and parenthesized string results. Each accepted leaf emits one zero-operand `IC_STR_CONST` with unchanged decoded bytes, internal `U8 *` address type, exact leaf span, zero flags, and consecutive identities. Function bodies and executable top-level roots share the same JIT and AOT path. Grouping consumes no identity. Storage allocation, terminator insertion, constant-pool placement, conversion-bearing uses, interpretation, and execution remain unavailable; invalid metadata or an unsupported surrounding operation exposes no partial sequence. | [Issue #490](https://github.com/frankischilling/holyc-ocaml/issues/490), three focused cases covering both owners and modes, exact binary payloads and dump escaping, identity and span retention, transparent grouping, metadata rejection, and unsupported conversion-bearing use |
| Canonical aggregate offset lowering | Implemented for completed function-body and executable top-level paths rooted at a source-visible aggregate publication or a checked zero-rank by-value aggregate object. Parameters, automatic and static locals, checked globals, and executable top-level module globals are accepted object roots. Each path emits one zero-operand internal-`I64` `IC_IMM_I64` with the retained final cumulative byte offset, exact expression span, zero flags, and consecutive identities. Direct and standalone `offset(...)` forms, including inherited, nested, grouped, integer, mixed-`F64`, and expression-statement uses, share the same JIT and AOT path. The lowerer validates retained query, object-type, publication, and member evidence before allocation and does not repeat member lookup or layout arithmetic. Pointer, array, callback, and primitive roots, ordinary object-member access, runtime address calculation, storage reads, interpretation, and execution remain unavailable without partial IR. | [Issues #492](https://github.com/frankischilling/holyc-ocaml/issues/492), [#496](https://github.com/frankischilling/holyc-ocaml/issues/496), [#522](https://github.com/frankischilling/holyc-ocaml/issues/522), and [#524](https://github.com/frankischilling/holyc-ocaml/issues/524), five expression cases and two statement cases covering both result owners and modes, all accepted object owners, exact payloads, types, operands, spans, flags, identities, deterministic dumps, numeric composition, source-refinement validation, identity exhaustion, and unsupported ordinary member access |
| Checked expression lowering | Implemented for integer and character literal trees using `IC_SHL`, `IC_SHR`, multiplication, division, modulo, bitwise operations, addition, subtraction, six comparisons, and the three logical binary operations. Pure `F64` literal trees additionally use multiplication, division, modulo, addition, and subtraction. Those five operations also accept mixed integer and `F64` operands: the retained integer producer carries `ICF_RES_TO_F64`, while its checked type and payload remain unchanged. Both shifts accept the same pure and mixed floating domains, remain unflagged, and produce internal `F64`. The three bitwise operations accept the same pure and mixed floating domains, remain unflagged, and produce internal `F64`. The three logical binary operations accept the same pure and mixed floating domains, leave the operation unflagged, and produce internal `I64`. Ordinary current-position leaves emit zero-operand `IC_RIP`, retain internal `RT_PTR` storage, and follow the integer composition path. Known ordinary `defined` leaves emit zero-operand `IC_IMM_I64`: visible local, source-visible module, and supplied outer-chain names produce one; non-name operands and complete outer-chain misses produce zero. This includes executable top-level expressions, whose module lookup stops at the publication prefix before the owning statement. A function candidate without the outer batch needed to finish its lookup remains deferred. Known `sizeof` leaves also emit zero-operand `IC_IMM_I64`. Unbound public primitives use the audited primitive table in both result owners, while checked zero-rank primitive objects, object pointers, aggregate pointers, and scalar function pointers use retained function-body binding metadata. Parentheses and unary plus are transparent and forward conversion requests. A retained fixed-argument root marks its final integer producer with `ICF_RES_TO_F64` or its final `F64` producer with `ICF_RES_TO_INT`, while keeping the checked payload, type, span, operands, and identities. Unary minus and logical not compose in both numeric domains; bitwise complement remains integer-only. Dereference and address-of may occur at any depth in integer trees; each retained operation follows its operand and keeps its checked result type and operator span. Address-of removes an immediately inner dereference before identity allocation while retaining its own instruction. Other pointer-typed intermediates stay confined to address and dereference and cannot enter a binary path. Primitive postfix casts follow their exact operand and keep their public target, full span, and `was_paren` payload. The module validates the whole tree before emitting a verified sequence, visits source-left before source-right, owns consecutive instruction and value allocation, and returns the next unused IDs. Function bodies and executable top-level statements share the same path in JIT and AOT mode. Exact `F64` literal bits are retained, and division by a literal zero is not evaluated. Assignments, identifiers, calls, pointer arithmetic, numerical conversion, short-circuit branches, storage, concrete RIP selection, floating evaluation, optimizer rewrites, and execution return an explicit unsupported result or remain later work. | [Issues #450](https://github.com/frankischilling/holyc-ocaml/issues/450), [#452](https://github.com/frankischilling/holyc-ocaml/issues/452), [#454](https://github.com/frankischilling/holyc-ocaml/issues/454), [#456](https://github.com/frankischilling/holyc-ocaml/issues/456), [#458](https://github.com/frankischilling/holyc-ocaml/issues/458), [#462](https://github.com/frankischilling/holyc-ocaml/issues/462), [#466](https://github.com/frankischilling/holyc-ocaml/issues/466), [#470](https://github.com/frankischilling/holyc-ocaml/issues/470), [#472](https://github.com/frankischilling/holyc-ocaml/issues/472), [#474](https://github.com/frankischilling/holyc-ocaml/issues/474), [#476](https://github.com/frankischilling/holyc-ocaml/issues/476), [#484](https://github.com/frankischilling/holyc-ocaml/issues/484), [#486](https://github.com/frankischilling/holyc-ocaml/issues/486), [#498](https://github.com/frankischilling/holyc-ocaml/issues/498), [#500](https://github.com/frankischilling/holyc-ocaml/issues/500), [#502](https://github.com/frankischilling/holyc-ocaml/issues/502), [#504](https://github.com/frankischilling/holyc-ocaml/issues/504), and [#506](https://github.com/frankischilling/holyc-ocaml/issues/506), focused lowering coverage for all 19 accepted integer binary operators, five accepted floating arithmetic operators, both floating-domain shifts, all three floating-domain bitwise operators, all three floating-domain logical operators, all five accepted integer prefixes, primitive numeric casts, ordinary `IC_RIP`, known ordinary `defined`, and known `sizeof`; exact operand and identity checks; both result owners and compilation modes; deterministic dumps; cancellation and exhaustion diagnostics; negative domain coverage; one 2,000-leaf, 6,499-instruction mixed integer tree; one 2,000-leaf, 3,999-instruction floating tree; and one 4,001-instruction mixed-domain tree with a single conversion flag |
| Canonical direct-function address lowering | Implemented for exact checked `&Function` results in function bodies and executable top-level code. The private expression plan validates the retained declaration, canonical symbol, JIT or AOT address path, origins, internal zero-depth `RT_PTR` type, value categories, and zero ranks before identity allocation. A resolved JIT function emits symbolic `IC_IMM_I64`; a resolved AOT function emits symbolic `IC_ABS_ADDR`; and an unresolved JIT extern emits symbolic `IC_IMM_I64` followed by an operand-linked, payload-free `IC_DEREF`. Every instruction keeps the exact `&` operator span. Checked conversion intent and direct-call `ICF_PUSH_RES` appear only on the final producer. `HCIRL0004` covers inconsistent retained evidence, and `HCIRL0005` covers identity exhaustion without exposing a partial sequence. Ordinary object and callback addresses and bare function values remain unsupported. Numeric address resolution, runtime slot access, import or relocation records, fixups, execution, indirect calls, and machine emission remain unavailable. | [Issue #562](https://github.com/frankischilling/holyc-ocaml/issues/562), five expression-lowering cases and one direct-call composition case covering all three paths, both result owners and compilation modes, replacement identity, exact dataflow, cursors, spans, types, flags, deterministic replay, unsupported boundaries, and atomic exhaustion |
| Primitive postfix-cast IR lowering | Implemented for checked primitive numeric casts inside accepted function-body and executable top-level expression trees. The lowerer emits `IC_HOLYC_TYPECAST` after its exact checked operand, keeps the public target type and full cast span, and stores `PrsUnaryModifier`'s `was_paren` bit as an integer payload. Only an ordinary parenthesized immediate source operand produces one; literals, binary nodes, and nested cast nodes produce zero unless individually grouped. Nested casts remain separate instructions. A cast on the integer side of mixed arithmetic carries `ICF_RES_TO_F64` on the cast producer. JIT and AOT share the path. Invalid semantic metadata reports `HCIRL0004` before identity allocation, and a checked operand outside the implemented numeric tree returns `Unsupported_expression` without exposing a partial sequence. Cast conversion, immediate folding, class transfer, optimizer removal, interpretation, and backend emission are not implemented. | [Issue #462](https://github.com/frankischilling/holyc-ocaml/issues/462), six focused cases inside the 29-case expression-lowering group, integer and `F64` targets, nested and grouped forms, both result owners, both compilation modes, exact operands, consecutive IDs, source spans, payloads, flags, deterministic dumps, invalid metadata, and unsupported checked operands |
| Checked expression-statement termination | Implemented for ordinary function-body and executable top-level statements whose values are accepted by the numeric expression lowerer, plus standalone checked top-level direct calls. The sequence appends one `IC_END_EXP` that consumes the final expression or `IC_CALL_END` value, produces no value, carries `ICF_RES_NOT_USED`, retains the complete statement span, and advances only the instruction identity. Direct calls first require their exact top-level target and reuse the fixed, variadic, opcode, and cleanup composer. This is the canonical pre-optimizer form; the source optimizer's flag transfer and terminator removal are not implemented. A mismatched target or nonstatement role reports `HCIRL0004`, identity exhaustion reports `HCIRL0005`, and an unsupported value returns without a partial sequence. Calls nested in larger expressions, defaults, internal operations, assignments, storage reads, side-effect execution, implicit output, optimization, interpretation, and backend emission remain unavailable. | [Issues #464](https://github.com/frankischilling/holyc-ocaml/issues/464) and [#546](https://github.com/frankischilling/holyc-ocaml/issues/546), fourteen focused cases, function and executable top-level owners, JIT and AOT modes, direct/extern/import calls, fixed and variadic arguments, exact operand and span checks, target ownership, separate producer flags, deterministic dumps, unsupported values, role validation, and identity exhaustion |
| Single-statement executable top-level bodies | Implemented for one exact standalone direct-call statement. The lowerer reuses the checked call and `IC_END_EXP` sequence, appends one spanless structural `IC_END`, builds one caller-identified entry block, and constructs an x87-verified `Ir.Top_level_body`. The body retains the statement's item position and complete span plus the caller's stream identity and compiler-option snapshot. Successful lowering returns the next unused instruction and value identities. Unsupported calls, mismatched ownership, invalid options, and identity exhaustion expose no partial sequence, graph, or body. Multiple statements, control flow, callbacks, defaults, internal operations, implicit output, `IET_MAIN`, interpretation, and machine emission remain unavailable. | [Issue #548](https://github.com/frankischilling/holyc-ocaml/issues/548), three focused cases covering JIT and AOT, direct, extern-slot, and import calls, fixed and variadic arguments, exact block and instruction shape, metadata, caller-owned identities, deterministic dumps, failure atomicity, option validation, and identity exhaustion |
| Ordinary-expression executable top-level bodies | Implemented for one exact ordinary expression statement accepted by the checked expression planner. The lowerer reuses its source-backed `IC_END_EXP`, then shares the direct-call path's spanless `IC_END`, one caller-identified entry block, `Ir.Top_level_body`, x87 verification, metadata, and cursor construction. Integer and mixed-F64 arithmetic, ordinary current position, resolved `defined`, primitive `sizeof`, and checked aggregate offsets work in JIT and AOT mode. Unsupported identifiers and storage reads, nonexpression control roots, invalid options, and identity exhaustion expose no partial body. Assignments, updates, callbacks, implicit output, control flow, `IET_MAIN`, interpretation, and machine emission remain unavailable. | [Issue #552](https://github.com/frankischilling/holyc-ocaml/issues/552), three new focused cases plus direct-call regressions covering six expression families, both modes, exact instruction endings, item positions, spans, compiler options, cursors, deterministic dumps, unsupported values, invalid roots, option validation, and exhaustion |
| Batched executable top-level ordinary-expression bodies | Implemented for an exact source-ordered batch of ordinary expression statements. Equal-length statement and compiler-option lists produce one independently verified body per statement through the single-statement expression path. Integer and mixed-F64 arithmetic, ordinary current position, resolved `defined`, primitive `sizeof`, and checked aggregate offsets work across JIT and AOT batches. Caller-seeded stream and block identities advance once per body, while instruction and value identities thread through each exact emitted sequence; empty input preserves every cursor. Count mismatches, invalid options, unsupported identifiers or storage reads, invalid control roots, child verification failures, and exhausted identities expose no partial body list, including failures after an internally completed prefix. Issue #556 adds a separate mixed direct-call/expression entry point; assignments, updates, callbacks, implicit output, control flow, combined graphs, `IET_MAIN`, interpretation, and machine emission remain unavailable. | [Issue #554](https://github.com/frankischilling/holyc-ocaml/issues/554), three focused cases covering empty, single, and multi-expression batches, both modes, all six supported expression families, per-statement options and metadata, exact cursor threading, deterministic dumps, count validation, prefix failure atomicity, unsupported values and roots, option failures, and identity exhaustion |
| Batched executable top-level direct-call bodies | Implemented for an exact source-ordered batch of standalone direct-call statements. Equal-length statement, target, and compiler-option lists produce one independently verified body per statement. Caller-seeded stream and block identities advance once per body, while instruction and value identities thread through each exact emitted sequence. Empty input preserves every cursor. Count mismatches, foreign targets, invalid options, unsupported calls, child verifier failures, and exhausted owner identities expose no partial body list; entered failures retain their statement index. Issue #556 adds source-ordered target selection for mixed statement kinds; combined graphs, callbacks, defaults, internal operations, implicit output, `IET_MAIN`, interpretation, and machine emission remain unavailable. | [Issue #550](https://github.com/frankischilling/holyc-ocaml/issues/550), three focused cases covering empty, single, and two-statement batches, JIT and AOT call access, fixed and variadic arguments, per-statement options, exact cursor threading, deterministic dumps, count and ownership validation, unsupported calls, option failures, and stream/block exhaustion |
| Mixed executable top-level statement batches | Implemented for one source-ordered list containing standalone direct calls and ordinary expression statements. `Ir.Top_level_batch_lowering.lower_mixed` accepts the checked statements, an independently ordered list of classified direct-call targets, and one compiler-option snapshot per statement. A target selects the direct-call path only when its semantic result ID and physical root-expression object both match a sole expression-statement root; target-list order never determines body order. A root identifier call is treated as a standalone direct call only when that exact call resolves to a source-visible module function publication. Nested calls and global or outer callback roots remain on the ordinary-expression path. Missing, duplicate, foreign, unrelated, unmatched, and ambiguous targets are rejected before any body can escape. Each accepted item retains its source position, span, options, mode-specific call access when applicable, and independent checked body. Stream and block IDs advance once per body, instruction and value cursors continue exactly, and empty input preserves all four cursors. Homogeneous APIs remain unchanged, and every target, lowering, validation, verifier, or exhaustion failure remains atomic. Callback invocation, broader storage reads, assignments, updates, implicit output, control flow, combined graphs, `IET_MAIN`, interpretation, optimization, and machine emission remain unavailable. | [Issue #556](https://github.com/frankischilling/holyc-ocaml/issues/556), seven new focused cases plus six homogeneous regressions covering empty and homogeneous parity, both mixed source orders, longer alternating batches, reversed target order, JIT and AOT access, exact result and physical-root matching, nested-call and callback classification, metadata, options, all four cursors, deterministic dumps, target validation, unsupported paths, and failure atomicity |
| Checked condition branch fragments | Implemented for function-body and executable top-level `if`, `while`, `do while`, and `for` conditions accepted by the expression lowerer. Each fragment appends `IC_BR_ZERO` or `IC_BR_NOT_ZERO` according to the checked condition role or branch sense, consumes the exact final condition value, produces no result, keeps the caller-owned block target and retained condition or expression-root span, carries zero flags, and advances only the instruction identity. Unsupported values expose no partial sequence; incomplete metadata and identity exhaustion use stable lowering diagnostics. Block placement, statement bodies, fallthrough edges, loop closure, top-level entry records, optimization, interpretation, and backend emission remain unavailable. | [Issues #508](https://github.com/frankischilling/holyc-ocaml/issues/508) and [#510](https://github.com/frankischilling/holyc-ocaml/issues/510), six focused cases covering both owners, all four roles, JIT and AOT modes, exact instruction order, operands, targets, spans, flags, identities, deterministic dumps, unsupported values, and exhaustion |
| Checked function return fragments | Implemented for valued and valueless function returns. A supported value lowers through the canonical expression planner and is followed by `IC_RETURN_VAL` with the exact final operand and declared target type. Every return appends `IC_JMP` to a caller-owned leave block. Both instructions keep the complete return span and zero flags; root conversion intent remains on the final producer. Unsupported values expose no partial sequence, and identity exhaustion uses `HCIRL0005`. Leave-block placement, active-try unwinding, warning policy, `IC_LEAVE`, `IC_RET`, optimization, interpretation, and backend emission remain unavailable. | [Issue #512](https://github.com/frankischilling/holyc-ocaml/issues/512), four focused cases covering valued and valueless forms, both conversion directions, JIT and AOT modes, exact instruction order, operands, target types, leave targets, spans, flags, identities, deterministic dumps, unsupported values, and exhaustion |
| Checked function goto and label fragments | Implemented for uniquely defined function-local language labels and their resolved goto references. Labels receive consecutive caller-seeded block identities in first-occurrence order. Source-ordered definitions emit `IC_LABEL`, references emit `IC_JMP`, and both carry the exact assigned block, complete occurrence span, and zero flags. `HCIRL0004` rejects incomplete metadata, `HCIRL0005` covers instruction or block exhaustion, and `HCIRL0006` keeps assembly forms outside the fragment without partial IR. Block partitioning, repeated assembly-address checks, composition with other statement fragments, full graph construction, optimization, interpretation, and backend emission remain unavailable. | [Issue #514](https://github.com/frankischilling/holyc-ocaml/issues/514), four focused cases covering forward and backward gotos, JIT and AOT modes, exact source order, targets, spans, flags, identities, deterministic dumps, unsupported assembly forms, incomplete metadata, and exhaustion |
| Checked function break fragments | Implemented for resolved function-body breaks. Every `while`, `do while`, `for` body, switch, and subswitch region receives a consecutive caller-seeded block identity, even when it contains no break. Source-ordered breaks emit `IC_JMP` with the assigned block payload, exact statement span, and zero flags. `HCIRL0004` rejects incomplete source metadata, `HCIRL0005` covers instruction or block exhaustion, and `HCIRL0006` rejects an inconsistent target mapping without partial IR. Block placement, loop back edges, switch dispatch, full graph construction, optimization, interpretation, and backend emission remain unavailable. | [Issue #518](https://github.com/frankischilling/holyc-ocaml/issues/518), four focused cases covering every region kind, nesting, JIT and AOT modes, empty regions, exact source order, targets, spans, flags, identities, deterministic dumps, provenance, incomplete metadata, and exhaustion |
| Hosted x86-64 | Not implemented | M6 |
| TempleOS assembler | The HolyC parser retains brace-delimited assembly as classified structural tokens and function-local direct assembly as classified instruction and directive items. The checked source database contains all 106 registers, 325 canonical opcodes, 49 aliases, and 924 instruction forms. Brace-block operation parsing, direct labels, operand and directive validation, directive effects, form selection, ModRM and SIB construction, encoding, fixups, disassembly, and the standalone `holyc asm` command are not implemented. | [Assembler notes](assembler.md), [issue #159](https://github.com/frankischilling/holyc-ocaml/issues/159), [issue #157](https://github.com/frankischilling/holyc-ocaml/issues/157), [issue #155](https://github.com/frankischilling/holyc-ocaml/issues/155), [issue #151](https://github.com/frankischilling/holyc-ocaml/issues/151), [issue #30](https://github.com/frankischilling/holyc-ocaml/issues/30), M7 |
| Ordinary function expression statement results | Implemented for ordinary expression statements inside checked function definitions. Each statement keeps a contiguous source identity, source origin, immutable expression result, source-visible type, integer or `F64` class, nested direct or callback call identity, and unused-result intent. The numeric subset accepted by the expression lowerer now creates `IC_END_EXP` through the separate issue #464 boundary. Other values do not yet lower, discard a runtime value, execute side effects, or execute a function. | [Issue #300](https://github.com/frankischilling/holyc-ocaml/issues/300), two focused cases inside the 77-case expression-result group, one constructor case in the call-resolution group, recursive statement positions, integer, F64, pointer, member, update, assignment, and direct-call values, JIT and AOT modes, included definition provenance, deterministic replay, source order, purity, and constructor validation |
| Implicit Print and PutChars statement results | Implemented for standalone string and character statements inside checked function definitions. Each source-ordered record keeps the synthetic target, marker origin, marker-backed or following-expression fixed form, checked values, nested call identity, and `ICF_RES_NOT_USED` intent. The target pass selects the newest visible module function header or an exact function entry from the supplied JIT or AOT outer chain. The lookup skips same-name nonfunction records as `HashFind` does with `HTT_FUN`; missing targets report `HCSEMA0048` at the marker. The argument binder consumes the selected checked header, distinguishes provided and defaulted fixed slots, records integer or `F64` conversion intent, and retains only the remaining `Print` values as variadic input. JIT and AOT default materialization remain distinct. Missing required or excess nonvariadic values report `HCSEMA0050` and `HCSEMA0051`. An outer target without an exact checked header stays explicitly deferred. Format validation, evaluated default payloads, vararg promotion, call lowering, output, and top-level execution remain unimplemented. | [Issue #302](https://github.com/frankischilling/holyc-ocaml/issues/302), [issue #304](https://github.com/frankischilling/holyc-ocaml/issues/304), [issue #306](https://github.com/frankischilling/holyc-ocaml/issues/306), two focused cases inside the 77-case expression-result group, four target-resolution cases, nine argument-binding cases, all target and fixed-source forms, recursive statement positions, source-order header replacement, typed outer lookup and deferral, defaults, fixed and variadic slots, integer, F64, aggregate, pointer, and function-pointer values, JIT and AOT modes, include and definition provenance, deterministic replay, purity, ownership, and stable diagnostics |
| Executable top-level implicit output | Target and argument binding are implemented for every parsed string or character output in the typed top-level expression batch. Each target groups one fixed root with its ordered trailing roots and retains the output identity, containing statement, `Print` or `PutChars` choice, marker-backed or following-expression form, and literal marker origin. Module lookup selects only the newest function header published before that statement; later headers remain invisible, same-name nonfunctions do not hide a function, and lookup then follows the exact JIT or AOT outer chain and assembler table. The argument pass consumes that exact target. It keeps provided and defaulted fixed slots distinct, records integer or `F64` conversion intent, distinguishes JIT immediate from AOT string-default materialization, and retains only values after the fixed parameter count as variadic roots. An outer target without a checked header for the exact symbol stays deferred. `HCSEMA0058` and `HCSEMA0059` cover target input and lookup; `HCSEMA0060`, `HCSEMA0061`, and `HCSEMA0062` cover argument input, missing required values, and excess nonvariadic values. Format parsing, evaluated defaults, vararg promotion, lowering, `IC_END_EXP`, and output remain unimplemented. | [Issues #346](https://github.com/frankischilling/holyc-ocaml/issues/346) and [#348](https://github.com/frankischilling/holyc-ocaml/issues/348), five target cases, six argument cases, nine unchanged function-body argument cases, JIT and AOT source-order comparisons, recursive and generated forms, marker and following-expression paths, module replacement, kind filtering, outer and assembler fallback, defaults, conversions, variadic tails, exact identity checks, deterministic replay, purity, ownership, mode, and stable diagnostics |
| TempleOS `.BIN` serialization and loading | Not implemented | M8 |
| Bootstrap | Not attempted | M9 |

The broad parsing row describes the shared declaration, expression, and statement foundation. The narrower rows record later slices and their remaining boundaries.

Direct function address typing is implemented under [issue #286](https://github.com/frankischilling/holyc-ocaml/issues/286) and [issue #288](https://github.com/frankischilling/holyc-ocaml/issues/288), with symbolic IR lowering under [issue #562](https://github.com/frankischilling/holyc-ocaml/issues/562). A checked `&Function` expression retains the newest source-visible function publication, its exact resolved declaration, and its canonical joined identity. The operand is a function value; the completed expression is an integer-class address with TempleOS's internal zero-depth `RT_PTR`/`RT_I64` type. JIT unresolved externs carry extern-slot intent, JIT resolved functions carry immediate intent, and AOT resolved functions carry absolute-address intent. The IR lowerer consumes those paths directly as `IC_IMM_I64` plus `IC_DEREF`, `IC_IMM_I64`, or `IC_ABS_ADDR`, with the canonical symbol, exact address-of operator span, consecutive identities, and final-producer flags. Ordinary AOT externs and imports fail with `HCSEMA0046` at `&`; `_extern` remains resolved, and `_intern` fails instead of receiving a false noninternal path. Local or global object shadowing and function-pointer variables keep their existing object or callback address behavior. Focused semantic, expression-lowering, and direct-call tests cover replacement and recursive publications, fixed and variadic positions, both result owners and compilation modes, include and definition provenance, declaration and canonical identity, path-specific dataflow, deterministic replay, unsupported boundaries, and exhaustion. This gate does not claim numeric address resolution, runtime slot access, import or relocation records, fixups, execution, indirect calls, or machine emission.

Fully indexed aggregate callback-member arrays are implemented under [issue #330](https://github.com/frankischilling/holyc-ocaml/issues/330) for checked function bodies and executable top-level expressions. Direct, pointer, inherited, multidimensional, and parenthesized callees retain one exact member lookup through every declared subscript. The stored recursive signature then supplies fixed slots, explicit holes, defaults, variadic values, and the return type. Partial and excessive indexing report `HCSEMA0039` or `HCSEMA0057`. One function expression-result case, two top-level cases, and strengthened negative and provenance cases cover JIT and AOT modes. This gate does not claim runtime address calculation, callback storage reads, bounds checks, default evaluation, IR, execution, or ABI placement.

The lexer corpus result is 528 of 528 files at TempleOS commit `c26482bb6ad3f80106d28504ec5db3c6a360732c`. The denominator is the complete committed set of files ending in `.HC`, `.HH`, or `.PRJ`. This is a raw-tokenization result, not a whole-compiler compatibility percentage. Preprocessing, parsing, semantic analysis, lowering, execution, native emission, and loader acceptance remain separate gates.
